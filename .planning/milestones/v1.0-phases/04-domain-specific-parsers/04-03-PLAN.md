---
phase: 04-domain-specific-parsers
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - advance-rag/rag/app/clinical.py
  - advance-rag/rag/svr/task_executor.py
  - advance-rag/tests/test_clinical_parser.py
  - advance-rag/tests/fixtures/sample_clinical_protocol.txt
  - fe/src/features/datasets/types/index.ts
  - fe/src/features/datasets/components/ParserSettingsFields.tsx
  - fe/src/i18n/locales/en.json
  - fe/src/i18n/locales/vi.json
  - fe/src/i18n/locales/ja.json
autonomous: true
requirements:
  - PRSR-04

must_haves:
  truths:
    - "Uploading a clinical document with the clinical parser auto-classifies it into regulatory, protocol, research, or administrative"
    - "Classification result is stored in parser_config.metadata_tags.clinical_classification"
    - "Classification uses existing LLMBundle/chat_mdl pipeline with first 2000 tokens only"
    - "New parser types (code, openapi, adr, clinical) appear in FE parser selector dropdown"
    - "Each new parser has a description and supported formats in the FE parser descriptions"
    - "ParserSettingsFields shows no special settings for clinical parser (classification is automatic)"
  artifacts:
    - path: "advance-rag/rag/app/clinical.py"
      provides: "Clinical document classifier using LLM"
      exports: ["chunk"]
    - path: "fe/src/features/datasets/types/index.ts"
      provides: "Updated PARSER_OPTIONS and PARSER_DESCRIPTIONS with 4 new types"
      contains: "code"
    - path: "advance-rag/tests/test_clinical_parser.py"
      provides: "Unit tests for clinical classifier with mocked LLM"
  key_links:
    - from: "advance-rag/rag/app/clinical.py"
      to: "db.services.llm_service.LLMBundle"
      via: "LLM chat for classification"
      pattern: "LLMBundle"
    - from: "fe/src/features/datasets/types/index.ts"
      to: "fe/src/features/datasets/components/ParserSettingsFields.tsx"
      via: "PARSER_OPTIONS import"
      pattern: "PARSER_OPTIONS"
---

<objective>
Implement the clinical document LLM-based classifier (PRSR-04) and register all four new parser types in the frontend UI (PARSER_OPTIONS, PARSER_DESCRIPTIONS, i18n).

Purpose: The clinical classifier auto-classifies documents into regulatory/protocol/research/administrative categories using the existing LLM pipeline. The FE integration makes all four new parsers selectable in the dataset creation and parser change dialogs.

Output: Working clinical classifier with mocked LLM tests, all four parsers visible in FE parser selector.
</objective>

<execution_context>
@/home/doana/.claude/get-shit-done/workflows/execute-plan.md
@/home/doana/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-domain-specific-parsers/04-CONTEXT.md
@.planning/phases/04-domain-specific-parsers/04-RESEARCH.md
@.planning/phases/04-domain-specific-parsers/04-01-SUMMARY.md

@advance-rag/CLAUDE.md
@fe/CLAUDE.md
@advance-rag/rag/app/tag.py
@fe/src/features/datasets/types/index.ts
@fe/src/features/datasets/components/ParserSettingsFields.tsx

<interfaces>
<!-- Parser contract -->
```python
def chunk(filename, binary=None, from_page=0, to_page=100000,
          lang="Chinese", callback=None, **kwargs):
    # kwargs: parser_config, kb_id, tenant_id
    # Returns: list[dict] with content_with_weight, content_ltks, content_sm_ltks, docnm_kwd, title_tks
```

<!-- LLM integration pattern (from task_executor.py lines 496-514) -->
```python
from db.services.llm_service import LLMBundle
from db.joint_services.tenant_model_service import get_model_config_by_type_and_name
from common.constants import LLMType

chat_model_config = get_model_config_by_type_and_name(
    task["tenant_id"], LLMType.CHAT, task["llm_id"]
)
chat_mdl = LLMBundle(task["tenant_id"], chat_model_config, lang=task["language"])
```

<!-- FE PARSER_OPTIONS (from types/index.ts) -->
```typescript
export const PARSER_OPTIONS = [
  { value: 'naive', label: 'General' },
  { value: 'qa', label: 'Q&A' },
  // ... 11 more existing entries
] as const;

export const PARSER_DESCRIPTIONS: Record<string, { title: string; description: string; formats: string }> = {
  naive: { title: '...', description: '...', formats: '...' },
  // ... 12 more existing entries
};
```

<!-- NO_CHUNK_SETTINGS_PARSERS in ParserSettingsFields.tsx -->
```typescript
const NO_CHUNK_SETTINGS_PARSERS = new Set(['one', 'picture', 'audio', 'email', 'resume', 'table'])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement clinical document classifier (PRSR-04)</name>
  <files>
    advance-rag/rag/app/clinical.py,
    advance-rag/rag/svr/task_executor.py,
    advance-rag/tests/test_clinical_parser.py,
    advance-rag/tests/fixtures/sample_clinical_protocol.txt
  </files>
  <read_first>
    advance-rag/rag/app/tag.py,
    advance-rag/rag/app/clinical.py,
    advance-rag/rag/svr/task_executor.py,
    .planning/phases/04-domain-specific-parsers/04-RESEARCH.md
  </read_first>
  <behavior>
    - Test: chunk() on a clinical protocol document classifies it as "protocol" and stores in tag_kwd
    - Test: Classification result stored as metadata field clinical_classification on chunks
    - Test: Only first 2000 tokens of document content sent to LLM (not full document)
    - Test: LLM returning invalid category falls back to "administrative" default
    - Test: When LLM is unavailable, chunks are still produced (classification skipped, not fatal)
    - Test: Every chunk dict has required fields (content_with_weight, content_ltks, content_sm_ltks, docnm_kwd, title_tks)
    - Test: Chunks are produced using naive-style text chunking (clinical parser is a classifier, not a structural parser)
  </behavior>
  <action>
    1. Create test fixture:
       - advance-rag/tests/fixtures/sample_clinical_protocol.txt: ~500 words of clinical trial protocol text.
         Title line: "Protocol XYZ-001: Phase III Randomized Controlled Trial"
         Content: study design, endpoints, inclusion criteria, sample size justification.

    2. Create advance-rag/tests/test_clinical_parser.py:
       - Mock LLMBundle and get_model_config_by_type_and_name to avoid real LLM calls.
       - Mock LLMBundle.chat() to return "protocol" (or use async_chat pattern).
       - Test all behaviors listed above.
       - NOTE: Since chunk() is synchronous (called via thread_pool_exec), but LLM calls may be async,
         the clinical classifier should do its LLM classification synchronously or use asyncio.run().
         Check how existing auto_keywords works -- it runs AFTER chunk() returns, in task_executor.
         DECISION: The clinical classifier runs classification INSIDE chunk(), not as a post-step.
         Use synchronous LLM call or run_in_executor pattern. For testing, mock the LLM call.

    3. Implement advance-rag/rag/app/clinical.py (replace stub):

       - CLINICAL_CLASSIFICATION_PROMPT: Template string asking LLM to classify into exactly one of:
         regulatory, protocol, research, administrative.
         Include category descriptions:
         - regulatory: FDA submissions, compliance docs, SOPs, audit reports
         - protocol: Clinical trial protocols, study designs, amendments
         - research: Journal articles, literature reviews, case studies, lab reports
         - administrative: Meeting minutes, budgets, contracts, correspondence
         Response format: "Respond with ONLY the category name (one word, lowercase)."

       - VALID_CATEGORIES = {"regulatory", "protocol", "research", "administrative"}

       - _classify_document(title: str, content_preview: str, tenant_id: str, llm_id: str, language: str) -> str:
         Build classification prompt with title and first 2000 tokens of content.
         Call LLMBundle synchronously (or use existing sync patterns from the codebase).
         Parse response -- strip whitespace, lowercase. Validate against VALID_CATEGORIES.
         If invalid, default to "administrative".
         If LLM call fails, log warning and return "administrative".
         NOTE: Check if the existing parsers make LLM calls inside chunk(). If not, store
         the classification intent in the chunk metadata and let the post-processing pipeline handle it.
         ALTERNATIVE (SIMPLER): Since chunk() runs in thread_pool_exec, and the task_executor
         has the async LLM infrastructure, consider making clinical classification a POST-parse step
         similar to auto_keywords. BUT the CONTEXT.md says "auto-classify when parser selected -- no extra toggle".
         RESOLUTION: Do the classification inside chunk() using a synchronous approach:
         - Import and use the LLM directly with a sync wrapper
         - Or mark chunks with a flag and have task_executor do the async classification after chunk()
         PREFERRED: Add classification as a chunk metadata field. The chunk() function does text chunking
         (reuse naive parser's approach). Then in task_executor.py, after the existing auto_keywords/
         auto_questions/enable_metadata pipeline, add a clinical_classification step that runs
         only when parser_id == "clinical". This follows the existing async pattern cleanly.
         BUT this would require modifying task_executor.py (shared file). Since Plan 01 already registered
         the FACTORY entries, we can add a small block to task_executor.py here.

         FINAL APPROACH: chunk() does naive-style text chunking and adds clinical_classification="" placeholder.
         The actual LLM classification happens in a helper function called from task_executor.py's
         post-processing pipeline (after auto_keywords). This is cleaner and follows established patterns.
         Add a clinical_classify block in task_executor.py after the enable_metadata block.

    4. In clinical.py chunk():
       - Decode binary to text (use get_text from deepdoc.parser.utils if available)
       - Split text into chunks using naive-style logic: split by paragraphs/newlines, combine until token limit
       - Build standard chunk dicts with tokenization fields
       - Add tag_kwd = ["clinical"] to each chunk
       - Return chunks (classification happens post-parse in task_executor)

    5. Add _classify_clinical_document() as a standalone async function in clinical.py:
       - Takes (chat_mdl, full_text: str, title: str) -> str
       - Builds prompt with first 2000 tokens
       - Calls chat_mdl.chat() or async_chat()
       - Returns category string

    6. Add clinical classification block to task_executor.py (after enable_metadata block, ~line 606):
       ```python
       if task["parser_id"] == ParserType.CLINICAL.value:
           from rag.app.clinical import classify_document
           # ... async classification using chat_mdl, chat_limiter
           # Store result as tag_kwd addition on all chunks
           # Store in doc metadata via DocMetadataService
       ```

       IMPORTANT: Follow the exact async pattern used by auto_keywords (lines 496-528).
       Use chat_limiter semaphore. Cache result with get_llm_cache/set_llm_cache.

    IMPORTANT: Include Google-style docstrings on all functions per CLAUDE.md.
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge && source .venv/bin/activate && python -m pytest advance-rag/tests/test_clinical_parser.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'def chunk(' advance-rag/rag/app/clinical.py
    - grep -q 'CLINICAL_CLASSIFICATION_PROMPT' advance-rag/rag/app/clinical.py
    - grep -q 'regulatory' advance-rag/rag/app/clinical.py
    - grep -q 'protocol' advance-rag/rag/app/clinical.py
    - grep -q 'content_with_weight' advance-rag/rag/app/clinical.py
    - grep -q 'rag_tokenizer' advance-rag/rag/app/clinical.py
    - grep -q 'clinical_classification\|ParserType.CLINICAL' advance-rag/rag/svr/task_executor.py
    - test -f advance-rag/tests/fixtures/sample_clinical_protocol.txt
    - All tests in test_clinical_parser.py pass
  </acceptance_criteria>
  <done>
    Clinical parser chunks documents using naive-style splitting and adds clinical tags. LLM-based classification runs as post-parse step in task_executor (following auto_keywords pattern). Classification stored as metadata tag. Invalid LLM responses default to "administrative". Tests pass with mocked LLM.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register all four parser types in FE (PARSER_OPTIONS, descriptions, i18n)</name>
  <files>
    fe/src/features/datasets/types/index.ts,
    fe/src/features/datasets/components/ParserSettingsFields.tsx,
    fe/src/i18n/locales/en.json,
    fe/src/i18n/locales/vi.json,
    fe/src/i18n/locales/ja.json
  </files>
  <read_first>
    fe/src/features/datasets/types/index.ts,
    fe/src/features/datasets/components/ParserSettingsFields.tsx,
    fe/src/i18n/locales/en.json,
    fe/src/i18n/locales/vi.json,
    fe/src/i18n/locales/ja.json
  </read_first>
  <action>
    1. Add four new parser entries to PARSER_OPTIONS array in fe/src/features/datasets/types/index.ts:
       { value: 'code', label: 'Code' }
       { value: 'openapi', label: 'API Spec' }
       { value: 'adr', label: 'ADR' }
       { value: 'clinical', label: 'Clinical' }

    2. Add four new entries to PARSER_DESCRIPTIONS record in the same file:
       code: {
         title: '"Code" Chunking method description',
         description: 'This method parses source code files using AST analysis.\n' +
           '- Chunks at function and class boundaries.\n' +
           '- Preserves import context as file-level metadata.\n' +
           '- Extracts function name, class name, parameters, and return type as metadata.',
         formats: 'Supported: PY, JS, TS, TSX, JSX, Java, Go, Rust, Ruby, C, C++, C#, PHP, Swift, Kotlin, Scala, Lua, Bash, R, Dart, Vue, Svelte.',
       }
       openapi: {
         title: '"API Spec" Chunking method description',
         description: 'This method parses OpenAPI and Swagger specification files.\n' +
           '- Creates one chunk per API endpoint (path + method).\n' +
           '- Resolves schema references and inlines them in each chunk.\n' +
           '- Extracts endpoint path, method, operation ID, and tags as metadata.',
         formats: 'Supported: YAML, JSON (OpenAPI 3.x, Swagger 2.0).',
       }
       adr: {
         title: '"ADR" Chunking method description',
         description: 'This method parses Architecture Decision Records.\n' +
           '- Detects MADR, Nygard, and Y-statement formats.\n' +
           '- Creates one chunk per section (Context, Decision, Consequences).\n' +
           '- Extracts ADR status, title, and date as metadata.',
         formats: 'Supported: MD, TXT.',
       }
       clinical: {
         title: '"Clinical" Chunking method description',
         description: 'This method parses clinical and healthcare documents.\n' +
           '- Automatically classifies documents as regulatory, protocol, research, or administrative.\n' +
           '- Classification stored as metadata tag for filtering and ABAC policies.\n' +
           '- Uses general chunking with LLM-based classification.',
         formats: 'Supported: PDF, DOCX, TXT, MD.',
       }

    3. Update NO_CHUNK_SETTINGS_PARSERS in ParserSettingsFields.tsx:
       Add 'clinical' to the set (clinical uses naive chunking, classification is automatic -- no special settings).
       The code, openapi, and adr parsers DO support chunk size settings.

    4. Add i18n keys to all three locale files (en.json, vi.json, ja.json):
       In the "datasets" namespace, add:
       - en: "parserCode": "Code", "parserOpenapi": "API Spec", "parserAdr": "ADR", "parserClinical": "Clinical"
       - vi: "parserCode": "Ma nguon", "parserOpenapi": "API Spec", "parserAdr": "ADR", "parserClinical": "Lam sang"
       - ja: "parserCode": "koodo", "parserOpenapi": "API shiyou", "parserAdr": "ADR", "parserClinical": "Rinshou"

       NOTE: Check what i18n key pattern the existing parsers use. If PARSER_OPTIONS labels are used directly
       without i18n (they use { label: 'General' } not t('...')), then i18n keys may not be needed for the
       dropdown itself. Add them anyway for consistency and future use. Also check if PARSER_DESCRIPTIONS
       titles use i18n -- if not, skip the translation of descriptions.

    5. Run FE build to verify no TypeScript errors:
       npm run build -w fe

    IMPORTANT: Follow JSDoc conventions per CLAUDE.md on any new exports.
    IMPORTANT: Support dark mode for any new UI elements (though this task only adds data, no new components).
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge && npm run build -w fe 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "'code'" fe/src/features/datasets/types/index.ts
    - grep -q "'openapi'" fe/src/features/datasets/types/index.ts
    - grep -q "'adr'" fe/src/features/datasets/types/index.ts
    - grep -q "'clinical'" fe/src/features/datasets/types/index.ts
    - grep -q "Code" fe/src/features/datasets/types/index.ts
    - grep -q "API Spec" fe/src/features/datasets/types/index.ts
    - grep -q "ADR" fe/src/features/datasets/types/index.ts
    - grep -q "Clinical" fe/src/features/datasets/types/index.ts
    - grep -q "clinical" fe/src/features/datasets/components/ParserSettingsFields.tsx
    - FE build succeeds without TypeScript errors
  </acceptance_criteria>
  <done>
    All four parser types (code, openapi, adr, clinical) appear in FE parser selector dropdown with descriptions and supported format info. Clinical parser added to NO_CHUNK_SETTINGS_PARSERS. i18n keys added for all 3 locales. FE builds successfully.
  </done>
</task>

</tasks>

<verification>
1. `python -m pytest advance-rag/tests/test_clinical_parser.py -x -v` passes
2. `npm run build -w fe` succeeds
3. PARSER_OPTIONS has code, openapi, adr, clinical entries
4. PARSER_DESCRIPTIONS has entries for all four new types
5. clinical.py stores classification in metadata tags
6. task_executor.py has clinical classification post-processing block
</verification>

<success_criteria>
- Clinical parser tests pass with mocked LLM -- documents classified into one of four categories
- FE build passes -- all four parser types visible in PARSER_OPTIONS with descriptions
- Clinical classification stored as metadata tag (searchable, ABAC-compatible)
- All i18n locales updated
</success_criteria>

<output>
After completion, create `.planning/phases/04-domain-specific-parsers/04-03-SUMMARY.md`
</output>
