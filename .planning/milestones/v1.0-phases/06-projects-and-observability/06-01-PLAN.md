---
phase: 06-projects-and-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - be/src/shared/db/migrations/20260320000000_add_tenant_id_to_projects.ts
  - be/src/shared/db/migrations/20260320000001_create_query_log.ts
  - be/src/shared/models/types.ts
  - be/src/shared/models/factory.ts
  - be/src/modules/projects/models/project.model.ts
  - be/src/modules/projects/services/projects.service.ts
  - be/src/modules/rag/services/query-log.service.ts
  - be/src/modules/rag/index.ts
autonomous: true
requirements:
  - PROJ-01
  - OBSV-01
must_haves:
  truths:
    - "Projects table has tenant_id column with NOT NULL constraint after backfill"
    - "query_log table exists with source, tenant_id, query, result_count, confidence_score, failed_retrieval columns"
    - "QueryLogService.logQuery() inserts a row into query_log without blocking the caller"
    - "ProjectModel.findByTenant(tenantId) returns only projects belonging to that org"
  artifacts:
    - path: "be/src/shared/db/migrations/20260320000000_add_tenant_id_to_projects.ts"
      provides: "tenant_id column on projects table"
      contains: "tenant_id"
    - path: "be/src/shared/db/migrations/20260320000001_create_query_log.ts"
      provides: "query_log table for analytics"
      contains: "query_log"
    - path: "be/src/modules/rag/services/query-log.service.ts"
      provides: "Async fire-and-forget query logging"
      exports: ["queryLogService"]
  key_links:
    - from: "be/src/modules/rag/services/query-log.service.ts"
      to: "query_log table"
      via: "ModelFactory.queryLog.create()"
      pattern: "ModelFactory\\.queryLog"
    - from: "be/src/modules/projects/models/project.model.ts"
      to: "projects table"
      via: "findByTenant query"
      pattern: "findByTenant"
---

<objective>
Create the database foundation for Phase 6: add tenant_id to the projects table for multi-tenant isolation, create the query_log table for analytics, add the QueryLogService for async logging, and make ProjectModel tenant-aware.

Purpose: All subsequent plans depend on these schema changes and services. Projects need tenant isolation (PROJ-01). Query analytics need the query_log table (OBSV-01).
Output: Two migrations, QueryLog type + model, QueryLogService, tenant-scoped ProjectModel methods.
</objective>

<execution_context>
@/home/doana/.claude/get-shit-done/workflows/execute-plan.md
@/home/doana/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-projects-and-observability/06-CONTEXT.md
@.planning/phases/06-projects-and-observability/06-RESEARCH.md

<interfaces>
From be/src/shared/models/types.ts:
```typescript
export interface Project {
  id: string
  name: string
  description?: string | null
  avatar?: string | null
  default_embedding_model?: string | null
  default_chunk_method?: string | null
  default_parser_config: any
  status: string
  is_private: boolean
  created_by?: string | null
  updated_by?: string | null
  created_at: Date
  updated_at: Date
}
// NOTE: No tenant_id field yet — migration adds it
```

From be/src/modules/projects/models/project.model.ts:
```typescript
export class ProjectModel extends BaseModel<Project> {
  protected tableName = 'projects'
  async findByCreator(userId: string): Promise<Project[]>
  async findActive(): Promise<Project[]>
}
```

From be/src/shared/models/factory.ts (pattern):
```typescript
// Models registered as: ModelFactory.<name> = new <Name>Model()
// Follow existing registration pattern for queryLog
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB migrations — tenant_id on projects + query_log table</name>
  <files>
    be/src/shared/db/migrations/20260320000000_add_tenant_id_to_projects.ts,
    be/src/shared/db/migrations/20260320000001_create_query_log.ts,
    be/src/shared/models/types.ts
  </files>
  <read_first>
    be/src/shared/db/migrations/20260318000000_answer_feedback.ts,
    be/src/shared/db/migrations/20260312000000_initial_schema.ts,
    be/src/shared/models/types.ts
  </read_first>
  <action>
1. Create migration `20260320000000_add_tenant_id_to_projects.ts`:
   - Add `tenant_id` column to `projects` table as nullable TEXT initially
   - Add index on `tenant_id`
   - Backfill from creator's org: `UPDATE projects p SET tenant_id = COALESCE((SELECT ut.tenant_id FROM user_tenant ut WHERE ut.user_id = p.created_by LIMIT 1), 'default') WHERE p.tenant_id IS NULL`
   - Alter to NOT NULL after backfill
   - Down: drop column

2. Create migration `20260320000001_create_query_log.ts`:
   - Table: `query_log`
   - Columns: id (text PK, gen_random_uuid), source (text NOT NULL, checkIn chat/search), source_id (text NOT NULL), user_id (text NOT NULL), tenant_id (text NOT NULL), query (text NOT NULL), dataset_ids (jsonb default '[]'), result_count (integer default 0), response_time_ms (integer nullable), confidence_score (float nullable), failed_retrieval (boolean default false), created_at (timestamptz default now)
   - Indexes: [tenant_id, created_at], [tenant_id, failed_retrieval], [source], [user_id]
   - Down: drop table

3. Update `be/src/shared/models/types.ts`:
   - Add `tenant_id: string` to Project interface (after status field)
   - Add new `QueryLog` interface with all columns from the migration
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge && npm run build -w be 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "tenant_id" be/src/shared/db/migrations/20260320000000_add_tenant_id_to_projects.ts
    - grep -q "query_log" be/src/shared/db/migrations/20260320000001_create_query_log.ts
    - grep -q "tenant_id.*string" be/src/shared/models/types.ts (in Project interface area)
    - grep -q "interface QueryLog" be/src/shared/models/types.ts
    - grep -q "confidence_score" be/src/shared/db/migrations/20260320000001_create_query_log.ts
    - grep -q "failed_retrieval" be/src/shared/db/migrations/20260320000001_create_query_log.ts
  </acceptance_criteria>
  <done>Both migrations exist with correct schema. Project interface has tenant_id. QueryLog interface defined.</done>
</task>

<task type="auto">
  <name>Task 2: QueryLog model + QueryLogService + tenant-scoped ProjectModel</name>
  <files>
    be/src/modules/rag/services/query-log.service.ts,
    be/src/modules/rag/index.ts,
    be/src/shared/models/factory.ts,
    be/src/modules/projects/models/project.model.ts,
    be/src/modules/projects/services/projects.service.ts
  </files>
  <read_first>
    be/src/shared/models/factory.ts,
    be/src/shared/models/base.model.ts,
    be/src/modules/rag/index.ts,
    be/src/modules/projects/models/project.model.ts,
    be/src/modules/projects/services/projects.service.ts
  </read_first>
  <action>
1. Create QueryLog model class in a new file or register directly. Follow existing pattern: create a simple model class extending BaseModel<QueryLog> with tableName 'query_log'. Register it in ModelFactory as `queryLog`.

2. Create `be/src/modules/rag/services/query-log.service.ts`:
   - Import ModelFactory and log (logger service)
   - Define `CreateQueryLogData` interface: { source: 'chat' | 'search', source_id: string, user_id: string, tenant_id: string, query: string, dataset_ids?: string[], result_count?: number, response_time_ms?: number, confidence_score?: number, failed_retrieval?: boolean }
   - Class `QueryLogService` with:
     - `logQuery(data: CreateQueryLogData): void` — fire-and-forget (no await, no throw). Wraps ModelFactory.queryLog.create(data) in try/catch, logs warning on failure. Uses `void ModelFactory.queryLog.create(data).catch(err => log.warn(...))` pattern (Pitfall 2: never block caller)
   - Export singleton `queryLogService`

3. Add export to `be/src/modules/rag/index.ts` barrel: `export { queryLogService } from './services/query-log.service.js'`

4. Update `ProjectModel` — add `findByTenant(tenantId: string)` method that queries `WHERE tenant_id = tenantId AND status = 'active' ORDER BY created_at DESC`.

5. Update `ProjectsService.getAccessibleProjects()`:
   - Add tenantId parameter requirement
   - For admin/superadmin: call `ModelFactory.project.findByTenant(tenantId)` instead of `findActive()`
   - For other roles: filter `findByTenant(tenantId)` instead of `findActive()`
   - This ensures org isolation (Pitfall 1 from RESEARCH.md)
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge && npm run build -w be 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "queryLogService" be/src/modules/rag/services/query-log.service.ts
    - grep -q "logQuery" be/src/modules/rag/services/query-log.service.ts
    - grep -q "fire-and-forget\|Non-blocking\|never fail" be/src/modules/rag/services/query-log.service.ts
    - grep -q "queryLog" be/src/shared/models/factory.ts
    - grep -q "findByTenant" be/src/modules/projects/models/project.model.ts
    - grep -q "tenantId" be/src/modules/projects/services/projects.service.ts
    - grep -q "queryLogService" be/src/modules/rag/index.ts
  </acceptance_criteria>
  <done>QueryLogService logs queries asynchronously without blocking. ProjectModel has findByTenant. ProjectsService filters by tenant. BE builds clean.</done>
</task>

</tasks>

<verification>
- `npm run build -w be` succeeds with no type errors
- grep confirms tenant_id in migrations and Project type
- grep confirms query_log table creation
- grep confirms QueryLogService with fire-and-forget pattern
- grep confirms findByTenant on ProjectModel
</verification>

<success_criteria>
- Two new migrations exist with correct schemas
- Project interface includes tenant_id field
- QueryLog interface defined with all columns
- QueryLogService.logQuery() is fire-and-forget (no await blocking)
- ProjectModel.findByTenant() filters by tenant_id
- ProjectsService uses tenant-scoped queries
- BE compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-projects-and-observability/06-01-SUMMARY.md`
</output>
