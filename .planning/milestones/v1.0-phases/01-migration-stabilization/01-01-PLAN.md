---
phase: 01-migration-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fe/playwright.config.ts
  - fe/e2e/fixtures/auth.setup.ts
  - fe/e2e/helpers/api.helper.ts
  - fe/e2e/helpers/wait.helper.ts
  - fe/e2e/dataset/dataset-crud.spec.ts
  - fe/.gitignore
autonomous: true
requirements: [STAB-01, STAB-04]
must_haves:
  truths:
    - "Playwright E2E infrastructure is configured and a test can run against the dev server"
    - "User can create a knowledge base via the UI without error"
    - "User can update a knowledge base name and description without error"
    - "User can delete a knowledge base without error"
    - "E2E tests reveal any dataset CRUD bugs through test failures"
  artifacts:
    - path: "fe/playwright.config.ts"
      provides: "Playwright E2E configuration"
      contains: "testDir.*e2e"
    - path: "fe/e2e/fixtures/auth.setup.ts"
      provides: "Authenticated browser state for all tests"
      contains: "storageState"
    - path: "fe/e2e/helpers/api.helper.ts"
      provides: "Direct API call helpers for test setup/teardown"
      exports: ["apiHelper"]
    - path: "fe/e2e/helpers/wait.helper.ts"
      provides: "Async polling helpers for document parsing/indexing waits"
      exports: ["waitForDocumentParsed"]
    - path: "fe/e2e/dataset/dataset-crud.spec.ts"
      provides: "E2E tests for dataset create, update, delete"
      contains: "test.*create.*dataset"
  key_links:
    - from: "fe/e2e/fixtures/auth.setup.ts"
      to: "fe/playwright.config.ts"
      via: "storageState path reference"
      pattern: "storageState.*auth/user\\.json"
    - from: "fe/e2e/dataset/dataset-crud.spec.ts"
      to: "fe/e2e/helpers/api.helper.ts"
      via: "API helper for cleanup"
      pattern: "apiHelper"
---

<objective>
Set up Playwright E2E test infrastructure and write dataset lifecycle CRUD tests that exercise create, update, and delete knowledge base flows through the browser UI. Fix any bugs the tests reveal.

Purpose: Establishes the test foundation all subsequent plans depend on, and stabilizes the most basic dataset operations (STAB-01). Every E2E test in later plans reuses the auth fixture, API helpers, and wait helpers created here.
Output: Working Playwright config, auth fixture, helpers, and green dataset CRUD tests.
</objective>

<execution_context>
@/home/doana/.claude/get-shit-done/workflows/execute-plan.md
@/home/doana/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-migration-stabilization/01-CONTEXT.md
@.planning/phases/01-migration-stabilization/01-RESEARCH.md

<interfaces>
<!-- Existing patterns the executor needs -->

From fe/src/features/chat/api/chatApi.ts (API call pattern):
```typescript
sendFeedback: async (conversationId: string, messageId: string, thumbup: boolean, feedback?: string): Promise<void> => {
  await api.post(`${BASE_URL}/conversations/${conversationId}/feedback`, { message_id: messageId, thumbup, feedback })
}
```

From be/src/modules/chat/routes/chat-conversation.routes.ts (route pattern):
```typescript
router.post('/conversations/:id/feedback', requireAuth, validate({ body: feedbackSchema, params: conversationIdParamSchema }), controller.sendFeedback.bind(controller))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up Playwright infrastructure and auth fixture</name>
  <files>fe/playwright.config.ts, fe/e2e/fixtures/auth.setup.ts, fe/e2e/helpers/api.helper.ts, fe/e2e/helpers/wait.helper.ts, fe/.gitignore</files>
  <read_first>
    - fe/package.json (confirm @playwright/test dependency, check existing scripts)
    - fe/.gitignore (to append e2e auth state exclusion)
    - fe/vite.config.ts (confirm dev server port)
    - be/src/modules/auth/routes/auth.routes.ts (confirm login endpoint path)
    - be/.env.example (confirm default admin credentials pattern)
    - .planning/phases/01-migration-stabilization/01-RESEARCH.md (Playwright config pattern, auth setup pattern, wait helper pattern)
  </read_first>
  <action>
1. Create `fe/playwright.config.ts` with these exact settings:
   - `testDir: './e2e'`
   - `fullyParallel: false` (stateful E2E flows must be sequential)
   - `retries: 1`
   - `workers: 1` (avoid race conditions on shared DB)
   - `reporter: 'html'`
   - `timeout: 120_000` (2 minutes per test -- parsing is slow)
   - `use.baseURL: 'http://localhost:5173'`
   - `use.trace: 'on-first-retry'`
   - `use.screenshot: 'only-on-failure'`
   - Two projects: `setup` (testMatch: `/.*\.setup\.ts/`) and `chromium` (uses `devices['Desktop Chrome']`, `storageState: 'e2e/.auth/user.json'`, `dependencies: ['setup']`)

2. Create `fe/e2e/fixtures/auth.setup.ts`:
   - Import `test as setup, expect` from `@playwright/test`
   - Navigate to `/login`
   - Fill email field with value from `process.env.E2E_ADMIN_EMAIL || 'admin@example.com'`
   - Fill password field with value from `process.env.E2E_ADMIN_PASSWORD || 'admin_password'`
   - Click sign-in button
   - Wait for redirect to dashboard or datasets page: `await expect(page).toHaveURL(/\/(dashboard|datasets)/)`
   - Save storage state to `'e2e/.auth/user.json'`

3. Create `fe/e2e/helpers/api.helper.ts`:
   - Export a class `ApiHelper` that wraps Playwright's `page.request` for direct API calls
   - Constructor takes `APIRequestContext` (from `page.request` or `request` fixture)
   - Methods: `createDataset(name: string, description?: string)`, `deleteDataset(id: string)`, `getDataset(id: string)`, `listDatasets()`, `uploadDocument(datasetId: string, filePath: string)`, `getDocument(datasetId: string, docId: string)`, `triggerParse(datasetId: string, docIds: string[])`
   - All methods use the base URL from environment or default `http://localhost:3001`
   - API prefix: `/api/rag/datasets` (check actual route paths in BE routes)

4. Create `fe/e2e/helpers/wait.helper.ts`:
   - Export `waitForDocumentParsed(request: APIRequestContext, datasetId: string, docId: string, timeoutMs = 60_000): Promise<void>`
   - Polls `GET /api/rag/datasets/${datasetId}/documents/${docId}` every 2 seconds
   - Returns when `data.run === '1'` or `data.progress >= 1.0`
   - Throws if `data.run === '-1'` with error message from `data.progress_msg`
   - Throws on timeout with clear message
   - Export `waitForChunksIndexed(request: APIRequestContext, datasetId: string, docId: string, minChunks = 1, timeoutMs = 30_000): Promise<void>`
   - Polls chunk count endpoint until at least `minChunks` chunks exist

5. Add to `fe/.gitignore` (append, do not overwrite):
   - `e2e/.auth/`
   - `playwright-report/`
   - `test-results/`

6. Add scripts to `fe/package.json`:
   - `"test:e2e": "playwright test --project=chromium"`
   - `"test:e2e:smoke": "playwright test --project=chromium --grep @smoke"`
   - `"test:e2e:setup": "playwright install chromium"`
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge/fe && cat playwright.config.ts | grep -q "testDir.*e2e" && cat e2e/fixtures/auth.setup.ts | grep -q "storageState" && cat e2e/helpers/api.helper.ts | grep -q "createDataset" && cat e2e/helpers/wait.helper.ts | grep -q "waitForDocumentParsed" && echo "PASS: All Playwright infrastructure files exist with expected content"</automated>
  </verify>
  <acceptance_criteria>
    - fe/playwright.config.ts contains `testDir: './e2e'` and `timeout: 120_000` and `workers: 1`
    - fe/e2e/fixtures/auth.setup.ts contains `storageState` and `'e2e/.auth/user.json'`
    - fe/e2e/helpers/api.helper.ts contains `createDataset` and `deleteDataset` and `uploadDocument`
    - fe/e2e/helpers/wait.helper.ts contains `waitForDocumentParsed` and `waitForChunksIndexed`
    - fe/.gitignore contains `e2e/.auth/`
    - fe/package.json contains `"test:e2e"` script
  </acceptance_criteria>
  <done>Playwright config, auth setup fixture, API helper, and wait helper files exist. All files contain the specified exports and patterns. .gitignore updated.</done>
</task>

<task type="auto">
  <name>Task 2: Write dataset CRUD E2E tests and fix discovered bugs</name>
  <files>fe/e2e/dataset/dataset-crud.spec.ts, fe/e2e/test-data/sample.pdf</files>
  <read_first>
    - fe/e2e/helpers/api.helper.ts (just created -- confirm API helper signatures)
    - be/src/modules/rag/routes/rag.routes.ts (confirm dataset CRUD route paths: POST/GET/PUT/DELETE)
    - be/src/modules/rag/controllers/rag.controller.ts (confirm controller method names and response shapes)
    - be/src/modules/rag/services/rag.service.ts (confirm service layer for dataset operations)
    - be/src/modules/rag/models/knowledgebase.model.ts (confirm UUID format handling)
    - fe/src/features/datasets/pages/DatasetListPage.tsx (confirm UI selectors for create/edit/delete)
    - fe/src/features/datasets/components/ (identify key UI components for dataset CRUD)
  </read_first>
  <action>
1. Create a small test PDF file at `fe/e2e/test-data/sample.pdf` -- generate a minimal valid PDF (use a script or copy a small existing PDF). It should be < 100KB with 1-2 pages of plain text content (e.g., "This is a test document for E2E testing. It contains sample text about knowledge management.")

2. Create `fe/e2e/dataset/dataset-crud.spec.ts` with these test cases:

   **Test: Create dataset via UI** (`@smoke` tag)
   - Navigate to datasets page
   - Click "Create Knowledge Base" button (identify exact button text/selector from DatasetListPage)
   - Fill in name: `E2E Test Dataset ${Date.now()}`
   - Fill in description: `Created by E2E test`
   - Select parser type if required (default should be fine)
   - Submit the form
   - Assert: New dataset appears in the list
   - Assert: API returns the dataset via GET with correct name and description

   **Test: Update dataset name and description**
   - Use API helper to create a dataset (setup)
   - Navigate to that dataset's settings/edit page
   - Change name to `Updated E2E Dataset ${Date.now()}`
   - Change description to `Updated description`
   - Save changes
   - Assert: Updated name visible in UI
   - Assert: API returns updated values

   **Test: Delete dataset**
   - Use API helper to create a dataset (setup)
   - Navigate to datasets page
   - Find the dataset, trigger delete (button/menu)
   - Confirm deletion dialog
   - Assert: Dataset no longer appears in list
   - Assert: API returns 404 for deleted dataset ID

   **Test: Create dataset with duplicate name** (edge case)
   - Create dataset via API
   - Attempt to create another with the same name via UI
   - Assert: Either succeeds (if allowed) or shows a meaningful error (not a 500)

3. Each test should clean up after itself in `afterEach` or `afterAll` using the API helper's `deleteDataset`.

4. If any test fails due to a bug in the backend or frontend:
   - Document the bug as a comment in the test file: `// BUG FOUND: [description]`
   - Fix the bug in the relevant BE/FE file
   - Re-run the test to confirm the fix
   - Note: UUID format mismatches between Knex and RAGFlow models are a known risk area
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge/fe && cat e2e/dataset/dataset-crud.spec.ts | grep -q "@smoke" && cat e2e/dataset/dataset-crud.spec.ts | grep -q "Create dataset" && cat e2e/dataset/dataset-crud.spec.ts | grep -q "Delete dataset" && ls e2e/test-data/sample.pdf && echo "PASS: Dataset CRUD spec exists with smoke tag and test PDF"</automated>
  </verify>
  <acceptance_criteria>
    - fe/e2e/dataset/dataset-crud.spec.ts contains test cases for "Create dataset", "Update dataset", "Delete dataset"
    - fe/e2e/dataset/dataset-crud.spec.ts contains `@smoke` tag on the create test
    - fe/e2e/dataset/dataset-crud.spec.ts uses `ApiHelper` for setup/teardown
    - fe/e2e/test-data/sample.pdf exists and is a valid PDF file
    - Any bugs found are fixed and documented with `// BUG FOUND:` comments
    - Tests exercise the full UI flow (not just API calls)
  </acceptance_criteria>
  <done>Dataset CRUD E2E tests exist covering create, update, delete, and duplicate-name edge case. Tests use API helpers for setup/cleanup. Any bugs discovered are fixed. A sample PDF test file exists for later plans.</done>
</task>

</tasks>

<verification>
1. Playwright config exists at `fe/playwright.config.ts` with correct settings
2. Auth setup fixture can authenticate against the running dev server
3. Dataset CRUD tests cover create, update, delete flows through UI
4. API helpers and wait helpers are reusable by subsequent plans
5. Any discovered dataset bugs are fixed with comments documenting the fixes
</verification>

<success_criteria>
- `fe/playwright.config.ts` exists with sequential execution config
- `fe/e2e/` directory structure matches the planned layout
- Dataset CRUD test file has at least 3 test cases (create, update, delete)
- At least one test tagged `@smoke` for quick validation
- All helper files export the documented functions
- .gitignore excludes auth state and playwright artifacts
</success_criteria>

<output>
After completion, create `.planning/phases/01-migration-stabilization/01-01-SUMMARY.md`
</output>
