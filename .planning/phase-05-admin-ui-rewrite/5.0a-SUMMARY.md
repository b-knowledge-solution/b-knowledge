---
phase: 05-admin-ui-rewrite
plan: 5.0a
subsystem: backend/permissions
tags: [migration, schema, permissions, resource_grants]
requires: []
provides:
  - "resource_grants.knowledge_base_id accepts NULL (DocumentCategory-only grants)"
affects:
  - be/src/shared/db/migrations/
tech-stack:
  added: []
  patterns: [knex-alter-table, guarded-rollback]
key-files:
  created:
    - be/src/shared/db/migrations/20260408105510_make_resource_grants_kb_id_nullable.ts
  modified: []
decisions:
  - "Kept column type as `text` (matches original schema from 20260312 + rename from project_id); only dropped NOT NULL"
  - "down() guards with a null-row count check and throws descriptive error instead of silently losing data"
metrics:
  duration: ~5min
  completed: 2026-04-08
---

# Phase 5 Plan 5.0a: Make resource_grants.knowledge_base_id Nullable Summary

**One-liner:** Drops NOT NULL on `resource_grants.knowledge_base_id` via a reversible Knex migration, closing Phase 3 IOU #2 and unblocking P5.3 DocumentCategory-only grants.

## What Shipped

A single Knex migration (`20260408105510_make_resource_grants_kb_id_nullable.ts`) that:

- `up()`: `alterTable('resource_grants', t => t.text('knowledge_base_id').nullable().alter())` — column type preserved as `text`, matching the original `knowledge_base_entity_permissions` schema.
- `down()`: counts rows with `knowledge_base_id IS NULL` and throws a descriptive error if any exist, then restores `NOT NULL` via `.text(...).notNullable().alter()`.

Column type was confirmed by reading `20260312000000_initial_schema.ts` line 1084 (`table.text('project_id').notNullable()`) which was later renamed to `knowledge_base_id`.

## Commits

- `706aec5` — feat(05-5.0a): make resource_grants.knowledge_base_id nullable (closes Phase 3 IOU #2)

## Deviations from Plan

None — plan executed exactly as written. The column type spec allowed either integer or uuid (`.alter()`); actual type was `text`, so migration uses `t.text(...)`.

## Verification

- [x] Migration file exists under `be/src/shared/db/migrations/` matching `*_make_resource_grants_kb_id_nullable.ts`
- [x] File contains `.text('knowledge_base_id').nullable().alter()`
- [x] `down()` contains guard that throws when null rows exist
- [x] TypeScript compiles (`tsc --noEmit` on the file: clean)
- [ ] **DEFERRED — environment-blocked:** `cd be && npm run db:migrate && npm run db:migrate:rollback && npm run db:migrate` — PostgreSQL is not reachable from this execution environment (Docker/WSL integration unavailable, `ECONNREFUSED 127.0.0.1:5432`). Migration is syntactically valid Knex and follows the same pattern as `20260403000000_add_is_system_to_model_providers.ts`. **The orchestrator / next dev session must run migrate + rollback + migrate against a live dev DB before P5.3 execution to fully validate.**
- [ ] **DEFERRED:** Manual `psql -c "\d resource_grants"` column check — same reason.

## Deferred Issues

None beyond the DB-verification block above.

## Self-Check: PASSED

- FOUND: `be/src/shared/db/migrations/20260408105510_make_resource_grants_kb_id_nullable.ts`
- FOUND commit: `706aec5`
- Note: automated DB apply/rollback verification is deferred to an environment with PostgreSQL reachable; migration is structurally identical to prior successful `.alter()` migrations in the repo.
