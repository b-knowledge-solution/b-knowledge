---
phase: 05-admin-ui-rewrite
plan: 5.4
type: execute
wave: 2
depends_on: [5.1, 5.2, 5.3]
files_modified:
  - fe/src/i18n/locales/en.json
  - fe/src/i18n/locales/vi.json
  - fe/src/i18n/locales/ja.json
  - fe/src/features/permissions/components/PermissionMatrix.tsx
  - fe/src/features/permissions/components/OverrideEditor.tsx
  - fe/src/features/permissions/components/EffectivePermissionsPanel.tsx
  - fe/src/features/permissions/components/ResourceGrantEditor.tsx
  - fe/src/features/permissions/components/PrincipalPicker.tsx
  - fe/src/features/permissions/components/PermissionKeyPicker.tsx
  - fe/src/features/users/pages/UserDetailPage.tsx
  - fe/src/features/users/components/UserProfileTab.tsx
  - .planning/phase-05-admin-ui-rewrite/uat/phase-5-darkmode-i18n-checklist.md
autonomous: false
requirements: [TS12]
must_haves:
  truths:
    - "Every new Phase 5 surface has dark-mode variants for all background, text, and border classes"
    - "Every new string has identical keys in en.json, vi.json, and ja.json"
    - "Matrix page, override editor, grant modals, effective panel, principal picker all pass light + dark visual review"
  artifacts:
    - path: ".planning/phase-05-admin-ui-rewrite/uat/phase-5-darkmode-i18n-checklist.md"
      provides: "Manual UAT checklist completed with screenshots or ticks"
  key_links:
    - from: "All Phase 5 components"
      to: "dark:* Tailwind utilities"
      via: "light/dark pairing"
      pattern: "dark:"
---

<objective>
Polish pass: sweep all Phase 5 components for missing `dark:` variants, verify i18n key consistency across en/vi/ja, audit for any hardcoded strings that slipped through, and produce a manual UAT checklist the user walks through.

Purpose: R-10 context demands dark mode + i18n parity. Plans 5.1–5.3 added locale keys incrementally; this plan verifies completeness.
Output: Dark-mode fixes, any missing translations added, UAT checklist document.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phase-05-admin-ui-rewrite/5-CONTEXT.md
@.planning/phase-05-admin-ui-rewrite/5-RESEARCH.md
@fe/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated i18n parity audit + fix any missing keys</name>
  <files>
    fe/src/i18n/locales/en.json
    fe/src/i18n/locales/vi.json
    fe/src/i18n/locales/ja.json
  </files>
  <read_first>
    - fe/src/i18n/locales/en.json (authoritative source)
    - 5-RESEARCH.md §8 (3-locale mandate)
    - The 5.1/5.2/5.3 SUMMARY files to see exactly which keys were added
  </read_first>
  <action>
    Write a small Node.js script (inline, one-off) that:
    1. Reads en.json, vi.json, ja.json
    2. Walks all leaf keys under the `permissions.admin`, `users.detail`, `users.notFound`, `sidebar.iam.permissions` namespaces
    3. Asserts every key present in en.json is also present in vi.json and ja.json
    4. Prints missing keys per locale

    Command:
    ```bash
    cd fe && node -e "
    const fs = require('fs');
    const walk = (obj, prefix='') => Object.entries(obj).flatMap(([k,v]) => typeof v === 'object' && v !== null ? walk(v, prefix+k+'.') : [prefix+k]);
    const en = JSON.parse(fs.readFileSync('src/i18n/locales/en.json'));
    const vi = JSON.parse(fs.readFileSync('src/i18n/locales/vi.json'));
    const ja = JSON.parse(fs.readFileSync('src/i18n/locales/ja.json'));
    const enKeys = new Set(walk(en));
    const viKeys = new Set(walk(vi));
    const jaKeys = new Set(walk(ja));
    const scope = (k) => k.startsWith('permissions.admin') || k.startsWith('users.detail') || k === 'users.notFound' || k.startsWith('sidebar.iam');
    const missingVi = [...enKeys].filter(k => scope(k) && !viKeys.has(k));
    const missingJa = [...enKeys].filter(k => scope(k) && !jaKeys.has(k));
    console.log('Missing in vi:', missingVi);
    console.log('Missing in ja:', missingJa);
    if (missingVi.length || missingJa.length) process.exit(1);
    "
    ```

    Fix any missing keys by adding translations to the missing locale. If unsure of the translation, use a clearly-marked placeholder `[TRANSLATE: <english>]` and flag in the UAT checklist for the user to fix before phase completion.
  </action>
  <verify>
    <automated>cd fe && node -e "const fs=require('fs');const walk=(o,p='')=>Object.entries(o).flatMap(([k,v])=>typeof v==='object'&&v!==null?walk(v,p+k+'.'):[p+k]);const en=JSON.parse(fs.readFileSync('src/i18n/locales/en.json'));const vi=JSON.parse(fs.readFileSync('src/i18n/locales/vi.json'));const ja=JSON.parse(fs.readFileSync('src/i18n/locales/ja.json'));const ek=new Set(walk(en)),vk=new Set(walk(vi)),jk=new Set(walk(ja));const sc=k=>k.startsWith('permissions.admin')||k.startsWith('users.detail')||k==='users.notFound';const mv=[...ek].filter(k=>sc(k)&&!vk.has(k));const mj=[...ek].filter(k=>sc(k)&&!jk.has(k));if(mv.length||mj.length){console.error('missing vi:',mv,'missing ja:',mj);process.exit(1)}console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The Node script above exits 0
    - No `[TRANSLATE:` placeholder survives without being logged in the UAT checklist
    - `cd fe && npm run build` exits 0
  </acceptance_criteria>
  <done>All Phase 5 i18n keys present in all 3 locales.</done>
</task>

<task type="auto">
  <name>Task 2: Dark mode audit — sweep every new component for missing dark: variants</name>
  <files>
    fe/src/features/permissions/components/PermissionMatrix.tsx
    fe/src/features/permissions/components/OverrideEditor.tsx
    fe/src/features/permissions/components/EffectivePermissionsPanel.tsx
    fe/src/features/permissions/components/ResourceGrantEditor.tsx
    fe/src/features/permissions/components/PrincipalPicker.tsx
    fe/src/features/permissions/components/PermissionKeyPicker.tsx
    fe/src/features/users/pages/UserDetailPage.tsx
    fe/src/features/users/components/UserProfileTab.tsx
  </files>
  <read_first>
    - fe/CLAUDE.md §"Dark mode" (class-based, `dark:` prefix, always support both themes)
    - 5-RESEARCH.md §9 (dark mode patterns + reusable classes from existing admin pages)
    - All 8 files listed above
  </read_first>
  <action>
    For each file, grep for light-theme color classes WITHOUT a paired `dark:` variant:
    - `bg-white`, `bg-gray-*`, `bg-slate-*`, `bg-zinc-*` (without `dark:`)
    - `text-gray-*`, `text-slate-*`, `text-black` (without `dark:`)
    - `border-gray-*`, `border-slate-*` (without `dark:`)

    For each offender, add the corresponding dark variant using the research-recommended patterns:
    - `bg-white` → `bg-white dark:bg-slate-800`
    - `text-gray-900` / `text-black` → `text-gray-900 dark:text-white`
    - `border-gray-200` → `border-gray-200 dark:border-slate-700`
    - `bg-gray-50` → `bg-gray-50 dark:bg-slate-700`
    - `bg-green-100` (check icons) → `bg-green-100 dark:bg-green-900/30`

    This task is manual-sweep-oriented. Fix every offender. No exceptions.
  </action>
  <verify>
    <automated>cd fe && npm run build && bash -c 'for f in src/features/permissions/components/*.tsx src/features/users/pages/UserDetailPage.tsx src/features/users/components/UserProfileTab.tsx; do if grep -E "(bg|text|border)-(white|black|gray-[0-9]+|slate-[0-9]+)" "$f" | grep -v "dark:" > /dev/null; then echo "UNPAIRED in $f"; grep -nE "(bg|text|border)-(white|black|gray-[0-9]+|slate-[0-9]+)" "$f" | grep -v "dark:"; fi; done'</automated>
  </verify>
  <acceptance_criteria>
    - The grep sweep above prints no "UNPAIRED" lines
    - `cd fe && npm run build` exits 0
    - Every new Phase 5 .tsx file visually works in both light and dark themes (verified in Task 3 checkpoint)
  </acceptance_criteria>
  <done>No unpaired light-theme color utilities remain in Phase 5 components.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UAT — screenshot light + dark for every new surface</name>
  <files>.planning/phase-05-admin-ui-rewrite/uat/phase-5-darkmode-i18n-checklist.md</files>
  <read_first>
    - 5-CONTEXT.md (all D-* decisions to verify)
    - 5.1-SUMMARY.md / 5.2-SUMMARY.md / 5.3-SUMMARY.md (what was built)
  </read_first>
  <action>
    Create `.planning/phase-05-admin-ui-rewrite/uat/phase-5-darkmode-i18n-checklist.md` with this checklist for the user to complete:

    ```markdown
    # Phase 5 Admin UI — Dark Mode + i18n UAT Checklist

    ## Surfaces to verify
    For each surface, verify in light mode AND dark mode, with locale switched to EN, VI, and JA:

    - [ ] /iam/permissions — Role × Permission Matrix (P5.1)
      - [ ] Light mode (en)  - [ ] Dark mode (en)
      - [ ] Light mode (vi)  - [ ] Dark mode (vi)
      - [ ] Light mode (ja)  - [ ] Dark mode (ja)
      - [ ] Toggle a cell → "Save changes (1)" footer appears
      - [ ] Save → toast shows session-refresh text in current locale
    - [ ] /iam/users/:id?tab=profile — User Profile Tab (P5.2)
    - [ ] /iam/users/:id?tab=permissions — Override Editor (P5.2)
      - [ ] Add allow override → picker opens → select key → row appears → toast shows
      - [ ] Click × on row → row disappears → toast shows
      - [ ] Expand Effective permissions panel → list renders
    - [ ] KB Permissions Modal on a KB (P5.3)
      - [ ] Public/Private toggle still works (writes is_private)
      - [ ] Current grants list renders
      - [ ] PrincipalPicker chip filters (All/Users/Teams/Roles) work
      - [ ] Add grant → row appears → toast shows
    - [ ] Category Permissions Modal (P5.3 — category branch)
      - [ ] Add user grant with DocumentCategory scope → succeeds (requires 5.0a migration applied)
    - [ ] Legacy chat/search Entity modal branches (P5.3 — NOT rewired)
      - [ ] Still work as before (legacy endpoints)

    ## Issues found
    (fill in)

    ## Sign-off
    - [ ] All surfaces verified in light + dark
    - [ ] All strings localized in en/vi/ja
    - [ ] No placeholder `[TRANSLATE:` markers remain
    ```

    Then pause for user verification.
  </action>
  <how-to-verify>
    1. Run `cd fe && npm run dev:fe` and log in as admin
    2. Open the checklist file
    3. Walk each surface in light mode, dark mode, and each locale
    4. Attach screenshots or mark ticks
    5. Reply "approved" if all pass; describe issues if not
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <acceptance_criteria>
    - Checklist file exists at `.planning/phase-05-admin-ui-rewrite/uat/phase-5-darkmode-i18n-checklist.md`
    - User replies "approved" or all listed issues are fixed in a follow-up
  </acceptance_criteria>
  <done>User confirms all Phase 5 surfaces pass dark-mode + i18n review.</done>
</task>

</tasks>

<verification>
- i18n parity script exits 0
- Dark mode grep sweep finds no unpaired classes
- User approves manual UAT
</verification>

<success_criteria>
Every Phase 5 surface is polished for dark mode + i18n in en/vi/ja. R-10 toast text is localized.
</success_criteria>

<output>
Create `.planning/phase-05-admin-ui-rewrite/5.4-SUMMARY.md` after completion.
</output>
