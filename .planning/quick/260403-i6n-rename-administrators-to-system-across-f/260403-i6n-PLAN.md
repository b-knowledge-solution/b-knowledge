---
phase: 260403-i6n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # BE module rename (admin/ -> system/)
  - be/src/modules/system/index.ts
  - be/src/modules/system/controllers/system.controller.ts
  - be/src/modules/system/controllers/system-history.controller.ts
  - be/src/modules/system/services/system-history.service.ts
  - be/src/modules/system/models/system-history.model.ts
  - be/src/modules/system/routes/system.routes.ts
  - be/src/modules/system/routes/system-history.routes.ts
  - be/src/shared/models/factory.ts
  - be/src/app/routes.ts
  # FE route + nav + API URL changes
  - fe/src/layouts/sidebarNav.ts
  - fe/src/app/routeConfig.ts
  - fe/src/app/App.tsx
  - fe/src/features/dashboard/api/dashboardApi.ts
  - fe/src/features/histories/api/historiesApi.ts
  - fe/src/features/broadcast/pages/BroadcastMessagePage.tsx
  - fe/src/features/broadcast/api/broadcastQueries.ts
  # i18n
  - fe/src/i18n/locales/en.json
  - fe/src/i18n/locales/vi.json
  - fe/src/i18n/locales/ja.json
autonomous: false
requirements: []
must_haves:
  truths:
    - "Sidebar shows 'System' group label instead of 'Administrators'"
    - "All /admin/* FE routes navigate to /system/* paths"
    - "All /api/admin/* BE endpoints respond at /api/system/* paths"
    - "BE module directory is be/src/modules/system/ (admin/ deleted)"
    - "i18n in all 3 locales uses 'system' namespace and nav.system label"
  artifacts:
    - path: "be/src/modules/system/index.ts"
      provides: "Renamed barrel exports for system module"
    - path: "be/src/app/routes.ts"
      provides: "Route registration using /system prefix"
    - path: "fe/src/layouts/sidebarNav.ts"
      provides: "nav.system label and /system/* paths"
    - path: "fe/src/app/routeConfig.ts"
      provides: "/system/* route metadata entries"
  key_links:
    - from: "fe/src/features/dashboard/api/dashboardApi.ts"
      to: "/api/system/dashboard"
      via: "fetch URL string"
      pattern: "/api/system/dashboard"
    - from: "fe/src/features/histories/api/historiesApi.ts"
      to: "/api/system/history"
      via: "fetch URL string"
      pattern: "/api/system/history"
    - from: "fe/src/app/App.tsx"
      to: "fe/src/layouts/sidebarNav.ts"
      via: "NavRoleGuard reads getRouteRoles"
      pattern: "system/"
---

<objective>
Rename the "Administrators" module to "System" across the full stack: BE module directory + files, API route prefixes, FE route paths, sidebar nav, API client URLs, and i18n keys in all 3 locales.

Purpose: Align naming with the actual purpose of the section (system configuration, not user administration) and improve clarity.
Output: Fully renamed module with no remaining "admin" references in module/route/nav/i18n contexts (role strings like "admin" role are intentionally preserved).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@be/src/modules/admin/index.ts
@be/src/app/routes.ts
@be/src/shared/models/factory.ts
@fe/src/layouts/sidebarNav.ts
@fe/src/app/routeConfig.ts
@fe/src/app/App.tsx
@fe/src/features/dashboard/api/dashboardApi.ts
@fe/src/features/histories/api/historiesApi.ts
@fe/src/i18n/locales/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename BE admin module to system</name>
  <files>
    be/src/modules/system/ (entire new directory),
    be/src/modules/admin/ (delete),
    be/src/shared/models/factory.ts,
    be/src/app/routes.ts
  </files>
  <action>
1. **Copy and rename `be/src/modules/admin/` to `be/src/modules/system/`** with all files renamed:
   - `controllers/admin.controller.ts` -> `controllers/system.controller.ts`
   - `controllers/admin-history.controller.ts` -> `controllers/system-history.controller.ts`
   - `services/admin-history.service.ts` -> `services/system-history.service.ts`
   - `models/admin-history.model.ts` -> `models/system-history.model.ts`
   - `routes/admin.routes.ts` -> `routes/system.routes.ts`
   - `routes/admin-history.routes.ts` -> `routes/system-history.routes.ts`
   - `index.ts` -> updated barrel exports

2. **Inside each renamed file**, update:
   - Class names: `AdminHistoryModel` -> `SystemHistoryModel`, `AdminHistoryService` -> `SystemHistoryService`, etc.
   - Import paths: all `./admin-*` references become `./system-*`
   - JSDoc `@module` and `@fileoverview` references from "admin" to "system"
   - Export names: `adminRoutes` -> `systemRoutes`, `adminHistoryRoutes` -> `systemHistoryRoutes`, `adminHistoryService` -> `systemHistoryService`

3. **Update `be/src/shared/models/factory.ts`**:
   - Import path: `@/modules/admin/models/admin-history.model.js` -> `@/modules/system/models/system-history.model.js`
   - Class reference: `AdminHistoryModel` -> `SystemHistoryModel`
   - Property name: keep `adminHistory` getter name for now (it is an internal accessor, not a route). Actually rename to `systemHistory` for full consistency.
   - Update private field: `adminHistoryModel` -> `systemHistoryModel`

4. **Update `be/src/app/routes.ts`**:
   - Import paths: `@/modules/admin/routes/admin.routes.js` -> `@/modules/system/routes/system.routes.js` (and admin-history similarly)
   - Route registration: `apiRouter.use('/admin', ...)` -> `apiRouter.use('/system', ...)`
   - `apiRouter.use('/admin/history', ...)` -> `apiRouter.use('/system/history', ...)`
   - `apiRouter.use('/admin/dashboard', ...)` -> `apiRouter.use('/system/dashboard', ...)`
   - Comment: "Admin routes" -> "System routes"

5. **Search entire `be/src/` for any remaining imports from `@/modules/admin/`** and update them. Check `ModelFactory.adminHistory` usages and rename to `ModelFactory.systemHistory`.

6. **Delete `be/src/modules/admin/` directory** after confirming new module works.

IMPORTANT: Role strings (`admin`, `super-admin`) in auth/permission checks MUST remain unchanged -- these are authorization roles, not module names.
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge && npm run build -w be 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `be/src/modules/system/` exists with all renamed files
    - `be/src/modules/admin/` is deleted
    - `be/src/app/routes.ts` registers under `/system` prefix
    - `ModelFactory.systemHistory` works (old `adminHistory` removed)
    - BE compiles successfully with zero errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename FE routes, nav, API URLs, and i18n</name>
  <files>
    fe/src/layouts/sidebarNav.ts,
    fe/src/app/routeConfig.ts,
    fe/src/app/App.tsx,
    fe/src/features/dashboard/api/dashboardApi.ts,
    fe/src/features/histories/api/historiesApi.ts,
    fe/src/features/broadcast/pages/BroadcastMessagePage.tsx,
    fe/src/features/broadcast/api/broadcastQueries.ts,
    fe/src/i18n/locales/en.json,
    fe/src/i18n/locales/vi.json,
    fe/src/i18n/locales/ja.json
  </files>
  <action>
1. **`fe/src/layouts/sidebarNav.ts`**:
   - Change group `labelKey: 'nav.administrators'` -> `labelKey: 'nav.system'`
   - Change all child paths from `/admin/*` to `/system/*`:
     - `/admin/dashboard` -> `/system/dashboard`
     - `/admin/audit-log` -> `/system/audit-log`
     - `/admin/system-tools` -> `/system/system-tools`
     - `/admin/system-monitor` -> `/system/system-monitor`
     - `/admin/tokenizer` -> `/system/tokenizer`
     - `/admin/broadcast-messages` -> `/system/broadcast-messages`
     - `/admin/llm-providers` -> `/system/llm-providers`
   - Update the section comment: "Administration" -> "System"

2. **`fe/src/app/routeConfig.ts`**:
   - Rename all ROUTE_CONFIG keys from `/admin/*` to `/system/*` (7 entries: dashboard, audit-log, system-tools, system-monitor, tokenizer, broadcast-messages, llm-providers)
   - Change `titleKey: 'admin.broadcastMessages'` -> `titleKey: 'system.broadcastMessages'`

3. **`fe/src/app/App.tsx`**:
   - Change all route path strings from `admin/*` to `system/*` (7 Route elements)
   - Update the section comment: "Administration" -> "System"

4. **`fe/src/features/dashboard/api/dashboardApi.ts`**:
   - Change all `/api/admin/dashboard/` URLs to `/api/system/dashboard/`

5. **`fe/src/features/histories/api/historiesApi.ts`**:
   - Change all `/api/admin/history/` URLs to `/api/system/history/`

6. **`fe/src/features/broadcast/pages/BroadcastMessagePage.tsx`** and **`fe/src/features/broadcast/api/broadcastQueries.ts`**:
   - Update any `t('admin.broadcast...')` i18n calls to `t('system.broadcast...')`

7. **i18n locales** (all 3 files: en.json, vi.json, ja.json):
   - `nav.administrators` -> `nav.system` with translated values:
     - en: "System"
     - vi: "Hệ thống"
     - ja: "システム"
   - Rename top-level `"admin"` namespace key to `"system"` (contains broadcastMessages and broadcast sub-keys). Keep all nested content the same, just rename the parent key.

IMPORTANT: Do NOT rename `"admin"` strings that refer to user roles (e.g., `"roles.admin": "Admin"` stays). Only rename the module/nav/namespace references.
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge && npm run build -w fe 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Sidebar shows "System" group (not "Administrators")
    - All FE routes use `/system/*` paths
    - All FE API calls use `/api/system/*` URLs
    - i18n keys use `nav.system` and `system.*` namespace in all 3 locales
    - FE compiles successfully with zero errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify full rename in browser</name>
  <files>N/A</files>
  <action>Human verifies the full rename works end-to-end in a running dev environment.</action>
  <what-built>Full rename of "Administrators" to "System" across BE module, API routes, FE routes, sidebar navigation, API client URLs, and i18n labels in all 3 locales.</what-built>
  <how-to-verify>
    1. Run `npm run dev` to start all services
    2. Open http://localhost:5173 and log in as admin
    3. Verify sidebar shows "System" group (not "Administrators")
    4. Click each item under "System" group: Dashboard, Audit Log, System Tools, System Monitor, Tokenizer, Broadcast Messages, LLM Providers
    5. Verify browser URL shows `/system/*` (not `/admin/*`) for each
    6. Verify each page loads data correctly (API calls hitting `/api/system/*`)
    7. Switch language to Vietnamese -- group label should show "He thong"
    8. Switch language to Japanese -- group label should show "システム"
  </how-to-verify>
  <verify>Manual verification by user</verify>
  <done>User confirms all pages load correctly under /system/* routes with correct labels in all locales</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `grep -r "modules/admin" be/src/` returns zero matches (module fully renamed)
- `grep -r "'/admin/" fe/src/` returns zero matches in route/nav/API files (only role-related "admin" strings remain)
- `grep "administrators" fe/src/i18n/locales/*.json` returns zero matches
- `npm run build` succeeds for both BE and FE
</verification>

<success_criteria>
- BE module lives at `be/src/modules/system/` with no `admin/` directory remaining
- All API routes respond at `/api/system/*` prefix
- FE sidebar displays "System" group with all child routes under `/system/*`
- FE API clients call `/api/system/*` endpoints
- i18n updated in all 3 locales (en, vi, ja)
- Both BE and FE compile without errors
- Role strings ("admin", "super-admin") are unchanged
</success_criteria>

<output>
After completion, create `.planning/quick/260403-i6n-rename-administrators-to-system-across-f/260403-i6n-SUMMARY.md`
</output>
