---
phase: 06-legacy-cleanup-opensearch-integration
plan: 6.5
type: execute
wave: 3
depends_on: [6.2]
files_modified:
  - be/src/shared/config/rbac.ts
  - be/src/shared/middleware/auth.middleware.ts
  - .planning/codebase/ADMIN_ROLES-preservation.md
autonomous: true
requirements: [TS13]
must_haves:
  truths:
    - "Each of the 3 active ADMIN_ROLES usage sites (rbac.ts:94, auth.middleware.ts:327, auth.middleware.ts:381) has a rationale comment immediately above it referencing R-9"
    - "An ADR-style note exists at .planning/codebase/ADMIN_ROLES-preservation.md documenting the deferral to milestone 2"
    - "No code logic is changed — this is a pure documentation pass"
  artifacts:
    - path: ".planning/codebase/ADMIN_ROLES-preservation.md"
      provides: "ADR-style note explaining intentional preservation of ADMIN_ROLES through milestone 1"
    - path: "be/src/shared/config/rbac.ts"
      provides: "Rationale comment above ADMIN_ROLES definition"
      contains: "R-9"
    - path: "be/src/shared/middleware/auth.middleware.ts"
      provides: "Rationale comments above two allowAdminBypass sites"
      contains: "R-9"
  key_links:
    - from: "ADMIN_ROLES-preservation.md"
      to: "R-9 in requirements backlog"
      via: "ADR reference"
      pattern: "R-9"
---

<objective>
Repurpose P6.5 as the R-9 ADMIN_ROLES documentation pass per D-11 / amendment A-4. Add a one-line rationale comment immediately above each of the 3 active `ADMIN_ROLES` usage sites surfaced by research (`rbac.ts:94`, `auth.middleware.ts:327`, `auth.middleware.ts:381`), and write an ADR-style note at `.planning/codebase/ADMIN_ROLES-preservation.md` explaining why this constant stays untouched through milestone 1 and what the plan is for milestone 2. No code rewrites — pure documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phase-06-legacy-cleanup-opensearch-integration/6-CONTEXT.md
@.planning/phase-06-legacy-cleanup-opensearch-integration/6-RESEARCH.md
@CLAUDE.md
@be/src/shared/config/rbac.ts
@be/src/shared/middleware/auth.middleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 6.5.1: Add rationale comments at the 3 active ADMIN_ROLES sites</name>
  <files>
    be/src/shared/config/rbac.ts
    be/src/shared/middleware/auth.middleware.ts
  </files>
  <read_first>
    - be/src/shared/config/rbac.ts lines 90-110 (find the `ADMIN_ROLES` definition and the `isAdmin` helper)
    - be/src/shared/middleware/auth.middleware.ts lines 320-340 and 375-390 (find the two `allowAdminBypass` branches)
    - .planning/phase-06-legacy-cleanup-opensearch-integration/6-CONTEXT.md D-11
    - .planning/phase-06-legacy-cleanup-opensearch-integration/6-RESEARCH.md §10 (ADMIN_ROLES audit table — 3 active sites identified)
  </read_first>
  <action>
    **Site 1 — `be/src/shared/config/rbac.ts` near line 94 (ADMIN_ROLES definition):**
    Insert IMMEDIATELY above the `export const ADMIN_ROLES = [...]` line this exact comment block:
    ```ts
    // ADMIN_ROLES preserved per R-9 through milestone 1. This constant is a
    // tenant-level metadata gate ("is this user a tenant operator?") and is
    // intentionally NOT migrated to the registry-driven useHasPermission model
    // yet. See .planning/codebase/ADMIN_ROLES-preservation.md for the full
    // rationale and the milestone-2 migration plan.
    ```

    **Site 2 — `be/src/shared/middleware/auth.middleware.ts` at line 327 (first allowAdminBypass branch):**
    Insert IMMEDIATELY above the `if` / check that references `ADMIN_ROLES` this exact comment:
    ```ts
    // ADMIN_ROLES preserved per R-9 — admin bypass for ownership checks.
    // Candidate for replacement with `requirePermission('<key>')` in milestone 2.
    // See .planning/codebase/ADMIN_ROLES-preservation.md.
    ```

    **Site 3 — `be/src/shared/middleware/auth.middleware.ts` at line 381 (second allowAdminBypass branch):**
    Insert the SAME exact comment as Site 2 immediately above the second site.

    **Critical constraints:**
    - Do NOT change any code logic. No if-condition edits, no helper additions, no refactors.
    - Do NOT touch `be/src/shared/permissions/legacy-mapping.ts` (the `AGENTS_MEMORY_ADMIN_ROLES` reference is a different constant — RESEARCH.md §10 classifies it out of scope).
    - Do NOT touch the migration files that reference `ADMIN_ROLES` (historical, no-doc per §10).
    - Verify the line numbers before inserting — research captured them as a snapshot; they may have drifted.
  </action>
  <verify>
    <automated>cd be && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "R-9" be/src/shared/config/rbac.ts` returns ≥ 1
    - `grep -c "ADMIN_ROLES-preservation.md" be/src/shared/config/rbac.ts` returns 1
    - `grep -c "R-9" be/src/shared/middleware/auth.middleware.ts` returns ≥ 2 (two bypass sites)
    - `grep -c "ADMIN_ROLES-preservation.md" be/src/shared/middleware/auth.middleware.ts` returns ≥ 2
    - `grep -c "ADMIN_ROLES preserved per R-9" be/src/shared/config/rbac.ts be/src/shared/middleware/auth.middleware.ts` returns ≥ 3
    - `cd be && npm run build` exits 0 (no code logic change, build must be unaffected)
    - `git diff be/src/shared/config/rbac.ts be/src/shared/middleware/auth.middleware.ts` shows only comment additions (no code deletions)
  </acceptance_criteria>
  <done>All 3 active ADMIN_ROLES sites documented with rationale comments, build clean.</done>
</task>

<task type="auto">
  <name>Task 6.5.2: Write ADR-style preservation note at .planning/codebase/ADMIN_ROLES-preservation.md</name>
  <files>.planning/codebase/ADMIN_ROLES-preservation.md</files>
  <read_first>
    - .planning/codebase/ (list existing files to match style — if the directory has an ARCHITECTURE.md or similar, copy its header shape)
    - .planning/phase-06-legacy-cleanup-opensearch-integration/6-CONTEXT.md D-11
    - .planning/phase-06-legacy-cleanup-opensearch-integration/6-RESEARCH.md §10 (full site table)
    - .planning/REQUIREMENTS.md (find R-9 if it exists as an explicit item — otherwise reference it as a backlog label)
  </read_first>
  <action>
    Create `.planning/codebase/ADMIN_ROLES-preservation.md` with this exact content:

    ```markdown
    # ADMIN_ROLES Preservation Note (R-9)

    **Status:** Active through milestone 1. Migration deferred to milestone 2.
    **Created:** Phase 6, task 6.5.2
    **Owner:** Permission overhaul milestone

    ## What this is

    `ADMIN_ROLES` is a hardcoded role-tier constant in `be/src/shared/config/rbac.ts`
    containing `['super-admin', 'admin', 'leader']`. Despite the Phase 3+ move to a
    registry-driven permission system fed through `useHasPermission` / `<Can>`,
    this constant is intentionally preserved for milestone 1 and all its usage
    sites are documented here.

    ## Why it still exists

    `ADMIN_ROLES` answers the question "is this user a tenant operator?" — a
    coarse metadata gate distinct from fine-grained feature permissions. During
    the milestone 1 transition, some branches of the middleware stack rely on
    this gate to apply admin-level bypasses (e.g. for ownership checks) where a
    permission-key check would be a larger refactor. Replacing these calls
    eagerly would:

    1. Expand the Phase 6 scope beyond cleanup into active refactor territory
       (out of scope per Phase 6 CONTEXT.md D-11).
    2. Risk regressing tenant operator flows that are not yet covered by the
       registry.
    3. Require designing new permission keys for operator-level bypass
       semantics, which is a milestone 2 design task.

    ## Active usage sites (3)

    | File | Line (snapshot) | Role |
    |------|-----------------|------|
    | `be/src/shared/config/rbac.ts` | ~94 | Definition of `ADMIN_ROLES = ['super-admin', 'admin', 'leader']` |
    | `be/src/shared/middleware/auth.middleware.ts` | ~327 | First `allowAdminBypass` branch in ownership check |
    | `be/src/shared/middleware/auth.middleware.ts` | ~381 | Second `allowAdminBypass` branch |

    Each site carries an inline rationale comment pointing back to this note
    (added in Phase 6 task 6.5.1).

    ## Explicitly out-of-scope sites

    - `be/src/shared/permissions/legacy-mapping.ts:228` defines a separate
      constant `AGENTS_MEMORY_ADMIN_ROLES` whose semantics differ. Not covered
      by this preservation note.
    - Historical migration files that reference the string `ADMIN_ROLES`
      (`20260407062700_phase1_seed_role_permissions.ts`,
      `20260407090000_phase02_patch_role_permissions_for_v2_parity.ts`) are
      frozen history and are not rewritten or commented.

    ## Milestone 2 migration plan

    In milestone 2 each of the 3 active sites should be replaced with a
    `useHasPermission('<key>')` call backed by a new registry permission key
    (tentative names: `ownership.admin_bypass`, `tenant.operator`). The exact
    key naming is a milestone 2 design decision.

    Until that work lands, the `check:legacy-roles` script (Phase 6.2) does
    NOT flag `ADMIN_ROLES` — it only targets the legacy `UserRole.SUPERADMIN`
    / `UserRole.MEMBER` / bare `'superadmin'` aliases. The preservation is
    intentional and recorded here so future contributors do not assume the
    constant is an oversight.

    ## Related references

    - Phase 6 CONTEXT.md — D-11 (P6.5 repurposed as documentation pass)
    - Phase 6 RESEARCH.md — §10 (ADMIN_ROLES audit results)
    - R-9 in the permission overhaul backlog
    ```

    If `.planning/codebase/` does not yet exist, create the directory first.
  </action>
  <verify>
    <automated>test -f .planning/codebase/ADMIN_ROLES-preservation.md && grep -c "R-9" .planning/codebase/ADMIN_ROLES-preservation.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/codebase/ADMIN_ROLES-preservation.md` succeeds
    - `grep -c "R-9" .planning/codebase/ADMIN_ROLES-preservation.md` returns ≥ 3
    - `grep -c "milestone 2" .planning/codebase/ADMIN_ROLES-preservation.md` returns ≥ 2
    - `grep -c "allowAdminBypass" .planning/codebase/ADMIN_ROLES-preservation.md` returns ≥ 2
    - `grep -c "Active usage sites" .planning/codebase/ADMIN_ROLES-preservation.md` returns 1
    - `grep -c "AGENTS_MEMORY_ADMIN_ROLES" .planning/codebase/ADMIN_ROLES-preservation.md` returns 1 (out-of-scope section present)
  </acceptance_criteria>
  <done>ADR-style preservation note exists with the 3 active sites, out-of-scope carve-out, and milestone 2 plan.</done>
</task>

</tasks>

<verification>
- `cd be && npm run build` exits 0 (no code changes)
- `grep -c "ADMIN_ROLES preserved per R-9" be/src/shared/config/rbac.ts be/src/shared/middleware/auth.middleware.ts` returns ≥ 3
- `test -f .planning/codebase/ADMIN_ROLES-preservation.md` succeeds
- `npm run check:legacy-roles` exits 0 (no regression from prior plans)
</verification>

<success_criteria>
R-9 ADMIN_ROLES documentation pass complete: 3 active sites carry rationale comments, ADR-style note exists at `.planning/codebase/ADMIN_ROLES-preservation.md`, zero code logic changes.
</success_criteria>

<output>
Create `.planning/phase-06-legacy-cleanup-opensearch-integration/6.5-SUMMARY.md` after completion.
</output>
