---
phase: 07-should-haves-versioning-expires-at
plan: 7.0
type: execute
wave: 0
depends_on: []
files_modified:
  - be/tests/permissions/catalog-versioning.test.ts
  - fe/tests/features/permissions/permissionsCatalogRefresh.test.tsx
  - fe/tests/features/permissions/permissionsApi.test.ts
  - fe/tests/lib/permissions.test.tsx
autonomous: true
requirements:
  - SH1
must_haves:
  truths:
    - "Phase 7 SH1 plans start from executable backend and frontend test files rather than missing-file verify gaps."
    - "Later SH1 plans can run targeted BE and FE verification commands immediately after each code task."
    - "The Wave 0 tests establish reusable harnesses for silent refresh behavior without forcing a later plan to create files mid-plan."
  artifacts:
    - path: "be/tests/permissions/catalog-versioning.test.ts"
      provides: "Backend SH1 test harness for catalog controller/service assertions"
    - path: "fe/tests/features/permissions/permissionsCatalogRefresh.test.tsx"
      provides: "Frontend SH1 refresh harness for socket and polling assertions"
    - path: "fe/tests/lib/permissions.test.tsx"
      provides: "Provider-level permission test coverage ready for runtime catalog assertions"
    - path: "fe/tests/features/permissions/permissionsApi.test.ts"
      provides: "FE API contract test coverage ready for the versioned catalog payload"
  key_links:
    - from: "be/tests/permissions/catalog-versioning.test.ts"
      to: "be/src/modules/permissions/services/permissions.service.ts"
      via: "targeted SH1 regression harness"
      pattern: "permissions.service"
    - from: "fe/tests/features/permissions/permissionsCatalogRefresh.test.tsx"
      to: "fe/src/features/permissions/api/permissionsQueries.ts"
      via: "catalog refresh test harness"
      pattern: "permissionsQueries|usePermissionCatalog"
    - from: "fe/tests/lib/permissions.test.tsx"
      to: "fe/src/lib/permissions.tsx"
      via: "provider-backed permission assertions"
      pattern: "useHasPermission|Permission"
---

<objective>
Create the missing SH1 backend and frontend test scaffolding as an explicit Wave 0 dependency so all later Phase 7 verification commands are runnable from the start of execution.

Purpose: close the checker-reported Nyquist gap in 7.1 and 7.3 without changing Phase 7 scope or behavior decisions.
Output: executable BE and FE SH1 test files plus baseline harness updates that later plans extend instead of creating mid-plan.
</objective>

<execution_context>
@/home/doana/.codex/get-shit-done/workflows/execute-plan.md
@/home/doana/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phase-07-should-haves-versioning-expires-at/7-CONTEXT.md
@.planning/phase-07-should-haves-versioning-expires-at/7-VALIDATION.md
@be/tests/permissions/permissions-module.test.ts
@be/tests/permissions/sync.test.ts
@fe/tests/features/permissions/permissionsApi.test.ts
@fe/tests/lib/permissions.test.tsx
@fe/src/lib/permissions.tsx
@fe/src/features/permissions/api/permissionsApi.ts
@fe/src/features/permissions/api/permissionsQueries.ts

<interfaces>
From `be/src/modules/permissions/services/permissions.service.ts`:
```ts
getCatalog(): readonly Permission[]
```

From `fe/src/features/permissions/api/permissionsApi.ts`:
```ts
getCatalog(): Promise<PermissionCatalogResult>
```

From `fe/src/lib/permissions.tsx`:
```ts
export function useHasPermission(key: PermissionKey): boolean
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the backend SH1 regression file</name>
  <files>be/tests/permissions/catalog-versioning.test.ts</files>
  <action>
    Create `be/tests/permissions/catalog-versioning.test.ts` as an executable SH1 harness that imports the existing permissions controller/service seams, reuses the project’s permissions test utilities, and runs green before 7.1 changes land. The file must include baseline controller/service smoke coverage plus clearly named `describe` blocks reserved for deterministic version and mutation-event assertions so 7.1 can extend the same file instead of creating it mid-plan. Keep the harness aligned with the current backend test layout and avoid watch-mode helpers or skipped-file placeholders.
  </action>
  <verify>
    <automated>npm test -w be -- --run tests/permissions/catalog-versioning.test.ts --reporter=dot</automated>
  </verify>
  <done>
    The backend SH1 test file exists, executes successfully on its own, and provides a reusable harness that 7.1 can extend for version-hash and emit assertions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Scaffold the frontend SH1 refresh harness</name>
  <files>
    fe/tests/features/permissions/permissionsCatalogRefresh.test.tsx
    fe/tests/features/permissions/permissionsApi.test.ts
    fe/tests/lib/permissions.test.tsx
  </files>
  <action>
    Create `fe/tests/features/permissions/permissionsCatalogRefresh.test.tsx` and extend the existing FE permissions tests so the SH1 refresh path has executable scaffolding before 7.3 begins. Set up the shared render/query/socket mocking utilities needed for runtime-provider assertions, keep the tests green against the current Phase 4 behavior, and structure the new file so 7.3 can layer in socket invalidation and polling assertions without recreating test infrastructure. Do not introduce skipped suites or future-task-only imports; this plan’s output must be immediately runnable.
  </action>
  <verify>
    <automated>npm run test:run:unit -w fe -- --reporter=dot tests/features/permissions/permissionsApi.test.ts</automated>
    <automated>npm run test:run:ui -w fe -- --reporter=dot tests/lib/permissions.test.tsx tests/features/permissions/permissionsCatalogRefresh.test.tsx</automated>
  </verify>
  <done>
    The FE SH1 refresh test file exists, related FE test utilities are in place, and both targeted FE commands run successfully before 7.3 extends the assertions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> permissions backend contract | The scaffold must exercise the real HTTP/service contract shape closely enough that later SH1 regressions catch drift. |
| test harness -> FE runtime catalog state | The scaffold must model authenticated refresh behavior without mutating runtime permission state through unrealistic shortcuts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-0-01 | T | backend SH1 scaffold | mitigate | Build the new test file on the existing permissions test utilities and real controller/service seams so later assertions cover the actual contract path. |
| T-07-0-02 | T | frontend SH1 scaffold | mitigate | Reuse the established FE permissions test files and query/provider setup instead of inventing a parallel harness that could drift from production wiring. |
| T-07-0-03 | D | targeted validation loop | mitigate | Keep each scaffold command isolated to the new SH1 files so Wave 0 feedback stays fast and deterministic. |
| T-07-0-04 | I | false confidence from inert tests | mitigate | Require the new BE and FE test files to execute green immediately, with no skipped-file placeholders or missing imports. |
</threat_model>

<verification>
- `npm test -w be -- --run tests/permissions/catalog-versioning.test.ts --reporter=dot`
- `npm run test:run:unit -w fe -- --reporter=dot tests/features/permissions/permissionsApi.test.ts`
- `npm run test:run:ui -w fe -- --reporter=dot tests/lib/permissions.test.tsx tests/features/permissions/permissionsCatalogRefresh.test.tsx`
</verification>

<success_criteria>
- The missing SH1 backend and frontend test files exist before any SH1 implementation plan runs.
- Every later SH1 task can reference a runnable automated command without relying on a future task in the same plan.
- Wave 0 closes the Nyquist dependency gap while preserving the existing Phase 7 scope.
</success_criteria>

<output>
After completion, create `.planning/phase-07-should-haves-versioning-expires-at/7.0-SUMMARY.md`
</output>
