---
phase: 07-should-haves-versioning-expires-at
plan: 7.2
type: execute
wave: 1
depends_on: []
files_modified:
  - be/src/shared/services/ability.service.ts
  - be/tests/shared/services/ability.service.test.ts
  - be/tests/permissions/tenant-isolation.test.ts
  - be/tests/permissions/grant-dataset-resolution.test.ts
autonomous: true
requirements:
  - SH2
must_haves:
  truths:
    - "Expired grants stop affecting effective permissions on the next server-side rebuild."
    - "Expired grant rows do not leak into the grant-derived dataset resolution path."
    - "Phase 7 does not introduce a cron sweeper for expiry handling."
  artifacts:
    - path: "be/tests/shared/services/ability.service.test.ts"
      provides: "Ability-builder regression coverage for expired inputs"
    - path: "be/tests/permissions/tenant-isolation.test.ts"
      provides: "DB-backed proof that expired grants are excluded by active read paths"
    - path: "be/tests/permissions/grant-dataset-resolution.test.ts"
      provides: "Grant-to-dataset regression coverage for expired grants"
  key_links:
    - from: "be/src/shared/services/ability.service.ts"
      to: "be/src/shared/models/resource-grant.model.ts"
      via: "findActiveForUser query-time filtering"
      pattern: "findActiveForUser"
    - from: "be/src/shared/services/ability.service.ts"
      to: "be/src/shared/models/user-permission-override.model.ts"
      via: "findActiveForUser query-time filtering"
      pattern: "userPermissionOverride\\.findActiveForUser"
---

<objective>
Close SH2 as a prove-and-tighten plan: verify that `expires_at` is already enforced on every active backend permission path and patch only the specific read path if execution finds a bypass.

Purpose: honor D-07 through D-11 by keeping query-time expiry enforcement authoritative, avoiding redundant rebuild work, and rejecting the old roadmap cron-sweeper branch.
Output: targeted backend regression coverage for expired grants and overrides, plus a narrowly scoped code fix only if testing exposes an active-path hole.
</objective>

<execution_context>
@/home/doana/.codex/get-shit-done/workflows/execute-plan.md
@/home/doana/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phase-07-should-haves-versioning-expires-at/7-CONTEXT.md
@.planning/phase-07-should-haves-versioning-expires-at/7-RESEARCH.md
@.planning/phase-06-legacy-cleanup-opensearch-integration/6.3-SUMMARY.md
@be/src/shared/services/ability.service.ts
@be/src/shared/models/resource-grant.model.ts
@be/src/shared/models/user-permission-override.model.ts
@be/tests/permissions/models.test.ts
@be/tests/permissions/tenant-isolation.test.ts
@be/tests/permissions/grant-dataset-resolution.test.ts

<interfaces>
From `be/src/shared/models/resource-grant.model.ts`:
```ts
async findActiveForUser(userId: string, tenantId: string, teamIds: readonly string[] = []): Promise<ResourceGrantRow[]>
```

From `be/src/shared/models/user-permission-override.model.ts`:
```ts
async findActiveForUser(userId: string, tenantId: string): Promise<UserPermissionOverrideRow[]>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove expired grants and overrides vanish from ability rebuilds</name>
  <read_first>
    - be/src/shared/services/ability.service.ts
    - be/src/shared/models/resource-grant.model.ts
    - be/src/shared/models/user-permission-override.model.ts
    - be/tests/shared/services/ability.service.test.ts
    - be/tests/permissions/tenant-isolation.test.ts
  </read_first>
  <files>
    be/src/shared/services/ability.service.ts
    be/tests/shared/services/ability.service.test.ts
    be/tests/permissions/tenant-isolation.test.ts
  </files>
  <acceptance_criteria>
    - `rg -n "expired|expires_at" be/tests/shared/services/ability.service.test.ts be/tests/permissions/tenant-isolation.test.ts` shows explicit expired-grant and expired-override coverage for the active V2 ability path.
    - If an active-path bypass exists, `rg -n "findActiveForUser" be/src/shared/services/ability.service.ts` still shows the fix reusing model-layer query-time filtering rather than adding JavaScript date filtering.
    - `npm test -w be -- --run tests/shared/services/ability.service.test.ts tests/permissions/tenant-isolation.test.ts --reporter=dot` exits 0.
  </acceptance_criteria>
  <behavior>
    - Test 1: expired resource grants do not produce CASL rules on the next `buildAbilityFor()` call.
    - Test 2: expired overrides do not produce allow or deny rules on the next `buildAbilityFor()` call.
    - Test 3: live future-dated rows still produce the expected rules.
  </behavior>
  <action>
    Extend `be/tests/shared/services/ability.service.test.ts` and `be/tests/permissions/tenant-isolation.test.ts` so SH2 is proven at both unit and DB-backed levels for the active V2 ability path. If execution finds an active permission path that bypasses `ResourceGrantModel.findActiveForUser()` or `UserPermissionOverrideModel.findActiveForUser()`, patch `be/src/shared/services/ability.service.ts` to route back through those model methods instead of layering duplicate JavaScript expiry logic. Do not add any cron job, scheduled cleanup, or row-deletion behavior in this task per D-07 and D-08.
  </action>
  <verify>
    <automated>npm test -w be -- --run tests/shared/services/ability.service.test.ts tests/permissions/tenant-isolation.test.ts --reporter=dot</automated>
  </verify>
  <done>
    Ability rebuild behavior is explicitly covered for expired-vs-live grants and overrides, and any active-path gap is fixed by reusing the existing SQL-filtered model reads instead of introducing duplicate expiry logic.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock grant-derived retrieval against expired rows</name>
  <read_first>
    - be/src/shared/services/ability.service.ts
    - .planning/phase-06-legacy-cleanup-opensearch-integration/6.3-SUMMARY.md
    - be/tests/permissions/grant-dataset-resolution.test.ts
  </read_first>
  <files>
    be/tests/permissions/grant-dataset-resolution.test.ts
    be/src/shared/services/ability.service.ts
  </files>
  <acceptance_criteria>
    - `rg -n "expiresAt|expired" be/tests/permissions/grant-dataset-resolution.test.ts` shows an assertion that expired grants do not expand into dataset IDs.
    - `rg -n "setInterval|node-cron|cron" be/src/shared/services/ability.service.ts be/tests/permissions/grant-dataset-resolution.test.ts` returns no matches.
    - `npm test -w be -- --run tests/permissions/grant-dataset-resolution.test.ts --reporter=dot` exits 0.
  </acceptance_criteria>
  <behavior>
    - Test 1: a live KB/category grant contributes dataset IDs.
    - Test 2: an expired grant contributes no dataset IDs.
    - Test 3: overlapping live and expired grants dedupe correctly and keep only active datasets.
  </behavior>
  <action>
    Extend `be/tests/permissions/grant-dataset-resolution.test.ts` to prove that `resolveGrantedDatasetsForUser()` keeps honoring the SQL-side `expires_at` filter introduced by the Phase 6 helper path. Only touch `be/src/shared/services/ability.service.ts` if the test exposes a concrete bypass in the retrieval-resolution flow. Keep SH2 strictly query-time enforced; explicitly avoid adding `node-cron`, sweepers, or delete-expired-row behavior because research confirmed those are unnecessary for this milestone.
  </action>
  <verify>
    <automated>npm test -w be -- --run tests/permissions/grant-dataset-resolution.test.ts --reporter=dot</automated>
  </verify>
  <done>
    Grant-derived dataset expansion has explicit expiry regression coverage and Phase 7 ships no cron/sweeper path for expired grants.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service layer -> model layer | Effective-permission reads must stay tenant-scoped and expiry-filtered when they cross from orchestration into SQL. |
| permission rules -> search dataset resolution | Grant-derived retrieval must not resurrect expired access through a secondary access path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-2-01 | E | `buildAbilityForV2` | mitigate | Verify and preserve the model-layer `findActiveForUser()` calls so expired rows never produce CASL rules after the next rebuild. |
| T-07-2-02 | I | `resolveGrantedDatasetsForUser` | mitigate | Add regression coverage proving expired grants do not expand to dataset IDs, preventing stale access through retrieval. |
| T-07-2-03 | T | expiry enforcement implementation | mitigate | Reject duplicated JavaScript time filtering and keep SQL `expires_at IS NULL OR expires_at > NOW()` as the single source of truth. |
| T-07-2-04 | D | cron/sweeper branch | accept | D-08 explicitly defers background cleanup; query-time filtering already satisfies SH2 without operational churn. |
</threat_model>

<verification>
- `npm test -w be -- --run tests/shared/services/ability.service.test.ts tests/permissions/tenant-isolation.test.ts tests/permissions/grant-dataset-resolution.test.ts --reporter=dot`
- `rg -n "setInterval|node-cron|cron" be/src/shared/services/ability.service.ts be/tests/permissions`
</verification>

<success_criteria>
- Expired grants and overrides are proven absent from the next ability rebuild.
- Expired grants are proven absent from the dataset-resolution path used by retrieval.
- No cron or sweeper code is introduced for Phase 7.
</success_criteria>

<output>
After completion, create `.planning/phase-07-should-haves-versioning-expires-at/7.2-SUMMARY.md`
</output>
