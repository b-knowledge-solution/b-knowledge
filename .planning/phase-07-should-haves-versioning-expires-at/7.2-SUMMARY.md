---
phase: 07-should-haves-versioning-expires-at
plan: 7.2
subsystem: backend
tags: [permissions, grants, ability, datasets, expiry]
requires:
  - phase: 06-legacy-cleanup-opensearch-integration
    provides: grant-derived dataset resolution helper that reuses active grant reads
provides:
  - explicit V2 ability-builder regression coverage for expired grants and overrides
  - scratch-DB proof that expired overrides and grants stay out of active permission reads
  - scratch-DB proof that expired grants do not re-expand into retrieval datasets
affects: [sh2, phase-07-plan-7.3]
tech-stack:
  added: []
  patterns: [sql-side expires_at enforcement, proof-only regression hardening]
key-files:
  created: []
  modified:
    - be/tests/shared/services/ability.service.test.ts
    - be/tests/permissions/tenant-isolation.test.ts
    - be/tests/permissions/grant-dataset-resolution.test.ts
key-decisions:
  - "Kept SQL-side findActiveForUser filtering as the single source of truth because no active-path bypass surfaced during verification."
  - "Shipped SH2 as proof-tightening only; no cron, sweeper, or duplicate JavaScript expiry logic was added."
patterns-established:
  - "Phase 7 expiry work should add regression coverage first and patch the service layer only if a failing path proves a bypass."
  - "Grant-derived dataset resolution must be validated with mixed live and expired rows in scratch-DB tests, not inferred from model tests alone."
requirements-completed: [SH2]
duration: 9 min
completed: 2026-04-09
---

# Phase 7 Plan 7.2: SH2 expiry enforcement proof for ability rebuilds and grant-derived dataset expansion

**Added backend regression proof that expired grants and overrides disappear on the next active read, without adding any sweeper or duplicate expiry logic.**

## Performance

- **Duration:** 9 min
- **Started:** 2026-04-09T12:31:00Z
- **Completed:** 2026-04-09T12:40:03Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Extended the unit-level ability service suite to prove the V2 builder only emits rules from active grant and override reads.
- Extended the scratch-DB tenant-isolation suite to prove expired allow and deny overrides vanish while future-dated overrides still affect the next rebuild.
- Extended the scratch-DB grant-dataset suite to prove mixed live and expired grants keep only active dataset IDs and still dedupe correctly.

## Task Commits

1. **Task 1: Prove expired grants and overrides vanish from ability rebuilds** - `3b4c229` (`test(7-2): prove expired inputs vanish from ability rebuilds`)
2. **Task 2: Lock grant-derived retrieval against expired rows** - `f78a525` (`test(7-2): lock dataset resolution against expired grants`)

## Files Created/Modified

- `be/tests/shared/services/ability.service.test.ts` - Added V2 builder regressions for expired grant/override omission and future-dated active rows.
- `be/tests/permissions/tenant-isolation.test.ts` - Added scratch-DB coverage for expired allow and deny overrides on the active V2 ability path.
- `be/tests/permissions/grant-dataset-resolution.test.ts` - Added mixed live-plus-expired grant coverage for retrieval dataset expansion.

## Decisions Made

- Reused the existing SQL-filtered `findActiveForUser()` model reads as-is because verification found no service-layer bypass to patch.
- Treated the TDD “green before code change” outcome as confirmation that SH2 behavior was already implemented; the task shipped as regression hardening rather than a production code change.

## Deviations from Plan

None - plan executed exactly as written, and no Rule 1-4 deviation was needed.

## Issues Encountered

- A transient `.git/index.lock` error interrupted one staging attempt, but retrying the targeted `git add` succeeded and did not affect the code or commit contents.

## User Setup Required

None.

## Next Phase Readiness

- SH2 is now backed by explicit unit and scratch-DB evidence across both the ability rebuild path and the grant-derived retrieval path.
- No follow-up code change is required unless a future phase introduces a new permission read path that bypasses the existing model helpers.

## Known Stubs

None.

## Self-Check: PASSED

- Summary file path verified: `.planning/phase-07-should-haves-versioning-expires-at/7.2-SUMMARY.md`
- Commit `3b4c229` verified in git history.
- Commit `f78a525` verified in git history.
