---
phase: 01-migrate-agent-features-from-ragflow-to-b-knowledge
plan: 04
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - fe/src/features/agents/api/agentApi.ts
  - fe/src/features/agents/api/agentQueries.ts
  - fe/src/features/agents/pages/AgentListPage.tsx
  - fe/src/features/agents/components/AgentCard.tsx
  - fe/src/features/agents/components/TemplateGallery.tsx
  - fe/src/features/chat/components/CreateChatAssistantDialog.tsx
  - fe/src/features/search/components/CreateSearchAppDialog.tsx
  - fe/src/app/App.tsx
  - fe/src/app/routeConfig.ts
  - fe/src/layouts/sidebarNav.ts
  - fe/src/i18n/locales/en.json
  - fe/src/i18n/locales/vi.json
  - fe/src/i18n/locales/ja.json
autonomous: true
requirements:
  - AGENT-LIST-UI
  - AGENT-FIRST-UNIFICATION
must_haves:
  truths:
    - "User can see a list of agents in a card grid layout"
    - "User can create a new agent via Create Agent button"
    - "User can navigate to Agents from the sidebar"
    - "Empty state shows Create Agent and Browse Templates CTAs"
    - "Chat assistant creation offers 'Create as Agent Workflow' option"
    - "Search app creation offers 'Create as Agent Workflow' option"
  artifacts:
    - path: "fe/src/features/agents/pages/AgentListPage.tsx"
      provides: "Agent list page with card grid, tabs, search"
      exports: ["AgentListPage"]
    - path: "fe/src/features/agents/api/agentApi.ts"
      provides: "Raw HTTP API calls for agents"
      exports: ["agentApi"]
    - path: "fe/src/features/agents/api/agentQueries.ts"
      provides: "TanStack Query hooks for agents"
      exports: ["useAgents", "useAgent", "useCreateAgent"]
  key_links:
    - from: "fe/src/layouts/sidebarNav.ts"
      to: "fe/src/features/agents/pages/AgentListPage.tsx"
      via: "sidebar nav entry path /agents"
      pattern: "path.*'/agents'"
    - from: "fe/src/app/App.tsx"
      to: "fe/src/features/agents/pages/AgentListPage.tsx"
      via: "React Router route definition"
      pattern: "AgentListPage"
    - from: "fe/src/features/chat/components/CreateChatAssistantDialog.tsx"
      to: "/agents/new?mode=chat"
      via: "navigation link for agent-first creation"
      pattern: "agents/new.*mode=chat"
---

<objective>
Build the agent API layer, list page UI, wire into navigation, and add agent-first unification entry points to chat/search creation flows.

Purpose: Give users the entry point to the agent system: a browsable list of agents with creation, duplication, deletion, and template browsing. Also implements the locked decision from CONTEXT.md: "new chat assistants and search apps created via agent canvas (agent-first)" by adding an advanced creation option in chat/search dialogs.

Output: API layer (agentApi.ts + agentQueries.ts), AgentListPage, AgentCard, TemplateGallery, sidebar nav entry, route config, i18n strings, agent-first buttons in chat/search creation.
</objective>

<execution_context>
@/home/doana/.claude/get-shit-done/workflows/execute-plan.md
@/home/doana/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-migrate-agent-features-from-ragflow-to-b-knowledge/01-CONTEXT.md
@.planning/phases/01-migrate-agent-features-from-ragflow-to-b-knowledge/01-UI-SPEC.md
@.planning/phases/01-migrate-agent-features-from-ragflow-to-b-knowledge/01-02-SUMMARY.md

<interfaces>
From fe/src/features/agents/types/agent.types.ts (Plan 02):
- Agent, AgentTemplate, CreateAgentDto, UpdateAgentDto interfaces
- AgentMode, AgentStatus types

From fe/src/lib/queryKeys.ts (Plan 02):
- queryKeys.agents.all, .lists(), .list(filters), .details(), .detail(id), .templates()

From fe/src/features/projects/api/projectApi.ts (existing pattern):
- api.get<T>(), api.post<T>(), api.put<T>(), api.delete() pattern
- PaginatedResponse<T> type

From fe/src/features/projects/pages/ProjectListPage.tsx (reference for list page pattern):
- Card grid layout, search bar, tabs, empty state, kebab menu actions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create agent API layer and TanStack Query hooks</name>
  <files>
    fe/src/features/agents/api/agentApi.ts
    fe/src/features/agents/api/agentQueries.ts
  </files>
  <read_first>
    fe/src/features/agents/types/agent.types.ts
    fe/src/lib/queryKeys.ts
    fe/src/features/projects/api/projectApi.ts
    fe/src/features/projects/api/projectQueries.ts
    fe/src/lib/api.ts
  </read_first>
  <action>
**agentApi.ts — Raw HTTP calls (NO React hooks):**

```typescript
import { api } from '@/lib/api'
import type { Agent, AgentTemplate, CreateAgentDto, UpdateAgentDto, AgentRun } from '../types/agent.types'

interface PaginatedResponse<T> {
  data: T[]
  total: number
  page: number
  page_size: number
}

interface ListAgentsParams {
  mode?: 'agent' | 'pipeline'
  status?: 'draft' | 'published'
  project_id?: string
  page?: number
  page_size?: number
  search?: string
}

export const agentApi = {
  list: (params?: ListAgentsParams) =>
    api.get<PaginatedResponse<Agent>>('/api/agents', { params }),

  getById: (id: string) =>
    api.get<Agent>(`/api/agents/${id}`),

  create: (data: CreateAgentDto) =>
    api.post<Agent>('/api/agents', data),

  update: (id: string, data: UpdateAgentDto) =>
    api.put<Agent>(`/api/agents/${id}`, data),

  delete: (id: string) =>
    api.delete(`/api/agents/${id}`),

  duplicate: (id: string) =>
    api.post<Agent>(`/api/agents/${id}/duplicate`),

  exportJson: (id: string) =>
    api.get<Agent>(`/api/agents/${id}/export`),

  // Versioning
  saveVersion: (id: string, data?: { version_label?: string }) =>
    api.post<{ version_number: number }>(`/api/agents/${id}/versions`, data),

  listVersions: (id: string) =>
    api.get<Agent[]>(`/api/agents/${id}/versions`),

  restoreVersion: (id: string, versionId: string) =>
    api.post<Agent>(`/api/agents/${id}/versions/${versionId}/restore`),

  deleteVersion: (id: string, versionId: string) =>
    api.delete(`/api/agents/${id}/versions/${versionId}`),

  // Templates
  listTemplates: () =>
    api.get<AgentTemplate[]>('/api/agents/templates'),

  // Runs
  listRuns: (agentId: string) =>
    api.get<AgentRun[]>(`/api/agents/${agentId}/runs`),
}
```

**agentQueries.ts — TanStack Query hooks:**

Implement hooks using queryKeys from lib/queryKeys.ts:
- `useAgents(filters)` — useQuery wrapping agentApi.list
- `useAgent(id)` — useQuery wrapping agentApi.getById, enabled only when id is truthy
- `useAgentVersions(id)` — useQuery wrapping agentApi.listVersions
- `useAgentRuns(agentId)` — useQuery wrapping agentApi.listRuns
- `useAgentTemplates()` — useQuery wrapping agentApi.listTemplates
- `useCreateAgent()` — useMutation wrapping agentApi.create, invalidates queryKeys.agents.lists()
- `useUpdateAgent()` — useMutation wrapping agentApi.update, invalidates queryKeys.agents.detail(id) + lists()
- `useDeleteAgent()` — useMutation wrapping agentApi.delete, invalidates queryKeys.agents.lists()
- `useDuplicateAgent()` — useMutation wrapping agentApi.duplicate, invalidates queryKeys.agents.lists()
- `useSaveVersion()` — useMutation wrapping agentApi.saveVersion
- `useRestoreVersion()` — useMutation wrapping agentApi.restoreVersion
- `useDeleteVersion()` — useMutation wrapping agentApi.deleteVersion

All hooks must have JSDoc with @description and @returns.
  </action>
  <verify>
    <automated>cd "/mnt/d/Project/b-solution/b-knowledge - Copy" && npx tsc --noEmit -p fe/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - fe/src/features/agents/api/agentApi.ts contains `export const agentApi`
    - fe/src/features/agents/api/agentApi.ts contains `api.get`
    - fe/src/features/agents/api/agentApi.ts contains `api.post`
    - fe/src/features/agents/api/agentApi.ts contains `/api/agents`
    - fe/src/features/agents/api/agentQueries.ts contains `useAgents`
    - fe/src/features/agents/api/agentQueries.ts contains `useCreateAgent`
    - fe/src/features/agents/api/agentQueries.ts contains `useDeleteAgent`
    - fe/src/features/agents/api/agentQueries.ts contains `useSaveVersion`
    - fe/src/features/agents/api/agentQueries.ts contains `queryKeys.agents`
  </acceptance_criteria>
  <done>Agent API layer with raw HTTP calls and 12 TanStack Query hooks for all CRUD + versioning operations</done>
</task>

<task type="auto">
  <name>Task 2: Create AgentListPage with navigation, routing, i18n, and agent-first unification entry points</name>
  <files>
    fe/src/features/agents/pages/AgentListPage.tsx
    fe/src/features/agents/components/AgentCard.tsx
    fe/src/features/agents/components/TemplateGallery.tsx
    fe/src/features/chat/components/CreateChatAssistantDialog.tsx
    fe/src/features/search/components/CreateSearchAppDialog.tsx
    fe/src/app/App.tsx
    fe/src/app/routeConfig.ts
    fe/src/layouts/sidebarNav.ts
    fe/src/i18n/locales/en.json
    fe/src/i18n/locales/vi.json
    fe/src/i18n/locales/ja.json
  </files>
  <read_first>
    fe/src/features/projects/pages/ProjectListPage.tsx
    fe/src/features/datasets/pages/DatasetListPage.tsx
    fe/src/app/App.tsx
    fe/src/app/routeConfig.ts
    fe/src/layouts/sidebarNav.ts
    fe/src/i18n/locales/en.json
    fe/src/features/agents/api/agentQueries.ts
    fe/src/features/agents/types/agent.types.ts
    fe/src/features/chat/components/CreateChatAssistantDialog.tsx
    fe/src/features/search/components/CreateSearchAppDialog.tsx
  </read_first>
  <action>
**AgentCard.tsx** — Card component for agent list items:
- Displays: agent name, mode badge (Agent/Pipeline), status badge (Draft/Published), description (truncated 2 lines), last modified date
- Kebab dropdown menu with actions: Edit (navigates to /agents/:id), Duplicate, Delete, Export JSON
- Delete action shows confirmation dialog requiring typing agent name (per UI-SPEC)
- Uses shadcn Card, Badge, DropdownMenu, Button
- Category color accent on left border based on mode
- JSDoc on component and all handlers

**TemplateGallery.tsx** — Template picker grid:
- Grid of template cards (3 cols desktop, 2 tablet, 1 mobile)
- Each card shows: template name, description, category badge, mode badge
- "Use Template" button on each card triggers createAgent with template_id
- Uses useAgentTemplates() hook
- Skeleton loading state
- JSDoc on component

**AgentListPage.tsx** — Main page per UI-SPEC layout:
- Heading: "Agents" with "Create Agent" primary button (accent color) top-right
- Tabs: "All" | "My Agents" | "Templates"
  - All tab: shows all agents in card grid
  - My Agents tab: filters by created_by = current user
  - Templates tab: shows TemplateGallery
- Search bar below tabs
- Mode filter dropdown (All / Agent / Pipeline)
- Card grid: 3 columns on desktop (>1280px), 2 (>768px), 1 (mobile) per UI-SPEC
- Empty state: heading "No agents yet", body "Build your first AI agent workflow. Start from scratch or pick a template.", actions "Create Agent" (primary) + "Browse Templates" (secondary) per UI-SPEC copywriting contract
- Loading: Skeleton cards
- Uses useAgents() with URL state for filters/pagination (useSearchParams)
- Create Agent button opens a simple dialog with name, description, mode fields (CreateAgentDto), then navigates to /agents/:id on success
- JSDoc on component

**App.tsx** — Add route:
```tsx
<Route path="/agents" element={<AgentListPage />} />
<Route path="/agents/:id" element={<AgentCanvasPage />} />  // placeholder lazy import
```
Wrap with FeatureErrorBoundary. Use lazy import for AgentListPage.

**routeConfig.ts** — Add route metadata:
```typescript
'/agents': { title: 'agents.pageTitle', featureId: 'agents', layout: 'main' },
'/agents/:id': { title: 'agents.canvasTitle', featureId: 'agents', layout: 'canvas' },
```

**sidebarNav.ts** — Add Agents as top-level nav item (per CONTEXT.md "first-class feature alongside Chat, Search, Datasets, Projects"):
Insert BEFORE the Chat entry:
```typescript
{
  path: '/agents',
  labelKey: 'nav.agents',
  icon: Workflow,  // from lucide-react
  iconSize: 20,
},
```
Import `Workflow` from lucide-react at the top.

**Agent-first unification entry points (per CONTEXT.md locked decision):**

Update **CreateChatAssistantDialog.tsx** (or equivalent chat creation component):
- Add a secondary action at the bottom of the creation dialog: a link/button labeled "Create as Agent Workflow (Advanced)" with a `Workflow` icon from lucide-react
- On click, navigate to `/agents/new?mode=chat` which will pre-configure the agent canvas with a chat-oriented template (Begin -> Generate -> Answer nodes)
- Style as a subtle link below the main "Create" button, not competing with the primary flow
- Tooltip or description: "Build a multi-step AI workflow with the visual agent canvas"

Update **CreateSearchAppDialog.tsx** (or equivalent search creation component):
- Same pattern: add "Create as Agent Workflow (Advanced)" link at the bottom
- Navigate to `/agents/new?mode=search` which pre-configures with a search-oriented template (Begin -> Retrieval -> Generate -> Answer nodes)
- Same subtle styling as in the chat dialog

This implements the CONTEXT.md locked decision: "new chat assistants and search apps created via agent canvas (agent-first). Existing ones keep legacy UI with gradual transition path." The existing simple creation flows are preserved as-is (quick setup), while the agent canvas is offered as the advanced alternative.

**i18n — Add keys to all 3 locales:**

en.json:
```json
"nav.agents": "Agents",
"agents.pageTitle": "Agents",
"agents.canvasTitle": "Agent Canvas",
"agents.createAgent": "Create Agent",
"agents.noAgentsYet": "No agents yet",
"agents.emptyDescription": "Build your first AI agent workflow. Start from scratch or pick a template.",
"agents.browseTemplates": "Browse Templates",
"agents.deleteAgent": "Delete Agent",
"agents.deleteConfirmation": "This will permanently delete \"{{name}}\" and all its run history. This cannot be undone.",
"agents.typeToConfirm": "Type the agent name to confirm",
"agents.duplicate": "Duplicate",
"agents.exportJson": "Export JSON",
"agents.edit": "Edit",
"agents.all": "All",
"agents.myAgents": "My Agents",
"agents.templates": "Templates",
"agents.agent": "Agent",
"agents.pipeline": "Pipeline",
"agents.draft": "Draft",
"agents.published": "Published",
"agents.searchPlaceholder": "Search agents...",
"agents.modeFilter": "Mode",
"agents.useTemplate": "Use Template",
"agents.agentSaved": "Agent saved",
"agents.agentCreated": "Agent created",
"agents.agentDeleted": "Agent deleted",
"agents.agentDuplicated": "Agent duplicated",
"agents.createAsAgentWorkflow": "Create as Agent Workflow (Advanced)",
"agents.agentWorkflowDescription": "Build a multi-step AI workflow with the visual agent canvas"
```

vi.json (Vietnamese):
```json
"nav.agents": "Tac tu",
"agents.pageTitle": "Tac tu",
"agents.createAgent": "Tao tac tu",
"agents.noAgentsYet": "Chua co tac tu nao",
"agents.emptyDescription": "Xay dung quy trinh AI dau tien cua ban. Bat dau tu dau hoac chon mau.",
"agents.browseTemplates": "Duyet mau",
"agents.createAsAgentWorkflow": "Tao bang quy trinh Agent (Nang cao)",
"agents.agentWorkflowDescription": "Xay dung quy trinh AI da buoc voi canvas agent truc quan",
... (all keys translated)
```

ja.json (Japanese):
```json
"nav.agents": "エージェント",
"agents.pageTitle": "エージェント",
"agents.createAgent": "エージェント作成",
"agents.noAgentsYet": "エージェントがありません",
"agents.emptyDescription": "最初のAIエージェントワークフローを構築しましょう。ゼロから始めるか、テンプレートを選択してください。",
"agents.browseTemplates": "テンプレートを見る",
"agents.createAsAgentWorkflow": "エージェントワークフローとして作成（上級）",
"agents.agentWorkflowDescription": "ビジュアルエージェントキャンバスで多段階AIワークフローを構築",
... (all keys translated)
```
  </action>
  <verify>
    <automated>cd "/mnt/d/Project/b-solution/b-knowledge - Copy" && npx tsc --noEmit -p fe/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - fe/src/features/agents/pages/AgentListPage.tsx contains `export.*AgentListPage`
    - fe/src/features/agents/pages/AgentListPage.tsx contains `useAgents`
    - fe/src/features/agents/pages/AgentListPage.tsx contains `No agents yet`  or `agents.noAgentsYet`
    - fe/src/features/agents/components/AgentCard.tsx contains `export.*AgentCard`
    - fe/src/features/agents/components/AgentCard.tsx contains `DropdownMenu`
    - fe/src/features/agents/components/TemplateGallery.tsx contains `export.*TemplateGallery`
    - fe/src/features/agents/components/TemplateGallery.tsx contains `useAgentTemplates`
    - fe/src/app/App.tsx contains `AgentListPage`
    - fe/src/app/App.tsx contains `path="/agents"`
    - fe/src/app/routeConfig.ts contains `'/agents'`
    - fe/src/layouts/sidebarNav.ts contains `nav.agents`
    - fe/src/layouts/sidebarNav.ts contains `Workflow`
    - fe/src/i18n/locales/en.json contains `agents.createAgent`
    - fe/src/i18n/locales/en.json contains `agents.createAsAgentWorkflow`
    - fe/src/i18n/locales/ja.json contains `エージェント`
    - fe/src/i18n/locales/vi.json contains `nav.agents`
    - fe/src/features/chat/components/CreateChatAssistantDialog.tsx contains `agents/new.*mode=chat`  or `createAsAgentWorkflow`
    - fe/src/features/search/components/CreateSearchAppDialog.tsx contains `agents/new.*mode=search`  or `createAsAgentWorkflow`
  </acceptance_criteria>
  <done>Agent list page with card grid, tabs, search, empty state, sidebar nav, routing, i18n for all 3 locales, and agent-first unification buttons in chat/search creation dialogs</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `npx tsc --noEmit -p fe/tsconfig.json`
- Agents visible in sidebar navigation
- Route /agents renders AgentListPage
- All i18n keys present in en, vi, ja locales
- Chat creation dialog has "Create as Agent Workflow (Advanced)" link navigating to /agents/new?mode=chat
- Search creation dialog has "Create as Agent Workflow (Advanced)" link navigating to /agents/new?mode=search
</verification>

<success_criteria>
- Complete agent list page with card grid, tabs (All/My Agents/Templates), search, mode filter
- API layer with all CRUD + versioning HTTP calls and TanStack Query hooks
- Sidebar navigation entry for Agents
- i18n strings in all 3 locales
- Agent-first unification: chat and search creation flows offer agent canvas as advanced option
</success_criteria>

<output>
After completion, create `.planning/phases/01-migrate-agent-features-from-ragflow-to-b-knowledge/01-04-SUMMARY.md`
</output>
