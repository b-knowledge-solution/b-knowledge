---
phase: 01-migrate-latest-ragflow-upstream-to-b-knowledge
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - advance-rag/pyproject.toml
  - advance-rag/db/db_models.py
  - advance-rag/db/services/common_service.py
  - advance-rag/db/services/document_service.py
  - advance-rag/db/services/canvas_service.py
  - advance-rag/db/services/user_canvas_version.py
  - advance-rag/db/services/doc_metadata_service.py
  - advance-rag/.env.example
  - be/src/shared/db/migrations/20260323140000_add_ragflow_upstream_columns.ts
autonomous: true
requirements:
  - DEP-UPDATE
  - DB-MIGRATION
  - FEATURE-PORT

must_haves:
  truths:
    - "pyproject.toml has pypdf>=6.8.0 and ebooklib>=0.18"
    - "Knex migration creates release column on user_canvas_version and version_title on api_4_conversation"
    - "Peewee db_models.py has matching field definitions for release and version_title"
    - "db/services/ files have upstream improvements (deadlock retry, release flag, metadata optimization)"
  artifacts:
    - path: "be/src/shared/db/migrations/20260323140000_add_ragflow_upstream_columns.ts"
      provides: "Knex migration for 2 new upstream columns"
      exports: ["up", "down"]
    - path: "advance-rag/pyproject.toml"
      provides: "Updated Python dependencies"
      contains: "pypdf"
  key_links:
    - from: "be/src/shared/db/migrations/20260323140000_add_ragflow_upstream_columns.ts"
      to: "user_canvas_version table"
      via: "knex.schema.alterTable"
      pattern: "user_canvas_version.*release"
    - from: "advance-rag/db/db_models.py"
      to: "user_canvas_version table"
      via: "Peewee model field"
      pattern: "release.*BooleanField"
---

<objective>
Tier 3: Integration layer updates — dependency bumps, Knex migrations for new columns, Peewee model field additions, and db/services/ upstream improvements.

Purpose: Update the b-knowledge integration layer to support new upstream features. This includes Python dependency updates, database schema changes (via Knex, per D-06/D-07), Peewee model field additions to match, and porting upstream improvements to protected db/services/ files.

Output: Updated pyproject.toml, new Knex migration, updated Peewee models, improved db/services/ with deadlock retry and release flag support.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-migrate-latest-ragflow-upstream-to-b-knowledge/01-01-SUMMARY.md
@.planning/phases/01-migrate-latest-ragflow-upstream-to-b-knowledge/01-02-SUMMARY.md
@patches/ragflow-port-v0.24.0-c732a1c.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Python dependencies and Peewee models, create Knex migration</name>
  <files>
    advance-rag/pyproject.toml
    advance-rag/db/db_models.py
    advance-rag/.env.example
    be/src/shared/db/migrations/20260323140000_add_ragflow_upstream_columns.ts
  </files>
  <read_first>
    advance-rag/pyproject.toml
    advance-rag/db/db_models.py
    advance-rag/.env.example
    be/src/shared/db/migrations/20260323120000_standardize_uuid_format.ts
    ragflow/api/db/db_models.py
  </read_first>
  <action>
**1. Update advance-rag/pyproject.toml** (per D-04, DEP-UPDATE):
- Find the `pypdf` dependency line and change version constraint from `>=4.0.0` (or whatever current) to `>=6.8.0`
- Add `ebooklib>=0.18` to the dependencies list (required for new EPUB parser)
- Do NOT change any other dependencies — only these two

**2. Create Knex migration** (per D-06, D-07):
Create `be/src/shared/db/migrations/20260323140000_add_ragflow_upstream_columns.ts`:
```typescript
import type { Knex } from 'knex'

/**
 * @description Adds upstream RAGFlow columns: release flag for canvas versions
 * and version_title for conversation sessions.
 */
export async function up(knex: Knex): Promise<void> {
  // Add release flag to canvas versions for published version tracking
  await knex.schema.alterTable('user_canvas_version', (table) => {
    table.boolean('release').notNullable().defaultTo(false).index()
  })

  // Add version title to conversation sessions for agent version labeling
  await knex.schema.alterTable('api_4_conversation', (table) => {
    table.string('version_title', 255).nullable()
  })
}

export async function down(knex: Knex): Promise<void> {
  await knex.schema.alterTable('user_canvas_version', (table) => {
    table.dropColumn('release')
  })
  await knex.schema.alterTable('api_4_conversation', (table) => {
    table.dropColumn('version_title')
  })
}
```

**3. Update Peewee db_models.py** (per Pitfall 3):
In advance-rag/db/db_models.py, find the `UserCanvasVersion` model class and add:
```python
release = BooleanField(default=False, index=True)
```
Find the `API4Conversation` model class and add:
```python
version_title = CharField(max_length=255, null=True)
```
Do NOT add or modify any Peewee auto-migration functions — Knex handles DDL (per D-07, Pitfall 6).

**4. Update advance-rag/.env.example** (per Open Question 3):
Add this line (with comment) near the other optional config vars:
```
# Docling server URL for remote Docling deployment (optional)
# DOCLING_SERVER_URL=http://localhost:5001
```
  </action>
  <verify>
    <automated>grep "pypdf.*6.8" advance-rag/pyproject.toml && grep "ebooklib" advance-rag/pyproject.toml && grep "release.*BooleanField" advance-rag/db/db_models.py && grep "version_title.*CharField" advance-rag/db/db_models.py && ls be/src/shared/db/migrations/20260323140000_add_ragflow_upstream_columns.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - advance-rag/pyproject.toml contains "pypdf" with version constraint including "6.8"
    - advance-rag/pyproject.toml contains "ebooklib"
    - advance-rag/db/db_models.py contains "release = BooleanField"
    - advance-rag/db/db_models.py contains "version_title = CharField"
    - be/src/shared/db/migrations/20260323140000_add_ragflow_upstream_columns.ts exists
    - Migration file contains "user_canvas_version" and "release"
    - Migration file contains "api_4_conversation" and "version_title"
    - Migration file exports both "up" and "down" functions
    - advance-rag/.env.example contains "DOCLING_SERVER_URL"
  </acceptance_criteria>
  <done>pyproject.toml updated with pypdf>=6.8.0 and ebooklib>=0.18. Knex migration created for 2 new columns. Peewee model fields added. .env.example updated with DOCLING_SERVER_URL.</done>
</task>

<task type="auto">
  <name>Task 2: Port upstream improvements to protected db/services/ files</name>
  <files>
    advance-rag/db/services/common_service.py
    advance-rag/db/services/document_service.py
    advance-rag/db/services/canvas_service.py
    advance-rag/db/services/user_canvas_version.py
    advance-rag/db/services/doc_metadata_service.py
  </files>
  <read_first>
    advance-rag/db/services/common_service.py
    advance-rag/db/services/document_service.py
    advance-rag/db/services/canvas_service.py
    advance-rag/db/services/doc_metadata_service.py
    ragflow/api/db/services/common_service.py
    ragflow/api/db/services/document_service.py
    ragflow/api/db/services/canvas_service.py
    ragflow/api/db/services/doc_metadata_service.py
  </read_first>
  <action>
Per D-03 (protected files — NEVER overwrite) and D-04 (port ALL features):

These files are in the protected `advance-rag/db/` directory. Do NOT copy upstream files over them. Instead, read the upstream version, identify new/changed code, and manually port the improvements into b-knowledge's versions.

**common_service.py — Add deadlock retry decorator:**
Read ragflow/api/db/services/common_service.py and find the `retry_deadlock_operation` decorator function (~32 lines). Copy this function into advance-rag/db/services/common_service.py. It requires:
- `from functools import wraps`
- `import time`
- `from peewee import OperationalError`
The function uses `_is_deadlock_error(e)` helper to check if the OperationalError is a deadlock — port that helper too.

**document_service.py — Add metadata query optimization and deadlock retry import:**
- Add import: `from db.services.common_service import retry_deadlock_operation` (if used)
- Port the aggregated parsing status method: a class method that does `GROUP BY run` on documents for a given kb_id, returns dict of status counts
- Port metadata query optimization: scope metadata queries to page-level document IDs instead of scanning all docs

**canvas_service.py — Add release flag support:**
- Add method to query latest released version: filter `UserCanvasVersion` where `release=True`, order by `create_time DESC`
- Add method to set release flag on a version
- Add version_title support to conversation creation queries

**doc_metadata_service.py — Port simplified metadata queries:**
- Read upstream changes (net -152 lines refactor)
- Port applicable simplifications to b-knowledge's version
- This is a refactor, not new feature — apply carefully

**For each file:** Translate any upstream `from api.db.*` imports to `from db.*` in the new code being added. Add Google-style docstrings to all new functions per project conventions.

**Check for user_canvas_version.py:** If ragflow/api/db/services/ has this file with release flag logic (protect released versions from overwrite), check if advance-rag/db/services/ has it. If not, check if the logic exists in canvas_service.py instead. Port the "protect released versions" guard wherever it fits in b-knowledge's structure.
  </action>
  <verify>
    <automated>cd advance-rag && grep "retry_deadlock_operation" db/services/common_service.py && grep -c "from api.db" db/services/common_service.py db/services/document_service.py db/services/canvas_service.py db/services/doc_metadata_service.py 2>/dev/null | grep -v ":0$" && echo "FAIL: found api.db imports" || echo "PASS: no api.db imports in db/services/"</automated>
  </verify>
  <acceptance_criteria>
    - advance-rag/db/services/common_service.py contains "def retry_deadlock_operation" (deadlock retry decorator)
    - advance-rag/db/services/common_service.py contains "_is_deadlock_error" (helper function)
    - advance-rag/db/services/canvas_service.py contains "release" (release flag support)
    - No file in advance-rag/db/services/ contains "from api.db." (all imports use b-knowledge pattern)
    - All new functions have Google-style docstrings (""" lines)
  </acceptance_criteria>
  <done>Protected db/services/ files updated with upstream improvements: deadlock retry decorator, aggregated parsing status, release flag support, metadata query optimization. All imports use b-knowledge db.services pattern.</done>
</task>

</tasks>

<verification>
- `grep "pypdf.*6.8" advance-rag/pyproject.toml` — dependency bumped
- `grep "ebooklib" advance-rag/pyproject.toml` — new dep added
- `npm run build --workspace=be` — TypeScript compilation passes with new migration
- `grep "retry_deadlock_operation" advance-rag/db/services/common_service.py` — deadlock retry present
- `grep "release" advance-rag/db/services/canvas_service.py` — release flag support
</verification>

<success_criteria>
Python deps updated (pypdf>=6.8.0, ebooklib>=0.18). Knex migration for 2 new columns created and compiles. Peewee model fields match. Protected db/services/ files have upstream improvements (deadlock retry, release flag, metadata optimization). No api.db imports anywhere.
</success_criteria>

<output>
After completion, create `.planning/phases/01-migrate-latest-ragflow-upstream-to-b-knowledge/01-03-SUMMARY.md`
</output>
