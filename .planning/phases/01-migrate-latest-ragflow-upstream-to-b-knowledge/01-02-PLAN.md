---
phase: 01-migrate-latest-ragflow-upstream-to-b-knowledge
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - advance-rag/rag/app/naive.py
  - advance-rag/rag/app/manual.py
  - advance-rag/rag/app/picture.py
  - advance-rag/rag/app/qa.py
  - advance-rag/rag/svr/task_executor.py
  - advance-rag/rag/graphrag/general/extractor.py
  - advance-rag/rag/graphrag/search.py
  - advance-rag/rag/raptor.py
  - advance-rag/rag/svr/discord_svr.py
  - advance-rag/rag/svr/sync_data_source.py
autonomous: true
requirements:
  - MANUAL-MERGE

must_haves:
  truths:
    - "All merged files use 'from db.services.*' imports (not 'from api.db.services.*')"
    - "All upstream changes (EPUB registration, removed get_picture, cross-KB guard, deadlock retry, Docling callback guard) are present"
    - "Python import smoke test passes for all merged files"
  artifacts:
    - path: "advance-rag/rag/app/naive.py"
      provides: "Main parser with EPUB support and db.services imports"
      contains: "EpubParser"
    - path: "advance-rag/rag/svr/task_executor.py"
      provides: "Task executor with cross-KB collision guard and db.services imports"
      contains: "doc_name_by_id"
  key_links:
    - from: "advance-rag/rag/app/naive.py"
      to: "advance-rag/db/services/llm_service.py"
      via: "from db.services.llm_service import"
      pattern: "from db\\.services\\."
    - from: "advance-rag/rag/svr/task_executor.py"
      to: "advance-rag/db/services/"
      via: "from db.services.* imports"
      pattern: "from db\\.services\\."
---

<objective>
Tier 2: Manual merge of ~10 b-knowledge-modified RAGFlow files using the hybrid approach.

Purpose: These files have both upstream changes AND b-knowledge-specific modifications (primarily `from db.services.*` import patterns instead of upstream's `from api.db.services.*`). They need careful merging to get upstream improvements while preserving b-knowledge integration.

Output: All ~10 modified files updated with upstream changes, b-knowledge import patterns preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-migrate-latest-ragflow-upstream-to-b-knowledge/01-01-SUMMARY.md
@patches/ragflow-port-v0.24.0-c732a1c.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge rag/app/ files (naive.py, manual.py, picture.py, qa.py)</name>
  <files>
    advance-rag/rag/app/naive.py
    advance-rag/rag/app/manual.py
    advance-rag/rag/app/picture.py
    advance-rag/rag/app/qa.py
  </files>
  <read_first>
    advance-rag/rag/app/naive.py
    advance-rag/rag/app/manual.py
    advance-rag/rag/app/picture.py
    advance-rag/rag/app/qa.py
    ragflow/rag/app/naive.py
    ragflow/rag/app/manual.py
    ragflow/rag/app/picture.py
    ragflow/rag/app/qa.py
  </read_first>
  <action>
Per D-01 (hybrid approach: copy upstream, then restore b-knowledge modifications):

For EACH file in rag/app/ (naive.py, manual.py, picture.py, qa.py):

1. Read the current b-knowledge version — note all `from db.services.*` and `from db.db_models` and `from db.joint_services.*` import lines.
2. Read the upstream ragflow/ version — identify the changes from c732a1c to df2cc32f5.
3. Copy upstream version over the b-knowledge version.
4. **Translate ALL imports** (per Pitfall 2):
   - `from api.db.services.llm_service` -> `from db.services.llm_service`
   - `from api.db.services.document_service` -> `from db.services.document_service`
   - `from api.db.services.knowledgebase_service` -> `from db.services.knowledgebase_service`
   - `from api.db.db_models` -> `from db.db_models`
   - `from api.db.joint_services.tenant_model_service` -> `from db.joint_services.tenant_model_service`
   - ANY other `from api.db.*` -> `from db.*`
5. Re-add any b-knowledge-specific docstrings that were in the original b-knowledge version.

**Specific changes per file:**

**naive.py:**
- Upstream adds EPUB support (elif block after HTML): `elif re.search(r"\.epub$", filename, re.IGNORECASE):` with EpubParser call
- Upstream removes `get_picture()` method (-34 lines)
- Upstream adds Docling callback guard
- Upstream removes `from rag.utils.lazy_image import LazyDocxImage` (moved to rag/nlp/)
- b-knowledge has 4 `from db.services.*` import locations — ALL must be restored after copy

**manual.py:**
- Upstream removes `get_picture()` method (-39 lines)
- Upstream moves `concat_img` to nlp module, removes PIL import
- No b-knowledge modifications found — straight copy + import translation should suffice

**picture.py:**
- Upstream has minimal changes
- b-knowledge has 2 `from db.services.*` import locations — must be restored after copy

**qa.py:**
- Upstream removes 12 lines
- No b-knowledge modifications found — straight copy + import translation should suffice
  </action>
  <verify>
    <automated>cd advance-rag && grep -c "from api.db" rag/app/naive.py rag/app/manual.py rag/app/picture.py rag/app/qa.py 2>/dev/null | grep -v ":0$" && echo "FAIL: found api.db imports" || echo "PASS: no api.db imports" && grep -l "EpubParser" rag/app/naive.py && echo "PASS: EPUB support present"</automated>
  </verify>
  <acceptance_criteria>
    - advance-rag/rag/app/naive.py contains "EpubParser" (EPUB support from upstream)
    - advance-rag/rag/app/naive.py contains "from db.services." (b-knowledge import pattern)
    - advance-rag/rag/app/naive.py does NOT contain "from api.db.services." (upstream pattern removed)
    - advance-rag/rag/app/naive.py does NOT contain "def get_picture" (removed upstream)
    - advance-rag/rag/app/manual.py does NOT contain "def get_picture" (removed upstream)
    - advance-rag/rag/app/picture.py contains "from db.services." (b-knowledge import pattern)
    - No file in advance-rag/rag/app/ contains "from api.db." (all imports translated)
  </acceptance_criteria>
  <done>All 4 rag/app/ files merged with upstream changes. b-knowledge db.services imports preserved. EPUB support added to naive.py. Removed get_picture methods per upstream.</done>
</task>

<task type="auto">
  <name>Task 2: Merge rag/svr/ and rag/graphrag/ files</name>
  <files>
    advance-rag/rag/svr/task_executor.py
    advance-rag/rag/svr/discord_svr.py
    advance-rag/rag/svr/sync_data_source.py
    advance-rag/rag/graphrag/general/extractor.py
    advance-rag/rag/graphrag/search.py
    advance-rag/rag/raptor.py
  </files>
  <read_first>
    advance-rag/rag/svr/task_executor.py
    advance-rag/rag/graphrag/general/extractor.py
    advance-rag/rag/graphrag/search.py
    advance-rag/rag/raptor.py
    ragflow/rag/svr/task_executor.py
    ragflow/rag/graphrag/general/extractor.py
    ragflow/rag/graphrag/search.py
    ragflow/rag/raptor.py
    ragflow/rag/svr/discord_svr.py
    ragflow/rag/svr/sync_data_source.py
  </read_first>
  <action>
Per D-01 (hybrid approach) — same process as Task 1 for each file:

**task_executor.py (MOST COMPLEX — 12 b-knowledge import locations):**
- Upstream adds cross-KB collision guard: `doc_name_by_id = {}` dict with DocumentService.get_by_id loop
- Upstream adds tenant_id fix for RAPTOR (+1 line)
- b-knowledge has 12 `from db.services.*` import locations — ALL must be restored
- Copy upstream version, then translate ALL `from api.db.services.*` to `from db.services.*`
- Translate `from api.db.db_models` to `from db.db_models`
- Translate `from api.db.joint_services.*` to `from db.joint_services.*`

**graphrag/general/extractor.py:**
- Upstream adds response normalization helper (+15 lines)
- Upstream adds truncated cache detection (+4 lines)
- b-knowledge has 1 `from db.services.*` import — must be restored

**graphrag/search.py:**
- Upstream has minor fix (+2 lines)
- b-knowledge has 3 `from db.services.*` imports — must be restored

**raptor.py:**
- Upstream removes 3 lines
- No b-knowledge modifications identified — copy + import translation

**discord_svr.py:**
- Upstream has minor fix (+2 lines)
- No b-knowledge modifications identified — copy + import translation

**sync_data_source.py:**
- Upstream has Jira connector changes (+15 lines)
- No b-knowledge modifications identified — copy + import translation

**CRITICAL VERIFICATION after each file:**
```bash
grep "from api.db" <file>  # MUST return nothing
```
  </action>
  <verify>
    <automated>cd advance-rag && grep -rc "from api.db" rag/svr/task_executor.py rag/graphrag/general/extractor.py rag/graphrag/search.py rag/raptor.py rag/svr/discord_svr.py rag/svr/sync_data_source.py 2>/dev/null | grep -v ":0$" && echo "FAIL: found api.db imports" || echo "PASS: no api.db imports" && grep "doc_name_by_id" rag/svr/task_executor.py && echo "PASS: cross-KB guard present"</automated>
  </verify>
  <acceptance_criteria>
    - advance-rag/rag/svr/task_executor.py contains "doc_name_by_id" (cross-KB collision guard)
    - advance-rag/rag/svr/task_executor.py contains "from db.services." (b-knowledge imports)
    - advance-rag/rag/svr/task_executor.py does NOT contain "from api.db.services." (upstream pattern removed)
    - advance-rag/rag/graphrag/general/extractor.py contains "from db.services." (b-knowledge imports)
    - advance-rag/rag/graphrag/search.py contains "from db.services." (b-knowledge imports)
    - No file in the list contains "from api.db." (all imports translated)
  </acceptance_criteria>
  <done>All 6 rag/svr/ and rag/graphrag/ files merged with upstream changes. b-knowledge db.services imports preserved. Cross-KB collision guard added. Response normalization in extractor added.</done>
</task>

</tasks>

<verification>
- `grep -r "from api.db" advance-rag/rag/app/ advance-rag/rag/svr/ advance-rag/rag/graphrag/ advance-rag/rag/raptor.py` returns NO results
- `grep "EpubParser" advance-rag/rag/app/naive.py` returns a match
- `grep "doc_name_by_id" advance-rag/rag/svr/task_executor.py` returns a match
- `grep "from db.services" advance-rag/rag/svr/task_executor.py` returns matches
</verification>

<success_criteria>
All ~10 modified files contain upstream df2cc32f5 changes. Zero `from api.db.*` imports remain — all translated to `from db.*`. EPUB support registered in naive.py. Cross-KB collision guard in task_executor.py. Response normalization in graphrag extractor.
</success_criteria>

<output>
After completion, create `.planning/phases/01-migrate-latest-ragflow-upstream-to-b-knowledge/01-02-SUMMARY.md`
</output>
