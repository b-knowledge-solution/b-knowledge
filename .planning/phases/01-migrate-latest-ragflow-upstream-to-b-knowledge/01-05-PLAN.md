---
phase: 01-migrate-latest-ragflow-upstream-to-b-knowledge
plan: 05
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03", "01-04"]
files_modified:
  - patches/ragflow-port-v0.25.0-df2cc32.md
autonomous: true
requirements:
  - UPSTREAM-DIFF
  - SAFE-COPY
  - MANUAL-MERGE
  - DEP-UPDATE
  - FEATURE-PORT
  - DB-MIGRATION

must_haves:
  truths:
    - "Full test suite passes (npm test + pytest)"
    - "TypeScript build passes (npm run build)"
    - "Python import smoke tests pass for all modified modules"
    - "Comprehensive patch note documents all changes"
  artifacts:
    - path: "patches/ragflow-port-v0.25.0-df2cc32.md"
      provides: "Patch documentation for the upstream merge"
      min_lines: 100
  key_links:
    - from: "patches/ragflow-port-v0.25.0-df2cc32.md"
      to: "patches/ragflow-port-v0.24.0-c732a1c.md"
      via: "follows same format"
      pattern: "## Source Copy Map"
---

<objective>
Tier 5: Validation and patch documentation — run full test suite, fix any regressions, and create comprehensive patch note.

Purpose: Per D-10/D-11, the acceptance gate is build + test suite pass. Per D-12, a patch note is mandatory. This plan validates the entire merge and creates the documentation artifact.

Output: Green test suite, comprehensive patch note at patches/ragflow-port-v0.25.0-df2cc32.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-migrate-latest-ragflow-upstream-to-b-knowledge/01-01-SUMMARY.md
@.planning/phases/01-migrate-latest-ragflow-upstream-to-b-knowledge/01-02-SUMMARY.md
@.planning/phases/01-migrate-latest-ragflow-upstream-to-b-knowledge/01-03-SUMMARY.md
@.planning/phases/01-migrate-latest-ragflow-upstream-to-b-knowledge/01-04-SUMMARY.md
@patches/ragflow-port-v0.24.0-c732a1c.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full validation suite and fix regressions</name>
  <files>
    (any files needing regression fixes)
  </files>
  <read_first>
    .planning/phases/01-migrate-latest-ragflow-upstream-to-b-knowledge/01-01-SUMMARY.md
    .planning/phases/01-migrate-latest-ragflow-upstream-to-b-knowledge/01-02-SUMMARY.md
    .planning/phases/01-migrate-latest-ragflow-upstream-to-b-knowledge/01-03-SUMMARY.md
    .planning/phases/01-migrate-latest-ragflow-upstream-to-b-knowledge/01-04-SUMMARY.md
  </read_first>
  <action>
Per D-10 and D-11 (build + test suite pass is the acceptance gate):

**1. Run TypeScript build:**
```bash
npm run build
```
If any compilation errors, fix them. Common issues:
- Type mismatches in new methods (check return types)
- Missing imports for new types
- Migration file TypeScript errors

**2. Run full test suite:**
```bash
npm test
```
If any test failures, analyze and fix. Common issues:
- Test mocks need updating for new method signatures
- New required fields in types that existing test factories don't include

**3. Run Python import smoke tests:**
```bash
cd advance-rag
python -c "from rag.llm import chat_model; print('LLM imports OK')"
python -c "from deepdoc.parser import EpubParser; print('EPUB parser OK')"
python -c "from rag.app.naive import chunk; print('naive.py OK')"
python -c "from rag.svr.task_executor import main_loop; print('task_executor OK')"
python -c "from db.services.common_service import CommonService; print('common_service OK')"
```
If any ImportError or ModuleNotFoundError, fix the imports. Most likely cause: missed `from api.db.*` -> `from db.*` translation.

**4. Run Python tests:**
```bash
cd advance-rag && python -m pytest tests/ -x --tb=short
```
If failures, analyze and fix. Common issues:
- Test imports changed due to upstream refactoring
- New function signatures breaking test mocks

**5. Fix any regressions** found in steps 1-4 before proceeding to Task 2.
  </action>
  <verify>
    <automated>npm run build && npm test 2>&1 | tail -20 && cd advance-rag && python -m pytest tests/ -x --tb=short 2>&1 | tail -10 && echo "FULL VALIDATION PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0 (TypeScript compilation succeeds)
    - `npm test` exits with code 0 (all workspace tests pass)
    - `cd advance-rag && python -m pytest tests/ -x` exits with code 0 (Python tests pass)
    - Python smoke imports for rag.app.naive, rag.svr.task_executor, deepdoc.parser.EpubParser all succeed
  </acceptance_criteria>
  <done>Full validation suite passes: TypeScript build green, all workspace tests green, Python tests green, import smoke tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Create comprehensive patch documentation</name>
  <files>
    patches/ragflow-port-v0.25.0-df2cc32.md
  </files>
  <read_first>
    patches/ragflow-port-v0.24.0-c732a1c.md
    .planning/phases/01-migrate-latest-ragflow-upstream-to-b-knowledge/01-CONTEXT.md
    .planning/phases/01-migrate-latest-ragflow-upstream-to-b-knowledge/01-RESEARCH.md
  </read_first>
  <action>
Per D-12 (MANDATORY patch note following format of existing patch note):

Create `patches/ragflow-port-v0.25.0-df2cc32.md` following the EXACT structure of `patches/ragflow-port-v0.24.0-c732a1c.md`. The new patch note MUST include these sections:

**Header:**
```markdown
# RAGFlow Port to advance-rag -- v0.25.0 (df2cc32f5)

## Overview
- Upstream commit: df2cc32f5 (nightly-4-gdf2cc32f5)
- Previous port: c732a1c8e (v0.24.0)
- Port date: 2026-03-23
- Changes: 49 commits, 60 files, +2,697/-1,516 lines
```

**Sections (match existing patch note structure):**

1. **Architecture Differences from RAGFlow** — same as v0.24.0 (copy and update if needed)

2. **Source Copy Map** — updated with new directories/files:
   - Same base map as v0.24.0
   - New: `ragflow/conf/ -> advance-rag/conf/` (LLM factory configs)
   - New file: `deepdoc/parser/epub_parser.py`
   - New file: `api/utils/image_utils.py`

3. **b-knowledge Integration Layer** — same as v0.24.0, note any additions

4. **New Features Ported:**
   - EPUB parser support (deepdoc/parser/epub_parser.py + naive.py registration)
   - Perplexity + MiniMax LLM providers (rag/llm/ copy)
   - PDF garbled text OCR fallback (deepdoc/ copy)
   - Deadlock retry decorator (db/services/common_service.py)
   - Aggregated parsing status (rag.service.ts concept port)
   - Chunk image support (api/utils/image_utils.py)
   - Docling server URL support (DOCLING_SERVER_URL env var)
   - Cross-KB collision guard (task_executor.py)
   - Similarity threshold bypass (rag-search.service.ts concept port)
   - Canvas version release flag (agent.service.ts + Knex migration)
   - version_title in conversations (agent.service.ts + Knex migration)
   - Delete-all documents/sessions (rag-document.service.ts, chat-conversation.service.ts)
   - Memory user_id tracking (memory-message.service.ts)
   - Metadata query optimization (rag.service.ts)
   - Empty doc filter fix (chat-conversation.service.ts)

5. **DB Schema Changes:**
   - `user_canvas_version.release` — BooleanField, default false, indexed
   - `api_4_conversation.version_title` — CharField(255), nullable
   - Migration: `20260323140000_add_ragflow_upstream_columns.ts`

6. **Dependency Changes:**
   - `pypdf`: `>=4.0.0` -> `>=6.8.0`
   - `ebooklib`: new, `>=0.18`

7. **b-knowledge TypeScript Improvements:**
   - List all concept ports from D-08

8. **Protected Files** — updated list

9. **Known Limitations** — update from v0.24.0

10. **Upgrade Workflow (Future RAGFlow Versions)** — same as v0.24.0, updated with any new learnings
  </action>
  <verify>
    <automated>ls patches/ragflow-port-v0.25.0-df2cc32.md && grep "df2cc32" patches/ragflow-port-v0.25.0-df2cc32.md && grep "EPUB" patches/ragflow-port-v0.25.0-df2cc32.md && grep "Source Copy Map" patches/ragflow-port-v0.25.0-df2cc32.md && grep "Schema Changes\|DB.*Changes\|Database" patches/ragflow-port-v0.25.0-df2cc32.md && wc -l patches/ragflow-port-v0.25.0-df2cc32.md | awk '{if ($1 >= 100) print "PASS: ",$1," lines"; else print "FAIL: only",$1,"lines"}'</automated>
  </verify>
  <acceptance_criteria>
    - patches/ragflow-port-v0.25.0-df2cc32.md exists
    - File contains "df2cc32" (upstream commit hash)
    - File contains "c732a1c" (previous port reference)
    - File contains "EPUB" (new feature documented)
    - File contains "Source Copy Map" section
    - File contains "release" (new column documented)
    - File contains "version_title" (new column documented)
    - File contains "pypdf" (dependency change documented)
    - File contains "ebooklib" (new dependency documented)
    - File contains "Upgrade Workflow" section
    - File is at least 100 lines (comprehensive documentation)
  </acceptance_criteria>
  <done>Comprehensive patch note created at patches/ragflow-port-v0.25.0-df2cc32.md documenting all upstream changes, new features, DB schema changes, dependency updates, TypeScript improvements, and upgrade workflow.</done>
</task>

</tasks>

<verification>
- Full test suite green: `npm run build && npm test && cd advance-rag && python -m pytest tests/ -x`
- Patch note exists and is comprehensive: `wc -l patches/ragflow-port-v0.25.0-df2cc32.md` >= 100 lines
- Patch note follows existing format: structure matches patches/ragflow-port-v0.24.0-c732a1c.md
</verification>

<success_criteria>
Full validation suite passes (build + all tests). Comprehensive patch note created following existing format. All upstream features documented. Ready for verification phase.
</success_criteria>

<output>
After completion, create `.planning/phases/01-migrate-latest-ragflow-upstream-to-b-knowledge/01-05-SUMMARY.md`
</output>
