---
phase: 03-document-management
plan: 05
type: execute
wave: 3
depends_on: ["03-02", "03-04"]
files_modified:
  - fe/src/features/datasets/components/ParserSettingsFields.tsx
  - fe/src/features/datasets/components/MetadataSchemaBuilder.tsx
  - fe/src/features/datasets/components/MetadataManageDialog.tsx
  - fe/src/features/datasets/components/DocumentTable.tsx
  - fe/src/features/datasets/api/datasetApi.ts
  - fe/src/features/datasets/api/datasetQueries.ts
  - fe/src/features/search/components/TagFilterChips.tsx
  - fe/src/features/search/pages/SearchPage.tsx
  - fe/src/features/system/pages/SystemToolsPage.tsx
  - fe/src/features/system/components/CronSchedulerSettings.tsx
  - fe/src/i18n/locales/en.json
  - fe/src/i18n/locales/vi.json
  - fe/src/i18n/locales/ja.json
autonomous: true
requirements:
  - DOCM-04
  - DOCM-05
  - DOCM-06

must_haves:
  truths:
    - "User can add custom metadata tags (key-value pairs) to documents via MetadataManageDialog"
    - "User can bulk-edit tags across multiple selected documents"
    - "Auto-extraction toggles (keywords, questions, metadata) appear in parser settings with counts"
    - "Visual metadata schema builder allows adding fields with key, description, and enum values"
    - "Search results can be filtered by metadata tags via inline filter chips"
    - "Admin can configure a cron parsing schedule in System Settings"
  artifacts:
    - path: "fe/src/features/datasets/components/MetadataSchemaBuilder.tsx"
      provides: "Visual schema builder for metadata extraction"
      contains: "MetadataSchemaBuilder"
    - path: "fe/src/features/search/components/TagFilterChips.tsx"
      provides: "Inline tag filter chips below search bar"
      contains: "TagFilterChips"
    - path: "fe/src/features/system/components/CronSchedulerSettings.tsx"
      provides: "Cron schedule configuration UI"
      contains: "CronSchedulerSettings"
  key_links:
    - from: "fe/src/features/datasets/components/ParserSettingsFields.tsx"
      to: "parser_config.auto_keywords, auto_questions, enable_metadata"
      via: "Toggle + count fields wired to parser_config JSONB"
      pattern: "auto_keywords|auto_questions|enable_metadata"
    - from: "fe/src/features/datasets/components/MetadataManageDialog.tsx"
      to: "/api/rag/datasets/bulk-metadata"
      via: "POST request for bulk metadata_tags update"
      pattern: "bulkUpdateMetadata|bulk-metadata"
    - from: "fe/src/features/search/components/TagFilterChips.tsx"
      to: "fe/src/features/search/pages/SearchPage.tsx"
      via: "Active filters passed as metadata_filter conditions to search API"
      pattern: "metadata_filter|TagFilterChips"
    - from: "fe/src/features/system/components/CronSchedulerSettings.tsx"
      to: "fe/src/features/system/pages/SystemToolsPage.tsx"
      via: "Rendered as section in SystemToolsPage"
      pattern: "CronSchedulerSettings"
---

<objective>
Build the frontend metadata management UI: parser settings auto-extraction toggles, metadata schema builder, bulk metadata dialog, search filter chips, and cron scheduler settings.

Purpose: Users can configure auto-extraction, manage tags, filter search by tags, and admins can schedule parsing.
Output: Extended ParserSettingsFields, MetadataSchemaBuilder, bulk MetadataManageDialog, TagFilterChips, CronSchedulerSettings.
</objective>

<execution_context>
@/home/doana/.claude/get-shit-done/workflows/execute-plan.md
@/home/doana/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-document-management/03-CONTEXT.md
@.planning/phases/03-document-management/03-UI-SPEC.md
@.planning/phases/03-document-management/03-02-SUMMARY.md
@.planning/phases/03-document-management/03-04-SUMMARY.md

<interfaces>
From advance-rag/rag/svr/task_executor.py (auto-extraction keys):
- `parser_config.auto_keywords` (integer, 0=off, N=extract N keywords)
- `parser_config.auto_questions` (integer, 0=off, N=generate N questions)
- `parser_config.enable_metadata` (boolean) + `parser_config.metadata` (array of field defs)

From advance-rag/common/metadata_utils.py:
```python
def turn2jsonschema(metadata_fields):
    # Expects: [{"key": "department", "description": "Which department", "enum": ["eng", "clinical"]}]
```

**Metadata key convention (CRITICAL):**
- `parser_config.metadata_tags` — user-set free-form key-value tags (stored by bulk-metadata API)
- `parser_config.metadata` — auto-extraction schema array for LLM ([{key, description, enum}])
- MetadataSchemaBuilder writes to `parser_config.metadata`
- MetadataManageDialog bulk mode writes to `parser_config.metadata_tags`

Backend endpoints created in 03-04:
- POST /api/rag/datasets/bulk-metadata — body: { dataset_ids, metadata_tags, mode }
- GET /api/rag/tags/aggregations?dataset_ids=... — returns [{ key, values }]
- GET /api/system/config/parsing_scheduler — returns { schedule, enabled }
- PUT /api/system/config/parsing_scheduler — body: { schedule, enabled }

From be/src/modules/rag/services/rag-search.service.ts (search filter integration):
- Line 83: `buildMetadataFilters(filter?)` builds extra OpenSearch filters from `metadata_filter` conditions
- Line 310-311: `extraFilters = this.buildMetadataFilters(req.metadata_filter)`
- TagFilterChips active filters must be passed as `metadata_filter.conditions` array to the search API:
  ```typescript
  metadata_filter: {
    logic: 'and',
    conditions: [{ name: 'tag_kwd', comparison_operator: 'eq', value: 'department:engineering' }]
  }
  ```

From fe/src/features/system/pages/SystemToolsPage.tsx:
- Existing admin system tools page — add CronSchedulerSettings as a new section
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parser settings extensions + MetadataSchemaBuilder + bulk MetadataManageDialog</name>
  <files>
    fe/src/features/datasets/components/ParserSettingsFields.tsx
    fe/src/features/datasets/components/MetadataSchemaBuilder.tsx
    fe/src/features/datasets/components/MetadataManageDialog.tsx
    fe/src/features/datasets/components/DocumentTable.tsx
    fe/src/features/datasets/api/datasetApi.ts
    fe/src/features/datasets/api/datasetQueries.ts
  </files>
  <read_first>
    fe/src/features/datasets/components/ParserSettingsFields.tsx
    fe/src/features/datasets/components/MetadataManageDialog.tsx
    fe/src/features/datasets/components/DocumentTable.tsx
    fe/src/features/datasets/api/datasetApi.ts
    fe/src/features/datasets/api/datasetQueries.ts
    .planning/phases/03-document-management/03-UI-SPEC.md
  </read_first>
  <action>
**1. Extend ParserSettingsFields.tsx** with auto-extraction toggles:
- Add section header "Auto-Extraction"
- `auto_keywords` toggle (Switch) + count input (Input type="number", default 5). When off, set parser_config.auto_keywords = 0. When on, set to count value.
- `auto_questions` toggle (Switch) + count input (default 3). Same pattern wired to parser_config.auto_questions.
- `enable_metadata` toggle (Switch). When on, show MetadataSchemaBuilder below. Wired to parser_config.enable_metadata.
- Note: ParserSettingsFields may already have auto_keywords/auto_questions sliders — check and adapt to toggle+count pattern per CONTEXT decision.
- All toggles OFF by default per CONTEXT decision.

**2. Create MetadataSchemaBuilder** `fe/src/features/datasets/components/MetadataSchemaBuilder.tsx`:
- Props: `{ fields: MetadataSchemaField[]; onChange: (fields: MetadataSchemaField[]) => void }`
- MetadataSchemaField = `{ key: string; description: string; enum?: string[] }`
- Render list of field rows: Input for key, Input for description, Input for enum values (comma-separated), delete button (X)
- "Add Field" button at bottom
- Stored in `parser_config.metadata` array — matches RAGFlow turn2jsonschema() format exactly
- Use shadcn Input, Button, Separator

**3. Extend MetadataManageDialog.tsx** for bulk operations:
- New prop: `datasetIds?: string[]` (bulk mode when provided)
- Bulk mode: heading "Edit Tags for N documents", merge/overwrite Switch, confirm calls bulkUpdateMetadata API
- Single mode: unchanged existing behavior
- CTA: "Apply Tag Changes" per UI-SPEC
- Bulk API uses `parser_config.metadata_tags` key (NOT `parser_config.metadata`)

**4. Add API functions** in `datasetApi.ts`:
```typescript
export async function bulkUpdateMetadata(datasetIds: string[], metadataTags: Record<string, string>, mode: 'merge' | 'overwrite'): Promise<void>
export async function getTagAggregations(datasetIds?: string[]): Promise<{ key: string; values: string[] }[]>
```

**5. Add query hooks** in `datasetQueries.ts`:
- `useBulkUpdateMetadata()` — mutation, invalidates datasets.all on success
- `useTagAggregations(datasetIds?)` — query with key ['tags', 'aggregations', datasetIds]

**6. Extend DocumentTable.tsx** with bulk metadata action:
- Add checkboxes column for multi-select (if not already present)
- When documents selected, show bulk action bar (bg-muted) with "Edit Tags" button
- "Edit Tags" opens MetadataManageDialog in bulk mode with selected dataset IDs
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge && npm run build -w fe</automated>
  </verify>
  <done>ParserSettingsFields has auto-extraction toggles + counts. MetadataSchemaBuilder writes to parser_config.metadata. MetadataManageDialog supports bulk mode writing to parser_config.metadata_tags. DocumentTable has bulk "Edit Tags" action. FE builds.</done>
</task>

<task type="auto">
  <name>Task 2: TagFilterChips in search + CronSchedulerSettings in System Tools + i18n</name>
  <files>
    fe/src/features/search/components/TagFilterChips.tsx
    fe/src/features/search/pages/SearchPage.tsx
    fe/src/features/system/components/CronSchedulerSettings.tsx
    fe/src/features/system/pages/SystemToolsPage.tsx
    fe/src/i18n/locales/en.json
    fe/src/i18n/locales/vi.json
    fe/src/i18n/locales/ja.json
  </files>
  <read_first>
    fe/src/features/search/pages/SearchPage.tsx
    fe/src/features/search/components/SearchAppConfig.tsx
    fe/src/features/system/pages/SystemToolsPage.tsx
    fe/src/i18n/locales/en.json
    .planning/phases/03-document-management/03-UI-SPEC.md
  </read_first>
  <action>
**1. Create TagFilterChips** `fe/src/features/search/components/TagFilterChips.tsx`:
- Props: `{ datasetIds?: string[]; activeFilters: Record<string, string>; onFilterChange: (filters: Record<string, string>) => void }`
- Fetch tags via `useTagAggregations(datasetIds)` from datasetQueries.ts
- Inactive chips: Badge variant="outline", clicking opens Popover with top values
- Active chips: Badge variant="secondary" showing key:value, X button to remove
- Active filters are additive (AND logic)
- Spinner in popover while loading per UI-SPEC
- Chip height h-7 (28px) per UI-SPEC

**2. Wire TagFilterChips into SearchPage.tsx**:
- Import TagFilterChips, render below search bar
- Maintain activeFilters state with useState
- Pass active filters to the search API call as `metadata_filter` parameter:
  ```typescript
  // Convert active tag filters to metadata_filter conditions for rag-search.service.ts
  const metadata_filter = {
    logic: 'and',
    conditions: Object.entries(activeFilters).map(([key, value]) => ({
      name: 'tag_kwd',
      comparison_operator: 'eq',
      value: `${key}:${value}`
    }))
  }
  ```
- This wires into `buildMetadataFilters()` in `rag-search.service.ts` (line 83) which already handles this shape

**3. Create CronSchedulerSettings** `fe/src/features/system/components/CronSchedulerSettings.tsx`:
- Fetches from GET /api/system/config/parsing_scheduler
- Render: Switch for enable/disable, Input for cron expression, Select for presets ("Weekdays 10 PM" = "0 22 * * 1-5", "Daily midnight" = "0 0 * * *", "Custom")
- Label: "Parsing Schedule", Description per UI-SPEC copywriting
- On save: PUT /api/system/config/parsing_scheduler

**4. Wire CronSchedulerSettings into SystemToolsPage.tsx**:
- Import CronSchedulerSettings
- Add as a new section (card/panel) in the system tools page

**5. Add i18n keys** to all 3 locale files:
- datasets: autoExtraction, autoKeywords, autoKeywordsDesc, autoQuestions, autoQuestionsDesc, enableMetadata, enableMetadataDesc, metadataSchema, addField, fieldKey, fieldDescription, fieldEnum, editTags, applyTagChanges, mergeMode, overwriteMode, noMetadata, noMetadataDesc
- system: parsingSchedule, parsingScheduleDesc
- search: filterByTags, clearFilters
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge && npm run build -w fe</automated>
  </verify>
  <done>TagFilterChips renders below search bar with active filters passed as metadata_filter conditions. CronSchedulerSettings section in SystemToolsPage. All i18n keys in 3 locales. FE builds.</done>
</task>

</tasks>

<verification>
1. `npm run build -w fe` passes
2. grep confirms: TagFilterChips imported in SearchPage.tsx, metadata_filter in search integration, CronSchedulerSettings in SystemToolsPage
3. grep confirms: parser_config.metadata_tags in MetadataManageDialog, parser_config.metadata in MetadataSchemaBuilder (separate keys)
</verification>

<success_criteria>
- ParserSettingsFields has three auto-extraction toggles with count inputs
- MetadataSchemaBuilder writes schema to parser_config.metadata (RAGFlow-compatible)
- MetadataManageDialog bulk mode writes tags to parser_config.metadata_tags (separate key)
- DocumentTable supports multi-select with "Edit Tags" bulk action
- TagFilterChips discovers tags via aggregation and passes active filters as metadata_filter conditions to search API
- CronSchedulerSettings UI in SystemToolsPage allows admin to configure schedule
- All locale files updated
- FE builds
</success_criteria>

<output>
After completion, create `.planning/phases/03-document-management/03-05-SUMMARY.md`
</output>
