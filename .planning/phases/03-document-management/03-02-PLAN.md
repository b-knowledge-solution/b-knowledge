---
phase: 03-document-management
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - be/src/modules/rag/services/rag-search.service.ts
  - fe/src/features/datasets/pages/ChunkDetailPage.tsx
  - fe/src/features/datasets/components/ChunkToolbar.tsx
  - fe/src/features/datasets/components/ChunkList.tsx
  - fe/src/components/DocumentPreviewer/DocumentPreviewer.tsx
  - fe/src/features/chat/components/CitationDocDrawer.tsx
  - fe/src/features/search/components/SearchResultDocDialog.tsx
  - fe/src/app/App.tsx
  - fe/src/app/routeConfig.ts
  - fe/src/features/datasets/components/DocumentTable.tsx
  - fe/src/i18n/locales/en.json
  - fe/src/i18n/locales/vi.json
  - fe/src/i18n/locales/ja.json
autonomous: true
requirements:
  - DOCM-02

must_haves:
  truths:
    - "Search results from newer version datasets rank higher than older versions via pagerank boost"
    - "User can click a document name in DocumentTable to navigate to chunk detail page"
    - "Chunk detail page shows document preview (left) and chunk list (right) in split view"
    - "User can edit, delete, and add chunks on the chunk detail page"
    - "Chat citation click opens document in a drawer; search result click opens document in a dialog"
  artifacts:
    - path: "be/src/modules/rag/services/rag-search.service.ts"
      provides: "rank_feature boost on pagerank_fea"
      contains: "rank_feature"
    - path: "fe/src/features/datasets/pages/ChunkDetailPage.tsx"
      provides: "Full page chunk detail view"
      contains: "ChunkDetailPage"
    - path: "fe/src/features/datasets/components/ChunkToolbar.tsx"
      provides: "Toolbar with chunk count, search, sort, add chunk"
      contains: "ChunkToolbar"
  key_links:
    - from: "fe/src/features/datasets/components/DocumentTable.tsx"
      to: "fe/src/features/datasets/pages/ChunkDetailPage.tsx"
      via: "navigate() on document name click"
      pattern: "navigate.*chunks"
    - from: "be/src/modules/rag/services/rag-search.service.ts"
      to: "OpenSearch pagerank_fea"
      via: "rank_feature query in should clause"
      pattern: "rank_feature.*pagerank_fea"
---

<objective>
Implement version-aware search with OpenSearch rank_feature boost on pagerank_fea, and build the chunk detail page with three document viewer patterns (full page, chat drawer, search dialog).

Purpose: Newer document versions rank higher in search results. Users can view and manage chunks through a dedicated page, and see document previews from chat citations and search results.
Output: Working rank_feature boost in search queries + ChunkDetailPage + CitationDocDrawer + SearchResultDocDialog.
</objective>

<execution_context>
@/home/doana/.claude/get-shit-done/workflows/execute-plan.md
@/home/doana/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-document-management/03-CONTEXT.md
@.planning/phases/03-document-management/03-RESEARCH.md
@.planning/phases/03-document-management/03-UI-SPEC.md
@.planning/phases/03-document-management/03-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from prior plan and existing code. -->

From be/src/modules/rag/services/rag-search.service.ts:
```typescript
export class RagSearchService {
    // hybridSearch, fullTextSearch, semanticSearch methods all build OpenSearch bool queries
    // Queries use: must (match), filter (term kb_id, available_int), should (boosting)
    // Must add rank_feature to should clause
}
export const ragSearchService = new RagSearchService()
```

From advance-rag/rag/utils/es_conn.py (Python pattern to mirror):
```python
# Line 270: rank_feature in should clause
bool_query.should.append(Q("rank_feature", field=fld, linear={}, boost=sc))
```

From advance-rag/conf/mapping.json:
- `pagerank_fea` field is type `rank_feature` in the OpenSearch index mapping

From fe/src/components/DocumentPreviewer/DocumentPreviewer.tsx:
- Existing split view component with document preview + chunk list
- Reuse as the core rendering component inside all three viewer patterns

From fe/src/features/datasets/components/ChunkEditInline.tsx:
- Existing inline chunk editing component — wire into ChunkDetailPage

From fe/src/features/datasets/components/AddChunkModal.tsx:
- Existing add chunk modal — trigger from ChunkToolbar "Add Chunk" button

From fe/src/lib/queryKeys.ts:
```typescript
chunks: (datasetId: string, params?: any) =>
    [...queryKeys.datasets.all, datasetId, 'chunks', params] as const,
```

Route pattern: `/data-studio/datasets/:id/documents/:docId/chunks`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rank_feature boost to RagSearchService queries</name>
  <files>
    be/src/modules/rag/services/rag-search.service.ts
  </files>
  <read_first>
    be/src/modules/rag/services/rag-search.service.ts
    advance-rag/rag/utils/es_conn.py
    advance-rag/conf/mapping.json
    .planning/phases/03-document-management/03-RESEARCH.md
  </read_first>
  <action>
**Add rank_feature boost for pagerank_fea** to all search query methods in `rag-search.service.ts`:

1. Find every method that builds an OpenSearch bool query (hybridSearch, fullTextSearch, semanticSearch, and any other search methods). Each has a `bool` query with `must`, `filter`, and potentially `should` clauses.

2. Add a `rank_feature` clause to the `should` array in each search method's bool query:
```typescript
should: [
  // Existing should clauses (if any)...
  // Boost by pagerank (version recency) — newer versions have higher pagerank values
  { rank_feature: { field: 'pagerank_fea', linear: {} } },
]
```

3. **CRITICAL RULES** from research:
   - `rank_feature` MUST be in a `should` clause, NOT `must` or `filter`. It provides scoring boost only.
   - The `linear` scoring function gives proportional boost (v2 scores 2x v1).
   - Documents without `pagerank_fea` field still match — they just get no boost. This is correct for non-versioned datasets.

4. Add JSDoc comment above the rank_feature clause explaining its purpose:
```typescript
// Boost newer document versions higher in results via pagerank_fea rank_feature field
// pagerank_fea maps to dataset.pagerank which equals version_number (v1=1, v2=2, etc.)
```

5. Do NOT change any filter logic or query structure — only add the `should` clause.
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge && npm run build -w be</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "rank_feature" be/src/modules/rag/services/rag-search.service.ts
    - grep -q "pagerank_fea" be/src/modules/rag/services/rag-search.service.ts
    - grep -q "linear" be/src/modules/rag/services/rag-search.service.ts
  </acceptance_criteria>
  <done>All search methods in RagSearchService include rank_feature boost on pagerank_fea in their should clause. Newer version datasets (higher pagerank) rank above older versions in search results. BE builds successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Build ChunkDetailPage, ChunkToolbar, and three document viewer patterns</name>
  <files>
    fe/src/features/datasets/pages/ChunkDetailPage.tsx
    fe/src/features/datasets/components/ChunkToolbar.tsx
    fe/src/features/datasets/components/ChunkList.tsx
    fe/src/components/DocumentPreviewer/DocumentPreviewer.tsx
    fe/src/features/chat/components/CitationDocDrawer.tsx
    fe/src/features/search/components/SearchResultDocDialog.tsx
    fe/src/app/App.tsx
    fe/src/app/routeConfig.ts
    fe/src/features/datasets/components/DocumentTable.tsx
    fe/src/i18n/locales/en.json
    fe/src/i18n/locales/vi.json
    fe/src/i18n/locales/ja.json
  </files>
  <read_first>
    fe/src/components/DocumentPreviewer/DocumentPreviewer.tsx
    fe/src/components/DocumentPreviewer/ChunkList.tsx
    fe/src/components/DocumentPreviewer/ChunkCard.tsx
    fe/src/features/datasets/components/ChunkEditInline.tsx
    fe/src/features/datasets/components/AddChunkModal.tsx
    fe/src/features/datasets/components/ChunkResultCard.tsx
    fe/src/features/datasets/components/DocumentTable.tsx
    fe/src/features/datasets/pages/DatasetDetailPage.tsx
    fe/src/features/datasets/pages/DocumentReviewerPage.tsx
    fe/src/app/App.tsx
    fe/src/app/routeConfig.ts
    fe/src/features/chat/components/
    fe/src/features/search/components/
    fe/src/i18n/locales/en.json
    .planning/phases/03-document-management/03-UI-SPEC.md
    .planning/phases/03-document-management/03-CONTEXT.md
  </read_first>
  <action>
**1. Create ChunkToolbar component** `fe/src/features/datasets/components/ChunkToolbar.tsx`:
- Props: `{ datasetId: string; docId: string; chunkCount: number; onSearch: (query: string) => void; onSortChange: (sort: string) => void }`
- Render: Badge showing chunk count, Input for search/filter chunks, Select for sort order (position, relevance), Button "Add Chunk" that opens AddChunkModal
- Use shadcn Button, Input, Select, Badge
- Follow UI-SPEC spacing: gap-2 between elements, h-7 for compact buttons

**2. Create ChunkDetailPage** `fe/src/features/datasets/pages/ChunkDetailPage.tsx`:
- Route: `/data-studio/datasets/:id/documents/:docId/chunks`
- Extract `id` (datasetId) and `docId` from route params via useParams
- Layout: full page split view — DocumentPreviewer on left (50%), chunk list on right (50%) per UI-SPEC
- Reuse existing DocumentPreviewer component for the left panel (pass document info for preview rendering)
- Right panel: ChunkToolbar at top + scrollable chunk list below
- Wire existing ChunkEditInline for inline editing on chunk cards
- Wire existing AddChunkModal triggered from ChunkToolbar "Add Chunk" button
- Wire chunk delete with confirm dialog (existing pattern)
- Show extracted keywords and questions as secondary badges below chunk text (per CONTEXT decisions)
- Responsive: xl = 50/50, md-lg = 40/60 with collapsible preview, sm = stacked (per UI-SPEC)
- Page heading: document name from API response

**3. Extend ChunkList.tsx** in `fe/src/components/DocumentPreviewer/ChunkList.tsx`:
- Ensure it supports chunk CRUD callbacks: onEdit, onDelete props
- Show keywords and questions per chunk (if available in chunk data) as Badge variant="outline" below chunk text
- Each chunk card should have edit and delete icon buttons with aria-labels and Tooltips per UI-SPEC accessibility rules

**4. Add route to App.tsx**:
- Add route: `<Route path="/data-studio/datasets/:id/documents/:docId/chunks" element={<ChunkDetailPage />} />`
- Wrap with FeatureErrorBoundary

**5. Add route metadata to routeConfig.ts**:
- Add entry for chunk detail page with title i18n key

**6. Make document name clickable in DocumentTable.tsx**:
- The document name column in the table should be a clickable link (not just text)
- On click: `navigate(\`/data-studio/datasets/${datasetId}/documents/${doc.id}/chunks\`)`
- Style: text-primary hover:underline per UI-SPEC (document name hover link color = accent)

**7. Create CitationDocDrawer** `fe/src/features/chat/components/CitationDocDrawer.tsx`:
- Uses shadcn Sheet (side drawer) to wrap DocumentPreviewer
- Props: `{ open: boolean; onOpenChange: (open: boolean) => void; datasetId: string; docId: string; chunkId?: string }`
- Renders DocumentPreviewer inside the Sheet with highlight scroll to the relevant chunk
- Wire into chat citation tooltip click (find where citations render in chat and add onClick handler)

**8. Create SearchResultDocDialog** `fe/src/features/search/components/SearchResultDocDialog.tsx`:
- Uses shadcn Dialog (max-w-5xl per UI-SPEC) to wrap DocumentPreviewer
- Props: `{ open: boolean; onOpenChange: (open: boolean) => void; datasetId: string; docId: string; chunkId?: string }`
- Renders DocumentPreviewer inside the Dialog with highlight scroll
- Wire into search result card click (find where search results render and add onClick handler)

**9. Add i18n keys** to all 3 locale files:
- `datasets.chunkDetail`: "Chunk Detail"
- `datasets.noChunks`: "No chunks yet"
- `datasets.noChunksDesc`: "This document has not been parsed. Click Parse to generate chunks."
- `datasets.addChunk`: "Add Chunk"
- `datasets.deleteChunk`: "Delete Chunk"
- `datasets.deleteChunkConfirm`: "This chunk will be permanently removed from the dataset."
- `datasets.chunkCount`: "{{count}} chunks"
- `datasets.keywords`: "Keywords"
- `datasets.questions`: "Questions"
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge && npm run build -w fe</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ChunkDetailPage" fe/src/features/datasets/pages/ChunkDetailPage.tsx
    - grep -q "ChunkToolbar" fe/src/features/datasets/components/ChunkToolbar.tsx
    - grep -q "CitationDocDrawer" fe/src/features/chat/components/CitationDocDrawer.tsx
    - grep -q "SearchResultDocDialog" fe/src/features/search/components/SearchResultDocDialog.tsx
    - grep -q "chunks" fe/src/app/App.tsx
    - grep -q "navigate" fe/src/features/datasets/components/DocumentTable.tsx
    - grep -q "chunkDetail" fe/src/i18n/locales/en.json
  </acceptance_criteria>
  <done>ChunkDetailPage renders at /data-studio/datasets/:id/documents/:docId/chunks with split view (preview + chunk list with CRUD). Document name in DocumentTable navigates to chunk detail. CitationDocDrawer opens from chat citations. SearchResultDocDialog opens from search results. FE builds without errors.</done>
</task>

</tasks>

<verification>
1. `npm run build -w be` passes with rank_feature in search queries
2. `npm run build -w fe` passes with new page and components
3. Manually: search with versioned datasets, verify newer versions appear higher
4. Manually: click document name in DocumentTable, verify chunk detail page loads with split view
5. Manually: on chunk detail page, verify add/edit/delete chunk operations work
6. Manually: click chat citation, verify drawer opens with document preview
7. Manually: click search result, verify dialog opens with document preview
</verification>

<success_criteria>
- RagSearchService includes rank_feature boost on pagerank_fea in all search methods
- ChunkDetailPage shows split view with document preview (left) and chunk list with toolbar (right)
- Document name in DocumentTable is clickable and navigates to chunk detail page
- ChunkToolbar has chunk count badge, search, sort, and "Add Chunk" button
- Chunk CRUD (edit inline, delete, add) works on the detail page
- CitationDocDrawer opens from chat citation clicks
- SearchResultDocDialog opens from search result clicks
- Keywords and questions displayed per chunk as badges
- Both BE and FE build successfully
</success_criteria>

<output>
After completion, create `.planning/phases/03-document-management/03-02-SUMMARY.md`
</output>
