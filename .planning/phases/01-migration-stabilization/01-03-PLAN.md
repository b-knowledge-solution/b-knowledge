---
phase: 01-migration-stabilization
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - fe/e2e/dataset/chunk-verify.spec.ts
  - fe/e2e/helpers/opensearch.helper.ts
autonomous: true
requirements: [STAB-03, STAB-04]
must_haves:
  truths:
    - "After parsing completes, chunks are indexed in OpenSearch and searchable"
    - "Chunk content matches the source document text"
    - "OpenSearch index contains vector embeddings for each chunk"
    - "Search queries against the dataset return relevant chunks"
    - "E2E tests reveal any chunking, embedding, or indexing bugs through test failures"
  artifacts:
    - path: "fe/e2e/dataset/chunk-verify.spec.ts"
      provides: "E2E tests for chunk generation, embedding, and search verification"
      contains: "test.*chunk"
    - path: "fe/e2e/helpers/opensearch.helper.ts"
      provides: "Helper for directly querying OpenSearch to verify chunk indexing"
      exports: ["opensearchHelper"]
  key_links:
    - from: "fe/e2e/dataset/chunk-verify.spec.ts"
      to: "fe/e2e/helpers/wait.helper.ts"
      via: "Wait for chunks to be indexed after parsing"
      pattern: "waitForChunksIndexed"
    - from: "fe/e2e/dataset/chunk-verify.spec.ts"
      to: "fe/e2e/helpers/opensearch.helper.ts"
      via: "Direct OpenSearch query for chunk verification"
      pattern: "opensearchHelper"
---

<objective>
Write E2E tests that verify the full chunking, embedding, and OpenSearch indexing pipeline after document parsing. Create an OpenSearch helper for direct index verification. Fix any bugs in the chunk generation or indexing flow.

Purpose: Validates STAB-03 (chunking/embedding/indexing bugs) and STAB-04 (systematic bug discovery). This is the critical link between document parsing and search -- if chunks are not properly indexed, search and chat will return no results.
Output: Green E2E tests confirming chunks are generated, embedded, indexed in OpenSearch, and searchable.
</objective>

<execution_context>
@/home/doana/.claude/get-shit-done/workflows/execute-plan.md
@/home/doana/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-migration-stabilization/01-CONTEXT.md
@.planning/phases/01-migration-stabilization/01-RESEARCH.md
@.planning/phases/01-migration-stabilization/01-01-SUMMARY.md
@.planning/phases/01-migration-stabilization/01-02-SUMMARY.md

<interfaces>
<!-- From prior plans -->

fe/e2e/helpers/api.helper.ts:
```typescript
class ApiHelper {
  createDataset(name: string, description?: string): Promise<{ id: string }>
  deleteDataset(id: string): Promise<void>
  uploadDocument(datasetId: string, filePath: string): Promise<{ id: string }>
  triggerParse(datasetId: string, docIds: string[]): Promise<void>
  getDocument(datasetId: string, docId: string): Promise<DocumentResponse>
}
```

fe/e2e/helpers/wait.helper.ts:
```typescript
function waitForDocumentParsed(request, datasetId, docId, timeoutMs?): Promise<void>
function waitForChunksIndexed(request, datasetId, docId, minChunks?, timeoutMs?): Promise<void>
```

<!-- From codebase: OpenSearch indexing patterns -->

From be/src/modules/rag/services/rag-search.service.ts:
- Index name pattern: `knowledge_{SYSTEM_TENANT_ID}`
- Chunk fields: content_with_weight, q_vec (embedding vector), kb_id, doc_id, docnm_kwd
- UUID format: 32-char hex (no hyphens) for RAGFlow-derived fields

From advance-rag/rag/svr/task_executor.py:
- Chunks are built by parser, then embedded, then bulk-indexed to OpenSearch
- Progress reported via Redis pub/sub
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OpenSearch helper and write chunk verification E2E tests</name>
  <files>fe/e2e/helpers/opensearch.helper.ts, fe/e2e/dataset/chunk-verify.spec.ts</files>
  <read_first>
    - fe/e2e/helpers/api.helper.ts (confirm helper patterns)
    - fe/e2e/helpers/wait.helper.ts (confirm waitForChunksIndexed signature)
    - be/src/modules/rag/services/rag-search.service.ts (confirm OpenSearch index name pattern, query structure, chunk field names)
    - be/src/modules/rag/models/rag-document.model.ts (confirm how chunk_num is tracked)
    - be/.env.example (confirm OPENSEARCH_HOST, SYSTEM_TENANT_ID env vars)
    - advance-rag/rag/svr/task_executor.py (confirm how chunks are indexed -- bulk_insert call, fields written)
    - fe/src/components/DocumentPreviewer/ChunkList.tsx (confirm how chunks are displayed in UI)
    - fe/src/components/DocumentPreviewer/ChunkCard.tsx (confirm chunk display fields)
  </read_first>
  <action>
1. Create `fe/e2e/helpers/opensearch.helper.ts`:
   - Export class `OpenSearchHelper` that makes direct HTTP requests to OpenSearch for test verification
   - Constructor takes base URL from `process.env.OPENSEARCH_URL || 'http://localhost:9201'`
   - Use `fetch` (Node built-in) for requests, with auth if configured
   - Methods:
     - `getChunksByDocId(indexName: string, docId: string): Promise<ChunkResult[]>` -- queries OpenSearch for all chunks with matching `doc_id` field
     - `searchChunks(indexName: string, query: string, kbId: string): Promise<SearchResult[]>` -- performs a text search against `content_with_weight` field filtered by `kb_id`
     - `getIndexName(): string` -- returns `knowledge_${process.env.SYSTEM_TENANT_ID || '1'}` (check actual pattern in rag-search.service.ts)
     - `refreshIndex(indexName: string): Promise<void>` -- forces an OpenSearch index refresh for immediate search consistency
   - Note: `doc_id` in OpenSearch uses 32-char hex UUID format (no hyphens). The helper must normalize UUIDs with `.replace(/-/g, '')` before querying.

2. Create `fe/e2e/dataset/chunk-verify.spec.ts` with these test cases:

   **Setup (beforeAll):**
   - Create a test dataset via API helper
   - Upload sample.pdf via API helper
   - Trigger parsing via API helper
   - Wait for parsing to complete via `waitForDocumentParsed`
   - Wait for chunks to be indexed via `waitForChunksIndexed`
   - Store dataset ID, document ID

   **Teardown (afterAll):**
   - Delete the test dataset via API helper

   **Test: Chunks exist in OpenSearch after parsing** (`@smoke` tag)
   - Use OpenSearch helper to query chunks by doc_id (normalized to 32-char hex)
   - Assert: At least 1 chunk exists in the index
   - Assert: Each chunk has `content_with_weight` field with non-empty text
   - Assert: Each chunk has `kb_id` matching the dataset ID (32-char hex format)
   - Assert: Each chunk has `doc_id` matching the document ID (32-char hex format)

   **Test: Chunks have embeddings**
   - Query chunks from OpenSearch
   - Assert: Each chunk has `q_vec` field (the embedding vector)
   - Assert: `q_vec` is an array of numbers with length > 0

   **Test: Chunk content matches source document**
   - Query chunks from OpenSearch
   - Assert: At least one chunk's `content_with_weight` contains text from the known sample.pdf content (e.g., "test document" or "knowledge management" -- whatever was put in the sample PDF)

   **Test: Chunks are searchable via dataset search API**
   - Navigate to the dataset detail page in the browser
   - If there's a chunk search/preview feature, use it to search for a term from sample.pdf
   - OR use the API to perform a search query against the dataset
   - Assert: Search returns at least one result matching the query term
   - Assert: Results include chunk content and source document reference

   **Test: Chunk count matches document metadata**
   - Get document metadata via API (check chunk_num field)
   - Get actual chunks from OpenSearch
   - Assert: Document's `chunk_num` field matches the actual number of chunks in OpenSearch

3. If any test fails:
   - Fix the bug in the relevant file
   - Document: `// BUG FOUND: [description] — Fixed in [file]`
   - Known risk areas: UUID format mismatch between API responses (36-char) and OpenSearch (32-char), OpenSearch refresh timing, embedding vector generation failures (model not configured)
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge/fe && cat e2e/helpers/opensearch.helper.ts | grep -q "getChunksByDocId" && cat e2e/dataset/chunk-verify.spec.ts | grep -q "@smoke" && cat e2e/dataset/chunk-verify.spec.ts | grep -q "content_with_weight" && cat e2e/dataset/chunk-verify.spec.ts | grep -q "q_vec" && echo "PASS: Chunk verify spec and OpenSearch helper exist"</automated>
  </verify>
  <acceptance_criteria>
    - fe/e2e/helpers/opensearch.helper.ts exports `OpenSearchHelper` with `getChunksByDocId`, `searchChunks`, `refreshIndex`
    - OpenSearch helper normalizes UUIDs to 32-char hex format before querying
    - fe/e2e/dataset/chunk-verify.spec.ts tests chunk existence, embeddings, content match, searchability, and count consistency
    - At least one test tagged `@smoke`
    - Tests use setup (create dataset + upload + parse + wait) and teardown (delete dataset)
    - Any bugs found are fixed with `// BUG FOUND:` comments
  </acceptance_criteria>
  <done>Chunk verification E2E tests confirm that after parsing, chunks exist in OpenSearch with correct content, embeddings, and metadata. OpenSearch helper enables direct index queries for verification. Chunk count matches document metadata. Any discovered bugs are fixed.</done>
</task>

</tasks>

<verification>
1. OpenSearch helper can connect to OpenSearch and query chunks by doc_id
2. After parsing sample.pdf, chunks exist in OpenSearch with content_with_weight and q_vec fields
3. Chunk count in OpenSearch matches document metadata chunk_num
4. Search queries against the dataset return relevant chunks
5. UUID format handling is correct (32-char hex in OpenSearch, normalization in helper)
</verification>

<success_criteria>
- OpenSearch helper connects to `http://localhost:9201` and queries the `knowledge_{tenant}` index
- After parsing, at least 1 chunk exists with non-empty `content_with_weight` and `q_vec`
- Chunk `kb_id` and `doc_id` match the dataset and document IDs (in 32-char hex format)
- Document `chunk_num` metadata matches actual OpenSearch chunk count
- Search query returns results matching known sample.pdf content
</success_criteria>

<output>
After completion, create `.planning/phases/01-migration-stabilization/01-03-SUMMARY.md`
</output>
