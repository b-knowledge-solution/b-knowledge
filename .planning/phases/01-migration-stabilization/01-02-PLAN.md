---
phase: 01-migration-stabilization
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - fe/e2e/dataset/document-upload.spec.ts
  - fe/e2e/dataset/document-parse.spec.ts
autonomous: true
requirements: [STAB-02, STAB-05]
must_haves:
  truths:
    - "User can upload a PDF document to a dataset without error"
    - "User can select the deepdoc parser and trigger parsing on an uploaded document"
    - "Parsing completes successfully and document status shows parsed"
    - "Parser types mapped in the Python FACTORY are exercised by at least one test"
    - "E2E tests reveal any document upload or parsing bugs through test failures"
  artifacts:
    - path: "fe/e2e/dataset/document-upload.spec.ts"
      provides: "E2E tests for document upload flow"
      contains: "test.*upload"
    - path: "fe/e2e/dataset/document-parse.spec.ts"
      provides: "E2E tests for document parse execution and status tracking"
      contains: "test.*parse"
  key_links:
    - from: "fe/e2e/dataset/document-upload.spec.ts"
      to: "fe/e2e/helpers/api.helper.ts"
      via: "API helper for dataset setup and document upload"
      pattern: "apiHelper.*createDataset"
    - from: "fe/e2e/dataset/document-parse.spec.ts"
      to: "fe/e2e/helpers/wait.helper.ts"
      via: "Wait helper for async parse completion polling"
      pattern: "waitForDocumentParsed"
---

<objective>
Write E2E tests for document upload and parsing pipeline, exercising the full flow of uploading a PDF, triggering deepdoc parsing, and verifying the document reaches parsed status. Fix any bugs the tests reveal in the upload or parsing pipeline.

Purpose: Validates STAB-02 (parsing pipeline bugs) and STAB-05 (parser migration completeness). The document parsing pipeline is the most complex async flow in the system (Redis queue -> Python worker -> OpenSearch indexing) and the most likely source of silent failures.
Output: Green E2E tests for upload and parse flows, with any discovered bugs fixed.
</objective>

<execution_context>
@/home/doana/.claude/get-shit-done/workflows/execute-plan.md
@/home/doana/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-migration-stabilization/01-CONTEXT.md
@.planning/phases/01-migration-stabilization/01-RESEARCH.md
@.planning/phases/01-migration-stabilization/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01-01: Playwright helpers created in previous plan -->

fe/e2e/helpers/api.helper.ts exports:
```typescript
class ApiHelper {
  createDataset(name: string, description?: string): Promise<{ id: string }>
  deleteDataset(id: string): Promise<void>
  getDataset(id: string): Promise<DatasetResponse>
  uploadDocument(datasetId: string, filePath: string): Promise<{ id: string }>
  getDocument(datasetId: string, docId: string): Promise<DocumentResponse>
  triggerParse(datasetId: string, docIds: string[]): Promise<void>
}
```

fe/e2e/helpers/wait.helper.ts exports:
```typescript
function waitForDocumentParsed(request: APIRequestContext, datasetId: string, docId: string, timeoutMs?: number): Promise<void>
function waitForChunksIndexed(request: APIRequestContext, datasetId: string, docId: string, minChunks?: number, timeoutMs?: number): Promise<void>
```

fe/e2e/test-data/sample.pdf — Small test PDF (created in 01-01)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write document upload E2E tests and fix discovered bugs</name>
  <files>fe/e2e/dataset/document-upload.spec.ts</files>
  <read_first>
    - fe/e2e/helpers/api.helper.ts (confirm upload method signature)
    - fe/e2e/helpers/wait.helper.ts (confirm wait helper signatures)
    - fe/e2e/test-data/sample.pdf (confirm test file exists)
    - be/src/modules/rag/routes/rag.routes.ts (confirm document upload route: POST /api/rag/datasets/:id/documents)
    - be/src/modules/rag/controllers/rag.controller.ts (confirm upload controller method)
    - be/src/modules/rag/services/rag-document.service.ts (confirm document creation service)
    - fe/src/features/datasets/pages/DatasetDetailPage.tsx (confirm UI flow for uploading documents)
    - fe/src/features/datasets/components/DocumentTable.tsx (confirm document list UI)
  </read_first>
  <action>
1. Create `fe/e2e/dataset/document-upload.spec.ts` with these test cases:

   **Setup (beforeAll):**
   - Create a test dataset via API helper
   - Store dataset ID for all tests

   **Teardown (afterAll):**
   - Delete the test dataset via API helper (cascading deletes documents)

   **Test: Upload PDF document via UI** (`@smoke` tag)
   - Navigate to dataset detail page: `/datasets/${datasetId}`
   - Click the upload/add document button (identify from DatasetDetailPage.tsx)
   - Use Playwright's file chooser to upload `e2e/test-data/sample.pdf`
   - Wait for upload to complete (progress indicator or document appearing in list)
   - Assert: Document appears in the document table with filename `sample.pdf`
   - Assert: Document status shows "uploaded" or equivalent pre-parse state
   - Assert: GET API returns the document with correct metadata (name, size, type)

   **Test: Upload duplicate filename**
   - Upload sample.pdf again to the same dataset
   - Assert: Either succeeds with a new document entry, or shows a clear error message (not 500)

   **Test: Upload invalid file type** (if the UI allows file type filtering)
   - Attempt to upload a file with a blocked extension (e.g., .exe)
   - Assert: Upload is rejected with a user-visible error, not a server 500

2. If any test fails due to a bug:
   - Fix the bug in the relevant BE/FE file
   - Document: `// BUG FOUND: [description] — Fixed in [file]`
   - Known risk areas: File validation, RustFS/S3 upload, document model creation, UUID format
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge/fe && cat e2e/dataset/document-upload.spec.ts | grep -q "@smoke" && cat e2e/dataset/document-upload.spec.ts | grep -q "Upload PDF" && cat e2e/dataset/document-upload.spec.ts | grep -q "sample.pdf" && echo "PASS: Document upload spec exists"</automated>
  </verify>
  <acceptance_criteria>
    - fe/e2e/dataset/document-upload.spec.ts contains test for "Upload PDF document"
    - Test uses API helper for dataset setup/teardown
    - Test uploads `e2e/test-data/sample.pdf` via UI file chooser
    - Test asserts document appears in document table after upload
    - At least one test tagged `@smoke`
    - Any bugs found are fixed with `// BUG FOUND:` comments
  </acceptance_criteria>
  <done>Document upload E2E tests exist and cover normal upload, duplicate filename, and invalid file type. Tests exercise the full UI upload flow including file chooser interaction.</done>
</task>

<task type="auto">
  <name>Task 2: Write document parsing E2E tests and fix discovered bugs</name>
  <files>fe/e2e/dataset/document-parse.spec.ts</files>
  <read_first>
    - fe/e2e/helpers/wait.helper.ts (confirm waitForDocumentParsed signature)
    - fe/e2e/helpers/api.helper.ts (confirm triggerParse and getDocument methods)
    - be/src/modules/rag/routes/rag.routes.ts (confirm parse trigger route)
    - be/src/modules/rag/services/rag-document.service.ts (confirm parse trigger service method)
    - advance-rag/rag/svr/task_executor.py (understand FACTORY parser mapping and task lifecycle)
    - advance-rag/rag/app/naive.py (understand deepdoc parser -- first ~50 lines for class structure)
    - fe/src/features/datasets/components/DocumentTable.tsx (confirm parse button/trigger UI and status display)
    - fe/src/features/datasets/components/ChangeParserDialog.tsx (confirm parser selection UI)
  </read_first>
  <action>
1. Create `fe/e2e/dataset/document-parse.spec.ts` with these test cases:

   **Setup (beforeAll):**
   - Create a test dataset via API helper
   - Upload sample.pdf via API helper
   - Store dataset ID and document ID

   **Teardown (afterAll):**
   - Delete the test dataset via API helper

   **Test: Trigger parsing and wait for completion** (`@smoke` tag)
   - Navigate to dataset detail page
   - Find the uploaded document in the table
   - Trigger parsing (click parse button or use bulk parse action)
   - Use `waitForDocumentParsed` helper to poll until parsing completes (60 second timeout)
   - Assert: Document status changes to "parsed" (run === '1')
   - Assert: Document progress shows 1.0 or 100%
   - Assert: Document has a non-zero `chunk_num` (chunks were generated)

   **Test: Parse with deepdoc parser (PDF)**
   - Verify the document's parser type is 'naive' (deepdoc) or set it explicitly via parser change dialog
   - Trigger parsing
   - Wait for completion
   - Assert: Chunks are generated (chunk_num > 0)
   - Assert: No error in progress_msg

   **Test: Re-parse already parsed document**
   - Trigger parsing on the already-parsed document
   - Wait for completion
   - Assert: Parsing completes without error
   - Assert: Chunks are regenerated (not duplicated -- chunk count should be similar, not doubled)

   **Test: Cancel parsing in progress** (if cancel is supported)
   - Upload a second document
   - Trigger parsing
   - Immediately attempt to cancel
   - Assert: Document status shows cancelled or returns to unparsed state, not stuck

2. If any test fails due to a bug:
   - Fix the bug in the relevant BE/FE/Python file
   - Document: `// BUG FOUND: [description] — Fixed in [file]`
   - Known risk areas: Redis queue consumption, task executor lifecycle, progress reporting, UUID format between BE and Python worker

3. For STAB-05 (parser migration): After PDF/deepdoc tests pass, add a tagged test section `@parsers` that verifies the parser FACTORY in task_executor.py maps all expected parser types. This can be done by checking the API returns the parser type list, or by reading the FACTORY mapping from the codebase and asserting completeness.
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge/fe && cat e2e/dataset/document-parse.spec.ts | grep -q "@smoke" && cat e2e/dataset/document-parse.spec.ts | grep -q "parsing" && cat e2e/dataset/document-parse.spec.ts | grep -q "waitForDocumentParsed" && echo "PASS: Document parse spec exists"</automated>
  </verify>
  <acceptance_criteria>
    - fe/e2e/dataset/document-parse.spec.ts contains test for "Trigger parsing and wait for completion"
    - Test uses `waitForDocumentParsed` from wait.helper.ts
    - Test asserts `chunk_num > 0` after successful parse
    - Test covers re-parse scenario (no chunk duplication)
    - At least one test tagged `@smoke`
    - Parser migration coverage: test or assertion that verifies parser type completeness (`@parsers` tag)
    - Any bugs found are fixed with `// BUG FOUND:` comments
  </acceptance_criteria>
  <done>Document parsing E2E tests exist covering trigger parse, deepdoc/PDF parsing, re-parse, and parser type completeness. Tests use wait helpers for async completion. Any discovered parsing bugs are fixed.</done>
</task>

</tasks>

<verification>
1. Document upload test uploads a real PDF file through the UI
2. Document parse test triggers parsing and waits for async completion via polling
3. After parsing, chunk_num > 0 (chunks were actually generated)
4. Parser type completeness is validated (STAB-05)
5. Any bugs found in upload or parsing are fixed and documented
</verification>

<success_criteria>
- Upload E2E test successfully uploads sample.pdf through the UI file chooser
- Parse E2E test triggers deepdoc parsing and confirms chunks are generated
- No test relies on fixed sleep waits -- all use polling with timeout
- Re-parse test confirms chunks are regenerated, not duplicated
- All discovered bugs are fixed and documented with `// BUG FOUND:` comments
</success_criteria>

<output>
After completion, create `.planning/phases/01-migration-stabilization/01-02-SUMMARY.md`
</output>
