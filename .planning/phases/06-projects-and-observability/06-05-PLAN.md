---
phase: 06-projects-and-observability
plan: 05
type: execute
wave: 3
depends_on: ["06-03"]
files_modified:
  - fe/src/features/dashboard/api/dashboardApi.ts
  - fe/src/features/dashboard/api/dashboardQueries.ts
  - fe/src/features/dashboard/types/dashboard.types.ts
  - fe/src/features/dashboard/components/QueryAnalyticsCards.tsx
  - fe/src/features/dashboard/components/TopQueriesTable.tsx
  - fe/src/features/dashboard/components/QueriesOverTimeChart.tsx
  - fe/src/features/dashboard/components/FailedQueriesTable.tsx
  - fe/src/features/dashboard/components/FeedbackSummaryCards.tsx
  - fe/src/features/dashboard/components/FeedbackTrendChart.tsx
  - fe/src/features/dashboard/components/NegativeFeedbackTable.tsx
  - fe/src/features/dashboard/pages/AdminDashboardPage.tsx
  - fe/src/i18n/en.json
  - fe/src/i18n/vi.json
  - fe/src/i18n/ja.json
autonomous: true
requirements:
  - OBSV-01
  - OBSV-02
  - OBSV-03
must_haves:
  truths:
    - "Dashboard has 3 tabs: Activity (existing), Query Analytics, RAG Quality"
    - "Query Analytics tab shows 4 gradient stat cards, queries-over-time chart, top 10 queries table, failed queries table"
    - "RAG Quality tab shows 4 gradient stat cards, feedback trend chart, negative feedback table with Langfuse links"
    - "Date range picker and refresh button control all tabs"
    - "Manual refresh with stale indicator (Last updated: X minutes ago)"
    - "View in Langfuse opens new tab with correct trace URL"
  artifacts:
    - path: "fe/src/features/dashboard/components/QueryAnalyticsCards.tsx"
      provides: "4 gradient stat cards for query metrics"
    - path: "fe/src/features/dashboard/components/QueriesOverTimeChart.tsx"
      provides: "Recharts AreaChart for query volume trend"
    - path: "fe/src/features/dashboard/components/NegativeFeedbackTable.tsx"
      provides: "Negative feedback table with Langfuse deep links"
    - path: "fe/src/features/dashboard/pages/AdminDashboardPage.tsx"
      provides: "Extended dashboard with 3-tab layout"
  key_links:
    - from: "fe/src/features/dashboard/pages/AdminDashboardPage.tsx"
      to: "/api/admin/dashboard/analytics/queries"
      via: "useQueryAnalytics hook"
      pattern: "useQueryAnalytics"
    - from: "fe/src/features/dashboard/pages/AdminDashboardPage.tsx"
      to: "/api/admin/dashboard/analytics/feedback"
      via: "useFeedbackAnalytics hook"
      pattern: "useFeedbackAnalytics"
    - from: "fe/src/features/dashboard/components/NegativeFeedbackTable.tsx"
      to: "Langfuse trace URL"
      via: "External link with trace_id"
      pattern: "langfuse.*trace|View in Langfuse"
---

<objective>
Build the analytics dashboard frontend: Query Analytics tab and RAG Quality tab with Recharts charts, stat cards, tables, and Langfuse deep links. Extends existing AdminDashboardPage with tab navigation.

Purpose: OBSV-01 (query analytics dashboard), OBSV-02 (RAG quality metrics), OBSV-03 (feedback analytics with Langfuse links). These are the admin-facing observability views.
Output: 7 new dashboard components, extended AdminDashboardPage with 3 tabs, API hooks, i18n in 3 locales.
</objective>

<execution_context>
@/home/doana/.claude/get-shit-done/workflows/execute-plan.md
@/home/doana/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-projects-and-observability/06-CONTEXT.md
@.planning/phases/06-projects-and-observability/06-UI-SPEC.md
@.planning/phases/06-projects-and-observability/06-RESEARCH.md
@.planning/phases/06-projects-and-observability/06-03-SUMMARY.md

<interfaces>
From Plan 03 API endpoints:
```
GET /api/admin/dashboard/analytics/queries?startDate=X&endDate=Y
Response: {
  totalQueries: number,
  avgResponseTime: number,
  failedRate: number,
  lowConfRate: number,
  topQueries: [{ query: string, count: number, avg_confidence: number }],
  trend: [{ date: string, count: number }]
}

GET /api/admin/dashboard/analytics/feedback?startDate=X&endDate=Y
Response: {
  satisfactionRate: number,
  totalFeedback: number,
  zeroResultRate: number,
  worstDatasets: [{ name: string, satisfactionRate: number }],
  trend: [{ date: string, total: number, positive: number }],
  negativeFeedback: [{ id: string, query: string, answer: string, trace_id: string | null, created_at: string }],
  langfuseBaseUrl: string
}
```

From fe/src/features/dashboard/components/StatCards.tsx:
```typescript
// Gradient stat card pattern: Card with CSS custom property gradient
// style={{ background: `linear-gradient(135deg, var(--dashboard-gradient-${N}-from), var(--dashboard-gradient-${N}-to))` }}
// Use gradientVar: 1 (blue), 2 (green), 3 (amber), 4 (purple)
```

From fe/src/features/dashboard/components/ActivityTrendChart.tsx:
```typescript
// Recharts AreaChart pattern: ResponsiveContainer 100% x 300px
// CartesianGrid strokeDasharray="3 3", vertical={false}
// XAxis/YAxis fontSize: 11, fill: '#94a3b8'
// Area type="monotone", dot={false}, activeDot={{ r: 4, strokeWidth: 2 }}
// Linear gradient fill from 30% to 2% opacity
// cssVar() helper for dark mode color resolution
```

From fe/src/lib/queryKeys.ts:
```typescript
// queryKeys.dashboard.all = ['dashboard']
// queryKeys.dashboard.stats(startDate, endDate) = [...all, 'stats', { startDate, endDate }]
// Add: queryKeys.dashboard.analytics(startDate, endDate)
//      queryKeys.dashboard.feedback(startDate, endDate)
// Use DISTINCT key segments to avoid cache conflicts (Pitfall 5 from RESEARCH.md)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard analytics API, hooks, types, and query keys</name>
  <files>
    fe/src/features/dashboard/api/dashboardApi.ts,
    fe/src/features/dashboard/api/dashboardQueries.ts,
    fe/src/features/dashboard/types/dashboard.types.ts,
    fe/src/lib/queryKeys.ts
  </files>
  <read_first>
    fe/src/features/dashboard/api/dashboardApi.ts,
    fe/src/features/dashboard/api/dashboardQueries.ts,
    fe/src/features/dashboard/types/dashboard.types.ts,
    fe/src/lib/queryKeys.ts
  </read_first>
  <action>
1. Add query keys to `queryKeys.ts` (Pitfall 5: use distinct segments):
   - `analytics: (startDate?: string, endDate?: string) => [...queryKeys.dashboard.all, 'analytics', { startDate, endDate }] as const`
   - `feedback: (startDate?: string, endDate?: string) => [...queryKeys.dashboard.all, 'feedback', { startDate, endDate }] as const`

2. Add types to `dashboard.types.ts`:
   ```typescript
   export interface QueryAnalytics {
     totalQueries: number
     avgResponseTime: number
     failedRate: number
     lowConfRate: number
     topQueries: { query: string; count: number; avg_confidence: number }[]
     trend: { date: string; count: number }[]
   }
   export interface FeedbackAnalytics {
     satisfactionRate: number
     totalFeedback: number
     zeroResultRate: number
     worstDatasets: { name: string; satisfactionRate: number }[]
     trend: { date: string; total: number; positive: number }[]
     negativeFeedback: { id: string; query: string; answer: string; trace_id: string | null; created_at: string }[]
     langfuseBaseUrl: string
   }
   ```

3. Add API functions to `dashboardApi.ts`:
   - `fetchQueryAnalytics(startDate?, endDate?): Promise<QueryAnalytics>` — GET /api/admin/dashboard/analytics/queries
   - `fetchFeedbackAnalytics(startDate?, endDate?): Promise<FeedbackAnalytics>` — GET /api/admin/dashboard/analytics/feedback

4. Add TanStack Query hooks to `dashboardQueries.ts`:
   - `useQueryAnalytics(startDate?, endDate?)` — useQuery with queryKeys.dashboard.analytics(...), staleTime: 5 * 60 * 1000 (5min for manual refresh pattern per CONTEXT.md)
   - `useFeedbackAnalytics(startDate?, endDate?)` — useQuery with queryKeys.dashboard.feedback(...), staleTime: 5 * 60 * 1000
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge && npm run build -w fe 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "fetchQueryAnalytics" fe/src/features/dashboard/api/dashboardApi.ts
    - grep -q "fetchFeedbackAnalytics" fe/src/features/dashboard/api/dashboardApi.ts
    - grep -q "useQueryAnalytics" fe/src/features/dashboard/api/dashboardQueries.ts
    - grep -q "useFeedbackAnalytics" fe/src/features/dashboard/api/dashboardQueries.ts
    - grep -q "QueryAnalytics" fe/src/features/dashboard/types/dashboard.types.ts
    - grep -q "FeedbackAnalytics" fe/src/features/dashboard/types/dashboard.types.ts
    - grep -q "analytics.*startDate\|dashboard.*analytics" fe/src/lib/queryKeys.ts
    - grep -q "feedback.*startDate\|dashboard.*feedback" fe/src/lib/queryKeys.ts
    - grep -q "staleTime" fe/src/features/dashboard/api/dashboardQueries.ts
  </acceptance_criteria>
  <done>API functions, query hooks with 5min staleTime, and types defined. Query keys use distinct segments to avoid cache conflicts.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard chart/table components + page tabs + i18n</name>
  <files>
    fe/src/features/dashboard/components/QueryAnalyticsCards.tsx,
    fe/src/features/dashboard/components/TopQueriesTable.tsx,
    fe/src/features/dashboard/components/QueriesOverTimeChart.tsx,
    fe/src/features/dashboard/components/FailedQueriesTable.tsx,
    fe/src/features/dashboard/components/FeedbackSummaryCards.tsx,
    fe/src/features/dashboard/components/FeedbackTrendChart.tsx,
    fe/src/features/dashboard/components/NegativeFeedbackTable.tsx,
    fe/src/features/dashboard/pages/AdminDashboardPage.tsx,
    fe/src/i18n/en.json,
    fe/src/i18n/vi.json,
    fe/src/i18n/ja.json
  </files>
  <read_first>
    fe/src/features/dashboard/components/StatCards.tsx,
    fe/src/features/dashboard/components/ActivityTrendChart.tsx,
    fe/src/features/dashboard/components/TopUsersTable.tsx,
    fe/src/features/dashboard/pages/AdminDashboardPage.tsx,
    fe/src/i18n/en.json
  </read_first>
  <action>
1. **QueryAnalyticsCards.tsx** — Reuse StatCards gradient pattern:
   - Props: { data: QueryAnalytics | null }
   - 4 cards: Total Queries (gradientVar 1, Search icon), Avg Response Time (gradientVar 2, Clock icon, format as "${ms}ms"), Failed Retrieval Rate (gradientVar 3, AlertTriangle icon, format as "${rate}%"), Low Confidence Rate (gradientVar 4, TrendingDown icon, format as "${rate}%")
   - Grid: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 mb-6`

2. **TopQueriesTable.tsx** — Reuse TopUsersTable pattern:
   - Props: { queries: QueryAnalytics['topQueries'] }
   - Card with CardHeader "Top Queries"
   - Table: Query text, Count, Avg Confidence (formatted to 2 decimal places)
   - Empty state if no queries

3. **QueriesOverTimeChart.tsx** — Reuse ActivityTrendChart pattern:
   - Props: { trend: QueryAnalytics['trend'] }
   - Card with CardHeader "Queries Over Time"
   - ResponsiveContainer height 300px
   - AreaChart with monotone interpolation, same axis/grid styling as ActivityTrendChart
   - Use --chart-primary color for area fill and stroke
   - Linear gradient fill (30% to 2% opacity)
   - Custom tooltip matching existing pattern

4. **FailedQueriesTable.tsx**:
   - Props: { queries: QueryAnalytics['topQueries'] filtered for low confidence }
   - Actually this should receive the topQueries data filtered to show queries with high failure or low confidence. Accept full topQueries and filter client-side for avg_confidence < 0.5, or receive a separate failed queries array from the API if available.
   - Table: Query text, Count, Avg Confidence
   - Full width card

5. **FeedbackSummaryCards.tsx** — Same gradient pattern:
   - Props: { data: FeedbackAnalytics | null }
   - 4 cards: Satisfaction Rate (gradientVar 1, ThumbsUp icon, format as "${rate}%"), Total Feedback (gradientVar 2, MessageCircle icon), Zero-Result Rate (gradientVar 3, SearchX icon, format as "${rate}%"), Lowest Satisfaction dataset name (gradientVar 4, Database icon, show name of worst dataset)

6. **FeedbackTrendChart.tsx** — Same Recharts pattern:
   - Props: { trend: FeedbackAnalytics['trend'] }
   - Card with CardHeader "Feedback Trend"
   - AreaChart with --chart-primary for total, --chart-secondary for positive
   - Two Area components, legend showing "Total" and "Positive"

7. **NegativeFeedbackTable.tsx**:
   - Props: { feedback: FeedbackAnalytics['negativeFeedback'], langfuseBaseUrl: string }
   - Card with CardHeader "Recent Negative Feedback"
   - Table: Query (truncated 100 chars), Answer (truncated 100 chars), Date (formatDistanceToNow), Actions
   - Actions column: if trace_id exists, render "View in Langfuse" link opening `${langfuseBaseUrl.replace(/\/$/, '')}/trace/${trace_id}` in new tab with ExternalLink icon and `rel="noopener noreferrer"`. If no trace_id, show disabled text with tooltip "Langfuse trace not available"

8. **Extend AdminDashboardPage.tsx**:
   - Wrap existing content in Tabs component with 3 tabs: "Activity" (existing content), "Query Analytics" (new), "RAG Quality" (new)
   - Date range picker and refresh button stay in header above tabs (shared across all tabs)
   - Query Analytics tab: imports useQueryAnalytics hook, renders QueryAnalyticsCards + grid row [QueriesOverTimeChart | TopQueriesTable] + FailedQueriesTable
   - RAG Quality tab: imports useFeedbackAnalytics hook, renders FeedbackSummaryCards + FeedbackTrendChart + NegativeFeedbackTable
   - Refresh button invalidates ALL dashboard query keys for current tab
   - Add "Last updated: X minutes ago" below date range picker using formatDistanceToNow from date-fns on the query's dataUpdatedAt
   - Loading overlay per tab (existing pattern: bg-background/50 + Spinner)
   - Empty states with EmptyState component when no data

9. **i18n**: Add all keys from UI-SPEC copywriting contract under dashboard.tabs.*, dashboard.analytics.*, dashboard.feedback.* namespaces to en.json, vi.json, ja.json. Include error state keys.
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge && npm run build -w fe 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "QueryAnalyticsCards" fe/src/features/dashboard/components/QueryAnalyticsCards.tsx
    - grep -q "TopQueriesTable" fe/src/features/dashboard/components/TopQueriesTable.tsx
    - grep -q "QueriesOverTimeChart" fe/src/features/dashboard/components/QueriesOverTimeChart.tsx
    - grep -q "FailedQueriesTable" fe/src/features/dashboard/components/FailedQueriesTable.tsx
    - grep -q "FeedbackSummaryCards" fe/src/features/dashboard/components/FeedbackSummaryCards.tsx
    - grep -q "FeedbackTrendChart" fe/src/features/dashboard/components/FeedbackTrendChart.tsx
    - grep -q "NegativeFeedbackTable" fe/src/features/dashboard/components/NegativeFeedbackTable.tsx
    - grep -q "Tabs\|TabsList\|TabsTrigger" fe/src/features/dashboard/pages/AdminDashboardPage.tsx
    - grep -q "Query Analytics\|queryAnalytics" fe/src/features/dashboard/pages/AdminDashboardPage.tsx
    - grep -q "RAG Quality\|ragQuality" fe/src/features/dashboard/pages/AdminDashboardPage.tsx
    - grep -q "View in Langfuse\|langfuse" fe/src/features/dashboard/components/NegativeFeedbackTable.tsx
    - grep -q "dashboard.tabs.queryAnalytics\|queryAnalytics" fe/src/i18n/en.json
    - grep -q "dashboard.tabs.ragQuality\|ragQuality" fe/src/i18n/en.json
    - grep -rq "dashboard.*analytics\|dashboard.*feedback" fe/src/i18n/vi.json
    - grep -rq "dashboard.*analytics\|dashboard.*feedback" fe/src/i18n/ja.json
  </acceptance_criteria>
  <done>All 7 dashboard components render with correct Recharts/gradient patterns. AdminDashboardPage has 3-tab layout. Langfuse deep links on negative feedback. i18n in 3 locales. FE builds clean.</done>
</task>

</tasks>

<verification>
- `npm run build -w fe` succeeds
- grep confirms all 7 new components exist
- grep confirms Tabs on AdminDashboardPage with 3 tabs
- grep confirms Langfuse links in NegativeFeedbackTable
- grep confirms staleTime for manual refresh pattern
- grep confirms i18n keys in all 3 locales
</verification>

<success_criteria>
- Dashboard shows 3 tabs: Activity, Query Analytics, RAG Quality
- Query Analytics shows 4 stat cards, area chart, top queries table, failed queries table
- RAG Quality shows 4 stat cards, feedback trend chart, negative feedback table
- Charts use existing Recharts pattern (gradient fill, dark mode CSS vars)
- Stat cards use existing gradient system
- "View in Langfuse" opens correct URL in new tab
- "Last updated: X minutes ago" stale indicator visible
- Refresh button invalidates current tab queries
- All strings in 3 locales
- FE compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-projects-and-observability/06-05-SUMMARY.md`
</output>
