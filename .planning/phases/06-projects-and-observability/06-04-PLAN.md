---
phase: 06-projects-and-observability
plan: 04
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - fe/src/features/projects/api/projectApi.ts
  - fe/src/features/projects/api/projectQueries.ts
  - fe/src/features/projects/components/ProjectMemberList.tsx
  - fe/src/features/projects/components/ProjectDatasetPicker.tsx
  - fe/src/features/projects/components/ProjectActivityFeed.tsx
  - fe/src/features/projects/pages/ProjectDetailPage.tsx
  - fe/src/features/projects/pages/ProjectListPage.tsx
  - fe/src/features/projects/types/project.types.ts
  - fe/src/i18n/en.json
  - fe/src/i18n/vi.json
  - fe/src/i18n/ja.json
  - fe/src/layouts/Sidebar.tsx
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
  - PROJ-03
  - PROJ-04
must_haves:
  truths:
    - "Projects page appears in Data Studio sidebar (CASL-gated)"
    - "Project detail page has Members tab with add/remove member functionality"
    - "Project detail page has dataset binding with multi-select picker"
    - "Project detail page has Activity tab with paginated feed"
    - "Project list is tenant-scoped showing only user's org projects"
    - "Delete project requires typing project name to confirm"
  artifacts:
    - path: "fe/src/features/projects/components/ProjectMemberList.tsx"
      provides: "Member table with role badges and remove action"
    - path: "fe/src/features/projects/components/ProjectDatasetPicker.tsx"
      provides: "Multi-select dataset binding dialog"
    - path: "fe/src/features/projects/components/ProjectActivityFeed.tsx"
      provides: "Paginated activity feed from audit log"
  key_links:
    - from: "fe/src/features/projects/components/ProjectMemberList.tsx"
      to: "/api/projects/:id/members"
      via: "TanStack Query hooks"
      pattern: "useProjectMembers|members"
    - from: "fe/src/features/projects/components/ProjectDatasetPicker.tsx"
      to: "/api/projects/:id/datasets"
      via: "TanStack Query hooks"
      pattern: "useProjectDatasets|bindDatasets"
    - from: "fe/src/features/projects/components/ProjectActivityFeed.tsx"
      to: "/api/projects/:id/activity"
      via: "TanStack Query hooks"
      pattern: "useProjectActivity|activity"
---

<objective>
Build the frontend project management UI: member list with add/remove, dataset binding picker, activity feed, and Data Studio sidebar integration. Extends existing ProjectDetailPage and ProjectListPage with new tabs and components.

Purpose: PROJ-01 (project CRUD UI with tenant scope), PROJ-02 (dataset binding UI), PROJ-03 (member management UI), PROJ-04 (cross-project search is auto-include, no explicit UI needed per decision).
Output: 3 new components, extended pages, API hooks, i18n keys in 3 locales.
</objective>

<execution_context>
@/home/doana/.claude/get-shit-done/workflows/execute-plan.md
@/home/doana/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-projects-and-observability/06-CONTEXT.md
@.planning/phases/06-projects-and-observability/06-UI-SPEC.md
@.planning/phases/06-projects-and-observability/06-02-SUMMARY.md

<interfaces>
From Plan 02 API endpoints:
```
GET  /api/projects/:id/members          -> { members: [{ id, user_id, email, name, role, created_at }] }
POST /api/projects/:id/members          <- { user_id: string } -> 201
DELETE /api/projects/:id/members/:userId -> 204
GET  /api/projects/:id/datasets         -> { datasets: [ProjectDataset with dataset_name, dataset_doc_count] }
POST /api/projects/:id/datasets         <- { dataset_ids: string[] } -> 200
DELETE /api/projects/:id/datasets/:datasetId -> 204
GET  /api/projects/:id/activity         -> { items: AuditLogEntry[], total: number }
GET  /api/projects/cross-project-datasets -> { dataset_ids: string[] }
```

From fe/src/features/projects/api/projectApi.ts:
```typescript
export interface Project {
  id: string; name: string; description: string | null; avatar: string | null;
  category: ProjectCategory; status: string; is_private: boolean;
  dataset_count?: number; created_by: string | null;
  created_at: string; updated_at: string;
}
export interface ProjectDataset {
  id: string; project_id: string; dataset_id: string;
  auto_created: boolean; created_at: string;
  dataset_name?: string; dataset_doc_count?: number;
}
```

From fe/src/lib/queryKeys.ts:
```typescript
// Pattern: queryKeys.<feature>.<entity>(params)
// Add: queryKeys.projects.members(projectId)
//      queryKeys.projects.datasets(projectId)
//      queryKeys.projects.activity(projectId)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project API functions, query hooks, and query keys</name>
  <files>
    fe/src/features/projects/api/projectApi.ts,
    fe/src/features/projects/api/projectQueries.ts,
    fe/src/features/projects/types/project.types.ts,
    fe/src/lib/queryKeys.ts
  </files>
  <read_first>
    fe/src/features/projects/api/projectApi.ts,
    fe/src/features/projects/api/projectQueries.ts,
    fe/src/features/projects/types/project.types.ts,
    fe/src/lib/queryKeys.ts
  </read_first>
  <action>
1. Add to `queryKeys.ts` under the projects key factory:
   - `members: (projectId: string) => [...queryKeys.projects.all, projectId, 'members'] as const`
   - `datasets: (projectId: string) => [...queryKeys.projects.all, projectId, 'datasets'] as const`
   - `activity: (projectId: string) => [...queryKeys.projects.all, projectId, 'activity'] as const`

2. Add types to `projectApi.ts`:
   - `ProjectMember`: { id: string, user_id: string, email: string, name: string, role: string, created_at: string }
   - `ActivityEntry`: { id: string, action: string, resource_type: string, resource_id: string, user_email: string, details: Record<string, unknown>, created_at: string }

3. Add API functions to `projectApi.ts`:
   - `fetchProjectMembers(projectId: string): Promise<ProjectMember[]>` — GET /api/projects/:id/members
   - `addProjectMember(projectId: string, userId: string): Promise<void>` — POST /api/projects/:id/members
   - `removeProjectMember(projectId: string, userId: string): Promise<void>` — DELETE /api/projects/:id/members/:userId
   - `fetchProjectDatasets(projectId: string): Promise<ProjectDataset[]>` — GET /api/projects/:id/datasets
   - `bindProjectDatasets(projectId: string, datasetIds: string[]): Promise<void>` — POST /api/projects/:id/datasets
   - `unbindProjectDataset(projectId: string, datasetId: string): Promise<void>` — DELETE /api/projects/:id/datasets/:datasetId
   - `fetchProjectActivity(projectId: string, limit?: number, offset?: number): Promise<{ items: ActivityEntry[], total: number }>` — GET /api/projects/:id/activity

4. Add TanStack Query hooks to `projectQueries.ts`:
   - `useProjectMembers(projectId)` — useQuery with queryKeys.projects.members(projectId)
   - `useAddMember(projectId)` — useMutation, invalidates members query key on success
   - `useRemoveMember(projectId)` — useMutation, invalidates members query key on success
   - `useProjectDatasets(projectId)` — useQuery with queryKeys.projects.datasets(projectId)
   - `useBindDatasets(projectId)` — useMutation, invalidates datasets query key on success
   - `useUnbindDataset(projectId)` — useMutation, invalidates datasets query key on success
   - `useProjectActivity(projectId, limit, offset)` — useQuery with queryKeys.projects.activity(projectId)

5. Re-export new types from `project.types.ts`.
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge && npm run build -w fe 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "fetchProjectMembers" fe/src/features/projects/api/projectApi.ts
    - grep -q "useProjectMembers" fe/src/features/projects/api/projectQueries.ts
    - grep -q "useAddMember" fe/src/features/projects/api/projectQueries.ts
    - grep -q "useProjectDatasets" fe/src/features/projects/api/projectQueries.ts
    - grep -q "useBindDatasets" fe/src/features/projects/api/projectQueries.ts
    - grep -q "useProjectActivity" fe/src/features/projects/api/projectQueries.ts
    - grep -q "members.*projectId\|projects.*members" fe/src/lib/queryKeys.ts
    - grep -q "ProjectMember" fe/src/features/projects/api/projectApi.ts
  </acceptance_criteria>
  <done>All project API functions and query hooks exist. Query keys registered. Types defined.</done>
</task>

<task type="auto">
  <name>Task 2: Project UI components + page extensions + sidebar + i18n</name>
  <files>
    fe/src/features/projects/components/ProjectMemberList.tsx,
    fe/src/features/projects/components/ProjectDatasetPicker.tsx,
    fe/src/features/projects/components/ProjectActivityFeed.tsx,
    fe/src/features/projects/pages/ProjectDetailPage.tsx,
    fe/src/features/projects/pages/ProjectListPage.tsx,
    fe/src/layouts/Sidebar.tsx,
    fe/src/i18n/en.json,
    fe/src/i18n/vi.json,
    fe/src/i18n/ja.json
  </files>
  <read_first>
    fe/src/features/projects/pages/ProjectDetailPage.tsx,
    fe/src/features/projects/pages/ProjectListPage.tsx,
    fe/src/layouts/Sidebar.tsx,
    fe/src/i18n/en.json,
    fe/src/features/projects/components/EntityPermissionModal.tsx,
    fe/src/features/dashboard/components/StatCards.tsx
  </read_first>
  <action>
1. Create `ProjectMemberList.tsx`:
   - Props: { projectId: string }
   - Uses useProjectMembers(projectId), useAddMember(projectId), useRemoveMember(projectId) hooks
   - "Add Member" button opens Dialog with user search (text input, searches org users via existing user API)
   - Table: columns = Name, Email, Role (Badge component), Actions (Remove button)
   - Remove triggers confirmation Dialog: "Remove {name} from this project? They will lose access to all project datasets."
   - Empty state: EmptyState component with "No members added yet" copy
   - i18n keys from UI-SPEC copywriting contract (projects.addMember, projects.removeMemberConfirm, projects.members.empty, projects.members.emptyDescription)
   - Dark mode support via Tailwind dark: prefix

2. Create `ProjectDatasetPicker.tsx`:
   - Props: { projectId: string }
   - Uses useProjectDatasets(projectId), useBindDatasets(projectId), useUnbindDataset(projectId)
   - "Bind Datasets" button opens Dialog with checkbox list of available org datasets (excluding already-bound)
   - Each checkbox shows dataset name and document count
   - Bound datasets shown in table with Name, Doc Count, and Remove (unbind) button
   - Unbind triggers confirmation Dialog: "Unbinding will immediately revoke access for all project members."
   - i18n keys: projects.bindDatasets, projects.unbindConfirm

3. Create `ProjectActivityFeed.tsx`:
   - Props: { projectId: string }
   - Uses useProjectActivity(projectId, 20, offset)
   - Renders list of activity entries: icon (based on action type) + description + relative time (formatDistanceToNow from date-fns)
   - "Load More" button at bottom (not infinite scroll, per CONTEXT.md decision)
   - Empty state: "No recent activity"
   - i18n keys: projects.tabs.activity, projects.activity.empty, projects.activity.emptyDescription, common.loadMore

4. Extend `ProjectDetailPage.tsx`:
   - Add "Members" and "Activity" tabs to existing tab navigation
   - Members tab renders ProjectMemberList
   - Activity tab renders ProjectActivityFeed
   - Datasets section renders ProjectDatasetPicker
   - Add delete project action to dropdown: opens Dialog requiring typing project name to confirm (per CONTEXT.md destructive action UX)
   - i18n keys: projects.deleteConfirm.title, projects.deleteConfirm.body

5. Extend `ProjectListPage.tsx`:
   - Ensure project cards show dataset_count and member count
   - Grid layout: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4` per UI-SPEC
   - Empty state: "No projects yet" with "Create a project to organize datasets and manage team access."

6. Update `Sidebar.tsx`:
   - Add "Projects" nav item in Data Studio section
   - Gate with CASL ability.can('read', 'Project')
   - Use FolderKanban icon from Lucide

7. Add i18n keys to all 3 locales (en.json, vi.json, ja.json):
   - All keys from UI-SPEC copywriting contract under projects.* namespace
   - Vietnamese and Japanese translations for all strings
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge && npm run build -w fe 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ProjectMemberList" fe/src/features/projects/components/ProjectMemberList.tsx
    - grep -q "ProjectDatasetPicker" fe/src/features/projects/components/ProjectDatasetPicker.tsx
    - grep -q "ProjectActivityFeed" fe/src/features/projects/components/ProjectActivityFeed.tsx
    - grep -q "Members\|members" fe/src/features/projects/pages/ProjectDetailPage.tsx
    - grep -q "Activity\|activity" fe/src/features/projects/pages/ProjectDetailPage.tsx
    - grep -q "projects.createProject\|createProject" fe/src/i18n/en.json
    - grep -q "projects.addMember\|addMember" fe/src/i18n/en.json
    - grep -q "Projects\|projects" fe/src/layouts/Sidebar.tsx
    - grep -rq "projects\." fe/src/i18n/vi.json
    - grep -rq "projects\." fe/src/i18n/ja.json
  </acceptance_criteria>
  <done>All 3 new components render with proper data hooks. Project detail page has Members and Activity tabs. Sidebar has Projects nav item. All i18n keys in 3 locales. FE builds clean.</done>
</task>

</tasks>

<verification>
- `npm run build -w fe` succeeds
- grep confirms all 3 new components exist
- grep confirms Members and Activity tabs on detail page
- grep confirms Projects in Sidebar
- grep confirms i18n keys in all 3 locales
</verification>

<success_criteria>
- Project member list shows members with role badges, add/remove works
- Dataset picker shows bound datasets, bind/unbind works with confirmation dialogs
- Activity feed shows paginated audit entries with "Load More"
- Project deletion requires typing project name
- Projects nav item in Data Studio sidebar, CASL-gated
- All strings in 3 locales (en, vi, ja)
- FE compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-projects-and-observability/06-04-SUMMARY.md`
</output>
