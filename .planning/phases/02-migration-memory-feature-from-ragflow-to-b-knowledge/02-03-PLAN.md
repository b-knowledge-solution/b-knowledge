---
phase: 02-migration-memory-feature-from-ragflow-to-b-knowledge
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - fe/src/features/memory/api/memoryApi.ts
  - fe/src/features/memory/api/memoryQueries.ts
  - fe/src/features/memory/components/MemoryCard.tsx
  - fe/src/features/memory/pages/MemoryListPage.tsx
  - fe/src/features/memory/index.ts
  - fe/src/layouts/sidebarNav.ts
  - fe/src/app/App.tsx
  - fe/src/app/routeConfig.ts
  - fe/src/i18n/en.json
  - fe/src/i18n/vi.json
  - fe/src/i18n/ja.json
  - fe/src/lib/queryKeys.ts
autonomous: true
requirements:
  - MEM-LIST-UI
  - MEM-SIDEBAR-NAV
  - MEM-FE-TYPES

must_haves:
  truths:
    - "Memory list page shows card grid of memory pools"
    - "Users can create, edit, and delete memory pools from the list page"
    - "Memory appears under Agents nav group in sidebar (D-12)"
    - "All UI strings available in en, vi, ja locales"
  artifacts:
    - path: "fe/src/features/memory/api/memoryApi.ts"
      provides: "Raw HTTP calls for memory CRUD"
      exports: ["memoryApi"]
    - path: "fe/src/features/memory/api/memoryQueries.ts"
      provides: "TanStack Query hooks"
      exports: ["useMemories", "useMemory", "useCreateMemory"]
    - path: "fe/src/features/memory/pages/MemoryListPage.tsx"
      provides: "Memory list page with card grid"
      exports: ["MemoryListPage"]
    - path: "fe/src/layouts/sidebarNav.ts"
      provides: "Agents group with Memory child link"
      contains: "nav.memory"
  key_links:
    - from: "fe/src/features/memory/pages/MemoryListPage.tsx"
      to: "fe/src/features/memory/api/memoryQueries.ts"
      via: "useMemories hook"
      pattern: "useMemories"
    - from: "fe/src/layouts/sidebarNav.ts"
      to: "/memory"
      via: "sidebar nav config"
      pattern: "path.*memory"
---

<objective>
Build the frontend memory list page with card grid, API layer, sidebar navigation, i18n, and routing.

Purpose: Users need to see and manage their memory pools. This is the primary UI entry point for the memory feature (D-12, D-13).
Output: Working /memory route showing memory pool cards with create/edit/delete actions.
</objective>

<execution_context>
@/home/doana/.claude/get-shit-done/workflows/execute-plan.md
@/home/doana/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-migration-memory-feature-from-ragflow-to-b-knowledge/02-CONTEXT.md
@.planning/phases/02-migration-memory-feature-from-ragflow-to-b-knowledge/02-RESEARCH.md
@.planning/phases/02-migration-memory-feature-from-ragflow-to-b-knowledge/02-01-SUMMARY.md
</context>

<interfaces>
From fe/src/features/memory/types/memory.types.ts:
```typescript
export interface Memory { id, name, description, avatar, memory_type, storage_type, memory_size, ... }
export interface CreateMemoryDto { name, description?, memory_type?, storage_type?, ... }
export const MemoryType = { RAW: 1, SEMANTIC: 2, EPISODIC: 4, PROCEDURAL: 8 }
export function hasMemoryType(bitmask: number, type: number): boolean
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: API layer + query hooks + query keys</name>
  <files>
    fe/src/features/memory/api/memoryApi.ts,
    fe/src/features/memory/api/memoryQueries.ts,
    fe/src/lib/queryKeys.ts
  </files>
  <read_first>
    fe/src/features/agents/api/agentApi.ts,
    fe/src/features/agents/api/agentQueries.ts,
    fe/src/lib/queryKeys.ts,
    fe/src/lib/api.ts,
    fe/src/features/memory/types/memory.types.ts
  </read_first>
  <action>
    1. Add to `queryKeys.ts` (following existing pattern):
       ```
       memory: {
         all: ['memory'] as const,
         lists: () => [...queryKeys.memory.all, 'list'] as const,
         list: (filters?: Record<string, unknown>) => [...queryKeys.memory.lists(), filters] as const,
         details: () => [...queryKeys.memory.all, 'detail'] as const,
         detail: (id: string) => [...queryKeys.memory.details(), id] as const,
         messages: (id: string) => [...queryKeys.memory.detail(id), 'messages'] as const,
       }
       ```

    2. Create `memoryApi.ts` following agentApi.ts pattern:
       - `getMemories(): Promise<Memory[]>` — GET /api/memory
       - `getMemory(id: string): Promise<Memory>` — GET /api/memory/:id
       - `createMemory(data: CreateMemoryDto): Promise<Memory>` — POST /api/memory
       - `updateMemory(id: string, data: UpdateMemoryDto): Promise<Memory>` — PUT /api/memory/:id
       - `deleteMemory(id: string): Promise<void>` — DELETE /api/memory/:id
       - `getMemoryMessages(id: string, params?: { page?: number, page_size?: number, keyword?: string, message_type?: number }): Promise<{ items: MemoryMessage[], total: number }>` — GET /api/memory/:id/messages
       - `searchMemoryMessages(id: string, query: string, topK?: number): Promise<MemorySearchResult[]>` — POST /api/memory/:id/search
       - `deleteMemoryMessage(id: string, messageId: string): Promise<void>` — DELETE /api/memory/:id/messages/:messageId
       - `forgetMemoryMessage(id: string, messageId: string): Promise<void>` — PUT /api/memory/:id/messages/:messageId/forget

    3. Create `memoryQueries.ts` following agentQueries.ts pattern:
       - `useMemories()` — useQuery with queryKeys.memory.lists()
       - `useMemory(id: string)` — useQuery with queryKeys.memory.detail(id), enabled: !!id
       - `useCreateMemory()` — useMutation that invalidates queryKeys.memory.lists()
       - `useUpdateMemory()` — useMutation that invalidates queryKeys.memory.lists() + detail
       - `useDeleteMemory()` — useMutation that invalidates queryKeys.memory.lists()
       - `useMemoryMessages(id: string, params?)` — useQuery with queryKeys.memory.messages(id)
       - `useSearchMemoryMessages()` — useMutation (search is a POST)
       - `useDeleteMemoryMessage()` — useMutation that invalidates messages
       - `useForgetMemoryMessage()` — useMutation that invalidates messages
  </action>
  <verify>
    <automated>cd "/mnt/d/Project/b-solution/b-knowledge - Copy" && npx tsc --noEmit -p fe/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "memory:" fe/src/lib/queryKeys.ts
    - grep -q "getMemories" fe/src/features/memory/api/memoryApi.ts
    - grep -q "useMemories" fe/src/features/memory/api/memoryQueries.ts
    - grep -q "useCreateMemory" fe/src/features/memory/api/memoryQueries.ts
    - grep -q "useMemoryMessages" fe/src/features/memory/api/memoryQueries.ts
    - grep -q "searchMemoryMessages" fe/src/features/memory/api/memoryApi.ts
  </acceptance_criteria>
  <done>Complete FE data layer: raw API calls, TanStack Query hooks for all memory operations, query keys registered centrally.</done>
</task>

<task type="auto">
  <name>Task 2: Memory list page + card + sidebar nav + routing + i18n</name>
  <files>
    fe/src/features/memory/components/MemoryCard.tsx,
    fe/src/features/memory/pages/MemoryListPage.tsx,
    fe/src/features/memory/index.ts,
    fe/src/layouts/sidebarNav.ts,
    fe/src/app/App.tsx,
    fe/src/app/routeConfig.ts,
    fe/src/i18n/en.json,
    fe/src/i18n/vi.json,
    fe/src/i18n/ja.json
  </files>
  <read_first>
    fe/src/features/agents/pages/AgentListPage.tsx,
    fe/src/features/agents/components/AgentCard.tsx,
    fe/src/layouts/sidebarNav.ts,
    fe/src/app/App.tsx,
    fe/src/app/routeConfig.ts,
    fe/src/i18n/en.json,
    fe/src/features/memory/api/memoryQueries.ts,
    fe/src/features/memory/types/memory.types.ts
  </read_first>
  <action>
    1. Create `MemoryCard.tsx` following AgentCard.tsx pattern:
       - Props: `memory: Memory`, `onEdit: (m: Memory) => void`, `onDelete: (m: Memory) => void`
       - Card displays: name, description (truncated), storage_type badge ('table' or 'graph'), memory type chips (show which of Raw/Semantic/Episodic/Procedural are enabled using hasMemoryType bitmask helper), extraction_mode badge, scope_type
       - Kebab dropdown menu with Edit and Delete actions
       - Brain icon from lucide-react as default avatar
       - Dark mode support with `dark:` variants

    2. Create `MemoryListPage.tsx` following AgentListPage.tsx pattern:
       - Uses `useMemories()` hook for data
       - Header with title (t('memory.title')) and Create button
       - Create dialog with form fields: name (required), description, memory_type (checkboxes for 4 types), storage_type (radio: table/graph), extraction_mode (radio: batch/realtime), permission (radio: me/team), scope_type (select: user/agent/team)
       - Card grid layout (responsive: 1 col mobile, 2 cols md, 3 cols lg)
       - Edit dialog reuses create dialog form with pre-filled values
       - Delete confirmation dialog with pool name
       - Empty state when no pools exist
       - Loading skeleton during fetch

    3. Convert Agents from top-level link to expandable group in `sidebarNav.ts` per D-12:
       - Change the `/agents` entry from `SidebarNavItem` to `SidebarNavGroup`:
         ```
         {
           labelKey: 'nav.agents',
           icon: Workflow,
           children: [
             { path: '/agents', labelKey: 'nav.agentList', icon: Workflow, iconSize: 18 },
             { path: '/memory', labelKey: 'nav.memory', icon: Brain, iconSize: 18 },
           ],
         }
         ```
       - Import `Brain` from lucide-react

    4. Add route in `App.tsx`:
       - Import `MemoryListPage` from `@/features/memory/pages/MemoryListPage`
       - Add `<Route path="memory" element={<FeatureErrorBoundary><MemoryListPage /></FeatureErrorBoundary>} />`
       - Place near agent routes

    5. Add route metadata in `routeConfig.ts`:
       - `'/memory': { title: 'Memory', featureId: 'memory' }`

    6. Add i18n keys to all 3 locale files:
       - en.json: `"memory": { "title": "Memory", "create": "Create Memory Pool", "edit": "Edit Memory Pool", "delete": "Delete Memory Pool", "deleteConfirm": "Are you sure you want to delete \"{{name}}\"? All stored memories will be permanently lost.", "name": "Name", "description": "Description", "memoryType": "Memory Types", "raw": "Raw", "semantic": "Semantic", "episodic": "Episodic", "procedural": "Procedural", "storageType": "Storage Type", "table": "Table", "graph": "Graph", "extractionMode": "Extraction Mode", "batch": "Batch", "realtime": "Real-time", "permission": "Permission", "permMe": "Me only", "permTeam": "Team", "scopeType": "Scope", "scopeUser": "User", "scopeAgent": "Agent", "scopeTeam": "Team", "empty": "No memory pools yet", "emptyHint": "Create a memory pool to help your agents remember across sessions" }`, `"nav": { ..., "agentList": "Agent List", "memory": "Memory" }`
       - vi.json: Vietnamese translations for same keys
       - ja.json: Japanese translations for same keys

    7. Update barrel `index.ts` to export MemoryListPage and all types
  </action>
  <verify>
    <automated>cd "/mnt/d/Project/b-solution/b-knowledge - Copy" && npx tsc --noEmit -p fe/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "MemoryListPage" fe/src/features/memory/pages/MemoryListPage.tsx
    - grep -q "MemoryCard" fe/src/features/memory/components/MemoryCard.tsx
    - grep -q "nav.memory" fe/src/layouts/sidebarNav.ts
    - grep -q "Brain" fe/src/layouts/sidebarNav.ts
    - grep -q "children" fe/src/layouts/sidebarNav.ts
    - grep -q "memory" fe/src/app/App.tsx
    - grep -q "memory.*title" fe/src/i18n/en.json
    - grep -q "agentList" fe/src/i18n/en.json
  </acceptance_criteria>
  <done>Memory list page renders card grid. Sidebar shows Memory under Agents group (D-12). Route /memory registered. All strings in 3 locales.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p fe/tsconfig.json` passes
- MemoryListPage exported and routed at /memory
- Sidebar nav converts Agents to group with Agent List + Memory children
- i18n keys present in all 3 locale files
</verification>

<success_criteria>
- /memory route renders memory pool card grid
- Create dialog allows setting all memory pool options
- Sidebar shows Memory under Agents expandable group
- All UI strings in en, vi, ja
</success_criteria>

<output>
After completion, create `.planning/phases/02-migration-memory-feature-from-ragflow-to-b-knowledge/02-03-SUMMARY.md`
</output>
