---
phase: 02-migration-memory-feature-from-ragflow-to-b-knowledge
plan: 05
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - fe/src/features/memory/pages/MemoryDetailPage.tsx
  - fe/src/features/memory/components/MemoryMessageTable.tsx
  - fe/src/features/memory/components/MemorySettingsPanel.tsx
  - fe/src/features/memory/components/ImportHistoryDialog.tsx
  - fe/src/features/memory/index.ts
  - fe/src/features/memory/api/memoryApi.ts
  - fe/src/features/memory/api/memoryQueries.ts
  - fe/src/app/App.tsx
  - fe/src/app/routeConfig.ts
  - fe/src/i18n/en.json
  - fe/src/i18n/vi.json
  - fe/src/i18n/ja.json
autonomous: true
requirements:
  - MEM-DETAIL-UI
  - MEM-IMPORT

must_haves:
  truths:
    - "Users can browse memory messages in a searchable table"
    - "Users can edit memory pool settings (embedding model, LLM, prompts, forgetting policy)"
    - "Users can import existing chat history into a memory pool"
    - "Message table supports pagination, search, and type filtering"
  artifacts:
    - path: "fe/src/features/memory/pages/MemoryDetailPage.tsx"
      provides: "Memory detail page with tabs for messages and settings"
      exports: ["MemoryDetailPage"]
    - path: "fe/src/features/memory/components/MemoryMessageTable.tsx"
      provides: "Searchable message browser table"
      exports: ["MemoryMessageTable"]
    - path: "fe/src/features/memory/components/MemorySettingsPanel.tsx"
      provides: "Settings form for pool configuration"
      exports: ["MemorySettingsPanel"]
    - path: "fe/src/features/memory/components/ImportHistoryDialog.tsx"
      provides: "Chat history import dialog"
      exports: ["ImportHistoryDialog"]
  key_links:
    - from: "fe/src/features/memory/pages/MemoryDetailPage.tsx"
      to: "fe/src/features/memory/api/memoryQueries.ts"
      via: "useMemory, useMemoryMessages hooks"
      pattern: "useMemory|useMemoryMessages"
    - from: "fe/src/features/memory/components/ImportHistoryDialog.tsx"
      to: "fe/src/features/memory/api/memoryApi.ts"
      via: "importChatHistory API call"
      pattern: "importChatHistory"
---

<objective>
Build the memory detail page with message browser, settings panel, and chat history import dialog.

Purpose: Users need to view stored memories, configure pool settings (D-13, D-14), and import past conversations (D-11).
Output: Working /memory/:id route with tabbed view (messages + settings) and import dialog.
</objective>

<execution_context>
@/home/doana/.claude/get-shit-done/workflows/execute-plan.md
@/home/doana/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-migration-memory-feature-from-ragflow-to-b-knowledge/02-CONTEXT.md
@.planning/phases/02-migration-memory-feature-from-ragflow-to-b-knowledge/02-RESEARCH.md
@.planning/phases/02-migration-memory-feature-from-ragflow-to-b-knowledge/02-03-SUMMARY.md
</context>

<interfaces>
From fe/src/features/memory/api/memoryQueries.ts:
```typescript
useMemory(id: string): UseQueryResult<Memory>
useMemoryMessages(id: string, params?): UseQueryResult<{ items: MemoryMessage[], total: number }>
useUpdateMemory(): UseMutationResult
useDeleteMemoryMessage(): UseMutationResult
useForgetMemoryMessage(): UseMutationResult
useSearchMemoryMessages(): UseMutationResult
```

From fe/src/features/memory/types/memory.types.ts:
```typescript
Memory, MemoryMessage, MemoryType, hasMemoryType, CreateMemoryDto, UpdateMemoryDto, MemorySearchResult
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add import API endpoints + message detail page with table and settings</name>
  <files>
    fe/src/features/memory/api/memoryApi.ts,
    fe/src/features/memory/api/memoryQueries.ts,
    fe/src/features/memory/components/MemoryMessageTable.tsx,
    fe/src/features/memory/components/MemorySettingsPanel.tsx,
    fe/src/features/memory/components/ImportHistoryDialog.tsx
  </files>
  <read_first>
    fe/src/features/memory/api/memoryApi.ts,
    fe/src/features/memory/api/memoryQueries.ts,
    fe/src/features/memory/types/memory.types.ts,
    fe/src/features/datasets/components/DocumentTable.tsx,
    fe/src/features/agents/pages/AgentCanvasPage.tsx,
    fe/src/components/ui/table.tsx,
    fe/src/components/ui/tabs.tsx
  </read_first>
  <action>
    1. Add to `memoryApi.ts`:
       - `importChatHistory(memoryId: string, sessionId: string): Promise<{ imported: number }>` -- POST /api/memory/:id/import with body { session_id }
       - `getChatSessions(): Promise<Array<{ id: string, name: string, created_at: string }>>` -- GET /api/chat/conversations (the existing endpoint for listing chat conversations; do NOT use /api/chat/sessions which does not exist)

    2. Add to `memoryQueries.ts`:
       - `useImportChatHistory()` -- useMutation calling memoryApi.importChatHistory, invalidates memory messages on success

    3. Create `MemoryMessageTable.tsx`:
       - Props: `memoryId: string, tenantId: string`
       - Uses `useMemoryMessages(memoryId, { page, page_size, keyword, message_type })` for data
       - Search input for keyword filtering (debounced 300ms)
       - Type filter dropdown (All / Raw / Semantic / Episodic / Procedural)
       - Table columns: Content (truncated to 200 chars with tooltip), Type (badge with color), Status (active/forgotten), Created At (relative time), Actions (forget, delete)
       - Pagination controls at bottom (page size selector: 10/20/50, page navigation)
       - Type badges: Raw=gray, Semantic=blue, Episodic=amber, Procedural=green
       - Forget action calls useForgetMemoryMessage, Delete action calls useDeleteMemoryMessage with confirmation
       - Empty state: "No memories stored yet"
       - Dark mode support

    4. Create `MemorySettingsPanel.tsx`:
       - Props: `memory: Memory, onSave: (data: UpdateMemoryDto) => void`
       - Form sections:
         a. General: name (input), description (textarea)
         b. Memory Types: checkboxes for Raw/Semantic/Episodic/Procedural (bitmask D-01)
         c. Storage: radio for table/graph (D-06)
         d. Extraction: radio for batch/realtime (D-02)
         e. Models: embd_id select (embedding models from LLM provider list), llm_id select (LLM models from provider list), temperature slider 0-2 (D-14)
         f. Prompts: system_prompt textarea, user_prompt textarea, with "Reset to default" buttons (D-03)
         g. Forgetting: memory_size number input (bytes, show as MB), forgetting_policy display (FIFO only, D-07)
         h. Access: permission radio (me/team), scope_type select (user/agent/team) (D-10)
       - Save button calls onSave with updated fields
       - Uses native useState for form state (no form library per FE conventions)

    5. Create `ImportHistoryDialog.tsx`:
       - Props: `memoryId: string, open: boolean, onOpenChange: (open: boolean) => void`
       - Dialog with:
         a. Title: "Import Chat History"
         b. Description explaining that past conversations will be processed through memory extraction
         c. Chat session selector (dropdown listing recent conversations from getChatSessions which calls GET /api/chat/conversations, or simple session ID input)
         d. Import button that calls useImportChatHistory
         e. Progress state: idle / importing / complete with imported count
         f. Close button after completion
  </action>
  <verify>
    <automated>cd "/mnt/d/Project/b-solution/b-knowledge - Copy" && npx tsc --noEmit -p fe/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "MemoryMessageTable" fe/src/features/memory/components/MemoryMessageTable.tsx
    - grep -q "MemorySettingsPanel" fe/src/features/memory/components/MemorySettingsPanel.tsx
    - grep -q "ImportHistoryDialog" fe/src/features/memory/components/ImportHistoryDialog.tsx
    - grep -q "importChatHistory" fe/src/features/memory/api/memoryApi.ts
    - grep -q "useImportChatHistory" fe/src/features/memory/api/memoryQueries.ts
    - grep -q "keyword" fe/src/features/memory/components/MemoryMessageTable.tsx
    - grep -q "memory_type" fe/src/features/memory/components/MemorySettingsPanel.tsx
    - grep -q "conversations" fe/src/features/memory/api/memoryApi.ts
  </acceptance_criteria>
  <done>Message browser table with search/filter/pagination. Settings panel with all D-01 through D-14 options. Import dialog for chat history (D-11).</done>
</task>

<task type="auto">
  <name>Task 2: Detail page assembly + routing + i18n</name>
  <files>
    fe/src/features/memory/pages/MemoryDetailPage.tsx,
    fe/src/features/memory/index.ts,
    fe/src/app/App.tsx,
    fe/src/app/routeConfig.ts,
    fe/src/i18n/en.json,
    fe/src/i18n/vi.json,
    fe/src/i18n/ja.json
  </files>
  <read_first>
    fe/src/features/memory/components/MemoryMessageTable.tsx,
    fe/src/features/memory/components/MemorySettingsPanel.tsx,
    fe/src/features/memory/components/ImportHistoryDialog.tsx,
    fe/src/features/memory/api/memoryQueries.ts,
    fe/src/app/App.tsx,
    fe/src/app/routeConfig.ts,
    fe/src/i18n/en.json
  </read_first>
  <action>
    1. Create `MemoryDetailPage.tsx`:
       - Uses `useParams()` to get memory id from URL
       - Uses `useMemory(id)` to load pool data
       - Uses `useUpdateMemory()` for settings save
       - Page layout:
         a. Header: Memory name as title, back button to /memory, Import History button (opens ImportHistoryDialog)
         b. Tabs component (shadcn/ui Tabs) with two tabs:
            - "Messages" (default): renders MemoryMessageTable
            - "Settings": renders MemorySettingsPanel
         c. Loading state with skeleton
         d. 404 state if memory not found
       - Settings save handler: calls updateMemory mutation, shows success toast

    2. Add route in App.tsx:
       - Import MemoryDetailPage
       - Add `<Route path="memory/:id" element={<FeatureErrorBoundary><MemoryDetailPage /></FeatureErrorBoundary>} />`

    3. Add route config:
       - `'/memory/:id': { title: 'Memory Detail', featureId: 'memory' }`

    4. Add i18n keys to all 3 locales:
       - en.json (add to memory namespace): `"messages": "Messages", "settings": "Settings", "importHistory": "Import Chat History", "importDescription": "Process past conversations through memory extraction to retroactively build knowledge.", "importing": "Importing...", "importComplete": "Successfully imported {{count}} memories", "selectSession": "Select a chat session", "search": "Search memories...", "filterType": "Filter by type", "allTypes": "All Types", "content": "Content", "type": "Type", "status": "Status", "active": "Active", "forgotten": "Forgotten", "createdAt": "Created", "actions": "Actions", "forget": "Forget", "forgetConfirm": "Mark this memory as forgotten?", "deleteMessageConfirm": "Permanently delete this memory?", "noMessages": "No memories stored yet", "noMessagesHint": "Memories will appear here after conversations with linked chat assistants", "settingsSaved": "Memory pool settings saved", "resetPrompt": "Reset to default", "memorySize": "Memory Size", "memorySizeMB": "{{size}} MB", "embeddingModel": "Embedding Model", "llmModel": "LLM Model", "systemPrompt": "System Prompt", "userPrompt": "User Prompt", "forgettingPolicy": "Forgetting Policy"`
       - vi.json: Vietnamese translations
       - ja.json: Japanese translations

    5. Update barrel index.ts to export MemoryDetailPage
  </action>
  <verify>
    <automated>cd "/mnt/d/Project/b-solution/b-knowledge - Copy" && npx tsc --noEmit -p fe/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "MemoryDetailPage" fe/src/features/memory/pages/MemoryDetailPage.tsx
    - grep -q "MemoryMessageTable" fe/src/features/memory/pages/MemoryDetailPage.tsx
    - grep -q "MemorySettingsPanel" fe/src/features/memory/pages/MemoryDetailPage.tsx
    - grep -q "ImportHistoryDialog" fe/src/features/memory/pages/MemoryDetailPage.tsx
    - grep -q "memory/:id" fe/src/app/App.tsx
    - grep -q "messages.*Messages" fe/src/i18n/en.json
    - grep -q "MemoryDetailPage" fe/src/features/memory/index.ts
  </acceptance_criteria>
  <done>Memory detail page at /memory/:id with tabbed messages/settings views, import dialog, and full i18n. All D-13 views implemented.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p fe/tsconfig.json` passes
- /memory/:id route renders with tabs
- Messages tab shows searchable/filterable table
- Settings tab shows all pool configuration options
- Import button opens chat history import dialog
</verification>

<success_criteria>
- Memory detail page accessible from card click on list page
- Message table supports search, type filter, pagination (D-13)
- Settings panel covers all per-pool options including model selection (D-14) and prompts (D-03)
- Import dialog processes chat sessions through extraction (D-11)
</success_criteria>

<output>
After completion, create `.planning/phases/02-migration-memory-feature-from-ragflow-to-b-knowledge/02-05-SUMMARY.md`
</output>
