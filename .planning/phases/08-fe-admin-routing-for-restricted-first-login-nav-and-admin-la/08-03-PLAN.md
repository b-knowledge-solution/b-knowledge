---
phase: 08-fe-admin-routing-for-restricted-first-login-nav-and-admin-la
plan: 08-03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - fe/src/features/chat/components/CreateChatAssistantDialog.tsx
  - fe/src/features/search/components/CreateSearchAppDialog.tsx
  - fe/src/features/agents/components/AgentToolbar.tsx
  - fe/src/features/agents/components/AgentCard.tsx
  - fe/src/features/agents/pages/AgentListPage.tsx
  - fe/src/features/memory/pages/MemoryDetailPage.tsx
  - fe/tests/features/agent/AgentToolbar.test.tsx
  - fe/tests/features/agent/AgentCard.test.tsx
  - fe/tests/features/agent/AgentListPage.test.tsx
  - fe/tests/features/memory/MemoryDetailPage.test.tsx
  - fe/tests/features/agent/AdminAgentEntryLinks.test.tsx
autonomous: true
requirements: [AR8-02, AR8-03]
must_haves:
  truths:
    - "Agent Studio buttons, cards, and list flows now navigate only to `/admin/agent-studio/...` paths"
    - "Creating a chat assistant or search app still uses `/admin/agent-studio/agents/:id` with `new` as the pseudo-id and `mode` in the query string"
    - "Memory detail back navigation returns to `/admin/agent-studio/memory`"
  artifacts:
    - path: "fe/src/features/chat/components/CreateChatAssistantDialog.tsx"
      provides: "Chat assistant create CTA using admin agent path builder"
    - path: "fe/src/features/search/components/CreateSearchAppDialog.tsx"
      provides: "Search app create CTA using admin agent path builder"
    - path: "fe/tests/features/agent/AdminAgentEntryLinks.test.tsx"
      provides: "Regression coverage for pseudo-id `new` path generation"
  key_links:
    - from: "CreateChatAssistantDialog"
      to: "fe/src/app/adminRoutes.ts"
      via: "build admin agent-canvas path with `new` pseudo-id"
      pattern: "new\\?mode=chat"
    - from: "AgentCard / AgentToolbar / AgentListPage"
      to: "admin agent route helper"
      via: "detail/back/create navigation"
      pattern: "/admin/agent-studio/agents/"
---

<objective>
Migrate the Agent Studio and memory route consumers onto the admin route contract while preserving the dynamic `:id` route behavior that treats `new` as a pseudo-id for creation flows.

Purpose: Agent creation links are a hidden dependency of this phase. If `new?mode=chat|search` stops resolving through `/admin/agent-studio/agents/:id`, the create flows break even though the route tree compiles.
Output: Updated agent/memory route producers plus focused tests for the moved paths and `new` pseudo-id behavior.
</objective>

<execution_context>
@/home/doana/.codex/get-shit-done/workflows/execute-plan.md
@/home/doana/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-fe-admin-routing-for-restricted-first-login-nav-and-admin-la/08-CONTEXT.md
@.planning/phases/08-fe-admin-routing-for-restricted-first-login-nav-and-admin-la/08-RESEARCH.md
@CLAUDE.md
@fe/CLAUDE.md
@fe/src/app/adminRoutes.ts
@fe/src/features/chat/components/CreateChatAssistantDialog.tsx
@fe/src/features/search/components/CreateSearchAppDialog.tsx
@fe/src/features/agents/components/AgentToolbar.tsx
@fe/src/features/agents/components/AgentCard.tsx
@fe/src/features/agents/pages/AgentListPage.tsx
@fe/src/features/memory/pages/MemoryDetailPage.tsx
</context>

<interfaces>
From the new admin route contract created in 08-01:
```ts
export function buildAdminAgentListPath(): string
export function buildAdminAgentCanvasPath(id: string): string
export function buildAdminMemoryListPath(): string
```

The `buildAdminAgentCanvasPath('new')` helper must intentionally produce the same route shape that the router mounts for `:id`, with `new` treated as the pseudo-id.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Move Agent Studio and memory route producers to admin path builders</name>
  <files>
    fe/src/features/chat/components/CreateChatAssistantDialog.tsx
    fe/src/features/search/components/CreateSearchAppDialog.tsx
    fe/src/features/agents/components/AgentToolbar.tsx
    fe/src/features/agents/components/AgentCard.tsx
    fe/src/features/agents/pages/AgentListPage.tsx
    fe/src/features/memory/pages/MemoryDetailPage.tsx
  </files>
  <action>
    Replace every legacy `/agent-studio/...` navigation in these files with shared builders from `fe/src/app/adminRoutes.ts`. Required migrated behaviors:
    - chat assistant creation CTA navigates to `/admin/agent-studio/agents/new?mode=chat`
    - search app creation CTA navigates to `/admin/agent-studio/agents/new?mode=search`
    - agent cards and list page navigate to `/admin/agent-studio/agents/:id`
    - toolbar back navigation returns to `/admin/agent-studio/agents`
    - memory detail back navigation returns to `/admin/agent-studio/memory`

    Preserve the existing pseudo-id contract exactly: `new` stays in the `:id` segment, not a separate route or modal.
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge && npm run build -w fe && ! rg -n "/agent-studio/" fe/src/features/chat/components/CreateChatAssistantDialog.tsx fe/src/features/search/components/CreateSearchAppDialog.tsx fe/src/features/agents fe/src/features/memory/pages/MemoryDetailPage.tsx</automated>
  </verify>
  <done>All agent and memory navigations emit only `/admin/agent-studio/...` paths while preserving the `new` pseudo-id flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update agent/memory navigation tests and add pseudo-id create-link coverage</name>
  <files>
    fe/tests/features/agent/AgentToolbar.test.tsx
    fe/tests/features/agent/AgentCard.test.tsx
    fe/tests/features/agent/AgentListPage.test.tsx
    fe/tests/features/memory/MemoryDetailPage.test.tsx
    fe/tests/features/agent/AdminAgentEntryLinks.test.tsx
  </files>
  <behavior>
    - Agent toolbar/card/list tests assert `/admin/agent-studio/agents...` targets instead of `/agent-studio/...`
    - Memory detail tests assert `/admin/agent-studio/memory` back navigation
    - New test file asserts the chat/search create-entry links build `/admin/agent-studio/agents/new?mode=chat|search`
  </behavior>
  <action>
    Update the existing navigation assertions in the touched agent/memory tests to the new admin-prefixed paths. Then add `fe/tests/features/agent/AdminAgentEntryLinks.test.tsx` to cover the hidden create-flow contract directly:
    - render the chat and search "Create as Agent Workflow" link components with a mocked `useNavigate`
    - assert the generated destination keeps `new` as the `:id` segment and preserves the `mode` query string

    Keep these tests narrowly route-focused. Do not expand them into canvas or API behavior tests.
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge && npm run test:run:ui -w fe -- fe/tests/features/agent/AgentToolbar.test.tsx fe/tests/features/agent/AgentCard.test.tsx fe/tests/features/agent/AgentListPage.test.tsx fe/tests/features/memory/MemoryDetailPage.test.tsx fe/tests/features/agent/AdminAgentEntryLinks.test.tsx --reporter=dot</automated>
  </verify>
  <done>Agent/memory route assertions match the `/admin/...` contract and the `new` pseudo-id create flows are regression-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| create-entry buttons → router | Hidden query-string flows can silently break when route prefixes move |
| agent card/list actions → router | Old list/detail links can still point to removed paths after the shell refactor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | D | `new` pseudo-id create flow | mitigate | Preserve the `:id` route contract and add direct regression tests for `new?mode=chat|search` |
| T-08-09 | I | stale Agent Studio URLs | mitigate | Replace inline `/agent-studio/...` strings with shared admin route builders across card/list/toolbar/create entry points |
| T-08-10 | T | memory back navigation | mitigate | Route the memory detail back button through a shared `/admin/agent-studio/memory` helper and assert it in tests |
</threat_model>

<verification>
- `npm run build -w fe`
- `npm run test:run:ui -w fe -- fe/tests/features/agent/AgentToolbar.test.tsx fe/tests/features/agent/AgentCard.test.tsx fe/tests/features/agent/AgentListPage.test.tsx fe/tests/features/memory/MemoryDetailPage.test.tsx fe/tests/features/agent/AdminAgentEntryLinks.test.tsx --reporter=dot`
- `! rg -n "/agent-studio/" fe/src/features/chat/components/CreateChatAssistantDialog.tsx fe/src/features/search/components/CreateSearchAppDialog.tsx fe/src/features/agents fe/src/features/memory/pages/MemoryDetailPage.tsx`
</verification>

<success_criteria>
Agent Studio and memory navigations are fully admin-prefixed, and the hidden `new` pseudo-id flows are explicitly protected by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/08-fe-admin-routing-for-restricted-first-login-nav-and-admin-la/08-03-SUMMARY.md`
</output>
