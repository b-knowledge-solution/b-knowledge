---
phase: 08-fe-admin-routing-for-restricted-first-login-nav-and-admin-la
plan: 08-02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - fe/src/features/knowledge-base/components/CodeTabRedesigned.tsx
  - fe/src/features/knowledge-base/components/DocumentListPanel.tsx
  - fe/src/features/knowledge-base/pages/KnowledgeBaseListPage.tsx
  - fe/src/features/knowledge-base/pages/KnowledgeBaseDetailPage.tsx
  - fe/src/features/code-graph/pages/CodeGraphPage.tsx
  - fe/src/features/datasets/components/DatasetCard.tsx
  - fe/src/features/datasets/components/DocumentTable.tsx
  - fe/src/features/datasets/pages/DatasetDetailPage.tsx
  - fe/src/features/datasets/pages/ChunkDetailPage.tsx
  - fe/src/features/users/pages/UserDetailPage.tsx
  - fe/src/features/users/components/RoleManagementTable.tsx
  - fe/src/features/permissions/pages/EffectiveAccessPage.tsx
  - fe/tests/features/users/UserDetailPage.test.tsx
  - fe/tests/features/permissions/EffectiveAccessPage.test.tsx
autonomous: true
requirements: [AR8-02, AR8-03]
must_haves:
  truths:
    - "Knowledge Base, dataset, document chunk, and code-graph links all navigate to `/admin/...` paths instead of removed legacy admin URLs (per D-03, D-04, D-06, D-07)"
    - "IAM deep links such as user detail and effective-access drill-ins now target `/admin/iam/...` paths only"
    - "The hidden code-graph route remains reachable via `/admin/code-graph/:kbId` from the Knowledge Base code tab"
  artifacts:
    - path: "fe/src/features/knowledge-base/components/CodeTabRedesigned.tsx"
      provides: "Full-graph button wired to `/admin/code-graph/:kbId`"
    - path: "fe/src/features/code-graph/pages/CodeGraphPage.tsx"
      provides: "Code-graph back navigation stays inside the `/admin` Data Studio flow"
    - path: "fe/src/features/permissions/pages/EffectiveAccessPage.tsx"
      provides: "User-detail deep-link builder updated to `/admin/iam/users/:id?tab=permissions`"
    - path: "fe/tests/features/users/UserDetailPage.test.tsx"
      provides: "IAM route expectation coverage"
    - path: "fe/tests/features/permissions/EffectiveAccessPage.test.tsx"
      provides: "Deep-link helper coverage for admin IAM paths"
  key_links:
    - from: "CodeTabRedesigned"
      to: "fe/src/app/adminRoutes.ts"
      via: "buildAdminCodeGraphPath"
      pattern: "code-graph"
    - from: "CodeGraphPage"
      to: "fe/src/app/adminRoutes.ts"
      via: "admin knowledge-base/detail back-link builder"
      pattern: "buildAdminKnowledgeBasePath"
    - from: "EffectiveAccessPage"
      to: "UserDetailPage"
      via: "admin IAM deep link"
      pattern: "/admin/iam/users/"
---

<objective>
Migrate the Data Studio, Knowledge Base, hidden code-graph, and IAM route consumers onto the new Phase 8 admin path contract so removed legacy admin URLs are no longer emitted anywhere in those flows.

Purpose: This plan closes the highest-risk deep-link drift identified in research: Data Studio back buttons, code-graph entry, and IAM drill-downs. It implements D-03, D-04, D-06, and D-07 at the page-consumer level while reusing the existing pages per D-12.
Output: Updated path helpers consumed by KB/dataset/IAM features plus targeted tests for IAM deep-link helpers.
</objective>

<execution_context>
@/home/doana/.codex/get-shit-done/workflows/execute-plan.md
@/home/doana/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-fe-admin-routing-for-restricted-first-login-nav-and-admin-la/08-CONTEXT.md
@.planning/phases/08-fe-admin-routing-for-restricted-first-login-nav-and-admin-la/08-RESEARCH.md
@CLAUDE.md
@fe/CLAUDE.md
@fe/src/app/adminRoutes.ts
@fe/src/features/knowledge-base/components/CodeTabRedesigned.tsx
@fe/src/features/knowledge-base/pages/KnowledgeBaseDetailPage.tsx
@fe/src/features/datasets/pages/DatasetDetailPage.tsx
@fe/src/features/users/pages/UserDetailPage.tsx
@fe/src/features/permissions/pages/EffectiveAccessPage.tsx
</context>

<interfaces>
From `fe/src/app/adminRoutes.ts` (created in 08-01), downstream consumers should use exported builders instead of hardcoded strings. Expected contract:
```ts
export const ADMIN_HOME_PATH: string
export function buildAdminKnowledgeBasePath(knowledgeBaseId: string): string
export function buildAdminDatasetPath(datasetId: string): string
export function buildAdminDatasetChunkPath(datasetId: string, docId: string): string
export function buildAdminCodeGraphPath(kbId: string): string
export function buildAdminUserDetailPath(userId: string | number, tab?: string): string
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Move Data Studio and hidden code-graph consumers to admin path builders</name>
  <files>
    fe/src/features/knowledge-base/components/CodeTabRedesigned.tsx
    fe/src/features/knowledge-base/components/DocumentListPanel.tsx
    fe/src/features/knowledge-base/pages/KnowledgeBaseListPage.tsx
    fe/src/features/knowledge-base/pages/KnowledgeBaseDetailPage.tsx
    fe/src/features/code-graph/pages/CodeGraphPage.tsx
    fe/src/features/datasets/components/DatasetCard.tsx
    fe/src/features/datasets/components/DocumentTable.tsx
    fe/src/features/datasets/pages/DatasetDetailPage.tsx
    fe/src/features/datasets/pages/ChunkDetailPage.tsx
  </files>
  <action>
    Replace every legacy admin route emission in these files with the shared builders from `fe/src/app/adminRoutes.ts`. This task must migrate all user-initiated navigation inside the Data Studio surface, including:
    - Knowledge Base list → detail
    - Knowledge Base detail back/delete flows
    - Dataset cards/detail/back buttons
    - document chunk drill-downs in both dataset and KB document tables
    - code-graph "View Full Graph" navigation in `CodeTabRedesigned`
    - `CodeGraphPage.tsx` back navigation so the hidden page returns to the admin Knowledge Base flow instead of `/`

    Hidden-route requirement: the code-graph button must target `/admin/code-graph/:kbId`, not the removed `/code-graph/:kbId`.
    Hidden-route completeness requirement: `CodeGraphPage.tsx` must consume an admin route helper for its back link so all in-page navigation stays inside `/admin` after the move.

    Keep behavior unchanged apart from path migration:
    - do not redesign these pages
    - do not move data fetching
    - do not add legacy redirects
    - prefer imported route builders over inline template strings so future route changes are single-source
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge && npm run build -w fe && rg -n "buildAdmin(CodeGraphPath|KnowledgeBasePath|DatasetPath|DatasetChunkPath)" fe/src/features/knowledge-base/components/CodeTabRedesigned.tsx fe/src/features/knowledge-base/components/DocumentListPanel.tsx fe/src/features/knowledge-base/pages/KnowledgeBaseListPage.tsx fe/src/features/knowledge-base/pages/KnowledgeBaseDetailPage.tsx fe/src/features/code-graph/pages/CodeGraphPage.tsx fe/src/features/datasets/components/DatasetCard.tsx fe/src/features/datasets/components/DocumentTable.tsx fe/src/features/datasets/pages/DatasetDetailPage.tsx fe/src/features/datasets/pages/ChunkDetailPage.tsx && ! rg -n "['\"]/(data-studio|code-graph)/" fe/src/features/knowledge-base/components/CodeTabRedesigned.tsx fe/src/features/knowledge-base/components/DocumentListPanel.tsx fe/src/features/knowledge-base/pages/KnowledgeBaseListPage.tsx fe/src/features/knowledge-base/pages/KnowledgeBaseDetailPage.tsx fe/src/features/code-graph/pages/CodeGraphPage.tsx fe/src/features/datasets/components/DatasetCard.tsx fe/src/features/datasets/components/DocumentTable.tsx fe/src/features/datasets/pages/DatasetDetailPage.tsx fe/src/features/datasets/pages/ChunkDetailPage.tsx</automated>
  </verify>
  <done>All Data Studio and code-graph navigations emit only `/admin/...` paths, the hidden code-graph page no longer escapes to `/`, and the FE build remains green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move IAM deep links to `/admin/iam/...` and update targeted tests</name>
  <files>
    fe/src/features/users/pages/UserDetailPage.tsx
    fe/src/features/users/components/RoleManagementTable.tsx
    fe/src/features/permissions/pages/EffectiveAccessPage.tsx
    fe/tests/features/users/UserDetailPage.test.tsx
    fe/tests/features/permissions/EffectiveAccessPage.test.tsx
  </files>
  <behavior>
    - `buildUserDetailUrl(42)` returns `/admin/iam/users/42?tab=permissions`
    - User detail route tests mount at `/admin/iam/users/:id` and still preserve `?tab=permissions`
    - Clicking IAM drill-down actions no longer emits `/iam/users/...`
  </behavior>
  <action>
    Update every IAM path producer in the touched files to use the shared admin IAM route builders. Required call sites:
    - `UserDetailPage` back navigation
    - `RoleManagementTable` row drill-downs
    - `EffectiveAccessPage` exported deep-link helper

    Then update the existing targeted tests to encode the new contract instead of the removed legacy path. Keep the test focus narrow:
    - `UserDetailPage.test.tsx` should continue verifying `?tab=permissions` state sync, but mount the route at `/admin/iam/users/:id`
    - `EffectiveAccessPage.test.tsx` should assert the helper returns `/admin/iam/users/:id?tab=permissions`

    Do not create a second set of tests for the old routes. The old routes are intentionally gone in this phase.
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge && npm run test:run:ui -w fe -- fe/tests/features/users/UserDetailPage.test.tsx fe/tests/features/permissions/EffectiveAccessPage.test.tsx --reporter=dot</automated>
  </verify>
  <done>IAM drill-downs use only `/admin/iam/...` paths and the targeted UI tests pass on the new route shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page-level nav actions → router | Buttons and links can keep emitting stale legacy paths after the router changes |
| KB code tab → hidden admin route | Hidden routes must stay reachable without bypassing authorization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | I | stale Data Studio links | mitigate | Replace inline route strings with shared admin route builders so KB/dataset pages never emit removed legacy URLs |
| T-08-06 | E | code-graph navigation | mitigate | Move the full-graph button to `/admin/code-graph/:kbId`, which is now protected by the admin shell and explicit route-permission mapping |
| T-08-07 | T | IAM helper drift | mitigate | Export and test a single helper for `/admin/iam/users/:id?tab=permissions` so drill-down routes cannot silently regress |
</threat_model>

<verification>
- `npm run build -w fe`
- `npm run test:run:ui -w fe -- fe/tests/features/users/UserDetailPage.test.tsx fe/tests/features/permissions/EffectiveAccessPage.test.tsx --reporter=dot`
- `! rg -n "(/data-studio/|/iam/|/code-graph/)" fe/src/features/knowledge-base fe/src/features/datasets fe/src/features/users fe/src/features/permissions`
</verification>

<success_criteria>
No Data Studio, Knowledge Base, code-graph, or IAM deep link in the migrated files emits a removed non-admin admin URL, and the targeted tests encode the new `/admin/...` contract.
</success_criteria>

<output>
After completion, create `.planning/phases/08-fe-admin-routing-for-restricted-first-login-nav-and-admin-la/08-02-SUMMARY.md`
</output>
