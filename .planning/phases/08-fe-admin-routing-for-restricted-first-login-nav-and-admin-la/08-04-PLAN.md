---
phase: 08-fe-admin-routing-for-restricted-first-login-nav-and-admin-la
plan: 08-04
type: execute
wave: 3
depends_on: [08-01, 08-02, 08-03]
files_modified:
  - fe/tests/app/AdminRouting.test.tsx
  - fe/tests/layouts/SidebarAdminShell.test.tsx
  - fe/tests/features/auth/AdminRoute.test.tsx
autonomous: true
requirements: [AR8-01, AR8-02, AR8-03]
must_haves:
  truths:
    - "The FE test suite proves `/admin` redirects to `/admin/data-studio/knowledge-base` and old non-admin admin URLs no longer resolve"
    - "The test suite proves non-admin users cannot access `/admin/...`, while admin-shell roles can"
    - "The user shell sidebar renders only Chat and Search, and the dropdown shows `Administrator` before `Logout` for admin-shell roles"
    - "The hidden routes `/admin/code-graph/:kbId` and `/admin/agent-studio/agents/new?mode=chat` remain routable after the migration"
  artifacts:
    - path: "fe/tests/app/AdminRouting.test.tsx"
      provides: "Router-level regression coverage for `/admin` redirects, removed legacy paths, and hidden routes"
    - path: "fe/tests/layouts/SidebarAdminShell.test.tsx"
      provides: "Sidebar shell and dropdown ordering coverage"
    - path: "fe/tests/features/auth/AdminRoute.test.tsx"
      provides: "Explicit-role admin gate coverage"
  key_links:
    - from: "AdminRouting.test.tsx"
      to: "fe/src/app/App.tsx"
      via: "rendered route tree"
      pattern: "/admin/"
    - from: "SidebarAdminShell.test.tsx"
      to: "fe/src/layouts/Sidebar.tsx"
      via: "admin entry visibility and ordering"
      pattern: "Administrator"
---

<objective>
Add the regression harness that proves Phase 8 shipped safely: shell separation works, `/admin` access is locked to the allowed roles, old non-prefixed admin URLs are gone, and the hidden admin paths still resolve.

Purpose: The risk in this phase is routing drift, not rendering. These tests are the acceptance barrier that prevents the repo from silently reintroducing mixed shells or legacy admin URLs later.
Output: Focused UI/jsdom tests for App routing, Sidebar behavior, and the explicit admin role gate.
</objective>

<execution_context>
@/home/doana/.codex/get-shit-done/workflows/execute-plan.md
@/home/doana/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-fe-admin-routing-for-restricted-first-login-nav-and-admin-la/08-CONTEXT.md
@.planning/phases/08-fe-admin-routing-for-restricted-first-login-nav-and-admin-la/08-RESEARCH.md
@CLAUDE.md
@fe/CLAUDE.md
@fe/src/app/App.tsx
@fe/src/layouts/Sidebar.tsx
@fe/src/features/auth/components/AdminRoute.tsx
@fe/tests/test-utils.tsx
@fe/tests/setup.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add router-level regression tests for `/admin` redirects, removed legacy paths, and hidden admin routes</name>
  <files>
    fe/tests/app/AdminRouting.test.tsx
  </files>
  <behavior>
    - `/admin` redirects to `/admin/data-studio/knowledge-base`
    - `/admin/code-graph/:kbId` renders through the protected admin route tree
    - `/admin/agent-studio/agents/new?mode=chat` resolves through the `:id` route with `new` intact
    - removed legacy admin paths such as `/iam/users` and `/data-studio/knowledge-base` end at `/404`
  </behavior>
  <action>
    Create a focused App-router test file that mounts `App.tsx` with mocked auth, permissions, and lazy pages. Exercise the actual route tree rather than helper functions only. Keep the tests fast by stubbing feature page modules with simple sentinels.

    Assertions must encode the phase contract directly:
    - no legacy admin aliases
    - `/admin` home redirect target is exact
    - hidden admin routes still resolve inside the admin shell
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge && npm run test:run:ui -w fe -- fe/tests/app/AdminRouting.test.tsx --reporter=dot</automated>
  </verify>
  <done>App-level routing regressions are covered by UI tests that encode the final `/admin` contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add sidebar and admin-role-gate regression tests</name>
  <files>
    fe/tests/layouts/SidebarAdminShell.test.tsx
    fe/tests/features/auth/AdminRoute.test.tsx
  </files>
  <behavior>
    - user shell sidebar shows only Chat/Search outside `/admin`
    - `Administrator` appears before `Logout` only for `leader`, `admin`, and `super-admin`
    - a `user` role attempting `/admin/...` is redirected to `/403`
    - allowed roles pass through the admin gate
  </behavior>
  <action>
    Add one sidebar-focused test file and one `AdminRoute`-focused test file. Mock only what is needed:
    - `SidebarAdminShell.test.tsx` should mount the sidebar with user-shell/admin-shell props and verify nav visibility plus dropdown ordering
    - `AdminRoute.test.tsx` should assert the locked role gate directly using mocked `useAuth`

    These tests are the phase-level guardrail for D-02, D-08, D-10, and D-11. Keep them narrowly scoped and independent from the larger App test where possible.
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge && npm run test:run:ui -w fe -- fe/tests/layouts/SidebarAdminShell.test.tsx fe/tests/features/auth/AdminRoute.test.tsx --reporter=dot</automated>
  </verify>
  <done>The phase’s shell-selection and explicit-role gating rules are locked in with targeted UI tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future FE edits → route tree | Later routing changes can silently reintroduce removed legacy paths or weaken admin gating |
| future FE edits → sidebar dropdown | Nav regressions can expose admin entry to the wrong roles or reorder destructive items |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | R | `/admin` routing contract | mitigate | Add router-level regression tests that fail if `/admin` redirect or hidden admin routes drift |
| T-08-12 | S | sidebar dropdown visibility | mitigate | Add tests asserting `Administrator` visibility/order only for the locked role set |
| T-08-13 | E | admin gate behavior | mitigate | Add direct `AdminRoute` tests proving `user` is denied and leader/admin/super-admin are allowed |
</threat_model>

<verification>
- `npm run test:run:ui -w fe -- fe/tests/app/AdminRouting.test.tsx fe/tests/layouts/SidebarAdminShell.test.tsx fe/tests/features/auth/AdminRoute.test.tsx --reporter=dot`
- `npm run test:run -w fe`
- `npm run build -w fe`
</verification>

<success_criteria>
The FE test suite captures the final Phase 8 contract: dual shells, explicit-role admin gate, `/admin` home redirect, hidden admin route coverage, removal of legacy non-admin admin URLs, and a green full frontend test run.
</success_criteria>

<output>
After completion, create `.planning/phases/08-fe-admin-routing-for-restricted-first-login-nav-and-admin-la/08-04-SUMMARY.md`
</output>
