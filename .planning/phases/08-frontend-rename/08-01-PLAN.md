---
phase: 08-frontend-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fe/src/features/knowledge-base/ (entire directory - renamed from fe/src/features/projects/)
  - fe/src/features/knowledge-base/api/knowledgeBaseApi.ts
  - fe/src/features/knowledge-base/api/knowledgeBaseQueries.ts
  - fe/src/features/knowledge-base/types/knowledge-base.types.ts
  - fe/src/features/knowledge-base/pages/KnowledgeBaseListPage.tsx
  - fe/src/features/knowledge-base/pages/KnowledgeBaseDetailPage.tsx
  - fe/src/features/knowledge-base/components/KnowledgeBaseActivityFeed.tsx
  - fe/src/features/knowledge-base/components/KnowledgeBaseDatasetPicker.tsx
  - fe/src/features/knowledge-base/components/KnowledgeBaseMemberList.tsx
  - fe/src/features/knowledge-base/components/KnowledgeBasePermissionModal.tsx
  - fe/src/features/knowledge-base/components/KnowledgeBaseSettingsSheet.tsx
  - fe/src/features/knowledge-base/components/CreateKnowledgeBaseModal.tsx
  - fe/src/features/knowledge-base/index.ts
  - fe/src/lib/queryKeys.ts
  - fe/src/app/App.tsx
  - fe/src/app/routeConfig.ts
  - fe/src/layouts/sidebarNav.ts
autonomous: true
requirements:
  - REN-04
must_haves:
  truths:
    - "Feature directory exists at fe/src/features/knowledge-base/ with all files"
    - "All Project-prefixed components renamed to KnowledgeBase-prefixed"
    - "API layer calls /api/knowledge-base/ endpoints instead of /api/projects/"
    - "TanStack Query keys use 'knowledge-base' namespace via centralized factory"
    - "Routes serve at /data-studio/knowledge-base/:knowledgeBaseId"
    - "Sidebar nav points to /data-studio/knowledge-base"
  artifacts:
    - path: "fe/src/features/knowledge-base/api/knowledgeBaseApi.ts"
      provides: "Raw HTTP API layer for knowledge base"
      contains: "/api/knowledge-base"
    - path: "fe/src/features/knowledge-base/api/knowledgeBaseQueries.ts"
      provides: "TanStack Query hooks for knowledge base"
      contains: "queryKeys.knowledgeBase"
    - path: "fe/src/features/knowledge-base/types/knowledge-base.types.ts"
      provides: "TypeScript types for knowledge base"
      contains: "KnowledgeBase"
    - path: "fe/src/lib/queryKeys.ts"
      provides: "Centralized query key factory with knowledgeBase section"
      contains: "knowledgeBase"
    - path: "fe/src/features/knowledge-base/index.ts"
      provides: "Barrel exports with renamed types and functions"
      contains: "KnowledgeBase"
  key_links:
    - from: "fe/src/app/App.tsx"
      to: "fe/src/features/knowledge-base/pages/KnowledgeBaseListPage.tsx"
      via: "React.lazy import"
      pattern: "features/knowledge-base/pages/KnowledgeBaseListPage"
    - from: "fe/src/features/knowledge-base/api/knowledgeBaseQueries.ts"
      to: "fe/src/lib/queryKeys.ts"
      via: "queryKeys.knowledgeBase.*"
      pattern: "queryKeys\\.knowledgeBase"
---

<objective>
Rename the core frontend feature from "projects" to "knowledge-base" -- directory, files, types, API layer, query keys, routes, and navigation.

Purpose: Complete the FE half of the Project-to-Knowledge-Base rename (backend done in Phase 7). After this plan, the feature directory, all file names, TypeScript types, API endpoints, query keys, routes, and sidebar nav all reference "knowledge-base" consistently.

Output: Renamed feature directory at `fe/src/features/knowledge-base/` with all internal references updated, routes serving at `/data-studio/knowledge-base/`, query keys using `knowledgeBase` namespace.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-frontend-rename/08-CONTEXT.md
@.planning/phases/08-frontend-rename/08-RESEARCH.md
@fe/CLAUDE.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From fe/src/lib/queryKeys.ts (current - to be renamed):
```typescript
projects: {
  all: ['projects'] as const,
  members: (projectId: string) => [...queryKeys.projects.all, projectId, 'members'] as const,
  datasets: (projectId: string) => [...queryKeys.projects.all, projectId, 'datasets'] as const,
  activity: (projectId: string) => [...queryKeys.projects.all, projectId, 'activity'] as const,
},
```

From fe/src/features/projects/index.ts (current barrel - to be updated):
```typescript
export { getProjects, getProjectById, createProject, ... type Project, type ProjectPermission, ... } from "./api/projectApi";
export { default as ProjectSettingsSheet } from "./components/ProjectSettingsSheet";
```

From fe/src/app/App.tsx (lines 82-83, 192-193):
```typescript
const ProjectListPage = lazy(() => import('@/features/projects/pages/ProjectListPage'));
const ProjectDetailPage = lazy(() => import('@/features/projects/pages/ProjectDetailPage'));
// routes:
<Route path="data-studio/projects" ... ><ProjectListPage /></Route>
<Route path="data-studio/projects/:projectId" ... ><ProjectDetailPage /></Route>
```

From fe/src/app/routeConfig.ts (line 83-87):
```typescript
'/data-studio/projects': { titleKey: 'projectManagement.title', guidelineFeatureId: 'projects', fullBleed: true }
```

From fe/src/layouts/sidebarNav.ts (lines 129-132):
```typescript
{ path: '/data-studio/projects', labelKey: 'nav.projects', icon: FolderOpen }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename feature directory and files, update types and API layer</name>
  <read_first>
    fe/src/features/projects/index.ts
    fe/src/features/projects/api/projectApi.ts
    fe/src/features/projects/api/projectQueries.ts
    fe/src/features/projects/types/project.types.ts
    fe/src/features/projects/pages/ProjectListPage.tsx
    fe/src/features/projects/pages/ProjectDetailPage.tsx
    fe/src/features/projects/components/ProjectActivityFeed.tsx
    fe/src/features/projects/components/ProjectDatasetPicker.tsx
    fe/src/features/projects/components/ProjectMemberList.tsx
    fe/src/features/projects/components/ProjectPermissionModal.tsx
    fe/src/features/projects/components/ProjectSettingsSheet.tsx
    fe/src/features/projects/components/CreateProjectModal.tsx
    fe/src/features/projects/components/CategoryFilterTabs.tsx
    fe/src/lib/queryKeys.ts
  </read_first>
  <files>
    fe/src/features/knowledge-base/ (entire directory)
    fe/src/lib/queryKeys.ts
  </files>
  <action>
    **Step 1: Directory rename via git mv (per D-06)**
    ```bash
    git mv fe/src/features/projects fe/src/features/knowledge-base
    ```

    **Step 2: Rename Project-prefixed files within the directory (per D-07, D-08, D-09)**
    In `fe/src/features/knowledge-base/`:
    - `git mv api/projectApi.ts api/knowledgeBaseApi.ts` (per D-08)
    - `git mv api/projectQueries.ts api/knowledgeBaseQueries.ts` (per D-08)
    - `git mv types/project.types.ts types/knowledge-base.types.ts` (per D-09)
    - `git mv pages/ProjectListPage.tsx pages/KnowledgeBaseListPage.tsx` (per D-07)
    - `git mv pages/ProjectDetailPage.tsx pages/KnowledgeBaseDetailPage.tsx` (per D-07)
    - `git mv components/ProjectActivityFeed.tsx components/KnowledgeBaseActivityFeed.tsx`
    - `git mv components/ProjectDatasetPicker.tsx components/KnowledgeBaseDatasetPicker.tsx`
    - `git mv components/ProjectMemberList.tsx components/KnowledgeBaseMemberList.tsx`
    - `git mv components/ProjectPermissionModal.tsx components/KnowledgeBasePermissionModal.tsx`
    - `git mv components/ProjectSettingsSheet.tsx components/KnowledgeBaseSettingsSheet.tsx`
    - `git mv components/CreateProjectModal.tsx components/CreateKnowledgeBaseModal.tsx`
    - DELETE `components/CategoryFilterTabs.tsx` (dead code per Phase 3 D-03-03, Claude's discretion)
    - KEEP `api/ragflowApi.ts` as-is (RAG dataset proxy, not a "project" concept)

    **Step 3: Update types in `knowledge-base.types.ts`**
    Rename all type names:
    - `Project` -> `KnowledgeBase`
    - `ProjectDataset` -> `KnowledgeBaseDataset`
    - `ProjectPermission` -> `KnowledgeBasePermission`
    - `ProjectChat` -> `KnowledgeBaseChat`
    - `ProjectSearch` -> `KnowledgeBaseSearch`
    - `ProjectSyncConfig` -> `KnowledgeBaseSyncConfig`
    - `ProjectMember` -> `KnowledgeBaseMember`
    - All `project_id` fields -> `knowledge_base_id` (matching BE Phase 7)

    **Step 4: Update API layer in `knowledgeBaseApi.ts`**
    - Rename all exported functions: `getProjects` -> `getKnowledgeBases`, `getProjectById` -> `getKnowledgeBaseById`, `createProject` -> `createKnowledgeBase`, `updateProject` -> `updateKnowledgeBase`, `deleteProject` -> `deleteKnowledgeBase`, etc.
    - Rename all `getProjectPermissions` -> `getKnowledgeBasePermissions`, `setProjectPermission` -> `setKnowledgeBasePermission`, `removeProjectPermission` -> `removeKnowledgeBasePermission`
    - Rename `getProjectChats` -> `getKnowledgeBaseChats`, `getProjectChatById` -> `getKnowledgeBaseChatById`, `createProjectChat` -> `createKnowledgeBaseChat`, etc.
    - Rename `getProjectSearches` -> `getKnowledgeBaseSearches`, `getProjectSearchById` -> `getKnowledgeBaseSearchById`, etc.
    - Rename `getProjectDatasets` -> `getKnowledgeBaseDatasets`, `linkProjectDataset` -> `linkKnowledgeBaseDataset`, `unlinkProjectDataset` -> `unlinkKnowledgeBaseDataset`
    - Change ALL endpoint URLs from `/api/projects/...` to `/api/knowledge-base/...`
    - Rename all `projectId` parameters to `knowledgeBaseId`
    - Update all type references to use new names (KnowledgeBase, KnowledgeBasePermission, etc.)

    **Step 5: Update query hooks in `knowledgeBaseQueries.ts`**
    - Rename all hook functions: `useProjects` -> `useKnowledgeBases`, `useProject` -> `useKnowledgeBase`, etc.
    - Replace ALL inline query keys like `['projects', 'list']` with centralized `queryKeys.knowledgeBase.list()` (per D-10)
    - Replace ALL inline query keys like `['projects', 'detail', id]` with `queryKeys.knowledgeBase.detail(id)`
    - Replace ALL inline query keys like `['projects', projectId, 'categories']` with `queryKeys.knowledgeBase.categories(knowledgeBaseId)`
    - Update all API function calls to use renamed functions from knowledgeBaseApi
    - Rename all `projectId` variables to `knowledgeBaseId`

    **Step 6: Update centralized query keys in `fe/src/lib/queryKeys.ts` (per D-10)**
    Rename `projects` section to `knowledgeBase` with root key `['knowledge-base']`:
    ```typescript
    knowledgeBase: {
      all: ['knowledge-base'] as const,
      list: () => [...queryKeys.knowledgeBase.all, 'list'] as const,
      detail: (id: string) => [...queryKeys.knowledgeBase.all, 'detail', id] as const,
      categories: (knowledgeBaseId: string) => [...queryKeys.knowledgeBase.all, knowledgeBaseId, 'categories'] as const,
      versions: (knowledgeBaseId: string, categoryId: string) => [...queryKeys.knowledgeBase.all, knowledgeBaseId, 'categories', categoryId, 'versions'] as const,
      documents: (knowledgeBaseId: string, categoryId: string, versionId: string) => [...queryKeys.knowledgeBase.all, knowledgeBaseId, 'categories', categoryId, 'versions', versionId, 'documents'] as const,
      chats: (knowledgeBaseId: string) => [...queryKeys.knowledgeBase.all, knowledgeBaseId, 'chats'] as const,
      searches: (knowledgeBaseId: string) => [...queryKeys.knowledgeBase.all, knowledgeBaseId, 'searches'] as const,
      permissions: (knowledgeBaseId: string) => [...queryKeys.knowledgeBase.all, knowledgeBaseId, 'permissions'] as const,
      members: (knowledgeBaseId: string) => [...queryKeys.knowledgeBase.all, knowledgeBaseId, 'members'] as const,
      datasets: (knowledgeBaseId: string) => [...queryKeys.knowledgeBase.all, knowledgeBaseId, 'datasets'] as const,
      activity: (knowledgeBaseId: string) => [...queryKeys.knowledgeBase.all, knowledgeBaseId, 'activity'] as const,
      syncConfigs: (knowledgeBaseId: string) => [...queryKeys.knowledgeBase.all, knowledgeBaseId, 'sync-configs'] as const,
    },
    ```
    Expand the factory to cover ALL inline keys currently in projectQueries.ts. Read projectQueries.ts to find every unique query key pattern and ensure the factory covers them all.

    **Step 7: Update all internal cross-references within the feature**
    - Every component in `fe/src/features/knowledge-base/components/` that imports from `../api/projectApi` or `../types/project.types` must be updated to import from `../api/knowledgeBaseApi` and `../types/knowledge-base.types`
    - Every component that references `Project` type must use `KnowledgeBase` type
    - Every component that uses `projectId` prop/variable must use `knowledgeBaseId`
    - Every component that uses `useParams<{ projectId: string }>()` must use `useParams<{ knowledgeBaseId: string }>()`
    - Update all JSDoc comments to reference "Knowledge Base" instead of "Project"

    **Step 8: Update barrel exports in `index.ts`**
    Update all exports to use new file paths and new names:
    - `from "./api/projectApi"` -> `from "./api/knowledgeBaseApi"`
    - `type Project` -> `type KnowledgeBase`
    - `type ProjectPermission` -> `type KnowledgeBasePermission`
    - `getProjects` -> `getKnowledgeBases`
    - `default as ProjectSettingsSheet` -> `default as KnowledgeBaseSettingsSheet`
    - etc. for all renamed exports

    **IMPORTANT: Do NOT rename:**
    - `ragflowApi.ts` (proxies RAG dataset endpoints, not a "project" concept)
    - Generic components (DocumentListPanel, DocumentsTab, CategoryModal, etc.) — they describe their content, not the parent entity
    - Any reference to "project" in ConnectorSourceFields context (Jira/Bitbucket/Asana external projects)
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge && test -d fe/src/features/knowledge-base && ! test -d fe/src/features/projects && test -f fe/src/features/knowledge-base/api/knowledgeBaseApi.ts && test -f fe/src/features/knowledge-base/types/knowledge-base.types.ts && echo "PASS: Directory and file renames verified"</automated>
  </verify>
  <acceptance_criteria>
    - `fe/src/features/projects/` does NOT exist
    - `fe/src/features/knowledge-base/` exists with all 47 files (48 minus deleted CategoryFilterTabs)
    - `grep -r "ProjectListPage\|ProjectDetailPage\|ProjectSettingsSheet\|ProjectActivityFeed\|ProjectDatasetPicker\|ProjectMemberList\|ProjectPermissionModal\|CreateProjectModal" fe/src/features/knowledge-base/` returns 0 hits (all renamed)
    - `grep -r "/api/projects" fe/src/features/knowledge-base/` returns 0 hits (all endpoints updated)
    - `grep -r "queryKeys.projects" fe/src/features/knowledge-base/` returns 0 hits
    - `grep "knowledgeBase:" fe/src/lib/queryKeys.ts` returns a match
    - `grep -c "projects:" fe/src/lib/queryKeys.ts` returns 0
  </acceptance_criteria>
  <done>
    Feature directory renamed to knowledge-base/, all Project-prefixed files renamed to KnowledgeBase-prefixed, types/API/query keys all reference new names, barrel exports updated, no stale "project" references in active code within the feature directory.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update routes, navigation, and App.tsx integration</name>
  <read_first>
    fe/src/app/App.tsx
    fe/src/app/routeConfig.ts
    fe/src/layouts/sidebarNav.ts
    fe/src/components/NavigationLoader.tsx
  </read_first>
  <files>
    fe/src/app/App.tsx
    fe/src/app/routeConfig.ts
    fe/src/layouts/sidebarNav.ts
    fe/src/components/NavigationLoader.tsx
  </files>
  <action>
    **App.tsx updates (per D-01, D-02):**
    - Line ~82: Change lazy import from `@/features/projects/pages/ProjectListPage` to `@/features/knowledge-base/pages/KnowledgeBaseListPage`
    - Line ~83: Change lazy import from `@/features/projects/pages/ProjectDetailPage` to `@/features/knowledge-base/pages/KnowledgeBaseDetailPage`
    - Rename const variables: `ProjectListPage` -> `KnowledgeBaseListPage`, `ProjectDetailPage` -> `KnowledgeBaseDetailPage`
    - Line ~192: Change route path from `data-studio/projects` to `data-studio/knowledge-base`, element from `<ProjectListPage />` to `<KnowledgeBaseListPage />`
    - Line ~193: Change route path from `data-studio/projects/:projectId` to `data-studio/knowledge-base/:knowledgeBaseId`, element from `<ProjectDetailPage />` to `<KnowledgeBaseDetailPage />`

    **routeConfig.ts update (per D-01):**
    - Change key from `'/data-studio/projects'` to `'/data-studio/knowledge-base'`
    - Change `titleKey` from `'projectManagement.title'` to `'knowledgeBase.title'`
    - Change `guidelineFeatureId` from `'projects'` to `'knowledge-base'`
    - Keep `fullBleed: true`

    **sidebarNav.ts update (per D-01, D-03):**
    - Change `path` from `'/data-studio/projects'` to `'/data-studio/knowledge-base'`
    - Change `labelKey` from `'nav.projects'` to `'nav.knowledgeBase'`
    - Keep `icon: FolderOpen`

    **NavigationLoader.tsx update:**
    - Update JSDoc example paths from `/projects` to `/knowledge-base` (comment-only change)

    **Build verification:**
    Run `npm run build -w fe` to verify TypeScript compilation succeeds with all import paths resolved.
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge && npm run build -w fe</automated>
  </verify>
  <acceptance_criteria>
    - `grep "features/projects" fe/src/app/App.tsx` returns 0 hits
    - `grep "knowledge-base" fe/src/app/App.tsx` matches the lazy imports and route paths
    - `grep "data-studio/knowledge-base" fe/src/app/routeConfig.ts` returns a match
    - `grep "data-studio/knowledge-base" fe/src/layouts/sidebarNav.ts` returns a match
    - `grep "knowledgeBase.title" fe/src/app/routeConfig.ts` returns a match
    - `grep "nav.knowledgeBase" fe/src/layouts/sidebarNav.ts` returns a match
    - `npm run build -w fe` succeeds with exit code 0
  </acceptance_criteria>
  <done>
    App routes serve at /data-studio/knowledge-base/:knowledgeBaseId, sidebar nav points to /data-studio/knowledge-base, routeConfig uses knowledgeBase.title, FE builds successfully.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run build -w fe` passes (TypeScript + Vite build)
2. No `fe/src/features/projects/` directory exists
3. `grep -r "features/projects" fe/src/` returns 0 hits (no stale imports)
4. `grep -r "/api/projects" fe/src/features/knowledge-base/` returns 0 hits
5. `grep "queryKeys.projects" fe/src/lib/queryKeys.ts` returns 0 hits
</verification>

<success_criteria>
- Feature directory fully renamed and operational at `fe/src/features/knowledge-base/`
- All TypeScript types use KnowledgeBase naming
- All API endpoints call `/api/knowledge-base/...`
- All query keys use `queryKeys.knowledgeBase.*` centralized factory
- Routes serve at `/data-studio/knowledge-base/:knowledgeBaseId`
- FE builds cleanly with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-frontend-rename/08-01-SUMMARY.md`
</output>
