---
phase: 08-frontend-rename
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - fe/src/i18n/locales/en.json
  - fe/src/i18n/locales/vi.json
  - fe/src/i18n/locales/ja.json
  - fe/src/features/chat/pages/ChatAssistantManagementPage.tsx
  - fe/src/features/search/pages/SearchAppManagementPage.tsx
  - fe/src/features/agents/api/agentApi.ts
  - fe/src/features/agents/types/agent.types.ts
autonomous: true
requirements:
  - REN-01
must_haves:
  truths:
    - "User sees 'Knowledge Base' in English UI everywhere 'Project' used to appear"
    - "User sees equivalent translations in Vietnamese and Japanese locales"
    - "i18n JSON keys use knowledgeBase.* namespace instead of projectManagement.* and projects.*"
    - "Chat assistant management page fetches from /api/knowledge-base and uses knowledgeBase query keys"
    - "Search app management page fetches from /api/knowledge-base and uses knowledgeBase query keys"
    - "Agent types and API use knowledge_base_id instead of project_id"
  artifacts:
    - path: "fe/src/i18n/locales/en.json"
      provides: "English translations with knowledgeBase namespace"
      contains: "knowledgeBase"
    - path: "fe/src/i18n/locales/vi.json"
      provides: "Vietnamese translations with knowledgeBase namespace"
      contains: "knowledgeBase"
    - path: "fe/src/i18n/locales/ja.json"
      provides: "Japanese translations with knowledgeBase namespace"
      contains: "knowledgeBase"
  key_links:
    - from: "fe/src/features/chat/pages/ChatAssistantManagementPage.tsx"
      to: "fe/src/lib/queryKeys.ts"
      via: "queryKeys.knowledgeBase.all"
      pattern: "queryKeys\\.knowledgeBase"
    - from: "fe/src/features/search/pages/SearchAppManagementPage.tsx"
      to: "fe/src/lib/queryKeys.ts"
      via: "queryKeys.knowledgeBase.all"
      pattern: "queryKeys\\.knowledgeBase"
---

<objective>
Update i18n strings across all 3 locales and fix cross-feature references outside the knowledge-base feature directory.

Purpose: Complete the user-visible rename (REN-01) by updating all display strings from "Project" to "Knowledge Base" and ensuring external features (chat, search, agents) reference the new API endpoints and query keys.

Output: All i18n keys renamed, all cross-feature `/api/projects` references eliminated, all `t('project.*')` calls updated throughout the codebase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-frontend-rename/08-CONTEXT.md
@.planning/phases/08-frontend-rename/08-RESEARCH.md
@.planning/phases/08-frontend-rename/08-01-SUMMARY.md
@fe/CLAUDE.md

<interfaces>
<!-- From Plan 01 output - query keys now available -->
From fe/src/lib/queryKeys.ts (after Plan 01):
```typescript
knowledgeBase: {
  all: ['knowledge-base'] as const,
  list: () => [...queryKeys.knowledgeBase.all, 'list'] as const,
  detail: (id: string) => [...queryKeys.knowledgeBase.all, 'detail', id] as const,
  members: (knowledgeBaseId: string) => [...queryKeys.knowledgeBase.all, knowledgeBaseId, 'members'] as const,
  datasets: (knowledgeBaseId: string) => [...queryKeys.knowledgeBase.all, knowledgeBaseId, 'datasets'] as const,
  activity: (knowledgeBaseId: string) => [...queryKeys.knowledgeBase.all, knowledgeBaseId, 'activity'] as const,
  // ... other sub-keys
},
```

Cross-feature files to update (from RESEARCH.md):
- ChatAssistantManagementPage.tsx: uses `queryKeys.projects.all`, fetches `/api/projects`, has `projectItems` variable
- SearchAppManagementPage.tsx: uses `queryKeys.projects.all`, fetches `/api/projects`, has `projectItems` variable
- agentApi.ts: uses `project_id` parameter
- agent.types.ts: has `project_id` field
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename i18n keys and update all t() call sites across the FE codebase</name>
  <read_first>
    fe/src/i18n/locales/en.json
    fe/src/i18n/locales/vi.json
    fe/src/i18n/locales/ja.json
  </read_first>
  <files>
    fe/src/i18n/locales/en.json
    fe/src/i18n/locales/vi.json
    fe/src/i18n/locales/ja.json
    fe/src/features/knowledge-base/ (multiple component files with t() calls)
    fe/src/app/routeConfig.ts (titleKey already updated in Plan 01, verify)
    fe/src/layouts/sidebarNav.ts (labelKey already updated in Plan 01, verify)
  </files>
  <action>
    **Step 1: Rename i18n JSON keys in en.json (per D-04, D-05)**
    - Rename the `"projectManagement"` top-level key to `"knowledgeBase"` — keep ALL nested keys and values, just change the namespace
    - Inside the renamed namespace, update English display values:
      - "Project" -> "Knowledge Base" in titles, labels, buttons
      - "project" -> "knowledge base" in descriptions, messages
      - "Projects" -> "Knowledge Bases" where plural
      - "projects" -> "knowledge bases" where plural lowercase
    - Rename `"projects"` secondary namespace keys and merge into `"knowledgeBase"` where applicable
    - Rename `"nav.projects"` to `"nav.knowledgeBase"` with value "Knowledge Base"
    - Do NOT rename any keys under `"datasets.connectors.fields"` or `"datasets.connectors.tooltips"` that reference Jira/Bitbucket/Asana "project" (these are external service terminology)

    **Step 2: Rename i18n JSON keys in vi.json (per D-05)**
    - Same namespace rename: `"projectManagement"` -> `"knowledgeBase"`
    - Update Vietnamese display values: replace project-related Vietnamese text with "Knowledge Base" equivalent (use "C\u01a1 s\u1edf tri th\u1ee9c" or keep "Knowledge Base" as the product term if that's the established convention -- check existing vi.json for precedent)
    - Rename `"nav.projects"` to `"nav.knowledgeBase"`

    **Step 3: Rename i18n JSON keys in ja.json (per D-05)**
    - Same namespace rename: `"projectManagement"` -> `"knowledgeBase"`
    - Update Japanese display values: replace project-related Japanese text with Knowledge Base equivalent (use "\u30ca\u30ec\u30c3\u30b8\u30d9\u30fc\u30b9" or keep "Knowledge Base" as the product term -- check existing ja.json for precedent)
    - Rename `"nav.projects"` to `"nav.knowledgeBase"`

    **Step 4: Update ALL t() call sites across the entire FE codebase**
    Run a comprehensive search to find every `t('projectManagement.` and `t('projects.` call:
    ```bash
    grep -rn "t('projectManagement\.\|t(\"projectManagement\.\|t('projects\.\|t(\"projects\." fe/src/
    ```
    Update every hit:
    - `t('projectManagement.xxx')` -> `t('knowledgeBase.xxx')`
    - `t('projects.xxx')` -> `t('knowledgeBase.xxx')`
    - `t('nav.projects')` -> `t('nav.knowledgeBase')`
    
    **IMPORTANT exclusions:**
    - Do NOT change `t('datasets.connectors.fields.projectOwner')` or similar connector field keys
    - Do NOT change `t('datasets.connectors.tooltips.jiraProjectKey')` or similar
    - Do NOT change any i18n key that refers to external service "projects" (Jira, Bitbucket, Asana)
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge && grep -c '"knowledgeBase"' fe/src/i18n/locales/en.json && grep -c '"projectManagement"' fe/src/i18n/locales/en.json | grep -q "^0$" && echo "PASS: en.json namespace renamed"</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"projectManagement"' fe/src/i18n/locales/en.json` returns 0 hits
    - `grep '"projectManagement"' fe/src/i18n/locales/vi.json` returns 0 hits
    - `grep '"projectManagement"' fe/src/i18n/locales/ja.json` returns 0 hits
    - `grep '"knowledgeBase"' fe/src/i18n/locales/en.json` returns 1+ hits
    - `grep "nav.knowledgeBase" fe/src/i18n/locales/en.json` returns a match
    - `grep -rn "t('projectManagement\." fe/src/features/` returns 0 hits
    - `grep -rn "t('projects\." fe/src/features/` returns 0 hits (excluding connector-related)
    - Connector field keys like `datasets.connectors.fields.projectOwner` are PRESERVED
  </acceptance_criteria>
  <done>
    All 3 locale files use knowledgeBase.* namespace, all t() calls throughout the FE reference new keys, external service "project" references preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update cross-feature references in chat, search, and agents features</name>
  <read_first>
    fe/src/features/chat/pages/ChatAssistantManagementPage.tsx
    fe/src/features/search/pages/SearchAppManagementPage.tsx
    fe/src/features/agents/api/agentApi.ts
    fe/src/features/agents/types/agent.types.ts
  </read_first>
  <files>
    fe/src/features/chat/pages/ChatAssistantManagementPage.tsx
    fe/src/features/search/pages/SearchAppManagementPage.tsx
    fe/src/features/agents/api/agentApi.ts
    fe/src/features/agents/types/agent.types.ts
  </files>
  <action>
    **ChatAssistantManagementPage.tsx:**
    - Change `queryKeys.projects.all` to `queryKeys.knowledgeBase.all`
    - Change any `/api/projects` fetch URL to `/api/knowledge-base`
    - Rename `projectItems` variable to `knowledgeBaseItems`
    - Rename any `projectId` variables to `knowledgeBaseId`
    - Update any `Project` type references to `KnowledgeBase` (import from `@/features/knowledge-base`)
    - Update JSDoc comments

    **SearchAppManagementPage.tsx:**
    - Same changes as ChatAssistantManagementPage: `queryKeys.projects.all` -> `queryKeys.knowledgeBase.all`
    - Change `/api/projects` fetch URL to `/api/knowledge-base`
    - Rename `projectItems` -> `knowledgeBaseItems`
    - Rename `projectId` -> `knowledgeBaseId`
    - Update type references and JSDoc

    **agentApi.ts:**
    - Change `project_id` parameter in agent list API call to `knowledge_base_id`
    - Update any `/api/projects` URLs if present
    - Update JSDoc comments

    **agent.types.ts:**
    - Change `project_id` field to `knowledge_base_id` in agent type definitions
    - Update JSDoc comments

    **Additional cross-feature search:**
    Run comprehensive search to find any remaining "project" references outside the knowledge-base feature:
    ```bash
    grep -rn "queryKeys\.projects\|/api/projects\|project_id\|projectId\|ProjectListPage\|ProjectDetailPage" fe/src/ --include="*.ts" --include="*.tsx" | grep -v "features/knowledge-base/" | grep -v "node_modules" | grep -v "ConnectorSourceFields"
    ```
    Fix any additional hits found (excluding connector-related files).

    **Build verification:**
    Run `npm run build -w fe` to verify everything compiles.
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge && npm run build -w fe</automated>
  </verify>
  <acceptance_criteria>
    - `grep "queryKeys.projects" fe/src/features/chat/pages/ChatAssistantManagementPage.tsx` returns 0 hits
    - `grep "queryKeys.projects" fe/src/features/search/pages/SearchAppManagementPage.tsx` returns 0 hits
    - `grep "/api/projects" fe/src/features/chat/` returns 0 hits
    - `grep "/api/projects" fe/src/features/search/` returns 0 hits
    - `grep "project_id" fe/src/features/agents/types/agent.types.ts` returns 0 hits
    - `grep "project_id" fe/src/features/agents/api/agentApi.ts` returns 0 hits
    - `npm run build -w fe` succeeds with exit code 0
  </acceptance_criteria>
  <done>
    All cross-feature references to "project" updated to "knowledge-base". Chat and search management pages use new API endpoints and query keys. Agent types use knowledge_base_id. FE builds cleanly.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run build -w fe` passes
2. `grep -rn "projectManagement\." fe/src/` returns 0 hits (excluding node_modules)
3. `grep -rn "queryKeys.projects" fe/src/` returns 0 hits
4. `grep -rn "/api/projects" fe/src/` returns 0 hits (excluding ConnectorSourceFields)
5. All 3 locale files have `knowledgeBase` namespace, no `projectManagement` namespace
</verification>

<success_criteria>
- User sees "Knowledge Base" (not "Project") in all English UI text (REN-01)
- Vietnamese and Japanese locales updated with equivalent translations (REN-01)
- All i18n keys use knowledgeBase.* namespace
- Cross-feature pages (chat management, search management) call correct API and query keys
- Agent feature uses knowledge_base_id consistently
- FE builds cleanly with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-frontend-rename/08-02-SUMMARY.md`
</output>
