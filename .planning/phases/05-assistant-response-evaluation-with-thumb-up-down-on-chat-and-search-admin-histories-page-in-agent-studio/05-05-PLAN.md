---
phase: 05-assistant-response-evaluation
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified:
  - fe/src/features/dashboard/types/dashboard.types.ts
  - fe/src/features/dashboard/components/FeedbackTrendChart.tsx
  - fe/src/features/dashboard/components/NegativeFeedbackTable.tsx
  - be/src/modules/admin/services/admin-history.service.ts
  - fe/src/features/search/components/SearchResults.tsx
  - be/src/modules/feedback/services/feedback.service.ts
  - fe/src/features/histories/api/historiesApi.ts
  - fe/src/features/histories/components/FeedbackExportButton.tsx
autonomous: true
requirements: [EVAL-01, EVAL-03, EVAL-04, EVAL-05]
gap_closure: true

must_haves:
  truths:
    - "Dashboard FeedbackTrendChart renders using 'count' and 'satisfactionRate' keys matching BE output"
    - "Dashboard NegativeFeedbackTable renders using 'answerPreview', 'traceId', 'createdAt' keys matching BE output"
    - "Admin chat/search detail views include feedback_thumbup and feedback_comment fields from BE"
    - "All admin history queries filter by tenant_id"
    - "Search thumb-down comment is passed through SearchResults to the feedback API"
    - "Feedback export includes user_email and honors current page filters (source, feedbackFilter)"
  artifacts:
    - path: "fe/src/features/dashboard/types/dashboard.types.ts"
      provides: "FE types aligned to actual BE response shape"
      contains: "answerPreview"
    - path: "be/src/modules/admin/services/admin-history.service.ts"
      provides: "Tenant-scoped queries and feedback-enriched detail endpoints"
      contains: "tenant_id"
    - path: "fe/src/features/search/components/SearchResults.tsx"
      provides: "Comment passthrough in onFeedback callback"
      contains: "comment"
  key_links:
    - from: "fe/src/features/dashboard/components/FeedbackTrendChart.tsx"
      to: "be/src/modules/dashboard/dashboard.service.ts"
      via: "trend data keys must match"
      pattern: "count.*satisfactionRate"
    - from: "fe/src/features/dashboard/components/NegativeFeedbackTable.tsx"
      to: "be/src/modules/dashboard/dashboard.service.ts"
      via: "negativeFeedback item keys must match"
      pattern: "answerPreview.*traceId.*createdAt"
    - from: "fe/src/features/search/components/SearchResults.tsx"
      to: "fe/src/features/search/components/SearchResultCard.tsx"
      via: "onFeedback callback must pass comment"
      pattern: "comment"
---

<objective>
Fix 5 critical/medium issues found during post-execution Codex review of Phase 05 plans 01-04.

Purpose: Plans 01-04 are implemented but have runtime bugs: dashboard FE types don't match BE response shape, chat/search detail views lack feedback enrichment, admin history queries miss tenant scoping, search comment is dropped by callback wrapper, and export is incomplete. This plan addresses all 5 issues.

Output: Corrected FE types, enriched BE detail queries, tenant-scoped admin queries, fixed search callback, enriched export.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-assistant-response-evaluation-with-thumb-up-down-on-chat-and-search-admin-histories-page-in-agent-studio/05-CONTEXT.md
@.planning/phases/05-assistant-response-evaluation-with-thumb-up-down-on-chat-and-search-admin-histories-page-in-agent-studio/05-REVIEWS.md
@.planning/phases/05-assistant-response-evaluation-with-thumb-up-down-on-chat-and-search-admin-histories-page-in-agent-studio/05-01-SUMMARY.md
@.planning/phases/05-assistant-response-evaluation-with-thumb-up-down-on-chat-and-search-admin-histories-page-in-agent-studio/05-03-SUMMARY.md
@.planning/phases/05-assistant-response-evaluation-with-thumb-up-down-on-chat-and-search-admin-histories-page-in-agent-studio/05-04-SUMMARY.md

<interfaces>
<!-- BE dashboard.service.ts ACTUAL output shape (lines 474-492) -->
The BE getFeedbackAnalytics() returns:
- trend: Array of { date: string, count: number, satisfactionRate: number }
- negativeFeedback: Array of { id, query, answerPreview, source, traceId, langfuseUrl, createdAt }

<!-- FE dashboard.types.ts CURRENT (WRONG) shape -->
The FE FeedbackAnalytics type currently defines:
- trend: { date: string; total: number; positive: number }[]  <-- WRONG, should be { date, count, satisfactionRate }
- negativeFeedback: { id, query, answer, source?, trace_id, created_at }[]  <-- WRONG keys, should be { answerPreview, traceId, createdAt }

<!-- FE FeedbackTrendChart.tsx uses dataKey="total" and dataKey="positive" (lines 182, 193) -->
<!-- FE NegativeFeedbackTable.tsx uses item.answer, item.trace_id, item.created_at (lines 134, 139, 143) -->

<!-- SearchResults.tsx line 283 wraps onFeedback: -->
onFeedback={onFeedback ? ((thumbup) => onFeedback(thumbup, result)) : undefined}
<!-- This drops the comment parameter. SearchResultCard calls onFeedback(thumbup, result, comment) but SearchResults only passes (thumbup) through -->

<!-- admin-history.service.ts getChatSessionDetails (line 267) returns raw rows without feedback join -->
<!-- admin-history.service.ts queries lack tenant_id filtering -->

<!-- feedback.service.ts exportFeedback (line 108) returns raw answer_feedback rows without user_email join -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix dashboard FE types + components to match BE response shape (Review Issue #1)</name>
  <files>
    fe/src/features/dashboard/types/dashboard.types.ts,
    fe/src/features/dashboard/components/FeedbackTrendChart.tsx,
    fe/src/features/dashboard/components/NegativeFeedbackTable.tsx
  </files>
  <read_first>
    fe/src/features/dashboard/types/dashboard.types.ts,
    fe/src/features/dashboard/components/FeedbackTrendChart.tsx,
    fe/src/features/dashboard/components/NegativeFeedbackTable.tsx,
    be/src/modules/dashboard/dashboard.service.ts (lines 437-493, getFeedbackAnalytics return shape)
  </read_first>
  <action>
    Addresses review concern: Dashboard FE types mismatch BE response (HIGH severity, Issue #1).

    The BE `getFeedbackAnalytics()` returns trend items with `{ date, count, satisfactionRate }` and negative feedback items with `{ id, query, answerPreview, source, traceId, langfuseUrl, createdAt }`. But the FE types use different keys (`total`/`positive` for trend, `answer`/`trace_id`/`created_at` for negative feedback).

    1. Update `fe/src/features/dashboard/types/dashboard.types.ts`:
       - Change the `trend` property in `FeedbackAnalytics` from:
         `trend: { date: string; total: number; positive: number }[]`
         to:
         `trend: { date: string; count: number; satisfactionRate: number }[]`
       - Change the `negativeFeedback` array item type from:
         `{ id: string; query: string; answer: string; source?: 'chat' | 'search' | 'agent'; trace_id: string | null; created_at: string }`
         to:
         `{ id: string; query: string; answerPreview: string; source?: 'chat' | 'search' | 'agent'; traceId: string | null; langfuseUrl: string | null; createdAt: string }`
       - Add `langfuseBaseUrl` only if not already present (it IS present, so just verify)

    2. Update `fe/src/features/dashboard/components/FeedbackTrendChart.tsx`:
       - Find the `<Area>` with `dataKey="total"` and change to `dataKey="count"`
       - Find the `<Area>` with `dataKey="positive"` -- this needs changing. The BE returns `satisfactionRate` (a percentage) not a raw count. Change `dataKey="positive"` to `dataKey="satisfactionRate"`
       - Update the custom tooltip (`CustomTooltip`) to display `count` and `satisfactionRate` instead of `total` and `positive`. Format satisfactionRate as a percentage (e.g., "85.2%")
       - Update any references to `payload.total` or `payload.positive` in the tooltip to use the new keys

    3. Update `fe/src/features/dashboard/components/NegativeFeedbackTable.tsx`:
       - Change `item.answer` references to `item.answerPreview`
       - Change `item.trace_id` references to `item.traceId`
       - Change `item.created_at` references to `item.createdAt`
       - The component already has a `buildLangfuseUrl` helper -- verify it works with `item.traceId` (not `item.trace_id`)
       - If the component directly accesses `langfuseBaseUrl` from the parent data object, ensure it uses `data.langfuseBaseUrl` (check prop drilling)
  </action>
  <verify>
    <automated>npm run build -w fe</automated>
  </verify>
  <acceptance_criteria>
    - fe/src/features/dashboard/types/dashboard.types.ts contains "count: number; satisfactionRate: number" in trend type (NOT "total" or "positive")
    - fe/src/features/dashboard/types/dashboard.types.ts contains "answerPreview: string" in negativeFeedback type (NOT "answer: string")
    - fe/src/features/dashboard/types/dashboard.types.ts contains "traceId: string" in negativeFeedback type (NOT "trace_id")
    - fe/src/features/dashboard/types/dashboard.types.ts contains "createdAt: string" in negativeFeedback type (NOT "created_at")
    - fe/src/features/dashboard/components/FeedbackTrendChart.tsx contains dataKey="count" (NOT dataKey="total")
    - fe/src/features/dashboard/components/NegativeFeedbackTable.tsx contains "answerPreview" (NOT "item.answer")
    - FE build succeeds with no TypeScript errors
  </acceptance_criteria>
  <done>Dashboard FE types and components use the exact same property names as the BE response: trend uses count/satisfactionRate, negativeFeedback uses answerPreview/traceId/createdAt</done>
</task>

<task type="auto">
  <name>Task 2: Enrich BE detail queries with feedback, add tenant scoping, fix search comment callback, fix export (Review Issues #2-5)</name>
  <files>
    be/src/modules/admin/services/admin-history.service.ts,
    fe/src/features/search/components/SearchResults.tsx,
    be/src/modules/feedback/services/feedback.service.ts,
    fe/src/features/histories/api/historiesApi.ts,
    fe/src/features/histories/components/FeedbackExportButton.tsx
  </files>
  <read_first>
    be/src/modules/admin/services/admin-history.service.ts (full file -- especially getChatSessionDetails around line 267, getSearchSessionDetails, getAgentRunHistory around line 447, getChatHistory around line 77),
    fe/src/features/search/components/SearchResults.tsx (lines 73 and 283 for onFeedback signature and wrapper),
    be/src/modules/feedback/services/feedback.service.ts (exportFeedback method around line 108),
    be/src/modules/feedback/models/answer-feedback.model.ts (findPaginated method),
    fe/src/features/histories/api/historiesApi.ts (exportFeedback function around line 125),
    fe/src/features/histories/components/FeedbackExportButton.tsx (CSV columns around line 43),
    be/src/modules/admin/controllers/admin-history.controller.ts (to see how tenantId is accessed from req)
  </read_first>
  <action>
    Addresses review concerns #2 (detail views missing feedback), #3 (no tenant scoping), #4 (search comment dropped), #5 (export incomplete).

    --- ISSUE #2 (HIGH): Enrich chat/search detail queries with feedback ---

    1. In `be/src/modules/admin/services/admin-history.service.ts`, update `getChatSessionDetails(sessionId)`:
       - After fetching the messages (both external and internal paths), LEFT JOIN feedback data.
       - For the external path (history_chat_messages), add a subquery or post-query enrichment:
         After getting `external` results, run a second query to get feedback for this session:
         ```typescript
         const feedbackRecords = await db('answer_feedback')
           .where('source', 'chat')
           .where('source_id', sessionId)
           .select('message_id', 'thumbup', 'comment')
         ```
         Then merge feedback into messages by matching `message_id` to `id`:
         ```typescript
         const feedbackMap = new Map(feedbackRecords.map(f => [f.message_id, f]))
         return external.map(msg => ({
           ...msg,
           feedback_thumbup: feedbackMap.get(msg.id)?.thumbup ?? null,
           feedback_comment: feedbackMap.get(msg.id)?.comment ?? null,
         }))
         ```
       - For the internal fallback path (chat_messages paired), apply the same feedback merge using the paired message IDs.

    2. Similarly update `getSearchSessionDetails(sessionId)` (find this method in the file):
       - After fetching search records, query feedback with `source = 'search'` and `source_id = sessionId`
       - Merge `feedback_thumbup` and `feedback_comment` into each record

    --- ISSUE #3 (HIGH): Add tenant scoping to admin history queries ---

    3. In `be/src/modules/admin/services/admin-history.service.ts`:
       - Add `tenantId: string` parameter to ALL methods that currently lack it:
         - `getChatHistory()` -- add `.where('history_chat_sessions.tenant_id', tenantId)` to the query. If `history_chat_sessions` does not have a `tenant_id` column, use a subquery through `history_chat_messages` or filter via `answer_feedback.tenant_id` in the feedback subqueries.
         - `getChatSessionDetails()` -- add tenantId param, filter the feedback query by `tenant_id`
         - `getSearchHistory()` -- add tenant filter
         - `getSearchSessionDetails()` -- add tenantId param, filter the feedback query by `tenant_id`
         - `getAgentRunHistory()` -- add `.where('agents.tenant_id', tenantId)` to filter agent runs by tenant
         - `getAgentRunDetails()` -- add tenantId param
       - IMPORTANT: Check the actual table schemas. The `history_chat_sessions` table may or may not have `tenant_id`. If it does not, you can scope via the `answer_feedback.tenant_id` in the feedback subqueries, or via `users` table join. Read the initial migration (lines 1200-1250 of 20260312000000_initial_schema.ts) to check the schema.
       - Update the controller methods to extract `tenantId` from `req.user.tenant_id` (or `req.tenantId` depending on the auth middleware pattern) and pass it to the service methods.

    --- ISSUE #4 (MEDIUM): Fix search comment callback ---

    4. In `fe/src/features/search/components/SearchResults.tsx`:
       - Line 73: Change the `onFeedback` prop type from:
         `onFeedback?: ((thumbup: boolean, result?: SearchResult) => void) | undefined`
         to:
         `onFeedback?: ((thumbup: boolean, result?: SearchResult, comment?: string) => void) | undefined`
       - Line 283: Change the callback wrapper from:
         `onFeedback={onFeedback ? ((thumbup) => onFeedback(thumbup, result)) : undefined}`
         to:
         `onFeedback={onFeedback ? ((thumbup, _result, comment) => onFeedback(thumbup, result, comment)) : undefined}`
         Note: SearchResultCard calls `onFeedback(thumbup, result, comment)` -- the `_result` parameter here is the result passed back from the card (same as `result` in the outer closure). We need to capture `comment` (3rd arg) and pass it through.
       - Also check lines 192-207 where there's inline feedback buttons for the "answer" display. If those call `onFeedback(true)` or `onFeedback(false)` directly without comment, that's fine (no popover there).

    --- ISSUE #5 (MEDIUM): Enrich export DTO with user_email and honor filters ---

    5. In `be/src/modules/feedback/services/feedback.service.ts`, update `exportFeedback()`:
       - Instead of using `findPaginated` (which returns raw `answer_feedback` rows without user info), build a custom query that joins `users` table:
         ```typescript
         let query = ModelFactory.answerFeedback.getKnex()
           .from('answer_feedback')
           .leftJoin('users', 'answer_feedback.user_id', 'users.id')
           .select('answer_feedback.*', 'users.email as user_email')
           .where('answer_feedback.tenant_id', filters.tenantId)
         ```
       - Apply all filter conditions: source, thumbup, startDate, endDate
       - Limit to 10000 records
       - Order by created_at desc

    6. In `fe/src/features/histories/api/historiesApi.ts`, update `exportFeedback()`:
       - Currently only passes `startDate`, `endDate`, and `thumbup` from feedbackFilter
       - Also pass `source` filter if the user has selected a specific source tab or source filter:
         If `filters.source` exists (or derive from the active tab), add `params.set('source', filters.source)`
       - Check the `FilterState` type to see if it has a `source` field. If not, the caller (FeedbackExportButton) should pass the active tab as a source param.

    7. In `fe/src/features/histories/components/FeedbackExportButton.tsx`:
       - Verify the CSV `headers` array includes `user_email` (it already does at line 43: `['query', 'answer', 'thumbup', 'comment', 'source', 'user_email', 'created_at']`)
       - The BE now returns `user_email` from the join, so CSV generation will work
       - If the component receives filter state, ensure it passes all relevant filters to the API call
  </action>
  <verify>
    <automated>npm run build -w be && npm run build -w fe</automated>
  </verify>
  <acceptance_criteria>
    - be/src/modules/admin/services/admin-history.service.ts getChatSessionDetails contains "feedback_thumbup" or "answer_feedback" join/merge
    - be/src/modules/admin/services/admin-history.service.ts getChatSessionDetails contains "tenant_id" parameter or filtering
    - be/src/modules/admin/services/admin-history.service.ts getAgentRunHistory contains "tenant_id" filtering
    - fe/src/features/search/components/SearchResults.tsx line with onFeedback wrapper contains "comment" parameter passthrough
    - fe/src/features/search/components/SearchResults.tsx onFeedback prop type includes "comment?: string"
    - be/src/modules/feedback/services/feedback.service.ts exportFeedback contains "user_email" or "users" join
    - Both BE and FE build succeed
  </acceptance_criteria>
  <done>Chat/search detail views return feedback fields, all admin queries are tenant-scoped, search comment passes through callback, export includes user_email and respects filters</done>
</task>

</tasks>

<verification>
- `npm run build -w be` -- BE TypeScript compiles without errors
- `npm run build -w fe` -- FE TypeScript compiles without errors
- `npm run test -w be -- --run` -- all BE tests pass
- `npm run test:run -w fe` -- all FE tests pass
</verification>

<success_criteria>
- Dashboard trend chart renders using BE's `count`/`satisfactionRate` keys (not `total`/`positive`)
- Dashboard negative feedback table renders using BE's `answerPreview`/`traceId`/`createdAt` keys
- Chat/search detail views include `feedback_thumbup` and `feedback_comment` from enriched BE response
- All admin history service methods filter by `tenant_id`
- Search thumb-down comment propagates through SearchResults to the feedback API
- Feedback export CSV includes `user_email` column with actual data
- Export respects current page filters (source, thumbup)
- Both FE and BE build and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-assistant-response-evaluation-with-thumb-up-down-on-chat-and-search-admin-histories-page-in-agent-studio/05-05-SUMMARY.md`
</output>
