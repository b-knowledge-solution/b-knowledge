---
phase: 05-assistant-response-evaluation
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - be/src/modules/admin/services/admin-history.service.ts
  - be/src/modules/admin/controllers/admin-history.controller.ts
  - be/src/modules/admin/routes/admin.routes.ts
  - fe/src/features/histories/types/histories.types.ts
  - fe/src/features/histories/api/historiesApi.ts
  - fe/src/features/histories/api/historiesQueries.ts
  - fe/src/features/histories/hooks/useHistoriesFilters.ts
  - fe/src/features/histories/components/AdminSessionListSidebar.tsx
  - fe/src/features/histories/components/AdminChatDetailView.tsx
  - fe/src/features/histories/components/AdminSearchDetailView.tsx
  - fe/src/features/histories/components/AdminFilterDialog.tsx
  - fe/src/features/histories/components/AdminAgentRunsDetailView.tsx
  - fe/src/features/histories/components/FeedbackExportButton.tsx
  - fe/src/features/histories/pages/HistoriesPage.tsx
  - fe/src/i18n/locales/en.json
  - fe/src/i18n/locales/vi.json
  - fe/src/i18n/locales/ja.json
autonomous: true
requirements: [EVAL-02, EVAL-03, EVAL-05]

must_haves:
  truths:
    - "Admin Histories page shows feedback indicators (thumb icons + counts) on session cards in sidebar"
    - "Admin Histories detail view shows feedback badges on individual messages with collapsible comments"
    - "Admin can filter sessions by feedback status (all/positive/negative/any/none)"
    - "Admin Histories page has an Agent Runs tab showing agent run history with feedback"
    - "Admin can export feedback records as CSV from Histories page"
  artifacts:
    - path: "fe/src/features/histories/components/AdminAgentRunsDetailView.tsx"
      provides: "Agent runs detail view for admin Histories"
      contains: "AdminAgentRunsDetailView"
      min_lines: 50
    - path: "fe/src/features/histories/components/FeedbackExportButton.tsx"
      provides: "CSV export button component"
      contains: "FeedbackExportButton"
    - path: "fe/src/features/histories/pages/HistoriesPage.tsx"
      provides: "Updated page with Agent Runs tab and export button"
      contains: "agentRuns"
  key_links:
    - from: "fe/src/features/histories/pages/HistoriesPage.tsx"
      to: "fe/src/features/histories/components/AdminAgentRunsDetailView.tsx"
      via: "conditional render on activeTab === 'agentRuns'"
      pattern: "AdminAgentRunsDetailView"
    - from: "fe/src/features/histories/components/FeedbackExportButton.tsx"
      to: "fe/src/features/histories/api/historiesApi.ts"
      via: "exportFeedback API call"
      pattern: "exportFeedback"
    - from: "fe/src/features/histories/components/AdminSessionListSidebar.tsx"
      to: "fe/src/features/histories/types/histories.types.ts"
      via: "feedback counts on session summaries"
      pattern: "positive_count|negative_count"
---

<objective>
Enhance admin Histories page with feedback indicators, feedback filter, Agent Runs tab, and CSV export button. Update backend admin-history service to enrich queries with feedback data.

Purpose: Per D-01, D-02, D-04, D-08 — admin sees feedback quality signals on the Histories page, can filter by feedback, view agent run feedback, and export feedback data.

Output: Enhanced Histories page with 3 tabs (Chat/Search/Agent Runs), feedback indicators, filter, and export.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-assistant-response-evaluation-with-thumb-up-down-on-chat-and-search-admin-histories-page-in-agent-studio/05-CONTEXT.md
@.planning/phases/05-assistant-response-evaluation-with-thumb-up-down-on-chat-and-search-admin-histories-page-in-agent-studio/05-RESEARCH.md
@.planning/phases/05-assistant-response-evaluation-with-thumb-up-down-on-chat-and-search-admin-histories-page-in-agent-studio/05-UI-SPEC.md
@.planning/phases/05-assistant-response-evaluation-with-thumb-up-down-on-chat-and-search-admin-histories-page-in-agent-studio/05-01-SUMMARY.md
@.planning/phases/05-assistant-response-evaluation-with-thumb-up-down-on-chat-and-search-admin-histories-page-in-agent-studio/05-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->
BE feedback endpoints (created in Plan 01):
- GET /api/feedback?source=chat&thumbup=true&startDate=...&endDate=...&page=1&limit=20 -> { data: AnswerFeedback[], total: number }
- GET /api/feedback/stats?startDate=...&endDate=... -> { sourceBreakdown: { chat, search, agent }, topFlagged: [...] }
- GET /api/feedback/export?source=...&thumbup=...&startDate=...&endDate=... -> AnswerFeedback[]

<!-- Existing types to extend -->
From fe/src/features/histories/types/histories.types.ts:
```typescript
export type HistoriesTab = 'chat' | 'search'  // MUST add 'agentRuns'
export interface FilterState { email: string; startDate: string; endDate: string; sourceName?: string }  // MUST add feedbackFilter
export interface ChatSessionSummary { session_id, user_email, user_prompt, created_at, message_count, source_name }  // MUST add positive_count, negative_count
export interface SearchSessionSummary { session_id, user_email, search_input, created_at, message_count, source_name }  // MUST add positive_count, negative_count
```

From fe/src/features/histories/api/historiesApi.ts:
- fetchExternalChatHistory(search, filters, page) -> ChatSessionSummary[]
- fetchExternalSearchHistory(search, filters, page) -> SearchSessionSummary[]
- fetchChatSessionDetails(sessionId) -> ExternalChatHistory[]
- fetchSearchSessionDetails(sessionId) -> ExternalSearchHistory[]

From fe/src/features/histories/pages/HistoriesPage.tsx:
- Uses AdminSessionListSidebar, AdminChatDetailView, AdminSearchDetailView
- handleTabSwitch toggles between chat/search
- filterState from useHistoriesFilters hook
- dataState from useHistoriesData hook
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BE admin-history feedback enrichment + FE types/API updates</name>
  <files>
    be/src/modules/admin/services/admin-history.service.ts,
    fe/src/features/histories/types/histories.types.ts,
    fe/src/features/histories/api/historiesApi.ts,
    fe/src/features/histories/api/historiesQueries.ts,
    fe/src/features/histories/hooks/useHistoriesFilters.ts
  </files>
  <read_first>
    be/src/modules/admin/services/admin-history.service.ts (full file),
    be/src/modules/admin/controllers/admin-history.controller.ts,
    be/src/modules/admin/routes/admin.routes.ts,
    fe/src/features/histories/types/histories.types.ts,
    fe/src/features/histories/api/historiesApi.ts,
    fe/src/features/histories/api/historiesQueries.ts,
    fe/src/features/histories/hooks/useHistoriesFilters.ts
  </read_first>
  <action>
    Per D-02, D-08: Enrich admin history queries with feedback data and add agent runs support.

    1. Update `be/src/modules/admin/services/admin-history.service.ts`:
       - In `getChatHistory()` method, add two subquery columns to the SELECT:
         ```sql
         db.raw(`(SELECT COUNT(*) FROM answer_feedback af WHERE af.source = 'chat' AND af.source_id = history_chat_sessions.session_id AND af.thumbup = true) as positive_count`)
         db.raw(`(SELECT COUNT(*) FROM answer_feedback af WHERE af.source = 'chat' AND af.source_id = history_chat_sessions.session_id AND af.thumbup = false) as negative_count`)
         ```
       - In `getSearchHistory()` (or equivalent search method), add same subquery pattern with `af.source = 'search'`
       - Add optional `feedbackFilter` parameter to both methods: `feedbackFilter?: 'positive' | 'negative' | 'any' | 'none'`
         - 'positive': `EXISTS (SELECT 1 FROM answer_feedback WHERE source_id = session_id AND thumbup = true)`
         - 'negative': `EXISTS (SELECT 1 FROM answer_feedback WHERE source_id = session_id AND thumbup = false)`
         - 'any': `EXISTS (SELECT 1 FROM answer_feedback WHERE source_id = session_id)`
         - 'none': `NOT EXISTS (SELECT 1 FROM answer_feedback WHERE source_id = session_id)`
       - Add new method `getAgentRunHistory(page, limit, search, email, startDate, endDate, feedbackFilter?)`:
         - Query `agent_runs` table joined with `agents` for agent name
         - Add feedback count subqueries with `source = 'agent'`
         - Return array of `{ run_id, agent_name, agent_id, status, input, output, started_at, completed_at, duration_ms, positive_count, negative_count }`
       - Add new method `getAgentRunDetails(runId)`:
         - Return single agent run with steps and feedback records
       - Update controller and routes to accept `feedbackFilter` query param and expose `/api/admin/history/agent-runs` and `/api/admin/history/agent-runs/:runId`

    2. Update `fe/src/features/histories/types/histories.types.ts`:
       - Add to `ChatSessionSummary`: `positive_count?: number; negative_count?: number`
       - Add to `SearchSessionSummary`: `positive_count?: number; negative_count?: number`
       - Change `HistoriesTab` to: `'chat' | 'search' | 'agentRuns'`
       - Add to `FilterState`: `feedbackFilter?: 'all' | 'positive' | 'negative' | 'any' | 'none'`
       - Add new interface `AgentRunSessionSummary`:
         ```typescript
         interface AgentRunSessionSummary {
           run_id: string; agent_name: string; agent_id: string; status: string;
           input: string; output: string; started_at: string; completed_at: string;
           duration_ms: number; positive_count?: number; negative_count?: number;
         }
         ```
       - Add `ExternalAgentRunDetail` interface with run details + feedback records

    3. Update `fe/src/features/histories/api/historiesApi.ts`:
       - Update `fetchExternalChatHistory` to include `feedbackFilter` in query params
       - Update `fetchExternalSearchHistory` to include `feedbackFilter` in query params
       - Add `fetchAgentRunHistory(search, filters, page)` calling `GET /api/admin/history/agent-runs`
       - Add `fetchAgentRunDetails(runId)` calling `GET /api/admin/history/agent-runs/:runId`
       - Add `exportFeedback(filters)` calling `GET /api/feedback/export` with filter params, returns AnswerFeedback[]

    4. Update `fe/src/features/histories/api/historiesQueries.ts`:
       - Add `useAgentRunHistory` query hook wrapping `fetchAgentRunHistory`
       - Add `useAgentRunDetails` query hook wrapping `fetchAgentRunDetails`
       - Add `useExportFeedback` mutation hook wrapping `exportFeedback`
       - Update existing `useHistoriesData` hook to support `agentRuns` tab

    5. Update `fe/src/features/histories/hooks/useHistoriesFilters.ts`:
       - Add `feedbackFilter` to `FilterState` initial value: `feedbackFilter: 'all'`
       - Update `isFiltered` to include feedbackFilter check: `|| (filters.feedbackFilter && filters.feedbackFilter !== 'all')`
  </action>
  <verify>
    <automated>npm run build -w be && npm run build -w fe</automated>
  </verify>
  <acceptance_criteria>
    - be/src/modules/admin/services/admin-history.service.ts contains "positive_count" and "negative_count" in SELECT subqueries
    - be/src/modules/admin/services/admin-history.service.ts contains "getAgentRunHistory" method
    - be/src/modules/admin/services/admin-history.service.ts contains "feedbackFilter" parameter
    - fe/src/features/histories/types/histories.types.ts contains "'chat' | 'search' | 'agentRuns'"
    - fe/src/features/histories/types/histories.types.ts contains "AgentRunSessionSummary"
    - fe/src/features/histories/api/historiesApi.ts contains "fetchAgentRunHistory" and "exportFeedback"
    - fe/src/features/histories/hooks/useHistoriesFilters.ts contains "feedbackFilter"
    - Both BE and FE build successfully
  </acceptance_criteria>
  <done>Admin history service enriched with feedback counts, agent run history endpoint added, FE types/API/hooks updated for feedback filter and agent runs tab</done>
</task>

<task type="auto">
  <name>Task 2: FE Histories page UI enhancements — feedback indicators, Agent Runs tab, filter, export</name>
  <files>
    fe/src/features/histories/components/AdminSessionListSidebar.tsx,
    fe/src/features/histories/components/AdminChatDetailView.tsx,
    fe/src/features/histories/components/AdminSearchDetailView.tsx,
    fe/src/features/histories/components/AdminFilterDialog.tsx,
    fe/src/features/histories/components/AdminAgentRunsDetailView.tsx,
    fe/src/features/histories/components/FeedbackExportButton.tsx,
    fe/src/features/histories/pages/HistoriesPage.tsx,
    fe/src/i18n/locales/en.json,
    fe/src/i18n/locales/vi.json,
    fe/src/i18n/locales/ja.json
  </files>
  <read_first>
    fe/src/features/histories/components/AdminSessionListSidebar.tsx,
    fe/src/features/histories/components/AdminChatDetailView.tsx,
    fe/src/features/histories/components/AdminSearchDetailView.tsx,
    fe/src/features/histories/components/AdminFilterDialog.tsx,
    fe/src/features/histories/pages/HistoriesPage.tsx,
    fe/src/features/histories/components/EmptyState.tsx,
    fe/src/components/ui/collapsible.tsx,
    fe/src/components/ui/select.tsx,
    .planning/phases/05-assistant-response-evaluation-with-thumb-up-down-on-chat-and-search-admin-histories-page-in-agent-studio/05-UI-SPEC.md
  </read_first>
  <action>
    Per D-01, D-02, D-04, D-08 and UI-SPEC Components 2-8:

    1. Update `AdminSessionListSidebar.tsx` (UI-SPEC Component 2):
       - For each session card, if `item.positive_count > 0 || item.negative_count > 0`, show a feedback summary badge:
         - `<span className="flex items-center gap-1 text-xs text-muted-foreground">` containing:
           - `<ThumbsUp className="h-3 w-3 text-green-500" /> {item.positive_count}`
           - `<ThumbsDown className="h-3 w-3 text-red-500" /> {item.negative_count}`
         - Position: below message_count, right-aligned on the card
       - Handle the new `agentRuns` tab: add a third tab button with `Bot` icon from lucide-react and label from `t('histories.tabs.agentRuns')`
       - Render agent run items differently: show agent_name, status badge, duration

    2. Update `AdminChatDetailView.tsx` (UI-SPEC Component 3):
       - For each message in the detail view, check if feedback data exists (the message response from backend should include feedback info via the enriched query)
       - If feedback exists: show small ThumbsUp (green-500) or ThumbsDown (red-500) icon (h-3 w-3) inline after the message
       - If feedback has a comment: show `<Collapsible>` below the message with trigger text `t('histories.viewFeedbackComment')`, content is the comment text styled with `text-sm text-muted-foreground border-l-2 border-primary pl-3`

    3. Update `AdminSearchDetailView.tsx` (UI-SPEC Component 3):
       - Same feedback indicator pattern as AdminChatDetailView
       - Show thumb icon + collapsible comment for search results with feedback

    4. Update `AdminFilterDialog.tsx` (UI-SPEC Component 4):
       - Add a new filter field after existing filters: "Feedback" select dropdown
       - Options: All (default), Positive only, Negative only, Any feedback, No feedback
       - Use shadcn `<Select>` component
       - Value maps to `feedbackFilter` in tempFilters
       - Label: `t('histories.filters.feedback')`

    5. Create `AdminAgentRunsDetailView.tsx` (UI-SPEC Component 5):
       - New component showing agent run detail when an agent run is selected
       - Display: agent name, status badge, trigger input, final output (scrollable), duration, timestamps
       - Include FeedbackCommentPopover for completed runs (import from `@/features/chat/components/FeedbackCommentPopover`)
       - Show feedback records if they exist
       - Empty state: use EmptyState component with `t('histories.agentRuns.emptyTitle')` and `t('histories.agentRuns.emptyDescription')`

    6. Create `FeedbackExportButton.tsx` (UI-SPEC Component 8):
       - Button with Download icon + `t('histories.exportFeedback')` label, variant="outline"
       - On click: call `exportFeedback(currentFilters)` API, convert JSON to CSV, trigger browser download
       - CSV columns: query, answer, thumbup, comment, source, user_email (need to join), created_at
       - CSV helper: `escapeCsvField(value)` wraps fields containing commas/quotes in double quotes
       - Loading state: spinner + `t('histories.exportingFeedback')`, button disabled
       - Error state: sonner toast with `t('histories.exportFeedbackError')`

    7. Update `HistoriesPage.tsx`:
       - Add `agentRuns` to tab handling: `handleTabSwitch` accepts `'chat' | 'search' | 'agentRuns'`
       - Add conditional render for `activeTab === 'agentRuns'` showing `<AdminAgentRunsDetailView>`
       - Add `<FeedbackExportButton>` in the top-right header area, next to filter/refresh buttons
       - Pass feedbackFilter through to data queries

    8. Add all i18n keys from UI-SPEC Copywriting Contract to en.json, vi.json, ja.json:
       - histories.filters.feedback, histories.filters.feedbackAll, feedbackPositive, feedbackNegative, feedbackAny, feedbackNone
       - histories.tabs.agentRuns
       - histories.exportFeedback, histories.exportingFeedback, histories.exportFeedbackError
       - histories.viewFeedbackComment, histories.feedbackCount
       - histories.agentRuns.emptyTitle, histories.agentRuns.emptyDescription
       - agents.runs.noFeedback
       (Vietnamese: use proper diacritics, Japanese: use proper kanji/hiragana — reference existing locale files for style)
  </action>
  <verify>
    <automated>npm run build -w fe</automated>
  </verify>
  <acceptance_criteria>
    - fe/src/features/histories/components/AdminSessionListSidebar.tsx contains "positive_count" and "negative_count" and "ThumbsUp"
    - fe/src/features/histories/components/AdminChatDetailView.tsx contains "Collapsible" and "viewFeedbackComment"
    - fe/src/features/histories/components/AdminFilterDialog.tsx contains "feedbackFilter" and "Select"
    - fe/src/features/histories/components/AdminAgentRunsDetailView.tsx exists and contains "AdminAgentRunsDetailView" and "FeedbackCommentPopover"
    - fe/src/features/histories/components/FeedbackExportButton.tsx exists and contains "FeedbackExportButton" and "text/csv"
    - fe/src/features/histories/pages/HistoriesPage.tsx contains "agentRuns" and "FeedbackExportButton" and "AdminAgentRunsDetailView"
    - en.json contains "histories.tabs.agentRuns" and "histories.exportFeedback"
    - FE build succeeds
  </acceptance_criteria>
  <done>Histories page has feedback indicators on sessions, feedback badges on messages with collapsible comments, Agent Runs tab, feedback filter, and CSV export button — all with i18n</done>
</task>

</tasks>

<verification>
- `npm run build -w fe` — TypeScript compiles without errors
- `npm run build -w be` — Backend compiles without errors
- `npm run test:run -w fe` — all FE tests pass
- `npm run test -w be -- --run` — all BE tests pass
</verification>

<success_criteria>
- Admin Histories shows feedback counts on session cards
- Detail views show feedback icons and collapsible comments
- Feedback filter dropdown works (all/positive/negative/any/none)
- Agent Runs tab renders agent run history with feedback
- Export button downloads CSV with feedback records
- All i18n keys present in 3 locales
- Both FE and BE build and test successfully
</success_criteria>

<output>
After completion, create `.planning/phases/05-assistant-response-evaluation-with-thumb-up-down-on-chat-and-search-admin-histories-page-in-agent-studio/05-03-SUMMARY.md`
</output>
