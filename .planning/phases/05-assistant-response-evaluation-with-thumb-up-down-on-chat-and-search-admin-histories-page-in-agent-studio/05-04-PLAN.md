---
phase: 05-assistant-response-evaluation
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - fe/src/features/dashboard/components/FeedbackSummaryCards.tsx
  - fe/src/features/dashboard/components/FeedbackTrendChart.tsx
  - fe/src/features/dashboard/components/NegativeFeedbackTable.tsx
  - fe/src/features/dashboard/types/dashboard.types.ts
  - fe/src/features/dashboard/api/dashboardApi.ts
  - fe/src/features/dashboard/api/dashboardQueries.ts
autonomous: true
requirements: [EVAL-04]

must_haves:
  truths:
    - "Dashboard feedback analytics include agent source in totals"
    - "Dashboard shows source breakdown including agent feedback count"
    - "Negative feedback table shows source column with chat/search/agent values"
  artifacts:
    - path: "fe/src/features/dashboard/components/FeedbackSummaryCards.tsx"
      provides: "Feedback stat cards with agent source included"
      contains: "agent"
    - path: "fe/src/features/dashboard/components/NegativeFeedbackTable.tsx"
      provides: "Negative feedback table with source column"
      contains: "source"
  key_links:
    - from: "fe/src/features/dashboard/components/FeedbackSummaryCards.tsx"
      to: "fe/src/features/dashboard/api/dashboardQueries.ts"
      via: "useFeedbackAnalytics hook"
      pattern: "useFeedbackAnalytics"
---

<objective>
Verify and enhance existing Dashboard feedback components to include agent feedback data after the source constraint migration.

Purpose: Per D-01, D-03 — the Dashboard "Response Quality" section already exists (FeedbackSummaryCards, FeedbackTrendChart, NegativeFeedbackTable). This plan ensures agent source data is properly included and a source breakdown is visible.

Output: Dashboard feedback components updated to display agent feedback alongside chat/search.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-assistant-response-evaluation-with-thumb-up-down-on-chat-and-search-admin-histories-page-in-agent-studio/05-CONTEXT.md
@.planning/phases/05-assistant-response-evaluation-with-thumb-up-down-on-chat-and-search-admin-histories-page-in-agent-studio/05-01-SUMMARY.md

<interfaces>
<!-- Existing dashboard components -->
From fe/src/features/dashboard/components/FeedbackSummaryCards.tsx:
- Takes `data: FeedbackAnalytics | null`
- Shows 4 stat cards: satisfaction rate, total feedback, zero-result rate, worst dataset
- Already works with backend getFeedbackAnalytics() which queries all sources

From fe/src/features/dashboard/types/dashboard.types.ts:
```typescript
interface FeedbackAnalytics {
  satisfactionRate: number
  totalFeedback: number
  zeroResultRate: number
  worstDatasets: { datasetId: string; name: string; satisfactionRate: number; feedbackCount: number }[]
  trend: { date: string; count: number; satisfactionRate: number }[]
  negativeFeedback: { id: string; query: string; answerPreview: string; traceId: string | null; langfuseUrl: string | null; createdAt: string }[]
}
```

From be/src/modules/dashboard/dashboard.service.ts:
- getFeedbackAnalytics() already queries all answer_feedback records (no source filter)
- Agent source data will be included automatically after migration
- New getFeedbackSourceBreakdown() method added in Plan 01
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance Dashboard feedback components for agent source visibility</name>
  <files>
    fe/src/features/dashboard/components/FeedbackSummaryCards.tsx,
    fe/src/features/dashboard/components/NegativeFeedbackTable.tsx,
    fe/src/features/dashboard/types/dashboard.types.ts,
    fe/src/features/dashboard/api/dashboardApi.ts,
    fe/src/features/dashboard/api/dashboardQueries.ts
  </files>
  <read_first>
    fe/src/features/dashboard/components/FeedbackSummaryCards.tsx (full file),
    fe/src/features/dashboard/components/FeedbackTrendChart.tsx (full file),
    fe/src/features/dashboard/components/NegativeFeedbackTable.tsx (full file),
    fe/src/features/dashboard/types/dashboard.types.ts,
    fe/src/features/dashboard/api/dashboardApi.ts,
    fe/src/features/dashboard/api/dashboardQueries.ts,
    fe/src/features/dashboard/pages/AdminDashboardPage.tsx (to see how components are used)
  </read_first>
  <action>
    Per D-01, D-03: Enhance existing dashboard feedback components to surface agent feedback data.

    1. Update `fe/src/features/dashboard/types/dashboard.types.ts`:
       - Add `sourceBreakdown?: { chat: number; search: number; agent: number }` to `FeedbackAnalytics` interface
       - Add `source?: 'chat' | 'search' | 'agent'` to the `negativeFeedback` array item type

    2. Update `fe/src/features/dashboard/api/dashboardApi.ts`:
       - Add function `fetchFeedbackSourceBreakdown(startDate?, endDate?)` calling `GET /api/feedback/stats` (from Plan 01)
       - Return type: `{ sourceBreakdown: { chat: number; search: number; agent: number }; topFlagged: Array<...> }`

    3. Update `fe/src/features/dashboard/api/dashboardQueries.ts`:
       - Add `useFeedbackSourceBreakdown(startDate?, endDate?)` query hook

    4. Update `FeedbackSummaryCards.tsx`:
       - Keep existing 4 stat cards as-is (they already show totals from getFeedbackAnalytics which includes all sources)
       - Add a 5th card or a breakdown row below the existing cards showing source distribution:
         - "By source: Chat {N} | Search {N} | Agent {N}" using the sourceBreakdown data
         - Use small colored badges: blue for chat, green for search, purple for agent
         - If sourceBreakdown is not yet loaded, show skeleton placeholders
       - Accept `sourceBreakdown` as an additional prop (or fetch via the new hook internally)

    5. Update `NegativeFeedbackTable.tsx`:
       - Add a "Source" column to the table showing the source type (chat/search/agent)
       - Display as a Badge: `<Badge variant="outline">{source}</Badge>`
       - If the backend negativeFeedback response does not currently include source, update the backend query in `dashboard.service.ts` to add `source` to the SELECT in the negative feedback subquery (line ~451): add `'source'` to the `.select('id', 'query', 'answer', 'trace_id', 'created_at')` list
       - Update the negativeFeedback mapping to include `source: r.source as string`

    6. Verify `FeedbackTrendChart.tsx`:
       - Read the component to check if it needs any changes
       - The trend data from getFeedbackAnalytics already aggregates all sources
       - If the chart does NOT show per-source breakdown, leave it as-is (this is fine for v1)
       - If it already has source breakdown, ensure 'agent' is included in the legend/colors
  </action>
  <verify>
    <automated>npm run build -w fe && npm run build -w be</automated>
  </verify>
  <acceptance_criteria>
    - fe/src/features/dashboard/types/dashboard.types.ts contains "sourceBreakdown" with "agent"
    - fe/src/features/dashboard/components/FeedbackSummaryCards.tsx contains "agent" in source breakdown display
    - fe/src/features/dashboard/components/NegativeFeedbackTable.tsx contains "source" column or "Source" header
    - fe/src/features/dashboard/api/dashboardApi.ts contains "fetchFeedbackSourceBreakdown"
    - Both FE and BE build successfully
  </acceptance_criteria>
  <done>Dashboard feedback components show agent source data, source breakdown visible, negative feedback table has source column</done>
</task>

</tasks>

<verification>
- `npm run build -w fe` — TypeScript compiles without errors
- `npm run build -w be` — Backend compiles without errors
- `npm run test:run -w fe` — all FE tests pass
</verification>

<success_criteria>
- Dashboard FeedbackSummaryCards shows source breakdown (chat/search/agent)
- NegativeFeedbackTable has source column
- Existing dashboard functionality preserved
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-assistant-response-evaluation-with-thumb-up-down-on-chat-and-search-admin-histories-page-in-agent-studio/05-04-SUMMARY.md`
</output>
