---
phase: 05-assistant-response-evaluation
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - fe/src/features/dashboard/components/FeedbackSummaryCards.tsx
  - fe/src/features/dashboard/components/FeedbackTrendChart.tsx
  - fe/src/features/dashboard/components/NegativeFeedbackTable.tsx
  - fe/src/features/dashboard/components/TopFlaggedSessionsCard.tsx
  - fe/src/features/dashboard/types/dashboard.types.ts
  - fe/src/features/dashboard/api/dashboardApi.ts
  - fe/src/features/dashboard/api/dashboardQueries.ts
  - be/src/modules/dashboard/dashboard.service.ts
autonomous: true
requirements: [EVAL-03, EVAL-04]

must_haves:
  truths:
    - "Admin sees satisfaction percentage with trend on the Dashboard Response Quality section"
    - "Admin sees a list of recent negative feedback entries on the Dashboard"
    - "Admin sees feedback count broken down by source (chat/search/agent) on the Dashboard"
    - "Admin sees top flagged sessions ranked by negative feedback count on the Dashboard"
    - "Negative feedback table shows which source (chat/search/agent) each entry came from"
  artifacts:
    - path: "fe/src/features/dashboard/components/FeedbackSummaryCards.tsx"
      provides: "Feedback stat cards including source breakdown"
      contains: "sourceBreakdown"
    - path: "fe/src/features/dashboard/components/TopFlaggedSessionsCard.tsx"
      provides: "Top flagged sessions stat card consuming topFlagged data"
      contains: "TopFlaggedSessionsCard"
      min_lines: 30
    - path: "fe/src/features/dashboard/components/NegativeFeedbackTable.tsx"
      provides: "Negative feedback table with source column"
      contains: "source"
  key_links:
    - from: "fe/src/features/dashboard/components/FeedbackSummaryCards.tsx"
      to: "fe/src/features/dashboard/api/dashboardQueries.ts"
      via: "useFeedbackAnalytics hook"
      pattern: "useFeedbackAnalytics"
    - from: "fe/src/features/dashboard/components/TopFlaggedSessionsCard.tsx"
      to: "fe/src/features/dashboard/api/dashboardQueries.ts"
      via: "useFeedbackSourceBreakdown hook providing topFlagged data"
      pattern: "topFlagged"
---

<objective>
Enhance Dashboard "Response Quality" section to render all 4 stat cards per D-03: satisfaction percentage, negative feedback list, source breakdown, and top flagged sessions. Also add source column to negative feedback table.

Purpose: Per D-01, D-03 -- the Dashboard must have a complete "Response Quality" section. The existing FeedbackSummaryCards handle satisfaction and negative feedback. This plan adds the missing source breakdown display and the top flagged sessions card, and ensures agent source data flows through.

Output: Complete Dashboard Response Quality section with all 4 D-03 stat cards, plus source column on negative feedback table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-assistant-response-evaluation-with-thumb-up-down-on-chat-and-search-admin-histories-page-in-agent-studio/05-CONTEXT.md
@.planning/phases/05-assistant-response-evaluation-with-thumb-up-down-on-chat-and-search-admin-histories-page-in-agent-studio/05-01-SUMMARY.md

<interfaces>
<!-- Existing dashboard components -->
From fe/src/features/dashboard/components/FeedbackSummaryCards.tsx:
- Takes `data: FeedbackAnalytics | null`
- Shows stat cards: satisfaction rate, total feedback, zero-result rate, worst dataset
- Already works with backend getFeedbackAnalytics() which queries all sources

From fe/src/features/dashboard/types/dashboard.types.ts:
```typescript
interface FeedbackAnalytics {
  satisfactionRate: number
  totalFeedback: number
  zeroResultRate: number
  worstDatasets: { datasetId: string; name: string; satisfactionRate: number; feedbackCount: number }[]
  trend: { date: string; count: number; satisfactionRate: number }[]
  negativeFeedback: { id: string; query: string; answerPreview: string; traceId: string | null; langfuseUrl: string | null; createdAt: string }[]
}
```

From be/src/modules/dashboard/dashboard.service.ts:
- getFeedbackAnalytics() already queries all answer_feedback records (no source filter)
- Agent source data will be included automatically after migration
- New getFeedbackSourceBreakdown() method added in Plan 01

From Plan 01 BE endpoints:
- GET /api/feedback/stats -> { sourceBreakdown: { chat, search, agent }, topFlagged: Array<{ source, source_id, negative_count, total_count }> }
- topFlagged is produced by AnswerFeedbackModel.getTopFlaggedSessions() created in Plan 01 Task 1
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance Dashboard feedback components for agent source visibility + source breakdown card</name>
  <files>
    fe/src/features/dashboard/components/FeedbackSummaryCards.tsx,
    fe/src/features/dashboard/components/NegativeFeedbackTable.tsx,
    fe/src/features/dashboard/types/dashboard.types.ts,
    fe/src/features/dashboard/api/dashboardApi.ts,
    fe/src/features/dashboard/api/dashboardQueries.ts,
    be/src/modules/dashboard/dashboard.service.ts
  </files>
  <read_first>
    fe/src/features/dashboard/components/FeedbackSummaryCards.tsx (full file),
    fe/src/features/dashboard/components/FeedbackTrendChart.tsx (full file),
    fe/src/features/dashboard/components/NegativeFeedbackTable.tsx (full file),
    fe/src/features/dashboard/types/dashboard.types.ts,
    fe/src/features/dashboard/api/dashboardApi.ts,
    fe/src/features/dashboard/api/dashboardQueries.ts,
    fe/src/features/dashboard/pages/AdminDashboardPage.tsx (to see how components are used),
    be/src/modules/dashboard/dashboard.service.ts
  </read_first>
  <action>
    Per D-01, D-03: Enhance existing dashboard feedback components to surface agent feedback data and source breakdown.

    1. Update `fe/src/features/dashboard/types/dashboard.types.ts`:
       - Add `sourceBreakdown?: { chat: number; search: number; agent: number }` to `FeedbackAnalytics` interface
       - Add `source?: 'chat' | 'search' | 'agent'` to the `negativeFeedback` array item type
       - Add new type: `TopFlaggedSession = { source: string; source_id: string; negative_count: number; total_count: number }`
       - Add new type: `FeedbackStatsResponse = { sourceBreakdown: { chat: number; search: number; agent: number }; topFlagged: TopFlaggedSession[] }`

    2. Update `fe/src/features/dashboard/api/dashboardApi.ts`:
       - Add function `fetchFeedbackStats(startDate?, endDate?)` calling `GET /api/feedback/stats` (from Plan 01)
       - Return type: `FeedbackStatsResponse`

    3. Update `fe/src/features/dashboard/api/dashboardQueries.ts`:
       - Add `useFeedbackStats(startDate?, endDate?)` query hook wrapping `fetchFeedbackStats`

    4. Update `FeedbackSummaryCards.tsx`:
       - Keep existing stat cards as-is (satisfaction rate, total feedback already include all sources)
       - Add a source breakdown display: a card or row showing "By source: Chat {N} | Search {N} | Agent {N}"
       - Use small colored badges: blue for chat, green for search, purple for agent
       - Accept `sourceBreakdown` as an additional prop (from the new useFeedbackStats hook)
       - If sourceBreakdown is not yet loaded, show skeleton placeholders

    5. Update `NegativeFeedbackTable.tsx`:
       - Add a "Source" column to the table showing the source type (chat/search/agent)
       - Display as a Badge: `<Badge variant="outline">{source}</Badge>`
       - If the backend negativeFeedback response does not currently include source, update the backend query in `dashboard.service.ts` to add `source` to the SELECT in the negative feedback subquery: add `'source'` to the `.select(...)` list
       - Update the negativeFeedback mapping to include `source: r.source as string`

    6. Verify `FeedbackTrendChart.tsx`:
       - Read the component to check if it needs any changes
       - The trend data from getFeedbackAnalytics already aggregates all sources
       - If the chart does NOT show per-source breakdown, leave it as-is (fine for v1)
       - If it already has source breakdown, ensure 'agent' is included in the legend/colors
  </action>
  <verify>
    <automated>npm run build -w fe && npm run build -w be</automated>
  </verify>
  <done>Dashboard feedback components show agent source data, source breakdown card visible, negative feedback table has source column</done>
</task>

<task type="auto">
  <name>Task 2: Create TopFlaggedSessionsCard rendering top flagged sessions from /api/feedback/stats</name>
  <files>
    fe/src/features/dashboard/components/TopFlaggedSessionsCard.tsx,
    fe/src/features/dashboard/pages/AdminDashboardPage.tsx
  </files>
  <read_first>
    fe/src/features/dashboard/pages/AdminDashboardPage.tsx (full file, to see where to add the new card),
    fe/src/features/dashboard/components/FeedbackSummaryCards.tsx (for card styling patterns),
    fe/src/features/dashboard/api/dashboardQueries.ts (for useFeedbackStats hook from Task 1)
  </read_first>
  <action>
    Per D-03 stat card #4: "top flagged sessions". Backend data exists (getTopFlaggedSessions in Plan 01, exposed via GET /api/feedback/stats -> topFlagged). This task renders that data.

    1. Create `fe/src/features/dashboard/components/TopFlaggedSessionsCard.tsx`:
       ```typescript
       interface TopFlaggedSessionsCardProps {
         /** Top flagged sessions data from /api/feedback/stats */
         topFlagged: Array<{ source: string; source_id: string; negative_count: number; total_count: number }> | undefined
         /** Whether data is loading */
         isLoading?: boolean
       }
       ```

       Component:
       - Renders a Card (shadcn) with CardHeader title "Top Flagged Sessions" and CardContent
       - If isLoading: show 3 Skeleton rows
       - If topFlagged is empty or undefined: show "No flagged sessions" text-muted-foreground
       - Otherwise: render a list (max 5 items) of flagged sessions:
         - Each row shows: source Badge (colored: blue=chat, green=search, purple=agent), truncated source_id (first 8 chars), negative_count as red text, ratio `{negative_count}/{total_count}` in muted text
         - Rows ordered by negative_count desc (already sorted from backend)
       - Add JSDoc comment explaining the component

    2. Update `AdminDashboardPage.tsx`:
       - Import TopFlaggedSessionsCard
       - Use the `useFeedbackStats` hook (from Task 1) to get `{ sourceBreakdown, topFlagged }`
       - Render `<TopFlaggedSessionsCard topFlagged={data?.topFlagged} isLoading={isLoading} />` in the Response Quality section, alongside the existing FeedbackSummaryCards
       - Position: in the 2x2 grid of stat cards per D-03. The 4 cards should be:
         (1) satisfaction % (existing in FeedbackSummaryCards)
         (2) negative feedback list (existing NegativeFeedbackTable)
         (3) source breakdown (added in Task 1 to FeedbackSummaryCards)
         (4) top flagged sessions (this new card)
       - Pass sourceBreakdown to FeedbackSummaryCards as well (from Task 1)

    3. Add i18n keys if needed:
       - dashboard.topFlaggedSessions (en: "Top Flagged Sessions", vi: "Phien bi danh dau nhieu nhat", ja: "フラグ付きセッション")
       - dashboard.noFlaggedSessions (en: "No flagged sessions", vi: "Khong co phien bi danh dau", ja: "フラグ付きセッションなし")
       (Use proper Vietnamese diacritics -- read existing locale files for style)
  </action>
  <verify>
    <automated>npm run build -w fe</automated>
  </verify>
  <done>TopFlaggedSessionsCard renders top flagged sessions from /api/feedback/stats topFlagged data, integrated into AdminDashboardPage Response Quality section, all 4 D-03 stat cards present</done>
</task>

</tasks>

<verification>
- `npm run build -w fe` -- TypeScript compiles without errors
- `npm run build -w be` -- Backend compiles without errors
- `npm run test:run -w fe` -- all FE tests pass
</verification>

<success_criteria>
- Dashboard FeedbackSummaryCards shows source breakdown (chat/search/agent) -- D-03 card #3
- NegativeFeedbackTable has source column -- D-03 card #2 enhanced
- TopFlaggedSessionsCard renders top flagged sessions list -- D-03 card #4
- All 4 D-03 stat cards are visible in the Response Quality section
- Existing dashboard functionality preserved
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-assistant-response-evaluation-with-thumb-up-down-on-chat-and-search-admin-histories-page-in-agent-studio/05-04-SUMMARY.md`
</output>
