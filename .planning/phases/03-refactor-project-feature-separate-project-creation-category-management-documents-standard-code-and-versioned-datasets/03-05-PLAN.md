---
phase: 03-refactor-project-feature
plan: 05
type: execute
wave: 4
depends_on: [03-04]
files_modified:
  - fe/src/features/projects/components/StandardCategoryView.tsx
  - fe/src/features/projects/components/CodeCategoryView.tsx
  - fe/src/features/projects/components/VersionList.tsx
  - fe/src/features/projects/components/VersionCard.tsx
  - fe/src/i18n/locales/en.json
  - fe/src/i18n/locales/vi.json
  - fe/src/i18n/locales/ja.json
  - fe/src/features/projects/index.ts
autonomous: false
requirements: [D-06, D-07, D-08, D-09]

must_haves:
  truths:
    - "Standard category shows single-dataset file list via DocumentListPanel"
    - "Code category shows single-dataset file list with language badges"
    - "Documents category shows version list with version cards"
    - "All UI strings exist in all 3 locale files (en, vi, ja)"
    - "New Version button in Documents creates version that auto-creates dataset"
  artifacts:
    - path: "fe/src/features/projects/components/StandardCategoryView.tsx"
      provides: "Standard category content view"
      contains: "DocumentListPanel"
    - path: "fe/src/features/projects/components/CodeCategoryView.tsx"
      provides: "Code category content view with language badges"
      contains: "Code2"
    - path: "fe/src/features/projects/components/VersionList.tsx"
      provides: "Version card list for Documents categories"
      contains: "VersionCard"
    - path: "fe/src/features/projects/components/VersionCard.tsx"
      provides: "Individual version card with status badge"
      contains: "version_label"
    - path: "fe/src/i18n/locales/en.json"
      provides: "English locale with all new project keys"
      contains: "projects.documentsTab"
  key_links:
    - from: "fe/src/features/projects/components/StandardCategoryView.tsx"
      to: "fe/src/features/projects/components/DocumentListPanel.tsx"
      via: "renders DocumentListPanel with category.dataset_id"
      pattern: "DocumentListPanel"
    - from: "fe/src/features/projects/components/VersionList.tsx"
      to: "fe/src/features/projects/api/projectApi.ts"
      via: "getCategoryVersions and createCategoryVersion"
      pattern: "getCategoryVersions|createCategoryVersion"
---

<objective>
Create Standard and Code category content views, extract VersionList/VersionCard for Documents tab, and add all i18n strings.

Purpose: Per D-06/D-07, Documents categories show a version list with version management. Per D-08/D-09, Code categories show a file list with language badges and optional git sync placeholder. Standard categories show a simple file list. All new UI strings must be in all 3 locale files (per RESEARCH.md Pitfall 6).
Output: StandardCategoryView, CodeCategoryView, VersionList, VersionCard components, updated i18n files, updated barrel exports
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-refactor-project-feature-separate-project-creation-category-management-documents-standard-code-and-versioned-datasets/03-CONTEXT.md
@.planning/phases/03-refactor-project-feature-separate-project-creation-category-management-documents-standard-code-and-versioned-datasets/03-UI-SPEC.md
@.planning/phases/03-refactor-project-feature-separate-project-creation-category-management-documents-standard-code-and-versioned-datasets/03-04-SUMMARY.md

<interfaces>
<!-- DocumentListPanel props (from existing component) -->
```typescript
// DocumentListPanel accepts a dataset_id or ragflow_dataset_id and shows file list
// Already handles file listing, upload status, and parsing progress
// Reuse as-is for Standard and Code category views
```

<!-- Version-related API functions from projectApi.ts -->
```typescript
export async function getCategoryVersions(projectId: string, categoryId: string): Promise<DocumentCategoryVersion[]>
export async function createCategoryVersion(projectId: string, categoryId: string, data: { version_label: string; metadata?: Record<string, unknown> }): Promise<DocumentCategoryVersion>
export async function deleteCategoryVersion(projectId: string, categoryId: string, versionId: string): Promise<void>
export async function archiveCategoryVersion(projectId: string, categoryId: string, versionId: string): Promise<DocumentCategoryVersion>
```

<!-- UI-SPEC Standard Category Content -->
Layout: Single dataset view, directly shows DocumentListPanel
Header: Category name + parser config summary + "Upload Files" button

<!-- UI-SPEC Code Category Content -->
Layout: Single dataset view + optional git sync panel
Header: Category name + detected languages badge list + "Upload Files" button
Git sync panel: Collapsible section (deferred to future phase per RESEARCH.md Open Question 1)

<!-- UI-SPEC Documents Category Content -->
Version list: Vertical list of version cards, newest first
Each card: version label, creation date, file count, parsing status badge
Active version: Click to expand inline, shows DocumentListPanel
New Version button: Secondary button at top
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StandardCategoryView, CodeCategoryView, VersionList, and VersionCard</name>
  <files>fe/src/features/projects/components/StandardCategoryView.tsx, fe/src/features/projects/components/CodeCategoryView.tsx, fe/src/features/projects/components/VersionList.tsx, fe/src/features/projects/components/VersionCard.tsx</files>
  <read_first>
    - fe/src/features/projects/components/DocumentListPanel.tsx (first 60 lines -- understand props interface)
    - fe/src/features/projects/components/DocumentsTab.tsx (lines 1-100 -- understand version rendering pattern)
    - fe/src/features/projects/api/projectApi.ts (getCategoryVersions, createCategoryVersion, DocumentCategoryVersion type)
    - fe/src/features/projects/components/VersionModal.tsx (understand version creation flow)
  </read_first>
  <action>
**Create fe/src/features/projects/components/StandardCategoryView.tsx:**

Props:
```typescript
interface StandardCategoryViewProps {
  projectId: string
  category: DocumentCategory
}
```

Implementation per UI-SPEC:
- Header: category name (heading), parser config summary badge, "Upload Files" button
- Body: Render `DocumentListPanel` passing `category.dataset_id` as the dataset reference
- If `category.dataset_id` is null (edge case), show error state "Dataset not found"
- All strings via `useTranslation()`

**Create fe/src/features/projects/components/CodeCategoryView.tsx:**

Props:
```typescript
interface CodeCategoryViewProps {
  projectId: string
  category: DocumentCategory
}
```

Implementation per UI-SPEC:
- Header: category name, language badges (auto-detected from file extensions if available), "Upload Files" button
- Body: Render `DocumentListPanel` passing `category.dataset_id`
- Git sync panel: Render a placeholder `Collapsible` section with "Git Sync (Coming Soon)" text. Per RESEARCH.md Open Question 1, git sync is deferred. Show a disabled "Connect Repository" button.
- Language badges: Use `Badge variant="outline"` with `Code2` icon. For now, show a static "Code" badge. Language detection from uploaded files will come with actual usage.
- All strings via `useTranslation()`

**Create fe/src/features/projects/components/VersionList.tsx:**

Props:
```typescript
interface VersionListProps {
  projectId: string
  categoryId: string
  versions: DocumentCategoryVersion[]
  onVersionCreated: () => void
  onVersionDeleted: () => void
}
```

Implementation per UI-SPEC:
- "New Version" button at top (secondary variant) that opens VersionModal
- Vertical list of VersionCard components, sorted newest first (by created_at descending)
- Click on a VersionCard expands it inline to show DocumentListPanel with version's ragflow_dataset_id
- Empty state: "No versions" heading with "Create the first version to upload documents." per copywriting contract
- All strings via `useTranslation()`

**Create fe/src/features/projects/components/VersionCard.tsx:**

Props:
```typescript
interface VersionCardProps {
  version: DocumentCategoryVersion
  isActive: boolean
  onClick: () => void
  onDelete: (id: string) => void
  onArchive: (id: string) => void
}
```

Implementation per UI-SPEC:
- Card layout: version label (e.g., "v3"), creation date formatted, file count (if available), parsing status badge
- Status badges per UI-SPEC: `parsing` = amber spinner, `ready` = green check, `error` = red x, `archived` = muted/gray
- Active state: expanded with DocumentListPanel below (controlled by parent VersionList)
- Context menu (DropdownMenu): Archive, Delete options
- All strings via `useTranslation()`
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge && ls -1 fe/src/features/projects/components/StandardCategoryView.tsx fe/src/features/projects/components/CodeCategoryView.tsx fe/src/features/projects/components/VersionList.tsx fe/src/features/projects/components/VersionCard.tsx 2>&1 | grep -c ".tsx"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files exist: StandardCategoryView.tsx, CodeCategoryView.tsx, VersionList.tsx, VersionCard.tsx
    - StandardCategoryView renders DocumentListPanel with category.dataset_id
    - CodeCategoryView renders DocumentListPanel with category.dataset_id and has Collapsible git sync placeholder
    - VersionList renders VersionCard components sorted newest first
    - VersionList has "New Version" button that opens VersionModal
    - VersionCard shows version_label, status badge, date
    - All 4 files use useTranslation() for user-visible strings
    - All 4 files have JSDoc on exported components
    - All 4 files support dark mode with dark: variants
  </acceptance_criteria>
  <done>Standard, Code, and Documents (version list) content views created</done>
</task>

<task type="auto">
  <name>Task 2: Add all i18n keys and update barrel exports</name>
  <files>fe/src/i18n/locales/en.json, fe/src/i18n/locales/vi.json, fe/src/i18n/locales/ja.json, fe/src/features/projects/index.ts</files>
  <read_first>
    - fe/src/i18n/locales/en.json (search for existing "projects" section to find insertion point)
    - fe/src/i18n/locales/vi.json (search for existing "projects" section)
    - fe/src/i18n/locales/ja.json (search for existing "projects" section)
    - fe/src/features/projects/index.ts (current barrel exports)
  </read_first>
  <action>
**Add i18n keys to all 3 locale files.** The following keys must be added inside the "projects" namespace. Add them after existing project keys.

**en.json additions:**
```json
{
  "projects": {
    "documentsTab": "Documents",
    "standardTab": "Standard",
    "codeTab": "Code",
    "newCategory": "New Category",
    "newVersion": "New Version",
    "createProject": "Create Project",
    "emptyTitle": "No projects yet",
    "emptyDescription": "Create your first project to organize documents, code, and knowledge bases.",
    "emptyCategoryTitle": "No categories",
    "emptyCategoryDescription": "Create a category to start adding files.",
    "emptyVersionTitle": "No versions",
    "emptyVersionDescription": "Create the first version to upload documents.",
    "loadError": "Failed to load project. Check your connection and try again.",
    "deleteConfirm": "This will permanently delete the project and all its categories, versions, and files. Type the project name to confirm.",
    "deleteCategoryConfirm": "This will delete the category and its dataset. This action cannot be undone.",
    "deleteVersionConfirm": "This will delete this version and its files. Other versions are not affected.",
    "settingsTitle": "Project Settings",
    "gitSyncTitle": "Git Sync",
    "gitSyncComingSoon": "Git repository sync coming soon",
    "connectRepository": "Connect Repository",
    "parserConfig": "Parser Config",
    "uploadFiles": "Upload Files",
    "versionCount": "{{count}} version(s)",
    "categoryCount": "{{count}} category(ies)"
  }
}
```

**vi.json additions (Vietnamese):**
```json
{
  "projects": {
    "documentsTab": "Tài liệu",
    "standardTab": "Tiêu chuẩn",
    "codeTab": "Mã nguồn",
    "newCategory": "Danh mục mới",
    "newVersion": "Phiên bản mới",
    "createProject": "Tạo dự án",
    "emptyTitle": "Chưa có dự án",
    "emptyDescription": "Tạo dự án đầu tiên để tổ chức tài liệu, mã nguồn và cơ sở tri thức.",
    "emptyCategoryTitle": "Chưa có danh mục",
    "emptyCategoryDescription": "Tạo danh mục để bắt đầu thêm tệp.",
    "emptyVersionTitle": "Chưa có phiên bản",
    "emptyVersionDescription": "Tạo phiên bản đầu tiên để tải lên tài liệu.",
    "loadError": "Không thể tải dự án. Kiểm tra kết nối và thử lại.",
    "deleteConfirm": "Thao tác này sẽ xóa vĩnh viễn dự án và tất cả danh mục, phiên bản, tệp. Nhập tên dự án để xác nhận.",
    "deleteCategoryConfirm": "Thao tác này sẽ xóa danh mục và tập dữ liệu. Không thể hoàn tác.",
    "deleteVersionConfirm": "Thao tác này sẽ xóa phiên bản và các tệp. Các phiên bản khác không bị ảnh hưởng.",
    "settingsTitle": "Cài đặt dự án",
    "gitSyncTitle": "Đồng bộ Git",
    "gitSyncComingSoon": "Đồng bộ kho Git sẽ có sớm",
    "connectRepository": "Kết nối kho",
    "parserConfig": "Cấu hình phân tích",
    "uploadFiles": "Tải lên tệp",
    "versionCount": "{{count}} phiên bản",
    "categoryCount": "{{count}} danh mục"
  }
}
```

**ja.json additions (Japanese):**
```json
{
  "projects": {
    "documentsTab": "ドキュメント",
    "standardTab": "スタンダード",
    "codeTab": "コード",
    "newCategory": "新しいカテゴリ",
    "newVersion": "新しいバージョン",
    "createProject": "プロジェクト作成",
    "emptyTitle": "プロジェクトがありません",
    "emptyDescription": "最初のプロジェクトを作成して、ドキュメント、コード、ナレッジベースを整理しましょう。",
    "emptyCategoryTitle": "カテゴリがありません",
    "emptyCategoryDescription": "カテゴリを作成してファイルの追加を開始します。",
    "emptyVersionTitle": "バージョンがありません",
    "emptyVersionDescription": "最初のバージョンを作成してドキュメントをアップロードします。",
    "loadError": "プロジェクトの読み込みに失敗しました。接続を確認してもう一度お試しください。",
    "deleteConfirm": "プロジェクトとすべてのカテゴリ、バージョン、ファイルが完全に削除されます。確認のためプロジェクト名を入力してください。",
    "deleteCategoryConfirm": "カテゴリとデータセットが削除されます。この操作は元に戻せません。",
    "deleteVersionConfirm": "このバージョンとファイルが削除されます。他のバージョンには影響しません。",
    "settingsTitle": "プロジェクト設定",
    "gitSyncTitle": "Git同期",
    "gitSyncComingSoon": "Gitリポジトリ同期は近日公開",
    "connectRepository": "リポジトリ接続",
    "parserConfig": "パーサー設定",
    "uploadFiles": "ファイルアップロード",
    "versionCount": "{{count}}バージョン",
    "categoryCount": "{{count}}カテゴリ"
  }
}
```

These keys must be MERGED into the existing "projects" namespace in each file, not replacing existing keys.

**Update fe/src/features/projects/index.ts barrel exports:**

Add exports for all new components:
```typescript
export { default as CategorySidebar } from './components/CategorySidebar'
export { default as ProjectSettingsSheet } from './components/ProjectSettingsSheet'
export { default as StandardCategoryView } from './components/StandardCategoryView'
export { default as CodeCategoryView } from './components/CodeCategoryView'
export { default as VersionList } from './components/VersionList'
export { default as VersionCard } from './components/VersionCard'
export type { DocumentCategoryType } from './api/projectApi'
```
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge && for key in documentsTab standardTab codeTab newCategory newVersion emptyTitle; do for locale in en vi ja; do grep -l "\"$key\"" fe/src/i18n/locales/$locale.json > /dev/null 2>&1 && echo "$locale:$key:OK" || echo "$locale:$key:MISSING"; done; done</automated>
  </verify>
  <acceptance_criteria>
    - en.json contains all 17 new keys within "projects" namespace
    - vi.json contains all 17 new keys within "projects" namespace (Vietnamese translations)
    - ja.json contains all 17 new keys within "projects" namespace (Japanese translations)
    - All 3 locale files have key parity (same set of keys)
    - fe/src/features/projects/index.ts exports CategorySidebar, ProjectSettingsSheet, StandardCategoryView, CodeCategoryView, VersionList, VersionCard
    - fe/src/features/projects/index.ts exports DocumentCategoryType type
    - No raw i18n key strings visible in any of the new components (all use t() function)
  </acceptance_criteria>
  <done>All i18n keys present in 3 locales, barrel exports updated with new components</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify full project feature refactor end-to-end</name>
  <files>fe/src/features/projects/pages/ProjectDetailPage.tsx</files>
  <action>Human verifies the complete project feature refactor by testing the full flow in the running application.</action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge && npm run build -w fe 2>&1 | tail -5</automated>
  </verify>
  <done>Full end-to-end flow works: create project, navigate, create categories of each type, manage content</done>
  <what-built>Complete project feature refactor: 3 category tabs (Documents, Standard, Code), category sidebar, settings sheet, simplified list page, type-discriminated category creation, all i18n strings</what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Navigate to /projects -- verify card grid with "Create Project" button only (no edit/delete on cards)
    3. Create a new project (should be single-step form, no category picker)
    4. Click the project card to navigate to detail page
    5. Verify 3 tabs visible: Documents, Standard, Code with icons and count badges
    6. Verify gear icon in header opens settings Sheet from right
    7. In Documents tab: click "New Category" in sidebar, create a category, verify it appears in sidebar
    8. Create a version in the Documents category, verify version card appears with status badge
    9. Switch to Standard tab: create a category, verify single dataset view loads
    10. Switch to Code tab: create a category, verify code view loads with git sync placeholder
    11. Verify tab and category persist in URL params (?tab=standard&category=xxx)
    12. Switch language to Vietnamese/Japanese, verify all strings translated
    13. Test dark mode toggle -- all components should render correctly
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- StandardCategoryView renders DocumentListPanel for standard categories
- CodeCategoryView renders DocumentListPanel with git sync placeholder
- VersionList renders VersionCards sorted newest first
- All i18n keys present in en.json, vi.json, ja.json with parity
- Barrel exports include all new components
- Visual verification confirms full flow works end-to-end
</verification>

<success_criteria>
- Standard category shows file list via DocumentListPanel
- Code category shows file list with language badges and git sync placeholder
- Documents category shows version list with expandable version cards
- All UI strings translated in 3 locales
- Full end-to-end flow works: create project -> navigate -> create categories of each type -> manage content
</success_criteria>

<output>
After completion, create `.planning/phases/03-refactor-project-feature-separate-project-creation-category-management-documents-standard-code-and-versioned-datasets/03-05-SUMMARY.md`
</output>
