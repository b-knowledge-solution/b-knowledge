---
phase: 04-domain-specific-parsers
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - advance-rag/rag/app/openapi.py
  - advance-rag/rag/app/adr.py
  - advance-rag/tests/test_openapi_parser.py
  - advance-rag/tests/test_adr_parser.py
  - advance-rag/tests/fixtures/sample_openapi3.yaml
  - advance-rag/tests/fixtures/sample_swagger2.json
  - advance-rag/tests/fixtures/sample_adr_madr.md
  - advance-rag/tests/fixtures/sample_adr_nygard.md
  - advance-rag/tests/fixtures/sample_adr_ystatement.md
autonomous: true
requirements:
  - PRSR-02
  - PRSR-03

must_haves:
  truths:
    - "An OpenAPI 3.x YAML spec is parsed into one chunk per endpoint with path, method, summary, parameters, request body, responses, and resolved schemas"
    - "A Swagger 2.0 JSON spec is parsed into per-endpoint chunks (converted to 3.x internally)"
    - "Each endpoint chunk has metadata: path, method, operation_id, tags, summary, security"
    - "An ADR in MADR format is parsed into per-section chunks with section_type metadata"
    - "An ADR in Nygard format is parsed into per-section chunks"
    - "A Y-statement ADR is detected and parsed into context/decision/consequences sections"
    - "ADR metadata includes status, title, date, superseded_by"
    - "Both YAML and JSON input formats are handled for OpenAPI specs"
  artifacts:
    - path: "advance-rag/rag/app/openapi.py"
      provides: "OpenAPI/Swagger endpoint parser"
      exports: ["chunk"]
    - path: "advance-rag/rag/app/adr.py"
      provides: "ADR template-aware section parser"
      exports: ["chunk"]
    - path: "advance-rag/tests/test_openapi_parser.py"
      provides: "Unit tests for OpenAPI parser"
    - path: "advance-rag/tests/test_adr_parser.py"
      provides: "Unit tests for ADR parser"
  key_links:
    - from: "advance-rag/rag/app/openapi.py"
      to: "rag.nlp.rag_tokenizer"
      via: "chunk tokenization"
      pattern: "rag_tokenizer\\.tokenize"
    - from: "advance-rag/rag/app/adr.py"
      to: "rag.nlp.rag_tokenizer"
      via: "chunk tokenization"
      pattern: "rag_tokenizer\\.tokenize"
---

<objective>
Implement the OpenAPI/Swagger spec parser (PRSR-02) and ADR template-aware parser (PRSR-03), replacing their stub modules from Plan 01.

Purpose: These two parsers are pure Python implementations with no LLM dependency. The OpenAPI parser uses prance for ref resolution and produces one chunk per endpoint. The ADR parser uses regex heuristics to detect MADR, Nygard, and Y-statement formats and chunks by section.

Output: Two working parser modules with tests passing.
</objective>

<execution_context>
@/home/doana/.claude/get-shit-done/workflows/execute-plan.md
@/home/doana/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-domain-specific-parsers/04-CONTEXT.md
@.planning/phases/04-domain-specific-parsers/04-RESEARCH.md
@.planning/phases/04-domain-specific-parsers/04-01-SUMMARY.md

@advance-rag/CLAUDE.md
@advance-rag/rag/app/tag.py

<interfaces>
<!-- Parser contract (from Plan 01) -->
```python
def chunk(filename, binary=None, from_page=0, to_page=100000,
          lang="Chinese", callback=None, **kwargs):
    # kwargs: parser_config, kb_id, tenant_id
    # Returns: list[dict] with content_with_weight, content_ltks, content_sm_ltks, docnm_kwd, title_tks
```

<!-- Tokenization pattern (from tag.py) -->
```python
from rag.nlp import rag_tokenizer
d["content_with_weight"] = chunk_text
d["content_ltks"] = rag_tokenizer.tokenize(chunk_text)
d["content_sm_ltks"] = rag_tokenizer.fine_grained_tokenize(d["content_ltks"])
d["docnm_kwd"] = filename
d["title_tks"] = rag_tokenizer.tokenize(re.sub(r"\.[a-zA-Z]+$", "", filename))
```

<!-- ADR section patterns (from RESEARCH.md) -->
```python
ADR_SECTION_PATTERNS = {
    "context": re.compile(r"^#+\s*(context|background|problem|motivation|issue|forces)", re.IGNORECASE),
    "decision": re.compile(r"^#+\s*(decision|decided|decision\s+outcome|chosen\s+option|resolution)", re.IGNORECASE),
    "consequences": re.compile(r"^#+\s*(consequences|implications|results?|outcome|pros?\s+and\s+cons?)", re.IGNORECASE),
    "status": re.compile(r"^#+\s*(status|state)", re.IGNORECASE),
    "options": re.compile(r"^#+\s*(options?\s*considered|alternatives?|considered\s+options?)", re.IGNORECASE),
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement OpenAPI/Swagger endpoint parser (PRSR-02)</name>
  <files>
    advance-rag/rag/app/openapi.py,
    advance-rag/tests/test_openapi_parser.py,
    advance-rag/tests/fixtures/sample_openapi3.yaml,
    advance-rag/tests/fixtures/sample_swagger2.json
  </files>
  <read_first>
    advance-rag/rag/app/tag.py,
    advance-rag/rag/app/openapi.py,
    .planning/phases/04-domain-specific-parsers/04-RESEARCH.md
  </read_first>
  <behavior>
    - Test: OpenAPI 3.0 YAML with 3 endpoints produces 3 chunks (one per path+method)
    - Test: Each endpoint chunk contains path, method, summary, parameters, request body schema, response schemas
    - Test: $ref pointers are resolved and schemas inlined in chunk content
    - Test: Swagger 2.0 JSON is parsed successfully into per-endpoint chunks
    - Test: Chunk metadata includes path, method, operation_id, tags, summary, security
    - Test: Both YAML and JSON format specs are accepted
    - Test: Every chunk dict has required fields (content_with_weight, content_ltks, content_sm_ltks, docnm_kwd, title_tks)
    - Test: Invalid/malformed spec produces empty list or graceful error, not crash
  </behavior>
  <action>
    1. Create test fixtures:
       - advance-rag/tests/fixtures/sample_openapi3.yaml: OpenAPI 3.0.3 spec with:
         - info block (title, version)
         - 3 endpoints: GET /users, POST /users, GET /users/{id}
         - $ref to a User schema in components/schemas
         - Parameters (query, path), request body (JSON), responses (200, 400, 404)
         - Tags, operationId, security requirement on one endpoint
       - advance-rag/tests/fixtures/sample_swagger2.json: Swagger 2.0 spec with:
         - 2 endpoints: GET /items, POST /items
         - definitions with Item schema
         - $ref usage

    2. Create advance-rag/tests/test_openapi_parser.py with tests for all behaviors above.

    3. Implement advance-rag/rag/app/openapi.py (replace stub):
       - _parse_spec(binary: bytes, filename: str) -> dict:
         Detect format (YAML vs JSON) from extension or content.
         Use prance.ResolvingParser with spec_string, backend="openapi-spec-validator", strict=False.
         Handle Swagger 2.0 by detecting "swagger" key — prance resolves refs in both formats.
         Return resolved spec dict.

       - _endpoint_to_chunk_text(path: str, method: str, operation: dict) -> str:
         Build human-readable text:
         "{METHOD} {path}\n\nSummary: {summary}\n\nParameters:\n{params}\n\nRequest Body:\n{body_schema}\n\nResponses:\n{responses}"
         Include full resolved schemas inline. For response schemas, include status code + description + schema.

       - _extract_endpoint_metadata(path, method, operation) -> dict:
         Return dict with: path, http_method, operation_id, tags (list), summary, security (list).

       - chunk(filename, binary=None, from_page=0, to_page=100000, lang="Chinese", callback=None, **kwargs):
         Parse spec via _parse_spec. Iterate paths -> methods -> operations.
         For each endpoint, build chunk dict:
           content_with_weight = _endpoint_to_chunk_text(path, method, op)
           content_ltks, content_sm_ltks = tokenize
           docnm_kwd = filename
           title_tks = tokenize(filename)
           tag_kwd = ["openapi", method.lower()] + operation tags
           Plus metadata fields from _extract_endpoint_metadata
         Call callback with progress. Return list.

       Handle errors gracefully: if prance fails to parse, log warning, return empty list.
       IMPORTANT: Include Google-style docstrings on all functions per CLAUDE.md.
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge && source .venv/bin/activate && python -m pytest advance-rag/tests/test_openapi_parser.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'def chunk(' advance-rag/rag/app/openapi.py
    - grep -q 'prance' advance-rag/rag/app/openapi.py
    - grep -q 'content_with_weight' advance-rag/rag/app/openapi.py
    - grep -q 'content_ltks' advance-rag/rag/app/openapi.py
    - grep -q 'operation_id' advance-rag/rag/app/openapi.py
    - grep -q 'rag_tokenizer' advance-rag/rag/app/openapi.py
    - test -f advance-rag/tests/fixtures/sample_openapi3.yaml
    - test -f advance-rag/tests/fixtures/sample_swagger2.json
    - All tests in test_openapi_parser.py pass
  </acceptance_criteria>
  <done>
    OpenAPI parser produces one chunk per endpoint with resolved schemas inlined. Swagger 2.0 and OpenAPI 3.x both supported. Endpoint metadata (path, method, operation_id, tags, security) stored on chunks. All tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ADR template-aware section parser (PRSR-03)</name>
  <files>
    advance-rag/rag/app/adr.py,
    advance-rag/tests/test_adr_parser.py,
    advance-rag/tests/fixtures/sample_adr_madr.md,
    advance-rag/tests/fixtures/sample_adr_nygard.md,
    advance-rag/tests/fixtures/sample_adr_ystatement.md
  </files>
  <read_first>
    advance-rag/rag/app/tag.py,
    advance-rag/rag/app/adr.py,
    .planning/phases/04-domain-specific-parsers/04-RESEARCH.md
  </read_first>
  <behavior>
    - Test: MADR-format ADR produces chunks for: title, status, context, decision, consequences sections
    - Test: Nygard-format ADR produces per-section chunks with section_type metadata
    - Test: Y-statement format ADR is detected and parsed into context/decision/consequences
    - Test: Each chunk has section_type metadata (context, decision, consequences, status, options)
    - Test: ADR metadata includes: adr_status (accepted/deprecated/superseded), title, date, superseded_by
    - Test: Heading variations are handled ("Decision" vs "Decision Outcome" vs "Decided")
    - Test: Every chunk dict has required fields (content_with_weight, content_ltks, content_sm_ltks, docnm_kwd, title_tks)
    - Test: Plain markdown without ADR structure produces a single chunk (graceful fallback)
  </behavior>
  <action>
    1. Create test fixtures:
       - advance-rag/tests/fixtures/sample_adr_madr.md: MADR format with:
         # ADR-001: Use PostgreSQL for primary storage
         ## Status: Accepted
         ## Context and Problem Statement (2-3 paragraphs)
         ## Decision Outcome: Chosen option (2 paragraphs)
         ## Consequences: Good/bad consequences
         ## Pros and Cons of the Options: 2 options listed

       - advance-rag/tests/fixtures/sample_adr_nygard.md: Nygard format with:
         # 1. Record architecture decisions
         Date: 2024-01-15
         ## Status: Accepted
         ## Context (2 paragraphs)
         ## Decision (2 paragraphs)
         ## Consequences (2 paragraphs)

       - advance-rag/tests/fixtures/sample_adr_ystatement.md: Y-statement with:
         # ADR-003: API Authentication
         In the context of securing our API, facing multiple authentication options,
         we decided to use JWT tokens, to achieve stateless authentication...
         Plus some additional sections.

    2. Create advance-rag/tests/test_adr_parser.py with tests for all behaviors above.

    3. Implement advance-rag/rag/app/adr.py (replace stub):

       - ADR_SECTION_PATTERNS dict: regex patterns mapping section keywords to section_type.
         context: context|background|problem|motivation|issue|forces
         decision: decision|decided|decision\s+outcome|chosen\s+option|resolution
         consequences: consequences|implications|results?|outcome|pros?\s+and\s+cons?
         status: status|state
         options: options?\s*considered|alternatives?|considered\s+options?

       - Y_STATEMENT_PATTERN: regex for "In the context of ... facing ... we decided ... to achieve ..."

       - _extract_adr_metadata(text: str) -> dict:
         Scan for Status line (regex: r"^#+\s*Status[:\s]+(.+)" or r"^Status[:\s]+(.+)").
         Extract: adr_status, title (from first H1), date (regex for date patterns), superseded_by.

       - _detect_format(text: str) -> str:
         Check if Y-statement pattern matches -> "y_statement"
         Check if MADR-style headings (## Context and Problem Statement) -> "madr"
         Check if Nygard-style headings (## Context, ## Decision) -> "nygard"
         Default: "unknown"

       - _split_by_sections(text: str) -> list[tuple[str, str, str]]:
         Split markdown by headings. For each heading, match against ADR_SECTION_PATTERNS.
         Return list of (section_type, heading_text, section_content).
         Unmatched headings get section_type="other".

       - _parse_y_statement(text: str) -> list[tuple[str, str, str]]:
         Extract context, decision, consequences from Y-statement pattern.
         Return synthetic sections.

       - chunk(filename, binary=None, from_page=0, to_page=100000, lang="Chinese", callback=None, **kwargs):
         Decode binary to text. Extract metadata via _extract_adr_metadata.
         Detect format. Split by sections (or parse Y-statement).
         For each section, build chunk dict:
           content_with_weight = section_content
           content_ltks, content_sm_ltks = tokenize
           docnm_kwd = filename
           title_tks = tokenize(filename)
           section_type = matched section type
           adr_status = from metadata
           adr_title = from metadata
           adr_date = from metadata
           tag_kwd = ["adr", section_type]
         If no sections detected (not an ADR), return single chunk with full content.
         Call callback with progress. Return list.

       IMPORTANT: Include Google-style docstrings on all functions per CLAUDE.md.
       IMPORTANT: Be forgiving with heading detection — use re.IGNORECASE and partial matches.
  </action>
  <verify>
    <automated>cd /mnt/d/Project/b-solution/b-knowledge && source .venv/bin/activate && python -m pytest advance-rag/tests/test_adr_parser.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'def chunk(' advance-rag/rag/app/adr.py
    - grep -q 'ADR_SECTION_PATTERNS' advance-rag/rag/app/adr.py
    - grep -q 'section_type' advance-rag/rag/app/adr.py
    - grep -q 'adr_status' advance-rag/rag/app/adr.py
    - grep -q 'Y_STATEMENT_PATTERN' advance-rag/rag/app/adr.py
    - grep -q 'rag_tokenizer' advance-rag/rag/app/adr.py
    - grep -q 'content_with_weight' advance-rag/rag/app/adr.py
    - test -f advance-rag/tests/fixtures/sample_adr_madr.md
    - test -f advance-rag/tests/fixtures/sample_adr_nygard.md
    - test -f advance-rag/tests/fixtures/sample_adr_ystatement.md
    - All tests in test_adr_parser.py pass
  </acceptance_criteria>
  <done>
    ADR parser detects MADR, Nygard, and Y-statement formats. Each section becomes a chunk with section_type metadata. ADR metadata (status, title, date, superseded_by) extracted. Heading variations handled with fuzzy matching. All tests pass.
  </done>
</task>

</tasks>

<verification>
1. `python -m pytest advance-rag/tests/test_openapi_parser.py advance-rag/tests/test_adr_parser.py -x -v` passes all tests
2. openapi.py resolves $ref pointers and produces per-endpoint chunks
3. adr.py detects all three ADR template formats
4. Both parsers produce chunks with all required tokenization fields
</verification>

<success_criteria>
- All OpenAPI parser tests pass — 3 endpoints from sample spec produce 3 chunks with metadata
- All ADR parser tests pass — MADR, Nygard, Y-statement formats detected and chunked by section
- Both parsers follow the chunk() contract and produce dicts with content_with_weight, content_ltks, content_sm_ltks, docnm_kwd, title_tks
</success_criteria>

<output>
After completion, create `.planning/phases/04-domain-specific-parsers/04-02-SUMMARY.md`
</output>
