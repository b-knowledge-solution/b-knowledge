---
phase: 04-fe-catalog-can-codemod
plan: 4.2
status: complete
completed: 2026-04-08
tasks_completed: 2
tasks_total: 2
commits:
  - 170f5c6 refactor(permissions): align FE Subjects union with BE (drop Project, add KnowledgeBase/Agent/Memory/DocumentCategory)
  - f3f8c37 docs(4-4.2): add <Can> vs useHasPermission decision tree to fe/CLAUDE.md
files_modified:
  - fe/src/lib/ability.tsx (+21/-2)
  - fe/CLAUDE.md (+54/-0)
requirements_addressed: [TS10, TS11]
decisions_satisfied: [D-01, D-03]
---

# Phase 4 Plan 4.2: Atomic Subjects Swap + `<Can>` Decision Tree — Summary

Atomic single-commit migration of the FE CASL `Subjects` union to match the BE entity model, plus a documented decision tree in `fe/CLAUDE.md` for when to use `<Can>` vs `useHasPermission`.

## Tasks Completed

### Task 1: Atomic Subjects union swap (`fe/src/lib/ability.tsx`)
- Dropped legacy `'Project'`.
- Added `'KnowledgeBase'`, `'Agent'`, `'Memory'`, `'DocumentCategory'`.
- Reformatted union as multi-line; added Phase 4 D-03 JSDoc rationale.
- All other entries (`Dataset`, `Document`, `ChatAssistant`, `SearchApp`, `User`, `AuditLog`, `Policy`, `Org`, `all`) preserved.
- **Commit:** `170f5c6` — single atomic commit per D-03.

### Task 2: `<Can>` vs `useHasPermission` decision tree (`fe/CLAUDE.md`)
- Appended new `## Permission Gating: <Can> vs useHasPermission (Phase 4+)` section before the existing `## Documentation Comments` section.
- Includes the rule statements, ASCII decision tree, three code examples (instance check, class check, flat capability check using real `PERMISSION_KEYS.DATASETS_CREATE`), four hard rules, and a "where the pieces live" file map.
- All four grep-verified literal phrases present.
- File grew from 178 → 232 lines (+54).
- **Commit:** `f3f8c37`.

## Verification

| Check | Result |
|-------|--------|
| `cd fe && npm run build` | exit 0 (built in 1m 39s) |
| `grep -c "'Project'" fe/src/lib/ability.tsx` | 0 |
| `grep -q "'KnowledgeBase'" ability.tsx` | OK |
| `grep -q "'Agent'" ability.tsx` | OK |
| `grep -q "'Memory'" ability.tsx` | OK |
| `grep -q "'DocumentCategory'" ability.tsx` | OK |
| `grep -q "## Permission Gating" fe/CLAUDE.md` | OK |
| `grep -q "Decision tree: <Can> vs useHasPermission"` | OK |
| `grep -q "isAdmin prop drilling is banned"` | OK |
| `grep -q "Bare string keys are banned in useHasPermission"` | OK |
| 2 new commits | confirmed (`170f5c6`, `f3f8c37`) |

## Blast Radius Confirmation

Research predicted **0 files** outside `ability.tsx` would need TypeScript fixes from the Subjects swap. Confirmed: `npm run build` (which runs `tsc` then Vite) completed cleanly with zero errors. No `<Can a="Project">` usages existed in `fe/src/`. The atomic swap was as cheap as research predicted.

## Notable Choice

Used the real `PERMISSION_KEYS.DATASETS_CREATE` (plural) in the decision tree example rather than a fictitious `DATASET_CREATE`, after verifying against `fe/src/constants/permission-keys.ts` (BE feature slug is `datasets`). This matches the executor heads-up note and avoids drift between docs and code.

## Deviations

None. Plan executed exactly as written.

## Self-Check: PASSED

- `fe/src/lib/ability.tsx` exists and contains the new union — verified via Edit success and grep.
- `fe/CLAUDE.md` exists and contains the new section — verified via grep of all four literal phrases.
- Both commits exist in `git log` — verified.
