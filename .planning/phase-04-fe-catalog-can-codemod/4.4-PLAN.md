---
phase: 04-fe-catalog-can-codemod
plan: 4.4
type: execute
wave: 2
depends_on: [4.1, 4.3]
files_modified:
  - fe/src/features/datasets/components/DocumentTable.tsx
  - fe/src/features/datasets/components/DatasetCard.tsx
  - fe/src/features/datasets/components/ConnectorListPanel.tsx
  - fe/src/features/datasets/pages/DatasetsPage.tsx
  - fe/src/features/datasets/pages/DatasetDetailPage.tsx
  - fe/src/features/glossary/pages/GlossaryPage.tsx
  - fe/src/features/glossary/components/TaskManagementTab.tsx
  - fe/src/features/glossary/components/KeywordManagementTab.tsx
  - fe/src/features/system/pages/SystemToolsPage.tsx
  - fe/src/features/knowledge-base/components/StandardTabRedesigned.tsx
requirements_addressed: [TS11]
autonomous: true
must_haves:
  truths:
    - "Every targeted page/component file no longer compares user.role to a string literal or accepts an isAdmin prop"
    - "FE still builds and tests still pass after the sweep"
  artifacts:
    - path: fe/src/features/datasets/components/DocumentTable.tsx
      provides: highest-concentration migration site (10 occurrences) — codemod-rewritten
  key_links:
    - from: migrated files
      to: fe/src/lib/permissions.tsx (useHasPermission)
      via: import statements inserted by codemod
    - from: migrated files
      to: fe/src/constants/permission-keys.ts (PERMISSION_KEYS)
      via: import statements inserted by codemod
---

<objective>
Run the Plan 4.3 codemod against pages and components — the meaty bulk of the sweep. Per D-02, every modified file gets its OWN commit so each diff is independently reviewable.

Scope (per RESEARCH inventory, MECH-classified files):
- `DocumentTable.tsx` (10 occurrences)
- `DatasetsPage.tsx` (5 occurrences)
- `DatasetDetailPage.tsx` (4 occurrences)
- `DatasetCard.tsx` (3 occurrences)
- `ConnectorListPanel.tsx` (5 occurrences)
- `GlossaryPage.tsx` (3 occurrences)
- `TaskManagementTab.tsx` (~9 occurrences)
- `KeywordManagementTab.tsx` (~9 occurrences)
- `SystemToolsPage.tsx` (1 occurrence)
- `StandardTabRedesigned.tsx` (1 occurrence — drop the `isAdmin` prop on `<ConnectorListPanel>`)

Out of scope (deferred to other plans or skip-listed): nav (`sidebarNav.ts`, `Sidebar.tsx`, `App.tsx NavRoleGuard`, `AdminRoute.tsx` → Plan 4.5), `teamQueries.ts`, `KnowledgeBaseMemberList.tsx`, `features/guideline/**` (all skip-listed in codemod).

Note: research shows `features/glossary/` is in the codemod skip list per CONTEXT — but `GlossaryPage.tsx`, `TaskManagementTab.tsx`, `KeywordManagementTab.tsx` are listed as MECH in RESEARCH. **Resolution:** the skip list in CONTEXT says `features/guideline/**` (not glossary). Glossary IS in scope. Re-verify by reading both files in `<read_first>` before starting.
</objective>

<context>
@.planning/phase-04-fe-catalog-can-codemod/4-CONTEXT.md
@.planning/phase-04-fe-catalog-can-codemod/4-RESEARCH.md
@fe/src/lib/permissions.tsx
@fe/src/constants/permission-keys.ts
@fe/scripts/codemod-permissions.mjs
@fe/scripts/codemod-permissions.lib.mjs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Codemod sweep — datasets feature (5 files, per-file commits)</name>
  <files>fe/src/features/datasets/components/DocumentTable.tsx, fe/src/features/datasets/components/DatasetCard.tsx, fe/src/features/datasets/components/ConnectorListPanel.tsx, fe/src/features/datasets/pages/DatasetsPage.tsx, fe/src/features/datasets/pages/DatasetDetailPage.tsx</files>
  <read_first>
    - .planning/phase-04-fe-catalog-can-codemod/4-RESEARCH.md ("Authoritative Codemod Inventory" rows 7-13 — exact line numbers and patterns)
    - .planning/phase-04-fe-catalog-can-codemod/4-CONTEXT.md ("Codemod Skip List" — confirm glossary IS in scope, only guideline is skipped)
    - fe/src/features/datasets/components/DocumentTable.tsx (the file with 10 occurrences — read fully before codemod runs)
    - fe/src/features/datasets/pages/DatasetsPage.tsx
    - fe/scripts/codemod-permissions.mjs (the CLI being invoked)
  </read_first>
  <action>
**STRICT FILE ORDER (do NOT reorder — this is W5 fix from plan-checker iteration 1):**

The codemod removes the `isAdmin` *prop usage* from `StandardTabRedesigned.tsx` AND removes the `isAdmin` *prop definition* from `ConnectorListPanel.tsx`. If `StandardTabRedesigned.tsx` is processed before `ConnectorListPanel.tsx` (or vice versa, depending on which side breaks first), there is a transient TypeScript error window between the two commits. To avoid this, **process `ConnectorListPanel.tsx` (the prop definition) BEFORE `StandardTabRedesigned.tsx` (the prop consumer in Plan 4.4 Task 2).**

Within Task 1 (datasets), run files in this exact order:
  1. `fe/src/features/datasets/components/DocumentTable.tsx` (10 sites — biggest, get the codemod stress-tested first)
  2. `fe/src/features/datasets/components/DatasetCard.tsx`
  3. `fe/src/features/datasets/components/ConnectorListPanel.tsx` (**MUST land before Task 2's StandardTabRedesigned.tsx** — its prop definition removal is the prerequisite)
  4. `fe/src/features/datasets/pages/DatasetsPage.tsx`
  5. `fe/src/features/datasets/pages/DatasetDetailPage.tsx`

For each file in this order:

1. **Dry-run first** to preview the diff:
   ```bash
   node fe/scripts/codemod-permissions.mjs --dry-run --files '<file>'
   ```
2. Eyeball the diff. If it looks wrong (ambiguity not flagged, wrong key chosen, etc.), STOP and fix the codemod lib. Do not patch the output by hand.
3. **Run for real:**
   ```bash
   node fe/scripts/codemod-permissions.mjs --files '<file>'
   ```
4. Run `cd fe &amp;&amp; npm run build` — if build fails, the heuristic mapping is wrong; fix the codemod lib (and its fixture tests) and re-run.
5. Run `cd fe &amp;&amp; npm run lint -- '<file>'` to confirm no new lint errors.
6. **Commit the single file** with message: `refactor(permissions): codemod migrate <filename> off isAdmin/role checks`
7. Move on to the next file.

After all 5 files:
- Run `cd fe &amp;&amp; npm run build` (full build)
- Run `cd fe &amp;&amp; npm run test:run` (full test suite)
- Verify `grep -r "isAdmin" fe/src/features/datasets/` returns zero matches
- Verify `grep -rE "user\\??.role ===" fe/src/features/datasets/` returns zero matches

If the codemod left a `// TODO(perm-codemod): review` in any datasets file, STOP and either (a) fix the heuristic mapping in `codemod-permissions.lib.mjs` and re-run, or (b) hand-edit only that line and explain in the commit message why automation couldn't handle it. Do NOT leave TODO markers in the final commits for this plan — those are for genuinely ambiguous cases that go to Plan 4.5 manual review.

Inline-comment any hand-edits with `// Manual fix per Plan 4.4 Task 1: <reason>`.
  </action>
  <verify>
    <automated>cd fe &amp;&amp; npm run build &amp;&amp; npm run test:run &amp;&amp; ! grep -rn "isAdmin" src/features/datasets/ --include='*.ts' --include='*.tsx' &amp;&amp; ! grep -rn "user\.role ===" src/features/datasets/ --include='*.ts' --include='*.tsx' &amp;&amp; ! grep -rn "user?\.role ===" src/features/datasets/ --include='*.ts' --include='*.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - All 5 datasets files no longer contain `isAdmin` (verify: `grep -r isAdmin fe/src/features/datasets/` empty)
    - All 5 datasets files no longer contain `user.role ===` or `user?.role ===` (grep empty)
    - Each migrated file imports `useHasPermission` from `@/lib/permissions` and `PERMISSION_KEYS` from `@/constants/permission-keys`
    - `cd fe &amp;&amp; npm run build` exits 0
    - `cd fe &amp;&amp; npm run test:run` exits 0
    - 5 separate git commits (one per file) — verify with `git log --oneline -10`
    - No `// TODO(perm-codemod):` markers remain in the 5 datasets files
  </acceptance_criteria>
  <done>Datasets feature is fully migrated and committed file-by-file.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Codemod sweep — glossary + system + knowledge-base shorthand (5 files, per-file commits)</name>
  <files>fe/src/features/glossary/pages/GlossaryPage.tsx, fe/src/features/glossary/components/TaskManagementTab.tsx, fe/src/features/glossary/components/KeywordManagementTab.tsx, fe/src/features/system/pages/SystemToolsPage.tsx, fe/src/features/knowledge-base/components/StandardTabRedesigned.tsx</files>
  <read_first>
    - .planning/phase-04-fe-catalog-can-codemod/4-RESEARCH.md ("Authoritative Codemod Inventory" rows 2-5, 14, 15)
    - .planning/phase-04-fe-catalog-can-codemod/4-CONTEXT.md (skip list: confirm `features/guideline/**` is skipped; `features/glossary/**` is NOT skipped)
    - fe/src/features/glossary/components/TaskManagementTab.tsx (~9 isAdmin sites)
    - fe/src/features/glossary/components/KeywordManagementTab.tsx (~9 isAdmin sites)
    - fe/src/features/knowledge-base/components/StandardTabRedesigned.tsx line 371 area (the shorthand `isAdmin` JSX prop)
  </read_first>
  <action>
Same per-file workflow as Task 1: dry-run, eyeball, run, build, test, lint, commit-one-file.

For each of the 5 files:
```bash
node fe/scripts/codemod-permissions.mjs --dry-run --files '<file>'
node fe/scripts/codemod-permissions.mjs --files '<file>'
cd fe &amp;&amp; npm run build &amp;&amp; npm run lint -- '<file>'
git add '<file>' &amp;&amp; git commit -m 'refactor(permissions): codemod migrate <filename> off isAdmin/role checks'
```

**Special case for `StandardTabRedesigned.tsx:371` — STRICT ORDERING (W5 fix):** This file's codemod run REMOVES the `isAdmin` prop from a `<ConnectorListPanel>` JSX usage. The prop definition on `ConnectorListPanel` itself is removed in **Task 1 step 3** above. **Task 2 MUST run after Task 1 step 3 has been committed.** Within Task 2, process `StandardTabRedesigned.tsx` LAST (after the four glossary/system files) to maximize the gap from any potentially-related Task 1 work.

If you find a transient `Property 'isAdmin' does not exist on type 'ConnectorListPanelProps'` error during Task 2, that means Task 1 step 3 didn't actually land — `git log` to verify and re-run Task 1 if needed.

**Special case for the glossary tab pair (TaskManagementTab + KeywordManagementTab) — PINNED KEYS (B2 follow-on):** The codemod's `FILE_KEY_MAP` (defined in Plan 4.3) already contains:
- `'fe/src/features/glossary/components/TaskManagementTab.tsx'` → `'GLOSSARY_EDIT'`
- `'fe/src/features/glossary/components/KeywordManagementTab.tsx'` → `'GLOSSARY_EDIT'`
- `'fe/src/features/glossary/pages/GlossaryPage.tsx'` → `'GLOSSARY_VIEW'`

Both `glossary.view` and `glossary.edit` are verified to exist in `be/src/modules/glossary/glossary.permissions.ts`. **Do NOT substitute another key.** If `PERMISSION_KEYS.GLOSSARY_VIEW` or `PERMISSION_KEYS.GLOSSARY_EDIT` is absent from `fe/src/constants/permission-keys.ts` after Plan 4.1 ran, that's a Plan 4.1 prerequisite failure — STOP and surface to user, do NOT run the codemod against these files until the snapshot is correct.

After all 5 files:
- `cd fe &amp;&amp; npm run build` exits 0
- `cd fe &amp;&amp; npm run test:run` exits 0
- `grep -r "isAdmin" fe/src/features/glossary/ fe/src/features/system/ fe/src/features/knowledge-base/components/StandardTabRedesigned.tsx` returns zero
  </action>
  <verify>
    <automated>cd fe &amp;&amp; npm run build &amp;&amp; npm run test:run &amp;&amp; ! grep -rn "isAdmin" src/features/glossary/ src/features/system/pages/SystemToolsPage.tsx src/features/knowledge-base/components/StandardTabRedesigned.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files no longer contain `isAdmin` or `user.role === '<literal>'` patterns
    - Each migrated file imports `useHasPermission` and `PERMISSION_KEYS`
    - `cd fe &amp;&amp; npm run build` exits 0
    - `cd fe &amp;&amp; npm run test:run` exits 0
    - 5 separate git commits, one per file
    - No `// TODO(perm-codemod):` markers remain in these 5 files (or, if any do, they are surfaced in the plan summary as items needing manual resolution)
  </acceptance_criteria>
  <done>All page/component migrations are committed and the FE is green.</done>
</task>

</tasks>

<verification>
- `grep -rn "isAdmin" fe/src/features/datasets/ fe/src/features/glossary/ fe/src/features/system/ fe/src/features/knowledge-base/components/StandardTabRedesigned.tsx --include='*.ts' --include='*.tsx'` returns zero matches
- `grep -rn "user\.role ===" fe/src/features/datasets/ fe/src/features/glossary/ fe/src/features/system/ --include='*.ts' --include='*.tsx'` returns zero matches
- `grep -rn "user?\.role ===" fe/src/features/datasets/ fe/src/features/glossary/ fe/src/features/system/ --include='*.ts' --include='*.tsx'` returns zero matches (separate grep — ERE `\?\?` matches literal `??` and silently misses optional-chain sites)
- `cd fe &amp;&amp; npm run build` exits 0
- `cd fe &amp;&amp; npm run test:run` exits 0
- ~10 sequential per-file commits in git log
</verification>

<success_criteria>
TS11 substantially advanced: every page/component MECH site is migrated with reviewable per-file commits.
</success_criteria>

<output>
After completion: `.planning/phase-04-fe-catalog-can-codemod/4.4-SUMMARY.md`
</output>
</content>
</invoke>