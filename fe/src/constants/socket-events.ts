/**
 * @description Canonical Socket.IO event names consumed by the frontend.
 * Must stay aligned with `be/src/shared/constants/socket-events.ts`.
 */
export const SOCKET_EVENTS = {
  PERMISSIONS_CATALOG_UPDATED: 'permissions:catalog-updated',
} as const

