/**
 * @description Keyboard key constants for event handling
 */

export const Key = {
  ENTER: 'Enter',
  ESCAPE: 'Escape',
  BACKSPACE: 'Backspace',
  PLUS: '+',
  EQUALS: '=',
  MINUS: '-',
} as const

/** Keyboard shortcut keys (used with Ctrl/Cmd) */
export const ShortcutKey = {
  UNDO: 'z',
  COMMAND_PALETTE: 'k',
  ROTATE: 'r',
} as const
